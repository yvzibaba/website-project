# ROADMAP — Phase 0–18 任务树、MVP 边界、阻塞与开放决策

来源：总控 Prompt 第37–43 节；宪法第2/4/18/26 条。执行方式：**小步、逐 Phase**，每完成一个 Phase 必须 修改代码 → 运行测试 → 检查错误 → 修复 → 更新文档 → 汇报状态（总控第37节 / 宪法第18条）。禁止一次性重写整个系统。

## 1. Phase 任务树

| Phase | 名称 | 关键交付 | 依赖 | 状态 |
|---|---|---|---|---|
| 0 | 项目分析 | 环境检查、现状盘点 | — | ✅ 本次完成 |
| 1 | 需求与信息架构 | PRODUCT_SPEC（用户/页面 IA/MVP） | 0 | ✅ 本次完成 |
| 2 | 技术架构 | ARCHITECTURE（选型/分层/Model Router） | 1 | ✅ 本次完成 |
| 3 | 数据库设计 | DATABASE（ER）+ prisma V1 核心 schema | 2 | 🟡 V1 核心已建；全量表分层待逐 Phase 补 |
| 4 | 项目骨架 | 目录结构、DB 连接、基础 UI、env、日志、错误处理、测试框架 | 3 + Postgres/依赖 | ✅ **Phase 4 全部完成**（里程碑 1+2+3）：DB 上线（17 表 + 11 枚举）、`src/lib/{env,logger,errors,prisma}` 核心库、Next.js shell（layout/page/error/not-found + `/api/health`）；里程碑 3 补设计 tokens（颜色/圆角/阴影亮暗双模式）、`src/components/ui/*` 11 组件 + `/ui` 演示页、`src/lib/{cn,api-client,validation,request-id}`（统一 fetch + AppError 映射 + 重试、Zod 校验含 11 个与 prisma 同步枚举、request-id 追踪）、`src/proxy.ts`（request-id 注入下游 + 访问日志 + 安全头）。Vitest 单元 **125** + 集成 5 全绿、`tsc` 0 错、`next build` Turbopack 产出 **4 路由 + Proxy**、HTTP 冒烟：request-id 服务端生成/客户端透传/非法拒绝全通过、`/ui` 200 全组件渲染 |
| 5 | 公共页面 | 首页/行业/案例列表/方案列表/搜索/关于/隐私/协议 | 4 | ✅ **Phase 5 全部完成**（里程碑 1+2+3）：**M1**——页面级布局组件（PageHeader/Breadcrumb/EmptyState）、行业数据层 `src/server/industries.ts`、`/industries` 列表（force-dynamic + 实时计数）、`/industries/[slug]` 详情（SSG + `dynamicParams=false` 保证真 404）、`/about`、`/privacy`+`/terms` 结构化占位（标注待法务审定，宪法第 21 条）、导航/页脚更新。**M2**——DEMO 可见性层 `src/server/demo.ts`（默认排除、`?demo=1` 显式纳入并打角标）、DEMO 种子脚本 `prisma/seed.ts`（**仅 6 个标注【DEMO】的案例，按创始人裁决不种子任何方案**——方案涉及定价与购买闭环须走真实多角色流水线，宪法第 20 条禁伪造可售商品；生产环境拒跑）、案例数据层 `src/server/cases.ts`（列表 `$transaction[findMany,count]` + 行业筛选/分页/排序 + 详情含证据分层/能力关联/商业模式，公开阶段白名单 + DEMO 门控 → `not_found`）、方案数据层 `src/server/solutions.ts`（仅 PUBLISHED，金额两位小数精显）、`/cases`+`/cases/[id]`+`/solutions`+`/solutions/[id]` 四页（购买按钮占位 disabled，Phase 12 接入）。**关键修复**：删除根 `src/app/loading.tsx`——实测其 Suspense shell 会让 force-dynamic 详情页对无效 id 先 flush 200、`notFound()` 无法回退状态码（3 个无效 id 冒烟全返 200）；移除后无效 id 真 404，SSG 路由本就免疫。**M3**——搜索编排层 `src/server/search.ts`（`searchPublic` 并行案例+方案、聚合 ok/hits、DB 失败软降级）、cases/solutions 列表加关键词维度（`buildCaseWhere`/`buildSolutionWhere` 用 `{AND:[...]}` 容纳关键词 OR 片段与 DEMO OR 片段）、`/search` 页（无 JS 可用 GET 表单 + 行业 chips + 引导/非法/失败/空/结果 五态 + 深链"查看全部"）、首页 `src/app/page.tsx` 照总控 §6 重写为六段体验（Hero+内联搜索 / 今日案例 / 今日方案 / 企业诊断 V1-B 即将开放 / 六步工作流 / 按行业浏览，移除 Phase 4 pg_stat 调试面板）、`/cases`+`/solutions` 关键词回显条、导航加 `/search`。测试基线 **152 单元 + 18 集成**全绿、`tsc`/`eslint` 0 错、`next build` 产出 `ƒ /search`、HTTP 冒烟 **31/31**（含 2 个真 404 回归 + `?demo=1` 命中/默认不泄露种子标题）。V1-A「用户查看」入口层（发现→检索→详情）打通；"购买"仍 Phase 12 占位 |
| 6 | 用户系统 | 注册/登录/会话/最小权限（下单身份） | 4 | 🟡 **M1 完成**：认证方案经创始人裁决选定 **Auth.js 自建**（总控 §21）——`next-auth@5` Credentials（邮箱+密码）+ **JWT 会话**（`trustHost:true`，**不挂 Prisma Adapter**，依赖最少）；`prisma` 加 `enum UserRole{USER/REVIEWER/ADMIN}` + `model User`（email unique / passwordHash / role 默认 USER），`Order` 加可空 `userId`（`ON DELETE SET NULL`），迁移 `20260905000000_add_user_auth` **纯加性**已 `migrate deploy` 到 Neon（表 17→18、枚举 11→12）；口令哈希 `src/lib/password.ts` 用 **node:crypto scrypt**（自描述串 + `timingSafeEqual`，畸形/超长一律 false，**零额外依赖**、绝不存明文）；`src/lib/validation.ts` 加 Email/Password/Register/Login/UserRole schema（邮箱归一小写、注册 8–128 位、登录口令 min1 防枚举）；`src/server/users.ts`（registerUser 判别联合 + P2002 归一 email_taken、getAuthUserByEmail、getProfileUserById 排除 passwordHash）；`src/auth.ts` + `api/auth/[...nextauth]`（authorize 对账号不存在/密码错误一律返 null，防枚举；jwt/session 透传 id/role；JWT 类型增强打在 `@auth/core/jwt`）；`/login`·`/register`·`/account` 三页（`useActionState` 共用 `AuthForm` + `"use server"` actions，注册后自动登录，账号页无会话重定向 /login + 登出）。测试基线 **167 单元 + 23 集成**全绿、`tsc`/`eslint` 0 错、`next build` 产出 `ƒ /login`·`ƒ /register`·`ƒ /account`·`/api/auth/[...nextauth]`、**HTTP 认证冒烟 26/26**（csrf→callback→session→account→signout，错误口令/不存在邮箱无差别失败）。V1-A「购买」下单身份就绪（订单绑定 Phase 12）。**M2 完成（RBAC 鉴权原语 + /admin 后台门禁，双层防御）**：`src/server/authz.ts` `hasRole` 纯函数 + `getCurrentUser`（角色只从 JWT 会话读、绝不信任客户端）+ `requireUser`/`requireRole(allowed)` 判别联合（unauthenticated/forbidden/ok，越权记非敏感审计 warn），`STAFF_ROLES=[REVIEWER,ADMIN]`；`src/server/admin.ts` `getAdminDashboardData` 只读聚合（总数由 groupBy 求和得，天然自洽、少查询）；`/admin` layout 拦未登录跳 /login + 越权渲染「无访问权限」面板不渲染 children，page 取数据前**再自鉴权**（`requireRole` 不过 → `return null`）——实测修正了"仅 layout 拦截时 Next 仍为 leaf page 生成 RSC flight 负载泄露概览"的问题，构成 layout+page 双层防御；`/account` 对员工条件显「进入管理后台」入口；`scripts/promote-user.ts`（npm `user:promote`）受控提权 CLI（只改 role、账号不存在不自动建号，注册仍永远只建 USER）。测试基线 **209 单元（+15 authz）+ 43 集成（+4 admin，真连 Neon）**全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /admin` 无回归、**HTTP 门禁冒烟 15/15**（未登录跳登录 / USER 得无权限且不泄露概览 / ADMIN 得概览+审计表）。版本 **0.14.0**。解锁后续：以 `requireRole` 保护案例 CRUD 写路由（Phase 13）、订单确认（Phase 12）。**M3+ 待做**：更细粒度权限矩阵（按资源/动作）随 Phase 8/12/13 需要再补，V1 保持最小三角色 |
| 7 | 案例系统 | 案例 CRUD、证据、评分、行业关联、案例详情页 | 5,6 | 🟡 **M1+M2 完成（评分内核 + 持久化复算）**：`src/server/scoring.ts` 纯函数把总控 §10「案例评分体系」落成可复算程序（宪法第 7 条：程序计算 > LLM 口算）——`OPPORTUNITY_DIMENSIONS` 10 维度权重**逐字照抄总控 §10**（满分恒 100，`reduce` 求和防失真），`竞争强度`/`实施难度` 标 **inverse** 反向极性（录入按直觉填负面强度、程序负责 `max-raw`，防填反方向）；`computeOpportunityScore` 经 Zod 逐维度校验、非法 → `{ok:false,issues}` **指名维度绝不静默截断**；`computeEvidenceConfidence`（类型权重 FACT1.0/ASSUMPTION0.5/INFERENCE0.4/PREDICTION0.3 + 无来源 0.6 折 + 缺省 confidence 50，`round(100·Σ(w·q·sf)/Σw)`，**总控只给示例值未给公式 → 标注为本项目 v1 假设、参数可调**）；`countKeyUnknowns`=非 FACT 证据数；`computeCaseScores` 一次出三件套 + `CaseScoresSchema` 供 M2 入库校验。`docs/SCORING_V1.md` 版本化公式文档（每个数字标注事实/假设/推断、两个可复算样例 机会→88 与总控示例交叉验证一致·证据→69、诚实记录缺口=总控 §11 证据等级 S/A/B/C/D 轴尚未建模 + 权重未经真实案例校准、变更流程 + 版本历史、重申"综合评分 ≠ 一定成功"规则 9）。`tests/unit/scoring.test.ts` **26 黄金样本**（最优100/全填max90/全填0 10/样例88/反向极性/越界拒绝/确定性/证据可信度精确Σ）。测试基线 **193 单元 + 23 集成**全绿、`tsc`/`eslint` 0 错、`next build` 全路由无回归（本里程碑不动 DB/页面，纯函数验证以单元黄金样本为准）、版本 **0.9.0**。**M2 完成（评分持久化 + 复算）**：Case 加 `scoreInput Json?`（10 维度录入分=输入）+ `scoreBreakdown Json?`（computeCaseScores 完整输出=可审计结果），迁移 `20260905100000_add_case_score_fields` 纯加性已 deploy Neon（表数不变 18）；`src/server/case-scores.ts` `recomputeCaseScores`/`recomputeAllCaseScores` 读输入+证据→复算→`CaseScoresSchema` 入库前复核→写 breakdown 与两标量（判别联合 computed/skipped/invalid/not_found/error，**无 scoreInput 诚实跳过绝不编造、非法不写库**）；`scripts/recompute-scores.ts`（`npm run db:recompute-scores`）批量复算 CLI 如实汇报计数（实跑 6 DEMO 无输入全 skipped）；`tests/integration/case-scores.test.ts` 6 cases（持久化读回/跳过/非法不写库/幂等/全量汇总自洽）。基线 **193 单元 + 29 集成**全绿、版本 **0.10.0**。**M3 完成（评分拆解上详情页）**：`getPublicCaseById` 返回 `scoreBreakdown`（`CaseScoresSchema` 校验库中 JSON，缺失/非法诚实返 null）；`/cases/[id]` 新增 `ScoreBreakdownCard`——机会总分 + 10 维贡献条（inverse 标「越低越好」并显录入值）+ 证据可信度/证据数/关键未知变量数 + 证据构成 + 公式版本 + 重申「综合评分 ≠ 一定成功」，无拆解则显示"暂未录入/复算不展示"；`prisma/seed.ts` 给 6 DEMO 案例补 `scoreInput` 并**内联 `computeCaseScores`** 派生 breakdown/标量（`db:recompute-scores` 现全 6 案例 skipped→computed）；集成测试 +2 例。基线 **193 单元 + 31 集成**全绿、`next build` 无回归、HTTP 冒烟 `?demo=1` 详情页实测渲染 64/39/3、版本 **0.11.0**。**M4 完成（§11 证据来源权威度分级 = 元数据 + 标签，不动分数）**：按创始人裁决「只记录 + 标签，不动分数」——`prisma` 加 `enum EvidenceGrade{S/A/B/C/D}` + `Evidence.grade?`（纯加性迁移 `20260905110000_add_evidence_grade` 已 deploy Neon，表数仍 18、枚举 12→13）；`validation.ts` 加 `EvidenceGradeSchema`；`getPublicCaseById` 随证据透出 grade，`/cases/[id]` 每条证据叠来源等级徽章 + 对「存在 D 级」「FACT 仅 D 级支撑」给 `Alert` 复核告警（呼应 §11「D 级不得表述为已确认事实」）；`prisma/seed.ts` 6 DEMO 案例 16 证据补 grade（保留 ai_vision FACT=D 演示告警）。**可信度公式与 `SCORING_RUBRIC_VERSION` 保持 1.0.0、不并入打分、历史 breakdown 仍有效无需重算**。基线 **194 单元 + 31 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归、grade groupBy 实测 A1/B2/C2/D10/S1、HTTP 冒烟 `?demo=1` 渲染「来源 D/B」+ D 级告警、版本 **0.12.0**。**M5 完成（案例 & 证据「数据层」CRUD，不含 HTTP/UI）**：`src/server/case-admin.ts`（server-only）落地 `createCase`/`updateCase`/`deleteCase`/`addCaseEvidence`/`removeCaseEvidence`，全部走 Zod 校验（录入边界只做结构校验，10 维度/区间合法性仍单一归评分内核，避免两套规则漂移）+ `$transaction` 写库与 `ChangeLog` 审计（entityType=Case，CREATE/UPDATE/DELETE 记 before/after 快照 + actor + reason），`updateCase` 对 `version` 手 +1（宪法第 13 条版本化）、`deleteCase` 对被 PUBLISHED 方案引用的案例返回 `blocked` 不硬删（Solution.caseId Restrict），凡评分输入变动即复用 `recomputeCaseScores` 回算并折叠进 `recompute` 状态；`actor` 仅审计、**非鉴权**（HTTP 写路由 + 角色门禁统一延 Phase 13，避免暴露无鉴权公共写端点）。无 schema 变更。`tests/integration/case-admin.test.ts` 8 例真连 Neon（最小建/带分建 computed 88-69/非法 industry 不写库/越界评分 invalid 不写库/update version 递增+空 patch 拒+不存在 404/增删证据可信度变then还原/被发布方案阻断删除后删方案成功级联/删不存在 404）。基线 **194 单元 + 39 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归、版本 **0.13.0**。**后续待做**：案例 CRUD 的 HTTP 路由 + 后台 UI + 角色门禁（Phase 13）、把 §11 grade 并入可信度公式的后续待决（属公式形态变更须走 SCORING §5 升版）、行业关联收尾 |
| 8 | 解决方案系统 | Solution.body 34 分节规范、方案详情、版本化、财务/未知变量 | 7 | 🟡 **M1 完成（方案数据层 CRUD + 审计 + 发布守卫）**：Phase 5 里程碑 2 刻意**不灌 DEMO 方案**（宪法第 20 条禁伪造可售商品），因此 Solution 表 0 行；M1 把 Phase 7 M5 案例侧的 CRUD 模式横向复制到方案侧，为 Phase 8 M4 AI 流水线与 Phase 13 后台 UI 打底。`src/server/solution-admin.ts`（server-only）：`createSolution`（**强制 status=DRAFT**、不允许直建 PUBLISHED，case FK 预检 + slug 冲突 P2002 归一 invalid）/`updateSolution`（version 自增、状态迁移、`publishedAt` 只在非 PUBLISHED→PUBLISHED 首次打时间戳、二次更新不刷新）/`deleteSolution`（守卫：**任一 Order 关联即 blocked**，V1 无退款流程属 ROADMAP #5）/`addSolutionFinancial`·`removeSolutionFinancial`（Decimal 一律 string 输入避 JS 浮点污染）/`addSolutionUnknown`·`removeSolutionUnknown`（**自动同步 `Solution.unknownVariableCount` = 表内实时条数**，规则 6/9：不确定性显式列出）；每个写操作 `$transaction` [写库 + 写 `ChangeLog`(entityType=Solution, before/after+actor+reason)]；**发布守卫 `publishGuard`（宪法第 21 条）**：升 PUBLISHED 需 price 非 null + 有 riskDomains 时必勾 needsProfessionalReview，否则返 `blocked` 且不改状态不落 publishedAt；**不做鉴权**（延 Phase 13 用 M2 的 `requireRole` 薄包一层）；**不自动评分**（方案侧公式形态未定，M1 留 null/人工填，`unknownVariableCount` 是唯一自动派生标量因为它是可计算事实）；Prisma 错误归一 P2002→invalid(slug)、P2003→invalid(relation)、P2025→not_found。无 schema/DB 变更。`tests/integration/solution-admin.test.ts` 9 例真连 Neon（成功建+CREATE 审计/slug 冲突/case 不存在/version 递增+空 patch 拒+not_found/无价 blocked/风险域未勾 review blocked/全条件通过 publishedAt 落库+不刷新/有 Order blocked→撤单成功+级联 financial+unknown+DELETE 审计/removeSolutionFinancial·Unknown 归属回查+unknownVariableCount 自动回写）。基线 **209 单元 + 52 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归、版本 **0.15.0**。**M2 完成（方案 34 分节正文结构化渲染）**：总控 §3「Solution Package」**已逐字给定 34 分节**（并非待与创始人共同定稿——规范来源就是总控本身），M1 能写 body 但详情页只占位不渲染，M2 补齐展示契约。`src/server/solution-body.ts`（新，纯函数零 DB 零 Next 依赖）：`SOLUTION_SECTIONS` 为 34 分节**单一真源**（稳定 key + 照抄总控的中文 title），`parseSolutionBody` 把任意入库 body 归一为固定 34 节有序视图（按 key 或中文 title 命中即 filled；空串/空数组/空对象/null/undefined 判 pending；数字 0/布尔 false 算有内容；契约外未知键收进 `extras` 不静默丢弃；回 `filledCount/totalCount/empty`）。`getPublishedSolutionById` 透出 `body:ParsedSolutionBody`；`/solutions/[id]` 新增 `SolutionBodySection`——空 body 出**诚实占位**（须走多角色流水线、禁单模型直出，与 Phase 7 M3 `scoreBreakdown=null` 同构），有 body 按 canonical 序渲染 34 张分节卡 + 完成度徽章 n/34 + 未填标「待补充」+ extras 审计提示，`renderSectionContent` 兼容字符串/标量/数组/嵌套对象。刻意保留与结构化卡片（财务/未知变量/证据）的可控冗余，待真实流水线接入再收敛。**不动 schema、不开 HTTP 写端点**。`tests/unit/solution-body.test.ts` 11 例锁死归一契约；基线 **220 单元 + 52 集成**全绿、`tsc`/`eslint` 0 错、`next build` 路由清单不变、**HTTP 渲染冒烟 15/15**（临时发布带 body 方案挂既有 DEMO 案例→验证各类型内容渲染 + 空 body 占位→即时硬删、leftover=0），版本 **0.16.0**。**M3+ 待做**：方案评分内核建模（引入复用度/供应链等新维度须走 SCORING §5 升版）、多角色 AI 流水线灌真实方案（阻塞 ROADMAP #4 模型 API Key）、HTTP 写路由 + 后台 UI（延 Phase 13）、购买闭环（阻塞 ROADMAP #5 支付）|
| 9 | AI Agent 系统 | Model Router、Agent 框架、Schema 校验、成本记录、评分算法程序 | 4,8 | ⬜ |
| 10 | GitHub Scout | 开源发现/去重/评分/许可证检查/技术匹配 | 9 | ⬜ |
| 11 | 企业诊断 | 企业画像、诊断会话、方案匹配、Top5、定制方案（V1-B） | 8,9 | ⬜ |
| 12 | 订单 | 下单、最简支付确认、后台确认、解锁；预留多渠道 | 6,8 | ✅ **M1 完成（订单「数据层」——购买闭环的可信底座，不含 HTTP/UI）**：`Order` 表早在 Phase 4/5 随 schema 建好（PENDING/PAID/REFUNDED/CANCELED 状态机 + `amount Decimal` + `paymentProvider` 预留），V1 购买流按总控/PRODUCT_SPEC 明确为「下单 → 支付说明（站外、无网关）→ 后台人工确认 PAID → 解锁」——**刻意不接支付网关**（ROADMAP #5 非阻塞），故本里程碑**零 schema 迁移**。`src/server/orders.ts`（server-only，判别联合返回，不抛裸异常，不做鉴权·信任上层门禁）：`createOrder` **金额只从服务端读取的 `Solution.price` 快照进 `Order.amount`、绝不采信客户端传入金额**（否者可把 1999 改成 0.01，"钱"的路径来源唯一、程序可复算，宪法第 7/20 条）；只能对 `status=PUBLISHED` 且有价方案下单（DRAFT/审核中→blocked，与公开详情门控一致）；**同一买家（userId 或归一 buyerEmail）对同一方案重复下单幂等去重**——已有 PENDING 直接复用（`deduped:true` 不新建）、已 PAID 返回 blocked 不重复收钱；身份至少 userId/email 其一否则 `invalid(identity)`。`confirmOrderPaid`（仅 PENDING→PAID、`paidAt` 只首次落库、再次确认幂等不刷新时间戳、version 自增）、`cancelOrder`（仅 PENDING→CANCELED，V1 无退款流程 PAID 不可撤，属 #5）、`getOrderById`、`listOrdersForBuyer`（OR userId/buyerEmail 找回名下）、`listOrdersForAdmin`（状态过滤 + 分页自洽）、`hasPaidEntitlement`（解锁判定：无身份/DB 异常一律保守 false，绝不错误放行付费内容）。每个写操作 `$transaction` 写库 + `ChangeLog`(entityType=Order, before/after+actor+reason)；Prisma 错误归一 P2003→invalid(relation)、P2025→not_found。**不动 schema、不开 HTTP 端点**（延 M2 复用 `api-guard` 统一门禁，届时 `MutationLike` 加 `orderId/deduped` 字段）。`tests/integration/orders.test.ts` **10 例**真连 Neon（金额服务端快照+status 不采信/缺身份/仅 PUBLISHED/不存在方案/重复下单幂等复用/confirmPaid 首落+幂等不刷新/已购 blocked/PAID 不可撤/hasPaidEntitlement 前后翻转·非属主 false·无身份 false/买后台列表分页）+ `tests/unit/orders-schema.test.ts` **7 例**无 DB 锁 schema 契约（白名单剥离 amount/status/currency/price、身份二选一、邮箱归一、buyerType 默认与枚举）。基线 **245 单元 + 66 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归（数据层不新增路由）、版本 **0.19.0**。**M2 完成（订单「HTTP 端点」——购买闭环经 HTTP 打通，复用 api-guard，不含 UI）**：把 M1 数据层用已就绪门禁薄包一层，**零业务、零 schema**。`api-guard.ts` 抽出共享纯函数 `sameOriginBlock`、**新增 `requireSameOriginActor`**（只做 CSRF 同源 + 会话注入 `userId/actor`、**不设角色门禁**——下单是任何登录用户/游客都可做的公开动作，与后台 staff 写明确分层）、`MutationLike` 显式补 `orderId/deduped/order`。4 路由全 `force-dynamic` + Next16 `params:Promise` await + 只「门禁→调数据层→`mutationResponse`」：`POST /api/orders`（公开下单，先 `delete raw.userId/actor` 再从会话注入，客户端伪造身份被丢弃；金额防篡改由 M1 白名单守）、`GET /api/admin/orders`（staff 只读，`requireRole`+`PaginationSchema`+状态过滤）、`POST /api/admin/orders/[id]/confirm`·`/cancel`（staff 写，`requireStaffWrite`）。**`hasPaidEntitlement` 刻意不暴露为公开端点**（无额外鉴权会引 IDOR），留 M3 在 RSC 服务端调用。`tests/unit/api-guard.test.ts` 扩到 **22 例**（+`requireSameOriginActor` 3 例、`mutationResponse` 透传 1 例）。基线 **249 单元（245+4）+ 66 集成**全绿、`tsc`/`eslint` 0 错、`next build` 新增 **4 条 `ƒ` 动态路由**无回归、**HTTP 端到端冒烟 29/29**（`smoke-orders.mjs`：金额服务端快照防篡改/游客+登录幂等去重/跨站 CSRF/缺身份 400/伪造 userId 被会话覆盖/后台 401·403·200 门禁矩阵/确认 PAID+paidAt+version=2+审计 `human:<adminId>`/重复确认幂等/已购再下单 409/取消状态机/跨站确认 403/状态过滤，末尾 leftover=0）、版本 **0.20.0**。**M3 完成（购买闭环「用户界面」——详情页解锁门控 + 支付说明 + 我的订单 + 后台确认台）**：给 M2 端点接上"人面"，**零 schema、零新数据层逻辑**，全复用已测 `orders.ts`+`api-guard`。`solutions.ts` 的 `SolutionDetail` 加 `isFree`（price 空/0 即免费）；`components/solutions/BuyButton.tsx`（client）`fetch` `POST /api/orders` **只提交 `solutionId`**（身份服务端注入、金额服务端快照，杜绝改价），成功跳支付说明页、409/401/错误如实提示（走 HTTP 而非另起 Server Action，第 16 条防漂移）；`/solutions/[id]` 服务端算 `entitled=isFree||isDemo?true:hasPaidEntitlement(...)`、购买卡四态渲染（免费·DEMO→开放徽章 / 已购→已解锁 + `#solution-body` 锚 / 待购登录→BuyButton / 游客→登录引导），**`SolutionBodySection` 加 `locked` 入参——未解锁时服务端根本不渲染 34 分节、只出「正文未解锁」锁面板**（正文是付费交付物，前端无从绕过）；`/orders/[id]` RSC 支付说明页（属主校验 userId 命中或归一 email 命中、不匹配统一 `notFound()` 防 IDOR、金额快照 + V1 站外付款三步指引·未接网关、PAID 引导回正文）；`/account/orders` 我的订单（登录门禁 + `listOrdersForBuyer` 属主过滤）+ `/account` 入口；`/admin/orders` 后台确认台（layout+page 双层防御 + 状态过滤/分页）+ `components/admin/AdminOrderActions.tsx`（确认收款/取消消费 M2 端点 + `router.refresh()`）+ `/admin` 订单卡入口。V1 有意识假设：只有 price>0 且非 DEMO 方案正文才锁（免费/DEMO 恒开放）、购买需登录（游客下单数据层支持但 UI 不暴露）、人工确认到账即解锁不接网关。`cases-solutions.test.ts` 扩 `isFree` 断言（¥1999=false、新种 price=0 免费=true）；基线 **315（249 单元 + 66 集成）**全绿、`tsc`/`eslint` 0 错、`next build` 新增 **3 条 `ƒ` 路由**无回归、**购买闭环 UI 端到端冒烟 29/29**（`smoke-orders-ui.mjs`：钱-shot——付款前游客/未购/他人详情页 HTML 全不含正文标记显锁面板、后台确认 PAID 后仅属主标记出现且显已解锁、他人访问该单支付页 404 防 IDOR、我的订单按属主隔离、后台 USER 无权限/ADMIN 见单、末尾 leftover=0），版本 **0.21.0**。**✅ Phase 12 全部完成（M1 数据层 + M2 端点 + M3 UI）**——「下单 → 支付说明（站外）→ 后台确认 PAID → 解锁读正文」购买闭环从用户侧端到端可用且经真实 HTTP + 服务端门禁验证成立；**不依赖 ROADMAP #5 支付网关**（人工确认即解锁），退款/企业批量授权/在线支付自动化属后续增量 |
| 13 | 后台 | 每日任务中心、各领域管理、审核发布、日志、成本看板 | 7–12 | 🟡 **M1+M2 完成（后台「写」HTTP 端点统一门禁 + 最小管理 UI）**：**M1** 兑现 Phase 7 M5 / Phase 8 M1 数据层「不做鉴权、HTTP 写路由延 Phase 13」的安全承诺，`src/server/api-guard.ts` 作为**唯一门禁 + 结果翻译点**（宪法第 16 条防逐端点漂移）——`actorOf` 只从服务端会话派生 `human:<id>`（绝不信客户端 actor/role）、`isSameOrigin` 纯函数做 CSRF 同源判定（无头放行 / 有头 host 精确相等 / 解析失败拒）、`requireStaffWrite` 先挡跨站 403（且不查角色）再 `requireRole(STAFF_ROLES)`（未登录 401 / USER 越权 403 带 `required`）、`mutationResponse` 把数据层判别联合统一翻译（ok→200、invalid→400、not_found→404、blocked→409、error→500 生产屏蔽）；10 个薄封装写路由，**零业务逻辑、零 schema 变更**，校验/审计/守卫/复算全在已测数据层；**仍不含限流/幂等**（V1 延后）。`tests/unit/api-guard.test.ts` 18 例锁门禁与状态映射、**HTTP 端到端冒烟 24/24**，版本 **0.17.0**。**M2** 补上「方向一」另一半——`src/server/admin-cases.ts` `listAdminCases()` 后台**全量**读层（含内部阶段 + DEMO、倒序、`_count`、硬上限 500 超出标 `truncated`），`NewCaseForm`（client）`fetch` 到 `POST /api/admin/cases`（刻意走端点而非 Server Action 防逻辑抄两遍漂移），`/admin/cases` layout+page 双层防御、`/admin` 加「案例管理」入口，有意最小竖切（仅案例「列表+新建」）。新增集成 4 例、基线 **238 单元 + 56 集成**全绿、`next build` 新增 `ƒ /admin/cases`、**HTTP 渲染冒烟 18/18**，版本 **0.18.0**。**M3+ 待做**：案例详情页/证据增删/删除、方案管理后台 UI（复用已就绪端点）、审核发布队列、成本看板、更细粒度权限矩阵（按资源/动作）|
| 14 | SEO | 独立 URL、meta/OG/结构化数据/canonical、sitemap | 5,7,8 | ⬜ |
| 15 | 测试 | Unit/Integration/API/DB/AI Workflow/Permission/Security/E2E | 各 Phase | ⬜ 随 Phase 进行 |
| 16 | 部署 | Serverless/低运维上线、环境变量、域名 | 4–14 + **托管资源** | ⬜ 阻塞 |
| 17 | 每日自动任务 | 定时流水线编排（23:00）、重试、FAILED_REVIEW | 9,13 | ⬜ |
| 18 | 生产验证 | 端到端验收（总控第42节）、成本可查、文档完整 | 全部 | ⬜ |

## 2. MVP 边界（宪法第2/24/26条）

- **V1-A（必须真正跑通）**：免费案例 → 标准方案 → 购买（含最小用户/登录）。对应 Phase 4–8 + 12 + 必要后台。
- **V1-B（闭环稳定后紧接）**：企业画像 + 企业诊断 + 方案适配（Phase 11）。
- **V2+ 延后**：POC、项目库、孵化、联合投资、实体运营；复杂社交/社区/融资交易/投资商城/供应商市场/知识图谱/微服务/K8s/模型训练/原生 App/小程序/复杂 CRM-ERP。

## 3. 阻塞与所需资源（总控第40节"停止条件"，诚实记录 — 宪法第20条）

进入 Phase 4 前必须由创始人提供/决策，否则无法真实"可运行/可测试"：

1. ~~**数据库**：本机无 PostgreSQL、无 Docker。~~ **已解除**：创始人提供 Neon 免费托管 Postgres `DATABASE_URL`（region `us-east-2`），已写入本地 `.env`（gitignored），`prisma migrate deploy` 成功建 17 表 + 11 枚举。
2. ~~**依赖安装网络**：npm 可达但重型依赖（Prisma 引擎等）下载 stall。~~ **已解除**：改用 npmmirror 源 + `PRISMA_ENGINES_MIRROR`，`npm install`（393 包）与 `prisma validate` 均跑通，首迁移已离线生成。
3. **代码推送**：命令行无法访问 `github.com`（仅 `api.github.com`）。目前经 API 提交；若要走正常 `git push`/CI，需要系统级/TUN 代理。
4. **模型密钥**：Model Router 需要 Qwen3.8-Max 等模型的 API Key（走环境变量，禁止入 Git）。→ 属"需要付费资源授权"。
5. **部署与支付**：Serverless 部署目标、域名、以及支付渠道（V1 可先"支付说明+后台确认"，但需确定收款方式）。→ 属"付费资源/业务决策"。
6. **对象存储**：企业文件/PDF 导出所需的对象存储供应商与凭证。

## 4. 开放决策（需创始人拍板 — 总控第40节"必须由人工决定的业务规则"）

1. **V1 范围**：企业诊断/适配放 V1-A 还是 V1-B？（本文档集默认 V1-B，理由见 PROJECT_RULES.md「已知冲突」）
2. ~~**认证方案**：Auth.js（自托管、免费、需自己配）vs 托管认证（Clerk/NextAuth 云等，省事但可能付费）。~~ **已决策**：创始人选定 **Auth.js 自建**（Credentials + JWT，用户表留自有 Neon 库、零厂商锁定），已于 Phase 6 M1 落地。
3. **数据库/部署供应商**与预算上限（影响成本路由与 Serverless 选择）。
4. **定价与币种**：标准方案定价区间、是否多币种（默认 CNY）。
5. **`Solution.body` 34 分节的最终 JSON 结构**（Phase 8 前定稿；我可先出草案供你改）。

## 5. 验收标准（总控第42–43节，Phase 18）

用户侧闭环可真实运行：访问首页 → 浏览案例 → 打开方案 → 注册 → （企业诊断 → 生成推荐）→ 查看方案 → 提交订单 → 管理员后台确认 → 用户解锁内容。
管理侧：查看每日任务 → 60 候选 → 筛选结果 → Agent 过程 → 3 个方案 → 审核 1 个 → 发布。
质量门槛：核心流程正常、AI 输出结构稳定、关键数据可追溯、无密钥泄露、DB 无结构性错误、普通用户能完成主流程、管理员能管理系统、可在生产运行、能查看 AI 成本、开发文档完整 —— 全部满足才可宣布 **V1.0 READY**。禁止虚构"已完成/测试通过/API 可用/许可证允许商用"（总控第44节）。
