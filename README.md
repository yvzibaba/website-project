# AI产业案例与解决方案引擎（website-project）

> 每天从全球发现高价值产业案例，把成功商业模式、技术路线、开源能力、中国供应链与本土条件进行 AI 重构，形成**可购买、可实施、可进一步定制**的产业解决方案。

这是一个面向**几乎零基础编程的个人创始人**、由 AI 团队工程化实现的产品。核心目标不是"做一个漂亮网站"，而是建立第一代 **AI 产业解决方案自动生产系统**，并从 V1 起保留向未来演化的接口。

## 治理文档（先读，按优先级）

1. [`PROJECT_RULES.md`](./PROJECT_RULES.md) — 文档索引与优先级说明
2. [`docs/PROJECT_RULES_V2.md`](./docs/PROJECT_RULES_V2.md) — **项目宪法 V2.0（最高优先级）**
3. [`docs/MASTER_PROMPT_V1.md`](./docs/MASTER_PROMPT_V1.md) — 全自动开发总控 Prompt V1.0
4. [`AGENTS.md`](./AGENTS.md) — AI Agent 工作约定

## 产品文档

- [`PRODUCT_SPEC.md`](./PRODUCT_SPEC.md) — 产品定位、用户、页面信息架构、MVP 范围
- [`ARCHITECTURE.md`](./ARCHITECTURE.md) — 技术选型与系统架构、Model Router
- [`DATABASE.md`](./DATABASE.md) — 数据库 ER 设计（全量目标 + V1 分层）
- [`AI_WORKFLOW.md`](./AI_WORKFLOW.md) — 12 个 Agent 与每日自动生产流水线
- [`SECURITY.md`](./SECURITY.md) — 隐私、密钥、权限、审计
- [`TESTING.md`](./TESTING.md) — 测试策略与质量门禁
- [`ROADMAP.md`](./ROADMAP.md) — Phase 0–18 任务树与 MVP 边界
- [`CHANGELOG.md`](./CHANGELOG.md) — 变更记录（每次修改追加）

## 技术栈（已定）

Next.js 16（App Router）+ React 19 + TypeScript · Tailwind CSS v4（+ 计划引入 shadcn/ui）· Prisma 6 + PostgreSQL（+ 计划 pgvector）· **Auth.js v5（next-auth beta）Credentials + JWT 会话 + node:crypto scrypt 口令哈希（零额外依赖）** · 统一 Model Router（核心模型 Qwen3.8-Max，禁止硬编码模型名）。

## 当前状态

- [x] 仓库与 Next.js 脚手架
- [x] 项目宪法 + 总控 Prompt 版本化入库
- [x] 产业能力数据库 V1 核心 schema（[`prisma/schema.prisma`](./prisma/schema.prisma)，12 实体 + ChangeLog）
- [x] Phase 0–3 基础文档集
- [x] 依赖安装 + `prisma validate` 通过 + 首迁移 `0_init/migration.sql` 离线生成（改用 npmmirror 源 + Prisma 引擎镜像，解除此前的下载卡顿阻塞）
- [x] **Phase 4 里程碑 1：数据库上线**——`DATABASE_URL`（Neon `us-east-2`）写入 `.env`（gitignored），`prisma migrate deploy` 成功建 17 张业务表 + 11 个枚举，`_prisma_migrations` 记录已落库（finished_at `2026-09-04T15:22:16.915Z`）
- [x] **Phase 4 里程碑 2：项目骨架 + 测试基线全绿**——`src/lib/{env,logger,errors,prisma}.ts` 核心库；`src/app/{layout,page,error,not-found}.tsx` + `api/health/route.ts` shell；`tests/unit/*` 44 cases + `tests/integration/db-smoke.test.ts` 5 cases（真连 Neon）全部通过；`tsc --noEmit` 0 错误；`next build` (Turbopack) 成功产出 3 路由；`next start` + HTTP 冒烟：`/api/health` 200 & `db.ok:true`，`/` 200 22129 字节含实时表计数，`/definitely-not-here` 404
- [x] **Phase 4 里程碑 3：设计 tokens + 基础 UI 组件库 + API 客户端层 + Proxy 中间件**——`globals.css` 颜色/圆角/阴影 tokens（亮暗双模式）；`src/components/ui/*` 11 个组件（Button/Card/Badge/Input+Field/Alert/Skeleton+Spinner/Container+Separator）+ `/ui` 演示页；`src/lib/{cn,api-client,validation,request-id}.ts`（统一 fetch + AppError 映射 + 重试、Zod 校验集含 11 个与 prisma 同步的业务枚举、request-id 追踪）；`src/proxy.ts`（Next.js 16 Proxy：request-id 注入下游 + 结构化访问日志 + 安全头）。`tests/unit/*` 扩到 **125 cases** + 集成 5 cases 全绿；`tsc --noEmit` 0 错误；`next build` 产出 **4 路由 + Proxy**；HTTP 冒烟：request-id 服务端生成/客户端透传/非法拒绝重生成全通过，`/ui` 200 全组件渲染
- [x] **Phase 4 完成**——总控 Phase 4 交付清单（Web 骨架 / 数据库连接 / 基础 UI / 环境变量 / 日志 / 错误处理 / 测试框架）全部达成
- [x] **Phase 5 里程碑 1：公共页面地基**——页面级布局组件 `src/components/page/*`（PageHeader/Breadcrumb/EmptyState）；行业数据层 `src/server/industries.ts`（枚举↔slug 映射 + `getIndustryCaseCounts` groupBy 计数 + DB 失败降级）；`/industries` 列表（force-dynamic 实时计数）、`/industries/[slug]` 详情（SSG + `dynamicParams=false` 保证真 404）、`/about` 关于、`/privacy`+`/terms` 结构化占位（明确标注待法务审定，宪法第 21 条）；导航/页脚更新（移除死链）。`tests/unit` 扩到 **140 cases** + 集成 **7 cases** 全绿；`tsc`/`eslint` 0 错；`next build` 产出 **6 新路由**（含 7 个 SSG 行业页）；HTTP 冒烟 **9/9**（含 `/industries/not-a-real-one` 真 404）。（本里程碑曾引入的根 `loading.tsx` Suspense 骨架已在里程碑 2 移除，见下）
- [x] **Phase 5 里程碑 2：案例/方案公共页 + DEMO 标注种子 + 真 404 修复**——DEMO 可见性层 `src/server/demo.ts`（默认排除、`?demo=1` 显式纳入并打角标，规避 Prisma `{not}` NULL 陷阱）；DEMO 种子脚本 `prisma/seed.ts`（`npm run db:seed`，**仅 6 个标注【DEMO】的案例，按创始人裁决不种子任何方案**——方案涉及定价/购买闭环须走真实多角色流水线，宪法第 20 条；生产环境拒跑、幂等清理重建）；案例数据层 `src/server/cases.ts` + 方案数据层 `src/server/solutions.ts`（`$transaction[findMany,count]` + 行业筛选/分页/排序 + DEMO 门控 + 详情含证据分层/能力关联/财务/未知变量，金额两位小数精显）；`/cases`+`/cases/[id]`+`/solutions`+`/solutions/[id]` 四页（购买按钮 disabled 占位，Phase 12 接入）；导航重新加回案例/方案链接。**关键修复：删除根 `src/app/loading.tsx`**——实测其 Suspense shell 会让 force-dynamic 详情页对无效 id 先 flush 200、`notFound()` 无法回退状态码（会导致无效 URL 被 SEO 误收录），移除后无效 id 真 404（SSG 路由本就免疫）。`tests/unit` 扩到 **147 cases** + 集成 **15 cases** 全绿；`tsc`/`eslint` 0 错；`next build` 产出 **4 新动态路由**；HTTP 冒烟 **14/14**（含 3 个真 404 + `?demo=1` 种子可见/默认隐藏）
- [x] **Phase 5 里程碑 3：搜索系统 + 首页组装（Phase 5 收官）**——搜索编排层 `src/server/search.ts`（`searchPublic` 用 `Promise.all` 并行案例 + 方案、聚合 `ok`/`hits`、DB 失败软降级为 `ok:false`）；`src/server/{cases,solutions}.ts` 列表新增可选关键词维度 `q`，重构出 `buildCaseWhere`/`buildSolutionWhere` 用 `{AND:[...]}` 容纳"关键词 OR 片段"与"DEMO OR 片段"（兄弟 OR 键不能并列）；`/search` 页（无 JS 可用的 GET 表单 + 行业 chips + 引导/非法/失败/空/结果 五态分离 + "查看全部 N 个"深链到 `/cases?q=`·`/solutions?q=`）；首页 `src/app/page.tsx` 照总控 §6 从 Phase 4 骨架整体重写为六段体验（Hero + 内联搜索 / 今日全球产业案例 / 今日产业解决方案 / 企业 AI 产业诊断 V1-B 即将开放 / 我们如何工作六步 / 按行业浏览），移除 Phase 4 遗留的 pg_stat 调试面板；`/cases`+`/solutions` 加关键词回显条；导航/页脚加 `/search`。V1 为关键词匹配（标题+摘要 ILIKE），语义搜索按宪法第 4 条 MVP 延后。`tests/unit` 扩到 **152 cases** + 集成 **18 cases** 全绿；`tsc`/`eslint` 0 错；`next build` 产出 `ƒ /search`；HTTP 冒烟 **31/31**（含 2 个真 404 回归 + `?demo=1` 命中种子标题/默认不泄露）
- [x] **Phase 5 完成**——总控 Phase 5「公共页面」交付清单（首页/行业/案例列表/方案列表/搜索/关于/隐私/协议）全部达成；V1-A 商业闭环的"用户查看"入口层（发现 → 检索 → 详情）打通，"购买"仍为 Phase 12 占位
- [x] **Phase 6 里程碑 1：用户系统最小闭环（注册 / 登录 / 会话 / 账号页）**——认证方案经创始人裁决选定 **Auth.js 自建**（总控 §21「用成熟现成方案」）；`prisma/schema.prisma` 新增 `enum UserRole{USER/REVIEWER/ADMIN}` + `model User`（email unique / passwordHash / role 默认 USER），`Order` 加可空 `userId` 关系（`ON DELETE SET NULL`），迁移 `20260905000000_add_user_auth` 纯加性并已 `migrate deploy` 到 Neon（表 17→18、枚举 11→12）；口令哈希 `src/lib/password.ts` 用 **node:crypto scrypt**（N=16384,r=8,p=1，自描述串 `scrypt$N$r$p$salt$hash`，`timingSafeEqual` 定长比较，畸形/超长一律 false，**零额外依赖**、绝不存明文）；`src/lib/validation.ts` 加 Email/Password/Register/Login/UserRole schema（邮箱归一小写、注册强制 8–128 位、登录口令只要求非空防账号枚举）；`src/server/users.ts`（registerUser 判别联合 + P2002 归一 email_taken、getAuthUserByEmail、getProfileUserById 排除 passwordHash）；`src/auth.ts`（Credentials + JWT 会话，jwt/session 回调透传 id/role，`trustHost:true`，**不挂 Prisma Adapter**，JWT 类型增强打在 `@auth/core/jwt`）+ `api/auth/[...nextauth]` handler；`/login`·`/register`·`/account` 三页（`useActionState` 驱动的共用 `AuthForm` + `"use server"` actions：注册后自动登录、登录失败统一"邮箱或密码不正确"、账号页无会话重定向 /login + 登出）；导航/页脚加登录/注册/我的账号；`.env` 加 `AUTH_SECRET`、`.env.example` 认证段改写。`tests/unit` 扩到 **167 cases**（+password 8 +认证 schema 7）+ 集成 **23 cases**（+users 5）全绿；`tsc`/`eslint` 0 错；`next build` 产出 `ƒ /login`·`ƒ /register`·`ƒ /account` + `/api/auth/[...nextauth]`；**HTTP 认证冒烟 26/26**（csrf → callback/credentials → session(email/role/id, 不含 passwordHash) → 登录后 /account 200 → 错误口令/不存在邮箱无差别失败 → signout 清 cookie → /account 重定向）
- [x] **Phase 7 里程碑 1：案例评分内核（可复算公式 + 黄金样本 + 版本化文档）**——`src/server/scoring.ts` 纯函数内核把总控 §10「案例评分体系」落成可复算程序：`OPPORTUNITY_DIMENSIONS` 10 维度权重**逐字照抄总控 §10**（满分恒 100，`reduce` 求和防硬编码失真），`竞争强度`/`实施难度` 标为 **inverse** 反向极性（录入按直觉填负面强度、程序负责 `max-raw`，防人工填反方向），`computeOpportunityScore` 经 Zod 逐维度校验、非法 → `{ok:false,issues}` **指名维度绝不静默截断**；`computeEvidenceConfidence` 用类型权重（FACT 1.0/ASSUMPTION 0.5/INFERENCE 0.4/PREDICTION 0.3）+ 无来源打 0.6 折 + 缺省 confidence 50，`value=round(100·Σ(w·q·sf)/Σw)`（**总控只给示例值未给公式，故标注为本项目 v1 假设、参数可调**）；`countKeyUnknowns`=非 FACT 证据数；`computeCaseScores` 一次出三件套 + `CaseScoresSchema` 供 M2 持久化校验。`docs/SCORING_V1.md` 把每个数字标注为事实/假设/推断（宪法第 6 条），含两个逐维度可复算样例（机会 →88 与总控示例交叉验证一致、证据 →69）、**诚实记录已知缺口**（总控 §11 证据等级 S/A/B/C/D 轴尚未建模、权重未经真实案例校准），重申"综合评分 ≠ 项目一定成功"（规则 9）。`tests/unit/scoring.test.ts` **26 黄金样本**锁定（最优 100/全填 max 90/全填 0 10/样例 88/反向极性/越界拒绝/确定性/证据可信度精确 Σ）。`tests/unit` 扩到 **193 cases** + 集成 **23 cases**（无回归，本里程碑不动 DB/页面）全绿；`tsc`/`eslint` 0 错；`next build` 全路由无回归；版本 **0.9.0**
- [x] **Phase 7 里程碑 2：评分持久化 + 可复算**——`prisma/schema.prisma` 给 Case 加 `scoreInput Json?`（10 维度录入分=输入）与 `scoreBreakdown Json?`（computeCaseScores 完整输出=可审计结果），迁移 `20260905100000_add_case_score_fields` 纯加性已 deploy Neon（表数不变 18）；`src/server/case-scores.ts` 的 `recomputeCaseScores`/`recomputeAllCaseScores` 读输入+证据→复算→Zod 复核→写 breakdown 与两标量，判别联合 computed/skipped(无输入)/invalid(越界不写库)/not_found/error，**无 scoreInput 诚实跳过绝不编造**；`scripts/recompute-scores.ts`（`npm run db:recompute-scores`）批量复算 CLI 如实汇报各状态计数（实跑：6 个 DEMO 无输入全 skipped、exit 0）。`tests/integration/case-scores.test.ts` 6 cases（持久化读回/无输入跳过/非法不写库/幂等/全量汇总自洽）。**193 单元 + 29 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归；版本 **0.10.0**
- [x] **Phase 7 里程碑 3：评分拆解上详情页（可审计展示 + DEMO 端到端）**——`src/server/cases.ts` 的 `getPublicCaseById` 返回 `scoreBreakdown`（用 `CaseScoresSchema` 校验库中 JSON，缺失/非法诚实返 null 不展示无效评分）；`/cases/[id]` 新增 `ScoreBreakdownCard`——机会评分总分 + 10 维度贡献条（`inverse` 维度标「越低越好」并显录入值）+ 证据可信度/证据条数/关键未知变量数 + 证据构成角标 + 公式版本，无拆解则显示"暂未录入/复算、不展示任何推算结果"，并重申「综合评分 ≠ 项目一定成功」（总控 §10 / 规则 9）；`prisma/seed.ts` 给 6 个 DEMO 案例补 `scoreInput` 并**内联 `computeCaseScores`** 派生 breakdown 与标量（`db:recompute-scores` 现 6 案例全部由 skipped→computed）。新增集成测试 2 例。**193 单元 + 31 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归、HTTP 冒烟 `?demo=1` 详情页实测渲染 64/39/3；版本 **0.11.0**
- [x] **Phase 7 里程碑 4：§11 证据来源权威度分级（元数据 + 标签，不动分数）**——按创始人 2026-09-05 裁决「只记录 + 标签，不动分数」把总控 §11 的 S/A/B/C/D 来源分级建为可存/可查/可见元数据：`prisma` 加 `enum EvidenceGrade` + `Evidence.grade?`（纯加性迁移 `20260905110000_add_evidence_grade` 已 deploy Neon，表数仍 18、枚举 12→13）；`validation.ts` 加 `EvidenceGradeSchema`；`getPublicCaseById` 随证据透出 grade，`/cases/[id]` 每条证据叠来源等级徽章（S/A/B/C/D 各色）并对「存在 D 级」「FACT 仅 D 级支撑」给 `Alert` 复核告警（呼应 §11「D 级不得表述为已确认事实」）；`prisma/seed.ts` 6 DEMO 案例 16 证据补 grade（保留 ai_vision FACT=D 演示告警）。**可信度公式与 `SCORING_RUBRIC_VERSION` 保持 1.0.0 不变**（不并入打分、历史 breakdown 仍有效、无需重算）。测试：`validation.test` +1、`db-smoke` 枚举 12→13、`cases-solutions.test` 断言 grade 透传；基线 **194 单元 + 31 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归、grade groupBy 实测 A1/B2/C2/D10/S1、HTTP 冒烟 `?demo=1` 渲染「来源 D/B」+ D 级告警；版本 **0.12.0**
- [x] **Phase 7 里程碑 5：案例与证据数据层 CRUD（受校验写入 + 审计 + 复算联动）**——`src/server/case-admin.ts`（server-only）提供 `createCase`（可内联证据）/`updateCase`（version 自增）/`deleteCase`（守卫：仍挂 PUBLISHED 方案则 `blocked` 拒绝、删前快照）/`addCaseEvidence`/`removeCaseEvidence`；入参 Zod 校验复用 `validation.ts` 枚举单一真源（`scoreInput` 只校结构、10 维越界交评分内核裁决）；每个写操作事务内落 `ChangeLog`（CREATE/UPDATE/DELETE + before/after + actor），凡影响评分的写入自动 `recomputeCaseScores` 联动复算并如实回报 `computed|skipped|invalid|none|error`；统一判别联合、Prisma P2003/P2025 归一、不抛裸异常。**本模块不做鉴权**——对外 HTTP 写路由与角色门禁有意延后 Phase 13（无鉴权公开写端点违反安全底线）。新增集成测试 8 例（真连 Neon，afterAll 按外键序清理）。基线 **194 单元 + 39 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归；版本 **0.13.0**
- [x] **Phase 6 里程碑 2：RBAC 鉴权原语 + `/admin` 后台门禁（双层防御）**——把"是谁"（M1 认证）补成"能干什么"（授权），满足总控 §13「后台必须有权限控制 + 管理员操作记录 Audit Log」，并为 M5 案例 CRUD 的 HTTP 化、Phase 12 订单确认提供可复用 `requireRole` 前置。`src/server/authz.ts`：`hasRole` 纯函数（权限测试主战场）+ `getCurrentUser`（角色**只从 JWT 会话读**、绝不信任客户端）+ `requireUser`/`requireRole(allowed)` 判别联合（unauthenticated/forbidden/ok，越权记非敏感审计 warn），`STAFF_ROLES=[REVIEWER,ADMIN]`；`src/server/admin.ts` `getAdminDashboardData` 只读聚合（**总数由各 groupBy 求和**得，天然自洽且少 4 次查询）；`/admin` **layout 拦未登录跳 /login + 越权渲染「无访问权限」面板不渲染 children，page 取数据前再自鉴权（不过则 `return null`）**——HTTP 冒烟实测发现"仅 layout 拦截时 Next 仍会为 leaf page 段生成 RSC flight 负载泄露概览"，遂加页面自守构成 layout+page 双层防御，敏感聚合越权时根本不被调用；`/account` 对员工条件显「进入管理后台」；`scripts/promote-user.ts`（npm `user:promote`）受控提权 CLI（只改 role、账号不存在不自动建号，注册仍永远只建 USER）。新增单元 15（vi.mock 会话）+ 集成 4（真连 Neon）。基线 **209 单元 + 43 集成**全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /admin` 无回归、**HTTP 门禁冒烟 15/15**（未登录跳登录 / USER 得无权限且不泄露概览 / ADMIN 得概览+审计表）；版本 **0.14.0**
- [x] **Phase 8 里程碑 1：方案数据层 CRUD（受校验写入 + 审计 + 发布守卫）**——把 Phase 7 M5 案例侧的 CRUD 模式横向复制到方案侧，让刻意"不灌 DEMO 方案"（宪法第 20 条：涉及定价/购买必须真实多角色流水线）而 0 行的 Solution 表有真实写入口，为 Phase 8 M4 AI 流水线与 Phase 13 后台 UI 打底。`src/server/solution-admin.ts`（server-only）：`createSolution`（**强制 status=DRAFT**、不允许直建 PUBLISHED，case FK 预检 + slug 冲突 P2002 归一 invalid）/`updateSolution`（version 自增、状态迁移、`publishedAt` 只在非 PUBLISHED→PUBLISHED 首次打时间戳，二次更新不刷新）/`deleteSolution`（守卫：**任一 Order 关联即 blocked**，V1 无退款流程属 ROADMAP #5）/`addSolutionFinancial`·`removeSolutionFinancial`（Decimal 一律 string 输入避 JS 浮点污染）/`addSolutionUnknown`·`removeSolutionUnknown`（**自动同步 `Solution.unknownVariableCount` = 表内实时条数**，规则 6/9：不确定性显式列出）；每个写操作 `$transaction` [写库 + 写 `ChangeLog`(entityType=Solution, before/after+actor+reason)]；**发布守卫 `publishGuard`（宪法第 21 条）**：升 PUBLISHED 需 price 非 null + 有 riskDomains 时必勾 needsProfessionalReview，否则返 `blocked` 且不改状态不落 publishedAt；**不做鉴权**（延 Phase 13 用 M2 的 `requireRole` 原语薄包一层）；**不自动评分**（方案侧公式形态未定，M1 留 null/人工填，`unknownVariableCount` 是唯一自动派生标量因为它是可计算事实）；Prisma 错误归一 P2002→invalid(slug)、P2003→invalid(relation)、P2025→not_found，判别联合不抛裸异常。新增集成 9 例（真连 Neon，afterAll 按外键序 order→solution→case→changeLog 兜底 + runId）。基线 **209 单元 + 52 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归；版本 **0.15.0**
- [x] **Phase 8 里程碑 2：方案 34 分节正文（Solution.body）结构化渲染**——总控 §3「Solution Package」已逐字给定一份可售方案的 34 分节（ROADMAP §8 的规范来源），M1 已能写 body 但详情页只占位不渲染。`src/server/solution-body.ts`（新，纯函数零 DB）：`SOLUTION_SECTIONS` 34 节单一真源（稳定 key + 照抄总控的中文 title），`parseSolutionBody` 把任意 body 归一为固定 34 节有序视图——按 key 或中文 title 命中即 filled、空值（空串/空数组/空对象/null/undefined）判 pending（数字 0/布尔 false 算有内容）、契约外未知键收进 `extras` 不静默丢弃、回 `filledCount/empty`。`getPublishedSolutionById` 透出 `body:ParsedSolutionBody`；`/solutions/[id]` 新增 `SolutionBodySection`——空 body 出**诚实占位**（须走多角色流水线、禁单模型直出，同构 Phase 7 M3 `scoreBreakdown=null`），有 body 按序渲染 34 张分节卡 + 完成度徽章 n/34 + 未填标「待补充」+ extras 审计提示；`renderSectionContent` 兼容字符串/标量/数组/嵌套对象。刻意保留与结构化卡片（财务/未知变量/证据）的可控冗余，待流水线接入再收敛。**不动 schema、不开 HTTP 写端点**。`tests/unit/solution-body.test.ts` 11 例锁死归一契约。基线 **220 单元 + 52 集成**全绿、`tsc`/`eslint` 0 错、`next build` 路由清单不变、**HTTP 渲染冒烟 15/15**（临时发布带 body 方案挂既有 DEMO 案例→验证分节渲染各类型内容 + 空 body 占位→即时硬删、leftover=0）；版本 **0.16.0**
- [x] **Phase 13 里程碑 1：后台「写」HTTP 端点（requireRole + CSRF 统一门禁）**——兑现 Phase 7 M5 / Phase 8 M1 两期数据层「不做鉴权、HTTP 写路由延 Phase 13」的安全承诺，把「谁在写」收敛到唯一门禁 + 翻译点 `src/server/api-guard.ts`：`actorOf` 从服务端会话派生 `human:<id>`（绝不信客户端传入 actor/role）、`isSameOrigin` 纯函数做 CSRF 同源判定（无头放行/有头 host 精确相等/解析失败拒）、`requireStaffWrite` 先挡跨站（403 且不查角色）再 `requireRole(STAFF_ROLES)`（未登录 401 / USER 403 带 required）、`mutationResponse` 把数据层判别联合统一翻译（ok→200、invalid→400、not_found→404、blocked→409、error→500 生产屏蔽）。10 个薄封装写路由（案例 create/update/delete + evidence add/remove、方案 create/update/delete + financial/unknown add/remove），**零业务逻辑、零 schema 变更**——校验/审计/守卫/复算全在已测数据层。**仍不含限流/幂等**（V1 延后）。`tests/unit/api-guard.test.ts` 18 例（`vi.mock` authz 锁门禁与状态映射）。基线 **238 单元 + 52 集成**全绿、`tsc`/`eslint` 0 错、`next build` 新增 10 条 `ƒ /api/admin/**` 无回归、**HTTP 端到端冒烟 24/24**（未登录 401 / USER 越权 403 / 跨站 CSRF 403 / 非法入参 400 / ADMIN 全链路写成功且审计 `changedBy=human:<adminId>`、临时数据 leftover=0）；版本 **0.17.0**
- [x] **Phase 13 里程碑 2：最小后台管理 UI（案例「列表 + 新建」竖切，消费 M1 写端点）**——补上「方向一」的另一半：让 M1 那批 `/api/admin/**` 端点获得真实调用方。`src/server/admin-cases.ts` `listAdminCases()` 后台**全量**读层（含 CANDIDATE/KEY_RESEARCH 内部阶段 + DEMO 夹具、`updatedAt` 倒序、`_count` 带证据/方案数、行业 enum→可读名；V1 不分页但硬上限 500、超出如实标 `truncated`；刻意不复用公开橱窗 `listPublicCases`——那门控掉了内部态与 DEMO，后台要看全量真相）；`src/components/admin/NewCaseForm.tsx`（client）`fetch` 到 `POST /api/admin/cases`——**刻意走 HTTP 端点而非另起 Server Action**，避免把 M1 的 requireRole/CSRF/结果翻译抄第二遍导致漂移（第 16 条），成功 `router.refresh()`、400 `details.fields` 逐字段回显、401/403/500 顶部如实报错；`src/app/admin/cases/page.tsx` layout+page 双层防御（越权 `return null`、无权限时 `listAdminCases` 根本不被调用）、`/admin` 加「案例管理」入口。**有意最小竖切**：详情页/证据增删/删除案例/方案管理留 M3（复用已就绪端点）。无 schema 变更、无新端点。新增集成 `admin-cases.test.ts` 4 例（内部阶段+DEMO 可见、_count 与建表一致、倒序+truncated 自洽、行业中文名）。基线 **238 单元 + 56 集成**全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /admin/cases` 无回归、**HTTP 渲染冒烟 18/18**（未登录跳登录 / USER 得无权限且不泄露表单与真实标题 / ADMIN 见表单+列表且服务端真实标题透传进 SSR HTML、leftover=0）；版本 **0.18.0**
- [x] **Phase 12 里程碑 1：订单「数据层」——购买闭环的可信底座（不含 HTTP/UI）**——把详情页那个 `disabled` 的「购买」假按钮背后断掉的"购买"环补上第一块真砖。V1 购买流按总控 §5/PRODUCT_SPEC 明确为「下单 → 支付说明（站外、无网关）→ 后台人工确认 PAID → 解锁」，**刻意不接支付网关**（ROADMAP #5 非阻塞），且 `Order` 表早已随 schema 建好（状态机 + `amount Decimal` + `paymentProvider` 预留）→ **零 schema 迁移**。`src/server/orders.ts`（server-only，判别联合返回、不抛裸异常、不做鉴权信任上层门禁）：`createOrder` **金额只从服务端读取的 `Solution.price` 快照进 `Order.amount`（`toFixed(2)` 两位小数字符串，不用 JS 浮点），客户端传入的 amount/currency/status 因不在 Zod 白名单被剥离**（守"钱"的路径，宪法第 7/20 条）+ 仅 `PUBLISHED` 且有价方案可下单 + **同买家同方案重复下单幂等去重**（已有 PENDING 复用 `deduped:true`、已 PAID 返 blocked 不重复收钱）+ 身份 userId/归一 buyerEmail 至少其一；`confirmOrderPaid`（仅 PENDING→PAID、paidAt 只首次落库、二次幂等不刷新、version 自增）/`cancelOrder`（仅 PENDING→CANCELED，V1 无退款 PAID 不可撤属 #5）/`getOrderById`/`listOrdersForBuyer`（OR userId/email 找回）/`listOrdersForAdmin`（状态过滤 + 分页自洽）/`hasPaidEntitlement`（**解锁判定**，无身份/DB 异常保守 false）；每个写操作 `$transaction`[写库 + `ChangeLog`(entityType=Order)]、P2003→invalid(relation)/P2025→not_found。无新 HTTP 端点（延 M2 走 `api-guard`）、无 UI。**修测记录**：集成测试 ⑨ 反例邮箱初写 `buyerEmail`（该邮箱在 ⑥ 已真实 PAID 故 entitlement 返 true 是代码对、断言错），改用从未下过单的 `stranger` 后绿。新增集成 `orders.test.ts` 10 例 + 无 DB 单测 `orders-schema.test.ts` 7 例（锁 schema 白名单/身份/归一/枚举契约）。基线 **245 单元 + 66 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归；版本 **0.19.0**
- [x] **Phase 12 里程碑 2：订单「HTTP 端点」——购买闭环经 HTTP 打通（复用 api-guard，不含 UI）**——把 M1 数据层用已就绪的 `api-guard` 门禁薄薄包一层，零业务、零 schema。`api-guard.ts` 抽出共享纯函数 `sameOriginBlock`、**新增 `requireSameOriginActor`**（只做 CSRF 同源 + 会话注入身份、**不设角色门禁**——下单是任何登录用户/游客都可做的公开动作，与后台 staff 写分层）、`MutationLike` 显式补 `orderId/deduped/order`。4 路由：`POST /api/orders`（公开，先 `delete raw.userId/actor` 再从会话注入，客户端伪造身份被丢弃；金额防篡改由 M1 白名单守）、`GET /api/admin/orders`（staff 只读，`requireRole`+分页+状态过滤）、`POST /api/admin/orders/[id]/confirm`·`/cancel`（staff 写，`requireStaffWrite`）；全 `force-dynamic`、Next16 `params:Promise` await、只「门禁→调数据层→`mutationResponse`」。**`hasPaidEntitlement` 刻意不暴露为公开端点**（无额外鉴权会引 IDOR），留 M3 在 RSC 服务端调用。`tests/unit/api-guard.test.ts` 扩到 **22 例**（+`requireSameOriginActor` 3 例、mutationResponse 透传 1 例）。基线 **249 单元 + 66 集成**全绿、`tsc`/`eslint` 0 错、`next build` 新增 **4 条 `ƒ` 动态路由**无回归、**HTTP 端到端冒烟 29/29**（`smoke-orders.mjs`：金额服务端快照防篡改、游客/登录幂等去重、跨站 CSRF、伪造 userId 被覆盖、后台 401/403/200 门禁矩阵、确认 PAID+审计、重复确认幂等、已购再下单 409、取消状态机、状态过滤，末尾 leftover=0）；版本 **0.20.0**
- [x] **Phase 12 里程碑 3：购买闭环「用户界面」——详情页解锁门控 + 支付说明 + 我的订单 + 后台确认台**——给 M2 端点接上"人面"，零 schema、零新数据层逻辑，全复用已测 `orders.ts`+`api-guard`。`solutions.ts` 的 `SolutionDetail` 加 `isFree`（price 空/0 即免费）；`components/solutions/BuyButton.tsx`（client）`fetch` `POST /api/orders` **只提交 `solutionId`**（身份服务端注入、金额服务端快照，杜绝改价），成功跳支付说明页、409/401/错误如实提示；`/solutions/[id]` 服务端算 `entitled=isFree||isDemo?true:hasPaidEntitlement(...)`、购买卡四态渲染（免费·DEMO→开放徽章 / 已购→已解锁+`#solution-body`锚 / 待购登录→BuyButton / 游客→登录引导），**`SolutionBodySection` 加 `locked` 入参——未解锁时服务端根本不渲染 34 分节、只出锁面板**（正文是付费交付物，前端无从绕过）；`/orders/[id]` RSC 支付说明页（属主校验：userId 命中或 email 命中，不匹配统一 `notFound()` 防 IDOR，展示金额快照 + V1 站外付款三步指引，未接网关）；`/account/orders` 我的订单（登录门禁 + `listOrdersForBuyer` 属主过滤）+ `/account` 入口；`/admin/orders` 后台确认台（layout+page 双层防御 + 状态过滤/分页）+ `components/admin/AdminOrderActions.tsx`（确认收款/取消消费 M2 端点 + `router.refresh()`）+ `/admin` 订单卡入口。V1 有意识假设：**只有 price>0 且非 DEMO 方案正文才锁**（免费/DEMO 恒开放）、**购买需登录**（游客下单数据层支持但 UI 不暴露）、人工确认到账即解锁不接网关。`cases-solutions.test.ts` 扩 `isFree` 断言（¥1999=false、新种 price=0 免费=true）；基线 **315（249 单元 + 66 集成）**全绿、`tsc`/`eslint` 0 错、`next build` 新增 **3 条 `ƒ` 路由**无回归、**购买闭环 UI 端到端冒烟 29/29**（`smoke-orders-ui.mjs`：钱-shot——付款前游客/未购/他人详情页 HTML 全不含正文标记显锁面板、后台确认 PAID 后仅属主标记出现且显已解锁、他人访问该单支付页 404 防 IDOR、我的订单按属主隔离、后台 USER 无权限/ADMIN 见单、末尾 leftover=0）；版本 **0.21.0**。**🎉 Phase 12 订单购买闭环全部达成（M1 数据层 + M2 端点 + M3 UI），「用户查看 → 购买 → 支付说明 → 后台确认 → 解锁读正文」商业闭环主干从用户侧端到端可用**
- [x] **Phase 13 里程碑 3：后台「方案管理 UI」——方案从录入到可售的运营入口（消费 M1 写端点）**——Phase 12 刚打通购买闭环，但方案在后台还只能裸 API 建、无人面入口，"方案生成→查看→购买"对运营仍断链。本里程碑补齐后台方案运营入口让闭环真正可操作。`src/server/admin-solutions.ts` `listAdminSolutions()` 后台**全量**读层（含 DRAFT/UNDER_HUMAN_REVIEW/挂 DEMO 案例方案、`updatedAt` 倒序、`include case` 带案例标题·行业·`isDemo`、`_count` 带财务/未知/订单数、价格 `toFixed(2)` 两位小数含未定价 null、硬上限 500+`truncated`；刻意不复用公开橱窗 `listPublishedSolutions` 只 PUBLISHED）；`NewSolutionForm.tsx`（client）`fetch` `POST /api/admin/solutions`（**走 HTTP 不另起 Server Action 防漂移第 16 条**；标题/slug/挂靠案例下拉/价格/币种/英文名/高风险领域/摘要/需专业确认勾选，案例选项由服务端把 `listAdminCases` 作 props 传入），成功 `router.refresh()`、400 逐字段回显、无案例禁用并引导去案例管理；`PublishSolutionButton.tsx`（client）`PATCH .../solutions/[id]` `{status:"PUBLISHED"}`——**发布守卫在数据层**，缺价/涉高风险未勾确认→409 `details.fields` 如实回显「发布被拦：<原因>」；`/admin/solutions` page layout+page 双层防御（越权 `return null`、无权限时读层根本不被调）+ 全量方案卡片列表 + 发布按钮；`/admin` 解决方案卡加入口。**有意最小竖切**：方案正文 34 分节 / 财务 / 未知变量 / 删除的逐条录入留 M4。无 schema 变更、无新写端点、无删除文件。新增集成 `admin-solutions.test.ts` 4 例；基线 **319（249 单元 + 70 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /admin/solutions` 无回归、**HTTP 渲染 + 金钱 shot 冒烟 33/33**（`smoke-admin-solutions.mjs`：未登录跳登录 / USER 无权限不泄露表单与列表 / ADMIN 见表单+列表 / 建未定价与带价草稿均 200 且标题透传进 SSR、显「未定价」徽章 / **发布未定价→409 含 price 原因、发布带价→200 且 DB PUBLISHED+publishedAt、已发布标题进入公开 `/solutions` 橱窗（闭环打通）未发布草稿不现** / 非法 400、跨站 403 CSRF、未登录 401、`/admin` 见入口链接，末尾 leftover=0）；版本 **0.22.0**
- [x] **Phase 13 里程碑 4：后台「方案内容编辑台」——把可售方案的付费交付主体变得可录入（复用 M1 写端点）**——M3 能建/发布方案，但正文 34 分节 / 财务测算 / 关键未知变量 / 删除此前只能裸 API 写，卖的是空壳、直接拖累「方案质量」。M4 把一份可售方案的全部可编辑面收敛到一处，零 schema、零新数据层写逻辑。`src/server/admin-solutions.ts` 新增 `getAdminSolutionDetail(id)`（早退挡无形 id 探测、带 case/financials/unknowns 倒序 + `_count:{orders}`、所有 Decimal `toFixed(2)`、`unknownVariableCount`/`financialCount` 取实时数、`isDemo` 由关联案例判定、body/unknowns 原样带回供编辑初值；**不鉴权**信任已过 `requireRole` 的后台页）；`src/components/admin/mutate.ts` 共享 `mutateJson`/`fieldHints` 收敛「fetch + 解析 api-guard 判别联合」供五组件复用（第 16 条防漂移）；`EditSolutionMetaForm`（PATCH 基本信息、**刻意不提交 status**）、`SolutionBodyEditor`（按 `SOLUTION_SECTIONS` canonical key 渲染 34 受控 Textarea、非空节写入空节省略、`{`/`[` 起头试 `JSON.parse`、34 外历史键原样透传不丢弃 + 显式提示「保留 N 个契约外字段」、已填 n/34 徽章）、`AdminSolutionFinancials`/`AdminSolutionUnknowns`（行内 POST/DELETE 增删、严重度 0–100 客户端先校验、`router.refresh()` 让服务端计数重取）、`DeleteSolutionButton`（`orderCount>0` 提前拦、被守卫拒时回显「仍有 N 条订单」）；`/admin/solutions/[id]` RSC 双层防御（越权 `return null` 前绝不调读层免正文进 flight）+ noindex；列表行加「编辑内容 →」链接。**顺带修一处真实竞态**：`listAdminSolutions`/`listAdminCases` 的 `$transaction([findMany, count])` 各有 READ COMMITTED 快照，并发插删可致 `total<items` 矛盾 → `total=Math.max(rawCount,items.length)` 兜底。新增集成 `admin-solution-detail.test.ts` 5 例；基线 **324（249 单元 + 75 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /admin/solutions/[id]` 无回归、**内容编辑台端到端冒烟 33/33**（`smoke-admin-solutions-edit.mjs -p 3126`：SSR 四块齐、正文+价 PATCH 200 初值回读、新增财务/未知变量回读见「严重度 60/100」、**买家下单后删方案→409 含「仍有…订单」、无订单草稿删→200 后详情 404**、跨站 403 CSRF、USER 无权限且不泄露正文/财务表单、未登录 307、不存在 id 404、末尾四项 leftover=0）；版本 **0.23.0**
- [x] **Phase 13 里程碑 5：后台「案例内容编辑台」——案例元信息 + 证据增删 + 删除的闭环 UI（复用 M1 写端点）**——M2 只能「列表 + 新建」案例，M1 的 PATCH/DELETE/证据端点却无 UI 消费；案例是"发现→拆解→证据可信度"最上游数据源，缺编辑台卡住后续评分与方案。M5 把一份案例的可编辑面收敛一处，零 schema、零新数据层写逻辑，与 M4 方案编辑台合龙成"案例 + 方案"两条后台内容竖切。`src/server/admin-cases.ts` 新增 `getAdminCaseDetail(id)`（早退挡无形 id、带 `evidences` 正序 + `_count:{solutions}` + `solutions:{where:PUBLISHED}` + `scoreBreakdown`、`isDemo` 由自身 `sourceType` 判定、证据 null 如实保留、`publishedSolutionCount` 作删除守卫依据、评分标量只读透传；**不鉴权**）；`EditCaseMetaForm`（PATCH 基本信息、**刻意不提交 `scoreInput`**——10 维度评分留 M5b，复用 `mutate.ts`）、`AdminCaseEvidences`（证据逐条列 + 行内 `POST/DELETE`、置信度 0–100 客户端先校验、增删在数据层**联动 `recomputeCaseScores`**、`router.refresh()` 重取可信度标量、置信度渲染为单模板串 `` {`置信度 ${x}/100`} `` 规避 SSR `<!-- -->` 断言坑）、`DeleteCaseButton`（案例删除守卫专指"仍挂 PUBLISHED 方案"→服务端 409、`publishedSolutionCount` 作提示、被拒回显「仍有 N 个已发布方案」）；`/admin/cases/[id]` RSC 双层防御（越权 `return null` 前绝不调读层免未公开案例进 flight）+ 只读评分快照卡 + noindex；列表行加「编辑内容 →」链接。顺带修 `NewSolutionForm` 内链裸 `<a>`→`<Link>`（新增 `[id]` 路由后 `no-html-link-for-pages` 开始拦截）。新增集成 `admin-case-detail.test.ts` 5 例；基线 **329（249 单元 + 80 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /admin/cases/[id]` 无回归、**案例编辑台端到端冒烟 33/33**（`smoke-admin-cases-edit.mjs -p 3127`：SSR 三块齐 + 证据回填「置信度 80/100」连续、PATCH 标题/阶段 200 透出改名与「重点研究」、新增 FACT→DB 确证 `evidenceConfidence` 被复算（**关键发现：recompute 有意 gated on `scoreInput`，无输入分诚实跳过（第 20 条），故预置合法 10 维 `scoreInput` 才真实演示联动**）、删证据后陈述消失、**案例挂 PUBLISHED 方案→删 409 含「仍有 1 个已发布方案」、撤方案后删 200 后详情 404**、跨站 403 CSRF、USER 无权限且不泄露「证据（案例拆解）」块、未登录 307、不存在 id 404、末尾四项 leftover=0）；版本 **0.24.0**
- [x] **Phase 13 里程碑 5b：后台「案例 10 维度机会评分录入台」——把造好的评分内核接上操作龙头（复用 M1 写端点）**——评分内核 Phase 7 早建成并锁 26 黄金样本，但后台从无录入 `scoreInput` 的 UI，运营只能靠种子/裸 API 打分，"拆解→打分→排优先级"在人工侧断链。M5b **零 schema、零新数据层写逻辑、零公式/权重改动**（故不触碰黄金样本与 `SCORING_RUBRIC_VERSION`），全复用 `PATCH /api/admin/cases/[id]`→`updateCase`→`recomputeCaseScores`。`src/server/admin-cases.ts` 的 `getAdminCaseDetail` 增透 `scoreInput`（脏形状诚实降 null 供表单取初值）+ `scoreBreakdown`（对持久化 json 再走 `CaseScoresSchema` 防御性复核，漂移降 null 不渲染半截数据，故 `hasScoreBreakdown` 与 `scoreBreakdown` 可不等价）；`src/components/admin/CaseOpportunityScoreForm.tsx`（client）10 个受控数字输入、维度/满分/极性全由服务端从 `OPPORTUNITY_DIMENSIONS` 常量传入（不抄权重防漂移）、校验对齐内核（0..max 整数、10 项齐全才可提交）、反向维度标「越低越好」由程序 `max-raw`、实时预览合计仅辅助**最终分以服务端复算为准**（`router.refresh()`）；`/admin/cases/[id]` 挂录入台 + 新增服务端只读「评分明细（服务端复算）」审计卡（逐维度表 + 合计 + 公式版本 + 按 FACT/假设/推断/预测分类证据数 + 关键未知数 + 越界 issues 告警 + 「综合评分 ≠ 成功」免责，数字比值渲染为单模板串规避 SSR `<!-- -->`）。`admin-case-detail.test.ts` 扩至 6 例（新夹具黄金 10 维→机会分 **74**、可信度 **69**、未知 **0** 三处一致；历史漂移 json 降 null）；基线 **330（249 单元 + 81 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 无新路由无回归、**评分录入台端到端冒烟 32/32**（`smoke-admin-cases-score.mjs -p 3128`：无评分显录入台+「尚未评分」不渲染明细卡 → PATCH 合法 10 维 `recompute=computed`、DB 机会分 74、SSR 出明细卡含「（反向）」→ 加 FACT 证据可信度复算 90 → **越界 `commercialValue:99` `recompute=invalid` 但 DB 机会分仍 74 未被污染** → 跨站 403 CSRF、USER 403、USER 不泄露录入台、未登录 307、末尾四项 leftover=0）；版本 **0.25.0**
- [x] **Phase 14 里程碑 1：SEO 元数据契约 + `robots.txt` / `sitemap.xml`（环境门控，先不开 JSON-LD）**——商业闭环「…→ 用户查看 → 购买」的上游获客依赖搜索引擎可发现性，但此前根 layout 写死 `robots:{index:false}`、无 robots/sitemap、各页无 canonical/OG（根 layout 注释本身即预告「Phase 14 打开 SEO 时再改」）。M1 **零 schema、零新数据层写逻辑**，只立元数据契约。`src/lib/site.ts`（新，client/server 两侧安全）作站点级 SEO **单一真源**——`SITE_URL/SITE_NAME/SITE_DESCRIPTION` + 纯函数 `isIndexable()`（**环境门控**：仅「生产构建 **且** `NEXT_PUBLIC_SITE_URL` 为非本地域名」才放开，localhost/127.0.0.1/0.0.0.0/::1/解析失败一律保守不索引；空串/纯空白归一回落本地，修 `??` 只兜 null 的坑）/`absoluteUrl()`/`seoMetadata()`（canonical + OpenGraph + Twitter card，缺 title/description 不写空字段、V1 无 OG 图不虚构）。根 `layout.tsx` 复用真源、`robots` 改由 `isIndexable()` 裁决；公开页（首页/行业列表/行业详情 SSG/案例列表/方案列表/about + 案例/方案详情 `generateMetadata` found 分支加 `canonical`+OG `type:article`、not_found 补 noindex、**DEMO 视图 `?demo=1` 恒 noindex**）；`/search` `{index:false,follow:true}`，隐私/条款仍各自 noindex（待法务审定占位草稿）。`src/server/seo.ts` `getSitemapEntries()` 只读枚举公开案例 + 已发布方案（AND 数组容纳 DEMO OR 片段、`take:4000`、**任一侧失败 `log.warn` 降级不拖垮整张 sitemap**）。`src/app/robots.ts`+`src/app/sitemap.ts` 两 MetadataRoute **均 `force-dynamic`**（关键教训：MetadataRoute 默认构建期快照，构建/运行域名不一致会致 robots 与 sitemap 状态分裂，故都转动态令同源）——不可索引 → robots `Disallow:/` + sitemap 空 urlset；可索引 → robots `Allow:/` + 禁抓私有前缀 + `Host`/`Sitemap`、sitemap = 静态页 + 7 行业页 + 动态案例/方案去重。新增 `tests/unit/site.test.ts` **17 例** + `tests/integration/seo.test.ts` **4 例**（真连 Neon，含「挂 DEMO 案例下的已发布方案被顺 case 排除」）；基线 **351（266 单元 + 85 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /robots.txt`+`ƒ /sitemap.xml` 无回归（行业 SSG 不受影响）、**SEO 端到端冒烟 20/20**（`smoke-seo.mjs` 起无域名/`example.com` 两实例对验二分枝）；版本 **0.26.0**
- [x] **Phase 9 里程碑 1：统一 Model Router 骨架（提供程序无关契约 + 确定性 StubProvider + 程序化成本记录，暂不接真实模型）**——宪法明令「模型经 Model Router 解耦、禁止绑定单一模型」「AI 成本须追踪（模型/tokens/耗时/预计成本/任务类型/成败）」「复杂模型只用于高价值推理」，总控 §16/§31/§32 亦把 Model Router 列为核心基础设施；此前全仓无任何模型调用抽象，Phase 8 M3（真实方案生成）/10（GitHub Scout）/11（企业诊断）一旦接入就会被迫各写各的、且无从统一计费与换模型。M1 是**纯加性竖切：零 schema、零 DB、零 HTTP 端点、零 UI、零新路由**，只立"业务代码只调 `runTask`、绝不直接 `import` 具体 SDK"的解耦契约，且**刻意不依赖任何 API Key**（真实 provider 阻塞 ROADMAP #4，故先用离线确定性 `StubProvider` 把契约跑满可测）。`src/server/model-router.ts`：`MODEL_TASK_KINDS`（6 类任务）/`MODEL_TIERS`（low/medium/high 三档）+ `ROUTING_POLICY`（任务类型→档位的**版本化路由策略**，如 `research→high`、`generate_text/embedding→low`）+ `MODEL_CATALOG`（每档 `ModelSpec` 含 `inputPer1kUsd/outputPer1kUsd` 单价，**单一真源**）；`estimateCost(model,usage)` 纯函数按单价×tokens 算美元、负数/NaN 归零、保留 ≤6 位小数（**成本由程序计算非 LLM 口算**，呼应宪法第 7 条）；`ChatProvider` 接口 + `StubProvider`（同 prompt 必得同输出、`respondWith→json` 透传、`embedding`→FNV-1a 派生的 dim-8 单位向量、否则 `[stub:<kind>] <prompt>`，**让全套契约在无 Key 下可确定复算**）；`CallRecord`（taskId/providerId/modelId/tier/taskKind/latencyMs/usage/estimatedCostUsd/status/agent?/caseId?/solutionId?）+ `CallRecorder` 可插拔记录器（`loggingRecorder` 默认走 `logger`、**绝不落密钥明文**；`createMemoryRecorder` 供测试累计 `totalCostUsd`，DB 持久化 `ModelCall` 表延 M2 避免投机建表）；核心 `runTask(req,deps)` 走判别联合返回 `ok | schema_invalid | provider_error`（provider 抛错被 try/catch 兜成 `provider_error` 不裸抛；带 `schema` 则 `safeParse` 校验结构化输出，§17 落地，非法回 `z.ZodError["issues"]` 但 `CallRecord` 仍照常产出计费），`finalize` 无论如何都落一条成本记录；6 个薄封装 `generateText/structuredOutput<T>/research/embed/vision/code` 全委派 `runTask`。新增 `tests/unit/model-router.test.ts` **18 例**（常量守护 + `estimateCost` 黄金值 high 1K+1K=0.032/负数与 NaN→0/≤6 位 + StubProvider 确定性与 embedding 维度 + `runTask` 档位覆写/schema_invalid 仍出记录/provider_error 不裸抛但记 status·tier/taskId 格式唯一/记忆记录器累计/agent·caseId·solutionId 透传）；基线 **369（284 单元 + 85 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 0（**纯服务端逻辑无新路由无 DB 无回归**）；版本 **0.27.0**
- [x] **Phase 13 里程碑 6：后台「审核发布队列」——跨实体人工裁决待办的单一入口（复用既有写端点，零 schema/零新端点）**——宪法核心分工「AI 做大量劳动、人做关键决策」，但后台至今只有两张各自的全量长列表，运营要逐屏翻找「哪些在等我拍板」、且要点按钮吃 409 才知道差什么。Phase 9 之后 AI 流水线会批量灌草稿（方案进 `UNDER_HUMAN_REVIEW`、案例停内部阶段），散点式审核直接卡住「…→购买」闭环吞吐。M6 **零 schema、零新写逻辑、零新 HTTP 端点**——队列动作全复用 M1 的 `PATCH /api/admin/{solutions,cases}/[id]`，服务端仍跑发布守卫 + 角色 + CSRF，UI 只是薄壳。`src/server/solution-admin.ts` 导出**只读**发布就绪预览 `solutionPublishBlockers()`——**复用同一个私有 `publishGuard`**、把拒绝原因摊平成人类可读串（空=就绪），杜绝「队列说能发、点了被拦」的口径漂移（第 16 条）；`src/server/admin-review.ts`（新）`getReviewQueue()` 单事务两查聚合「待审方案（UNDER_HUMAN_REVIEW，带案例/价格/riskDomains/就地 `publishBlockers`/实时 `_count.unknowns`·`orders`、`isDemo` 由关联案例判定）」与「待晋升案例（`stage∈{CANDIDATE,KEY_RESEARCH}`，带证据 `_count`/评分标量/行业中文名）」，`take:LIMIT+1` 判 `truncated`、`updatedAt` 倒序、导出 `PENDING_CASE_STAGES`/`PENDING_SOLUTION_STATUS` 常量口径；`src/components/admin/ReviewQueueActions.tsx`（新 client）多动作按钮 `{label,body,variant,confirm}` 经共享 `mutateJson` 打既有端点、成功 `router.refresh()`、被拒回显 409 字段原因、**UI 不自判能否发布**；`ƒ /admin/review` 页 layout+page **双层防御**（越权 `return null` 前不调读层）+ noindex、两栏 + 缺口 `Alert`；`/admin`「内容入口」加队列链接。新增单测 `review-readiness.test.ts` **5 例** + 集成 `admin-review.test.ts` **4 例**（真连 Neon，锁「只收待审态/内部阶段、干扰项不入队、缺口口径、isDemo/行业名、倒序·truncated 自洽」；修测：缓存标量 `unknownVariableCount` 直插不同步 → 改实时 `_count.unknowns`）；基线 **378（289 单元 + 89 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /admin/review` 无回归、**审核队列端到端冒烟 29/29**（`smoke-admin-review.mjs -p 3129`：未登录 307 / USER 200 无权限不泄露队列 / ADMIN 见两栏 + 未定价「发布前需补齐…价格」缺口 + 就绪「条件齐备」 / 就绪方案→PUBLISHED 200·未定价→409 含 price·候选案例→DEEP_CASE 200 且 DB 变更 / 发布·晋升后离队 / 跨站 403 CSRF、USER 403 / leftover=0）；版本 **0.28.0**
- [x] **Phase 9 里程碑 2：§33 多角色研究流水线骨架（Research→Bull→Bear→Judge→QA，纯离线可测，零 schema/DB/HTTP/UI）**——宪法/总控 §33 把「高价值方案须走多角色对抗式研究、禁止单模型直出最终方案、结论须区分事实/假设/推断/预测、AI 做劳动人做决策」列为硬约束，但 Phase 9 M1 只立了 `runTask` 单调用解耦缝，**尚无把多角色按证据正反展开→裁决→独立质检串成可复算流程的编排**——将来 Phase 8 M3 灌真实方案若直接 `generateText` 一把梭就违反「禁单模型直出」。M2 是**纯加性竖切：零 schema、零 DB、零 HTTP 端点、零 UI、零新路由**，且**不依赖 API Key（#4）**即可离线测死（真实 provider 到位只需注入 `ChatProvider`，本模块一行不改）。`src/server/research-pipeline.ts`（与 `scoring.ts`/`model-router.ts` 同构的纯函数 + 判别联合）：`PIPELINE_ROLES` 固定序 `research→bull→bear→judge→qa`（`PIPELINE_VERSION 1.0.0`）+ `DEFAULT_ROLE_TIERS`（research/judge→high、bull/bear/qa→medium，v1 假设、可 `tierOverrides`）+ 各角色 Zod schema（research 带 `evidenceKind∈FACT/ASSUMPTION/INFERENCE/PREDICTION`、bull 正方、bear 反方、judge `verdict∈supported|mixed|weakened`、qa `approved/qualityScore/needsHumanReview`，**数组 `.default([])` 允许空=诚实不逼编造**）；`buildPrompt` **确定性**把前段结构化输出并入后段 prompt（无时钟无随机可复算）；`runResearchPipeline` 全委托 `runTask`（每角色打 `agent=pipeline:<role>`、透传 `caseId/solutionId` 归因、离线 `fixtures→respondWith`），**任一角色 `provider_error`/`schema_invalid` → 诚实早退 `failed`（带 stage+kind）保留已完成记录绝不臆造续跑**，五段皆 ok 后过 **QA 门禁**：未批准/低阈值/标记人工 → `needs_human_review`（暂缓、非终态、**绝不静默放行 complete**），否则 `complete`；末尾聚合整条链**程序计算成本**（§31）。新增 `tests/unit/research-pipeline.test.ts` **15 例**（无 DB/key：常量守护 + schema 黄金/缺字段拒/空数组允 + complete 全路径（5 记录按 agent 归因·cost 自洽·归因透传·roleTiers 覆写）+ QA 门禁三态 + `qaThreshold` 翻转 + 诚实早退（schema_invalid 保留前段不臆造 / provider 抛错 stage 定位不裸抛 / 首角色即抛）+ 确定性复算 + `scriptedProvider` 断言 taskKind 序与上下文链）；基线 **393（304 单元 + 89 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` **0 新增路由无回归**；版本 **0.29.0**
- [x] **Phase 10 里程碑 1：GitHub Scout 治理逻辑骨架（许可证/依赖/安全/实测准入门 + 去重键，纯离线可测，零 schema/新表/HTTP/UI）**——商业闭环「GitHub/开源匹配 → 本土化重构」风险最高的一环是把带传染性许可证/有漏洞/未实测的开源项目直接包装成售卖方案（宪法第 11 条「GitHub=能力供应链非成品」「禁止 发现→复制→包装→出售」「许可证未知/GPL/AGPL/含商业限制→人工复核」）。M1 **零 schema、零新表**（直接复用 `OpenSourceProject` 已有的 `licenseType/licenseReviewStatus/dependencyChecked/securityChecked/tested`），把这条法务+工程准入门落成可复算契约（与 `scoring.ts`/`model-router.ts`/`research-pipeline.ts` 同构）。`src/server/scout.ts`：`LICENSE_CATEGORIES`（permissive/weak_copyleft/strong_copyleft/proprietary/unknown，**v1 假设非法律意见**）+ `LICENSE_CATEGORY_BY_TYPE` 逐字覆盖 11 枚举成员、`licenseCategory` 未知值保守回落 unknown；`decideLicense`→判别式 `{verdict:approved|needs_human_review|rejected,category,reasons[]}`，**自动层只分类不越权批准**（NOT_REVIEWED 一律 needs_human_review 并按分档补强理由，人显式 APPROVED/REJECTED 才尊重）；`projectReadiness` 合成 `{blockers,readyForReuse,license}`——**GPL/AGPL/专有/未知即便三检查位全 true 也不自动就绪**，从代码焊死捷径；`normalizeRepoUrl` 确定性去重键（小写/剥协议·www·查询·`.git`·尾斜杠、保留 host、幂等）；`ScoutCandidateSchema` 复用 `@/lib/validation` 枚举防漂移、缺省即最保守；`evaluateCandidate`→判别联合 `invalid(指名 issues)|evaluated(candidate+dedupKey+readiness)` 永不裸抛。新增 `tests/unit/scout.test.ts` **24 例**（常量守护/分类黄金值/判定矩阵含 APPROVED-REJECTED-NEEDS-五种 NOT_REVIEWED 分档/就绪聚合含「未复核+全检查仍不就绪只剩法律门 1 条」/去重五写法同键·host 区分·幂等/schema 最保守默认/evaluateCandidate 脏输入不裸抛·dedupKey 相等）；基线 **328 单元（304→+24）+ 89 集成** 全绿、`tsc`/`eslint` 0 错、`next build` **0 新增路由无回归**；版本 **0.30.0**
- [x] **Phase 9 里程碑 3：真实供应商接入 Model Router（DeepSeek·OpenAI 兼容，依赖注入离线测死，`runTask` 一行不改）**——M1/M2 之下唯一执行体是确定性 `StubProvider`，AI「研究→拆解→生成」还不能真跑；用户裁决「给 #4 模型 Key 解核心锁」并提供 DeepSeek 密钥，遂把 M1 预留的 `ChatProvider` 注入接缝真正填上。新增 `src/server/deepseek-provider.ts`（第一个真接网络的 `ChatProvider`，`id="deepseek"`，走 OpenAI 兼容 `POST <base>/chat/completions`）：**依赖注入 `fetchImpl`**（单测注假 fetch、无网络无 key 也把请求构造/响应解析/错误归一整套离线测死）、**模型名不硬编码进业务**（用 `req.model.id`，真实模型来自可 env 覆写的 `MODEL_CATALOG`）、**成本仍由 Model Router 程序计算**（只回传真实 `usage`）、**绝不在日志/错误落密钥**（`Bearer` 头只在发出请求、非 2xx 抛「`HTTP <status>`」不回显响应体）、**诚实边界**（DeepSeek 无 embedding→直抛不支持交 `runTask` 归一 `provider_error` 不假装返回向量；结构化任务带 `response_format:json_object` 解析 content→`json` 交 Zod，解析不出即 `json` undefined→诚实 `schema_invalid`）；附 `deepSeekConfigFromEnv`（无/空白 key→null）+ 工厂 `createChatProvider`（**有 key 用真实 DeepSeek、无 key 回落确定性 `StubProvider`**，开发/CI 自动走桩、生产配 key 即真调、业务零分支）。改 `src/server/model-router.ts`（最小侵入）：`MODEL_ROUTER_VERSION` `1.0.0→1.1.0`，`MODEL_CATALOG` 改为**经 env 可覆写、缺省回落与 1.0.0 完全一致的占位默认**（`MODEL_ID_<TIER>`·`MODEL_PRICE_IN/OUT_<TIER>`），故无 env 时离线单测成本黄金值不变；`.env.example` Model Router 段扩为带文档的 DeepSeek 块（单价全标「示例占位、以官网为准」），真实密钥只进本地 gitignored `.env`。新增 `tests/unit/deepseek-provider.test.ts` **23 例**（配置解析三态/工厂有无 key 回落/构造期空 key 早失败不回显/请求构造含 `Bearer`·真实 model id·结构化才带 `response_format`·`signal` 透传·自定义 base 拼接/响应解析合法·非法 JSON·usage 缺失或负数归零·model 回落/embedding 直抛不 fetch·非 2xx 不含 key/`runTask`×真 provider 集成 ok+算成本 0.008·抛错归一 provider_error 不外溢·非法 JSON→schema_invalid）；`model-router.test.ts` 版本黄金同步 `1.1.0`；基线 **351 单元（328→+23）+ 89 集成** 全绿、`tsc`/`eslint` 0 错、`next build` **0 新增路由无回归（仍 45 条）**；**另做一次真实端到端冒烟**（脚本放 scratch 不进仓库）：`createChatProvider()` 经 env 真 key 打真 DeepSeek——`generate_text`→「你好！」`modelId=deepseek-chat`、`structured_output`→经 Zod 校验的 `{"language":"中文","greeting":"你好，世界！"}`，端到端证接缝可用；版本 **0.31.0**
- [x] **Phase 8 里程碑 3：§33 流水线接真实方案生成（灌 DRAFT 正文、人工门控、绝不自发布，DeepSeek 真跑通）**——Phase 9 M3 接通真实供应商后，商业闭环里杠杆最高的一环「案例拆解→技术能力→产业解决方案」终于能真跑：把 §33 多角色研究流水线的产出**映射进一份可售方案的 34 分节正文**，但恪守宪法「禁止 AI 自动公开发布」「禁单模型直出」「结论区分事实/假设/推断/预测」。M3 是**加性竖切、零 schema 变更**（复用既有 `Solution.body` JsonB + 已测 `updateSolution`（自动 version++/`ChangeLog`）+ `api-guard`）。`src/server/solution-generation.ts`（server-only 纯函数 + 编排）：`mapPipelineToSolution` **纯映射**流水线输出→只填 `bullCase/bearCase/riskAnalysis/unknowns/aiAnnotations` **五节**（`riskAnalysis` 取 bear 风险按 severity 倒序 + judge 裁决；`unknowns` = 非 FACT findings `{statement,kind,confidence}`；`aiAnnotations` 记 `generationVersion/pipelineVersion/factualStatementCount` + 四分类计数 + judge/qa + **显式 `notGenerated:["costModel","revenueModel","roi","payback","sensitivity","sources"]`——财务/来源等刻意不伪造**），空 `points/findings` 不写对应节、`filledSectionKeys⊆SOLUTION_SECTION_KEYS`；`generateSolutionContent`——载入方案（不存在 `not_found`）、**已 `PUBLISHED` 直接 `blocked`（AI 生成不自动改写线上正文）**、问题缺省由标题确定性派生、`provider=deps.provider ?? createChatProvider()` 跑 `runResearchPipeline`、有内容才 `{...existingBody,...gen.body}` **合并**（人工已填键原样保留）经 `updateSolution`（`actor` 缺省 `"ai:pipeline"`）落库、无内容 `wroteSections:0` 不落库不空递增，**返回体绝不含 `status:"PUBLISHED"`**。新薄路由 `POST /api/admin/solutions/[id]/generate`（`requireStaffWrite` 门禁 → 可选 `{question}` → `mutationResponse`）。**顺带修两处真实供应商才暴露、离线桩永远测不出的缺陷**：① DeepSeek `json_object` 要求 prompt 字面含 "json" 否则 400 → 在 `deepseek-provider.ts` 适配器给结构化任务补「respond with valid JSON only」尾串（业务/流水线仍 provider-agnostic）；② 模型回 `finding` 非 `statement`、中文证据类型、0~1 小数置信度致 `schema_invalid` → 在 `research-pipeline.ts` 各 `roleInstruction` 内嵌**精确 JSON 契约**（大写英文枚举、0–100 整数置信度），并按宪法第 13 条为 prompt 变更升 `PIPELINE_VERSION 1.0.0→1.1.0`。新增单测 `solution-generation.test.ts` **15 例** + `deepseek-provider.test.ts` **+1 例**（23→24，锁「结构化 prompt 被补强含字面 json」）+ 集成 `solution-generation.test.ts` **5 例**（注入确定性 `StubProvider`+fixtures，**无论本地是否配真 key 皆可复算**：complete 五节合并且人工键保留·version 递增·仍 DRAFT·`publishedAt` null·审计 `changedBy=ai:pipeline` / 已发布 `blocked` 且不改线上正文 / bull 非法仍诚实写 research 段 / research 空不写库 / 不存在 `not_found`）；`research-pipeline.test.ts`·`solution-generation.test.ts` 版本断言同步 `1.1.0`；基线 **461（367 单元 + 94 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 新增 **1 条受门禁路由 `ƒ /api/admin/solutions/[id]/generate`** 无回归；**真实 DeepSeek 端到端冒烟成功**（scratch 脚本跑完即删）：五角色全跑通、成本程序算约 **$0.0042**、**5 节真实中文正文**正确落库（英文枚举 + 整数置信度）、QA 门把低质草稿判 `needs_human_review` → **status 仍 DRAFT 绝未自发布**，同时验证正向内容路径与人工门控安全契约两条腿；版本 **0.32.0**
- [x] **Phase 13 里程碑 7：后台「一键生成 AI 正文草稿」按钮——给 Phase 8 M3 的生成能力接上运营人面入口（复用受门禁端点，零 schema/零新数据层写逻辑/零新端点）**——Phase 8 M3 已把 §33 流水线接入真实方案生成，但只交付裸端点 `POST /api/admin/solutions/[id]/generate`（供 RSC/脚本调用），后台无运营可点的人面入口，「AI 做大量劳动」的能力对人是断链的。本里程碑落成「人点一下 → AI 起草 → 人在正文编辑台核对 → 人决定是否发布」的最小运营闭环，严守「禁止 AI 自动公开发布」「人做关键决策」。`src/components/admin/GenerateBodyButton.tsx`（新，client，挂编辑台正文上方）：可选「研究聚焦问题」文本框（留空则服务端按方案/案例标题确定性派生，前端二次 `slice(0,2000)` 与端点同口径），点击经共享薄封装 `mutateJson` 打**已受 `requireStaffWrite` 门禁**的 M3 端点（不另起 Server Action 防漂移第 16 条），成功读服务端回传的 `generation` 元数据如实回报「已生成 N 节 / 流水线终态 / 成本约 $x」并 `router.refresh()` 让正文编辑台即时反映新分节、无可写分节诚实显示「未改正文」、被守卫拒（如已发布 409）原样回显字段原因且 **UI 不自判能否发布、不自证成功**；**已发布方案禁用按钮 + `Alert` 说明**（与端点 `blocked` 契约一致但服务端仍唯一权威），常驻说明转达 M3 的 `notGenerated` 语义（成本/收入/ROI/回收期/来源刻意不生成、须程序计算 + 人工溯源才算完整可售）。`src/components/admin/mutate.ts` **加性扩展** `MutateResult.data`（成功时透出剔除 `ok` 后的完整响应体，供需读服务端元数据的调用方复用同一薄封装，既有 5 组件行为不变）；`page.tsx` 于 `EditSolutionMetaForm` 与 `SolutionBodyEditor` 间插说明性 `Card` 承载按钮。新增单测 `mutate.test.ts` **8 例**（`vi.stubGlobal("fetch")` 锁归一契约：成功透出 `data`·非 2xx·2xx 但 `ok` 非真·json 解析抛错不崩·fetch reject→status 0·`body=undefined`·`fieldHints`）；基线 **469（375 单元 + 94 集成）** 全绿、`tsc`/`eslint` 0 错（含消除一处 `_ok` 未用变量告警改 delete 法）、`next build` **无新增路由**（复用既有 `/admin/solutions/[id]` 页与 M3 端点）；版本 **0.33.0**
- [x] **Phase 9 里程碑 5：`ModelCall` AI 成本落库 + 后台成本看板（把 §31 成本可追踪从「只进日志」升级为「跨进程可查询、可累计、可视」）**——宪法 §31「AI 成本须追踪（模型/tokens/耗时/预计成本/任务类型/成败）」此前只落在结构化日志里（进程死即散、无法跨调用累计、更无法可视），Phase 9 M1 的 `createMemoryRecorder` 明确「DB 持久化 `ModelCall` 表延 M2 避免投机建表」、 ROADMAP Phase 9 亦列「`ModelCall` 成本表 + 看板」。M5 是**加性竖切：零改 `runTask`/流水线/生成业务代码、零新 HTTP 端点、零新路由**——生产者与持久化经 `recorder` 依赖注入彻底解耦，复用 `CallRecord` 作单一成本真源（第 16 条防漂移）。`prisma/schema.prisma` 新增 `model ModelCall`（append-only 观测语义：`String` 存 provider/model/tier/kind/status 而非枚举防漂移、`Decimal(12,6)` 存次分成本、`agent/caseId/solutionId` 归因列**不建外键**使成本史在实体删除后仍留存、仅 `createdAt`），迁移 `20260905120000_add_model_call` 纯加性已 deploy Neon（表 18→19）。`src/server/model-calls.ts`：`toModelCallCreateData`（纯映射 `CallRecord→prisma.create` 入参，负数/NaN 的 latency·tokens·cost 归零、小数 tokens `trunc`、非法时间戳省略 `createdAt`）、`createDbCallRecorder`（best-effort 旁路记录器，`Promise.resolve()` 链 + `.catch` 吞异常绝不影响主流程、`flush()` await 在途写集合）、`summarizeModelCalls`（纯聚合，空集 `avgLatencyMs=null` 不除零、`byAgent` 把 `null` 归 `(unattributed)` 并按成本降→次数降→名 `localeCompare` 稳定序、`round6`）、`getCostSummary`（读层唯一触库处，近窗口行数聚合默认 take 2000）。`api/admin/solutions/[id]/generate/route.ts` 改一行接缝：请求作用域新建 `createDbCallRecorder()` 注入 `generateSolutionContent`（`PipelineDeps/GenerateDeps.recorder` 早已存在并透传进 `runTask`）、响应前 `await recorder.flush()`；`/admin` 概览新增「AI 成本观测（ModelCall）」**只读**卡（累计成本/调用次数/失败/平均延迟 + `byTier`/`byStatus`/`byAgent` Top12 + 诚实空态指引）。新增单测 `model-calls.test.ts` **13 例**（`vi.mock("@/lib/prisma")`，纯函数无连库；吞异常路径刻意不做 mock 单测——vitest 单反检测器误报，改由静态 `.catch` + 生产 warn + 下方集成成功往返实证）+ 集成 `model-calls.test.ts` **3 例**（真连 Neon，recorder 三写→flush→`findMany` 逐字段读回含 `Number(estimatedCostUsd)≈0.0042` 与 `agent:null`、`getCostSummary` 聚合自洽、直读喂 `summarizeModelCalls` 黄金期望），`db-smoke` 存在性 **18→19 表**；基线 **485（388 单元 + 97 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` **无新增路由**（复用既有生成端点与 `/admin` 页）；版本 **0.34.0**。**刻意边界**：V1 用近窗口行数聚合而非时间序列、不做成本告警/预算闸门、归因列不建外键、不改 `runTask` 路由决策（自动选模型是独立里程碑）
- [x] **Phase 10 里程碑 2：GitHub Scout 抓取→治理判定→落库（把「开源匹配」从离线纯函数缝上真实输入与持久层）**——承 M1 只离线判定、元数据来源与落库去处全悬空。M2 是**加性竖切：零 schema（复用 `OpenSourceProject` 已有列）、零新 HTTP 端点、零新路由、零新 UI**，复用 `scout.ts` 的 `evaluateCandidate` 作治理单一真源（第 16 条）。`src/server/scout-github.ts`（纯逻辑 + 可注入 fetch、无 DB）：`SPDX_TO_LICENSE_TYPE`+`parseSpdxLicense`（无 LICENSE→`UNKNOWN`、`NOASSERTION`/表外 SPDX→保守落 `OTHER`，**绝不猜宽松档**、原始名留 `licenseNote`）、`mapGitHubRepoToCandidate`（**刻意省略三检查位** 交 schema 落最保守 false，脏 stars 归零/`trunc`）、`parseRepoRef`（仅首段含点才剥 host、拒 `.`/`..`）、`fetchGitHubRepo`（非 2xx/网络错归一 `ScoutFetchError`、**绝不回显响应体或令牌**）；`SCOUT_GITHUB_VERSION 1.0.0`。`src/server/scout-ingest.ts`（server-only 编排、判别联合永不裸抛）：`ingestGitHubRepo`/`ingestCandidate` 拉元数据→`evaluateCandidate`→按 `dedupKey`（归一 `repoUrl`）upsert `OpenSourceProject`+`ChangeLog`；**核心治理不变式（写进代码）**——重抓更新分支只改客观元数据（`name/owner/stars/licenseType/licenseNote/lastCheckedAt/version++`），**绝不带 `licenseReviewStatus` 与三检查位**，防自动层冲回人工法律门（第 11 条）；结果 `ok|invalid|fetch_error|error`，脏候选不写库、P2002 如实回报、本模块不鉴权（沿用 `case-admin` 口径）。新增 `tests/unit/scout-github.test.ts` **16 例** + `scout-ingest.test.ts` **7 例**（`vi.hoisted`+`vi.mock`，无 DB）、`tests/integration/scout-ingest.test.ts` **4 例**（真连 Neon、`HAS_DB` 门控，含「人工 APPROVED+已实测条目重抓后 review 态与三检查位原样保留、仅 stars 刷新」核心不变式）；基线 **512（411 单元 + 101 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` **无新增路由**（纯 server 逻辑）；版本 **0.35.0**。**刻意边界**：不做依赖/安全扫描的**执行**与能力匹配 `fitScore` 打分（须外部工具/实测，M2 假装即违第 20 条），HTTP/UI/接审核队列延 M3

> **🧭 战略转向（2026-09-05）**：创始人下发《项目中途重构总控 Prompt》，产品重定位为「AI 产业案例与可视化解决方案平台」，V1 唯一焦点=「新能源重卡 + 光伏 + 储能 + 充电」一体化**产业项目可视化决策沙盘**。此指令 **supersede 上述旧 Phase 里程碑树**，改用精简 R0→R7 任务树（详见仓库 `REFACTORING_ASSESSMENT_V1.md`）。原则=不推倒重来、优先复用（认证/后台/审计/Evidence 四分类/GitHub Scout/Order 闭环原样保留），只补六支柱里当前全零的三根柱：**参数引擎 + 程序化经济计算引擎 + 模型联动可视化**。§14 优先级第一位=参数系统，§4 命脉=改参数须触发下游全链重算（绝不仅改页面数字），§7=关键财务指标必须程序算 LLM 只解释，§17=沙盘 E2E 主链未通不得宣布核心完成。

- [x] **中途重构 R1：分层参数引擎（§4/§5 命脉的可复算地基）**——把「一切关键变量皆参数、改参数须触发下游重算」落成纯函数内核，是 R0（契约设计）+ R1.1 的成果。**纯加性竖切：零 schema、零新 HTTP 端点、零新路由、零新 UI、零改任何既有模块**，沿用 `scoring.ts`/`scout.ts` 的「纯函数 + Zod 校验 + 判别联合返回 + 版本常量 + 黄金样本单测」范式。`src/server/parameter-engine.ts`：`PARAM_ENGINE_VERSION 1.0.0`。① `ParameterSpec`（每参数带 `kind(numeric|boolean|select)/group(region|policy|project|technology|finance)/exposure(basic|advanced|pro)/defaultValue/min/max/editable/source/confidence/evidenceKind(复用 `EvidenceTypeSchema`、未核实占位默认标 ASSUMPTION)/dependsOn/derived`）+ `ParameterSpecSchema`（`superRefine` 拒 `min>max`、derived 无 dependsOn）+ `ParameterSetSchema`（拒重复 key）；② `ValueLayer`（地区值 / 政策值数组 / 用户值各一层，带 `source/confidence/effectiveFrom/effectiveUntil/evidenceKind/bounds`，天然承载 §6「选地区载出电价·光照·政策…且用户可区间内覆写、须存来源/生效时间」）；③ 核心 `resolveParameters(specs,{region,policy[],user,derived,now})`——优先级 `user(仅 editable)>有效政策>地区>默认`、**过期政策跳过留痕绝不当现行默认**、数值裁剪进 `spec∩layer` 更紧区间、**派生值在基础值之后拓扑式有界迭代求值**（无 eval、不依赖真实时钟）、缺依赖/非有限/无函数三条**诚实降级不裸抛不假装**，返回判别联合 `ResolveResult`（含逐参数 `ResolvedParameter` 与供计算引擎直接消费的仅数值快照 `numeric`）；④ helpers `getValue`/`filterByExposure`/`ResolvedParameterSchema`。新增 `tests/unit/parameter-engine.test.ts` **29 例**黄金样本（含实测修出的一处真 bug：数值快照初版按「可否转数字」误收布尔默认→改严格按 `kind==="numeric"` 过滤），实证 §4「改上游 capex→派生值重算」机制成立；集成 101 不变（纯函数无 DB）。基线 **541（440 单元 + 101 集成）** 全绿、`tsc`/`eslint` 0 错；版本 **0.36.0**。**刻意边界**：R1 只是"解析器"，无任何行业参数/公式/沙盘/持久层——产品价值须待 R1.2 模板 + R2 计算引擎接上后才可被 §17 E2E 验证

- [x] **中途重构 R1.2：光储充重卡沙盘参数模板（把 §5 参数引擎落成一份具体行业参数目录 + 给 R2 的稳定接缝）**——R1 是通用机制、不认识"光伏装机/储能时长"等具体变量，本里程碑给「重卡+光伏+储能+充电」沙盘编第一份参数目录。建于已测 `parameter-engine.ts` 之上，**纯加性竖切：零 schema/端点/路由/UI/零改既有模块**（第 16 条不重造轮子）。`src/server/sandbox-params.ts`：`SANDBOX_PARAMS_VERSION 1.0.0`。① `SANDBOX_PARAMETER_SPECS` 40 条（点分命名空间 `<group>.<entity>.<metric>`，键即下游单一真源）覆盖 region(电价/峰谷价差/等效小时/需量/土地)/policy(补贴/上网电价/碳价)/project(重卡数·单车电量·光伏 kWp·储能功率容量·桩数功率·并网·利用率 + boolean 配储能)/technology(PR/衰减/三类单位造价·运维/往返效率/寿命/充电效率)/finance(折现率·计算期·通胀·税率·资本金·贷款利率·残值)，exposure basic/advanced/pro 分层；② **诚实焊死**——每条 `evidenceKind=ASSUMPTION`·`confidence≤50`·`source` 带 `【占位假设·待核实】`、`SANDBOX_PARAMS=ParameterSetSchema.parse(...)` 模块加载即校验；③ 三条结构性派生（只读、纯输入算术示范 §4 改参数→下游变，不含 R2 技术经济计算）：日充电总量=车×单车电量·充电总功率=桩×单桩功率·储能时长=容量÷功率（功率 0 返 undefined 诚实降级）；④ `resolveSandbox(layers)`（自动挂派生注册表）+ `resolveSandboxBaseline()`。新增 `tests/unit/sandbox-params.test.ts` **20 例**（含**★诚实不变式遍历全参数断言恒 ASSUMPTION/带占位前缀/confidence≤50**、派生键↔spec↔注册表三者一致无悬空依赖、基线派生真值、§4 改桩数→总功率即时重算、派生被直接覆写仍被计算覆盖、除零不诈算 Infinity、过期政策不吞、boolean 不进 numeric 快照、分层曝光、确定性）。基线 **561（460 单元 + 101 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 0 错无新增路由；版本 **0.37.0**。**刻意边界**：只声明输入 + 结构聚合，光伏年发电量/CAPEX/NPV/IRR 一律留 R2；所有默认是被测试锁死的占位假设，经核数据 + 真实地区参数属 R5/数据核实专项

- [x] **中途重构 R2 基础层（上）：财务评价原语 + 技术能耗模型（§7「关键经济指标必须程序算、LLM 只解释」的计算内核）**——R1/R1.2 焊死参数层命脉后，沙盘还缺一台真正"算数"的引擎。本里程碑交付两块**可独立测死的纯函数叶子**（**纯加性竖切：零 schema/端点/路由/UI/零改既有模块**；沿用「纯函数 + 判别联合 + 版本常量 + 黄金样本 + calcRef 溯源」范式，两块均未 import 任何 route/page 故 build 零新增路由）。① `src/server/sandbox-finance.ts`（R2.2·`FINANCE_VERSION 1.0.0`）：`round` 定点舍入去浮点尾噪、`npv`（flows[0] 落 t=0 不折现，空/`rate≤−1`/非有限→NaN）、`irr` **区间二分法**返回判别联合 `IrrResult` 含 **no_sign_change/no_bracket/invalid_input/not_converged 四条诚实降级 + 多根示警**（绝不编造假比率）、`simple/discountedPaybackYears`（累计转正线性插值，从不回本→null 不假设"迟早回本"）、`roiPct`（无投资→no_investment）、`buildAnnualNetCashFlow`（`[-capex,net×life]` 可直喂 npv/irr）——**Decimal 取舍**：沙盘是模型估算非台账，用 Number+舍入，货币精确算术留 R3 Prisma Decimal（不为算 NPV 引重型依赖，第 15 条）。② `src/server/sandbox-tech.ts`（R2.1·`TECH_VERSION 1.0.0`）：`TECH_METHODOLOGY`+`TRANSPARENT_SIMPLIFICATIONS`（S1–S5 明示"透明简化年度能量平衡、非 8760h 逐时、须专业人工确认"）+ 原子纯函数 `pvAnnualEnergyYear1`/`pvDegradedAnnualEnergy`/`annualChargeEnergyDelivered`/`acLoadFromDelivered`(除零→NaN)/`annualEnergyBalance`(自用=·min、余电/下网=·max、比率除零记 0)/`storageAnnualThroughput`(受运营天数与循环寿命÷日历寿命双封顶) + 聚合 `computeTechModel(numeric)` 消费 `resolveSandbox` 快照返回判别联合 `TechResult`（缺键→missing_inputs、含非有限/除零→invalid_inputs、成功带 `inputsUsed` 溯源 + `needsProfessionalReview:true`，储能键为 0 降级但整体成功），键名与 R1.2 单一真源对齐。新增 `tests/unit/sandbox-finance.test.ts` **33 例**（手算整数/干净小数 + 四条 IRR 诚实路径 + 回收期从不回本 + 除零非法）与 `tests/unit/sandbox-tech.test.ts` **25 例**（R1.2 基线逐项手算 500×1200×0.82=492000、5,250,000÷0.94=5,585,106、储能 400×0.88×350=123,200 + **★§4 命脉实证「改日充电量/光伏装机→负荷·下网·渗透率全部重算」** + 缺参/除零/储能降级 + 确定性）。基线 **619（518 单元 + 101 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 0 错无新增路由；版本 **0.38.0**。**刻意边界**：tech 只算能量不算钱、finance 只认现金流不知沙盘——用快照串成端到端 `CalcResult` 是 R2.4 编排，敏感性 tornado + 高风险标记是 R2.3；**§17 E2E 主链未通不得宣布核心完成**

- [x] **中途重构 R2 收官：经济编排引擎 + 敏感性/高风险标记（§4 命脉端到端汇合点，R4/R6 可消费的唯一 `CalcResult` 真源）**——把 v0.38.0 互不知晓的"钱/能"两块地基用一台编排引擎焊成 §4 整链。**加性竖切：零 schema/端点/路由/UI**（R4/R6 未接线故 build 零新增路由）。① `src/server/sandbox-model.ts`（R2.4·`MODEL_VERSION 1.0.0`）：文件头逐条钉死 **ECONOMIC_MODEL E1–E8 透明简化口径**（CAPEX 三类+建设补贴抵扣 / OPEX 四类 / 收入=充电量×综合单价+余电×上网电价+运营补贴 / 购电=下网×电价 / 光伏按衰减逐年递减且**所有名义单价随通胀同步放大** / 正净额按 `(1−税率)` 简化计税无折旧抵税 / 末年残值 / flows 喂 npv·irr·payback·roi）；导出纯函数 `computeEconomics(numeric)`（可脱离 resolve 单测）+ 主入口 `runSandboxModel(layers)`/`runSandboxModelBaseline()`（`resolveSandbox`→`computeTechModel`→逐年税后现金流→§7 指标），返回判别联合 `CalcResult`（`ok:true` 带 capex/opexY1/revenueY1 三张分解表 + `annualCashFlow[]` + `metrics` + `breakEvenChargingPriceY1` + `engineVersions{model,tech,finance,params}` + `needsProfessionalReview`；失败 `tech_error`/`missing_econ_inputs`/`invalid_econ_inputs`）。② `src/server/sandbox-sensitivity.ts`（R2.3·`SENSITIVITY_VERSION 1.0.0`）：`computeTornado` 对关键参数 **one-at-a-time ±delta、重复调 `runSandboxModel` 整链重算**（真走 §4）取 NPV/IRR/ROI 算 swing、按 |swing| 降序出龙卷风（`normalized` 条宽 + `mostSensitiveKey`），指标不可得→`swing=null` 沉底绝不冒充；`deriveRiskFlags` 依 §16 派生「须专业人工确认」清单。③ `sandbox-params.ts` **加性新增** basic `project.chargingPrice`（综合充电单价·收益第一杠杆），`SANDBOX_PARAMS_VERSION` 1.0.0→**1.1.0**（既有 20 例仍全绿）。新增 `tests/unit/sandbox-model.test.ts` **18 例**（CAPEX 3,710,000·净 3,524,500 / OPEX 336,300 / 收入 4,987,500 / 购电 3,565,174 / flows[0]=−3,524,500·flows[1]=814,519 / NPV≈4,277,409·IRR≈0.2376·回收 4.2y+5.28y·ROI≈4.0·盈亏平衡单价 0.7431 全手算 + **★§4 端到端实证：改充电单价→NPV/现金流/IRR 全升、电价↑→NPV↓、装机置 0→光伏分量归 0 且 NPV 降、storagePower=0→储能分量归 0 但整体成功** + 技术缺口先拦/缺键非法/低单价致 NPV<0 不粉饰）与 `tests/unit/sandbox-sensitivity.test.ts` **15 例**（chargingPrice 最敏感 swing 正·elecPrice 次之 swing 负·|swing| 降序·normalized∈[0,1] 首行=1·不存在键→swing=null·风险标记动态触发）。基线 **652（551 单元 + 101 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 0 错无新增路由；版本 **0.39.0**。**R2 计算引擎就此收官**——`CalcResult` 即 R4 可视化绑定源 / R6 报告读数源。**刻意边界与诚实**：口径是明文简化的 E1–E8 非可研级、输入全占位假设、结果恒 `needsProfessionalReview`；`runSandboxModel` 尚未被任何 route/UI import，**§17 E2E 主链仍未通、核心产品不得宣布完成**（须待 R3 持久 + R4 可视化 + R6 报告/AI 解释串起）

- [x] **中途重构 R3：沙盘「项目模型」持久层（§14 第 2 项——把 `CalcResult` 连同其输入参数分层一起存下来、货币 Decimal、版本快照可回滚）**——R2 的 `runSandboxModel→CalcResult` 此前只在内存跑一次即蒸发，§4 命脉要"改参数→重算→结果变"这条链能在真实项目上被留存、对比、回滚。**纯加性竖切：不改任何既有表/枚举/端点/路由/UI、沙盘引擎零改动**，货币精确算术**只在持久边界**首次启用 Prisma `Decimal`（内核仍 Number+定点舍入，对齐 `Order.amount`/`SolutionFinancial`）。① `prisma/schema.prisma` 新枚举 `ProjectStatus{DRAFT ACTIVE ARCHIVED}` + 三表：`Project`（`regionId?→Region`/`ownerId?→User` 皆 `SetNull` 不牵连删既有数据、`industry` 默认 `NEW_ENERGY`）；`ProjectScenario`（一项目多"参数情景"，`paramLayers Json`=§4 输入分层 / `paramSnapshot Json` 解析快照 / `calcResult Json?` 现算输出 / `calcStatus` 判别 / **五列汇总 Decimal** `capexNet/npv/irrPct/paybackYears/roiRatio` 便于列表排序不必展开 JSON）；`ProjectVersion`（append-only 快照 `@@unique([scenarioId,seq])`）。迁移 `20260905140000_add_sandbox_project` 经 `migrate diff --from-url` 离线生成、`migrate deploy` Neon（表 19→22、枚举 13→14）、`prisma generate`；**刻意不给 `Project` 挂 `versions` 反向关系**（版本经 scenario 访问、`ProjectVersion.projectId` 反范式免 FK）。② `src/server/sandbox-store.ts`（新·`STORE_VERSION 1.0.0`）：**存的是输入分层 + 现算结果**，回滚=取历史 `paramLayers` **重跑** `runSandboxModel` 而非贴回死数字（§4 反面）；纯函数 `projectCalcToColumns`（失败→五列全 null+`calcStatus=reason`、`decimalStr` 非有限→null 防崩 Decimal、irr×100、payback 取折现、roi 存 ratio）+ `jsonSafe`(NaN→null)；DB 函数 `createProject`（事务 + 基线情景 + 现算落列 + `ChangeLog` CREATE）/`updateScenarioLayers`（改参→重跑→`version++`→UPDATE 审计）/`saveScenarioAsVersion`（seq=max+1 冻结）/`restoreScenarioFromVersion`（校验归属否则 `forbidden`，再重算式回滚）/`getProjectWithScenarios`/`listScenarioVersions`；无鉴权（属 R6/后台）。新增 `tests/unit/sandbox-store.test.ts` **11 例**（无 DB：对 R2 golden `CalcResult` 断言 Decimal 串 `capexNet "3524500.00"/npv "4277409.00"/irrPct "23.7553"/paybackYears "5.28"/roiRatio "4.0035"`、NaN/无解各降级为 null、`chargingPrice 0.5→npv 负`、`tech_error→全 null`、确定性）+ `tests/integration/sandbox-store.test.ts` **6 例**（真连 Neon 单跑 6/6：建项目现算落库对齐 golden + CREATE 审计 / 改单价→汇总列即变（§4 持久化实证）/ `updateScenarioLayers` NPV 升 + version 递增 + UPDATE 审计 / 越界单价裁剪仍算得出 / 版本冻结 seq 递增 + restore 重算回旧态 + 跨情景 forbidden / null·空表）；`tests/integration/db-smoke.test.ts` 两处硬编码计数**如实修正**（枚举 13→14、业务表 19→22）。基线 **668（562 单元 + 集成，sandbox-store 6 单跑绿、并行套件受共享 Neon 连接池争用偶发抖动非本轮引入）** 全绿、`tsc`/`eslint` 0 错、`next build` 0 错无新增路由；版本 **0.40.0**。**刻意边界与诚实**：`sandbox-store` 尚未被任何 route/UI import，**§17 E2E 主链仍未通、核心产品不得宣布完成**；入参全是 R1.2 占位假设（`needsProfessionalReview` 恒 true）

- [x] **中途重构 R4：沙盘可视化与交互工作台（§14 第 4 项 / §8「图表绑定模型」——§4 命脉第一次可被人看见）**——R1–R3 把参数/计算/持久三根柱焊齐，但沙盘无任何人可交互面，命脉只在单测里成立。本里程碑交付第一个上线页 `/sandbox`：左侧拖参数、右侧技术/经济/能量/敏感性图表**纯前端即时重算联动**。**加性竖切：不改任何既有表/枚举/端点/计算内核；仅新增前端 + 一个静态路由 `/sandbox` + 新依赖 `recharts ^3.10.1`**。① `src/lib/sandbox-view.ts`（新·`VIEW_VERSION 1.0.0`·**纯函数、零 React/recharts/网络/时钟**）：把 `CalcResult`/`TechResult`/`TornadoResult` 确定性投影成图表数据 + 中文格式化——`formatMoney`（元/万元/亿元自适应、非有限/null→「—」不伪装 0）等格式化原语 + `cashFlowSeries`（当年+累计前缀和）/`capexItems`/`opexItems`/`revenueItems`/`year1MoneyComparison`/`energyBalanceItems`/`tornadoSeries`（相对基线位移、缺失保 null）/`summaryCards`（NPV/IRR/回收期/ROI/盈亏平衡五卡含着色与诚实提示）+ 总装配 `buildSandboxViewModel`（**`calc.ok=false` 只回错误不出脏图**）；数字全来自引擎、视图层不重算（第 16 条单一真源）。② `src/components/sandbox/SandboxWorkbench.tsx`（client·§4 兑现点）：把 client-safe 的 `runSandboxModel`/`computeTechModel`/`computeTornado` **打进浏览器 bundle**、参数覆写经 `useMemo` 触发即时重算整链；**参数驱动 UI**（控件从 `SANDBOX_PARAMS` 的 `exposure` 层级自动渲染、basic 6 项 + advanced 展开、只读展示三条 `derived.*` 令"改上游→派生即变"显性化）；顶部恒声明占位假设 + 需专业人工确认 + §17 未通。③ `src/components/sandbox/SandboxCharts.tsx`（recharts 展示件：现金流柱+累计折线穿越 0 即回收期 / 首年收入 vs 成本 / CAPEX·收入·OPEX·能量平衡分解条 / 敏感性龙卷风），布局页头尾各加「沙盘」导航，新静态页 `src/app/sandbox/page.tsx`。新增 `tests/unit/sandbox-view.test.ts` **21 例**（六类格式化黄金值含 1e4/1e8 边界/负号/非有限/从不回本、各投影形状、五卡着色诚实、失败不出脏图；**★§8 图表绑定真实模型实证**：喂真引擎输出断言现金流年数=flows 长度、累计=前缀和、NPV 卡值=格式化引擎 NPV、最敏感标签来自 tornado，并证改充电单价→引擎 NPV 升→视图呈现升，§4 穿透呈现层）。基线 **673（583 单元 + 107 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 0 错**唯一新增静态路由 `○ /sandbox`**；版本 **0.41.0**。**刻意边界与诚实**：本页纯前端即时重算刷新即丢，R3 `sandbox-store` 项目持久化/版本保存 **UI 尚未接到本页**（属 R6）、R5 山西真实地区参数种子未到（地区切换暂用占位默认）、AI 解释属 R6、敏感性龙卷风为基线锚定非当前情景锚定（已标注）；**§17 E2E 主链仍未全线打通、核心产品不得宣布完成**（待 R6）

- [ ] **中途重构 R5 → R7（V1 沙盘剩余地基，今后以此为准、取代旧 Phase 路线）**：~~R1.2~~（✅ **v0.37.0** 参数目录 + `resolveSandbox` 接缝）、~~R2.1–R2.4~~（✅ **v0.38.0 + v0.39.0** 技术/财务/编排/敏感性四件套，§4 计算链完整、`CalcResult` 就位）、~~R3~~（✅ **v0.40.0** 项目/情景/版本三层持久层、货币 Decimal、版本快照重算式回滚）、~~R4~~（✅ **v0.41.0** 沙盘可视化 + `/sandbox` 交互工作台、recharts 绑 `CalcResult`、改参数即时重算联动）；**R5** 地区政策（先 1 省=山西，载默认电价/光照/补贴参数 + 种子，接进 `/sandbox` 地区切换）；**R6** 动态报告 + AI 解释打通（报告读最新 `CalcResult`、`runTask` 解释「为何变/最敏感变量/what-if」、把 R3 项目保存/版本 UI 接到沙盘页、**§17 E2E 主链跑通**）；**R7** 企业个性化方案。**建议 R5 → R6（E2E 打通）→ R7**。**E2E 未通不得宣布核心产品完成（§17）**

- [ ] Phase 7 后续 / Phase 8 M3+ / Phase 9 M6+ / Phase 10 M3+ / Phase 13 后续 / Phase 14 M2：更细粒度权限矩阵（按资源/动作）随后续需要再补；**Phase 13 后续**：审核驳回理由填写 + 重新提交细粒度状态机、审核人分配、队列批量操作；**Phase 9 M6+**（真实 DeepSeek provider 已于 M3 接通、`#4` 解阻塞；§33 流水线已于 Phase 8 M3 灌进真实方案生成、`PIPELINE_VERSION` 现 `1.1.0`；`ModelCall` 成本表 + 后台看板已于 **M5 / v0.34.0** 落成）：真实 embedding（DeepSeek 无此端点、须配独立向量供应商）、各角色 prompt 继续调优（随真实模型表现升 `PIPELINE_VERSION`）、成本看板时间序列（按日/按方案，现为近窗口行数聚合）、按成本/成败自动选模型（§32，数据源已就绪）；案例 `scoreInput` 清除入口 + `regionId/businessModelId` 关联选择器收尾；§11 grade 并入可信度公式的后续待决（属公式形态变更，须走 SCORING §5 升版 + 改 26 黄金样本）；Phase 8 方案评分内核建模（引入复用度/供应链等新维度须升 RUBRIC 版本）；**Phase 10 M3+**（Scout 治理判定骨架已于 M1 离线测死、真实 GitHub 抓取 + 判定 + 落库 `OpenSourceProject` 已于 M2 / v0.35.0 打通）：Scout 结果后台展示（需 HTTP/UI）、依赖/安全扫描的**执行**接线（M1/M2 只读其布尔结果做准入聚合）、能力匹配打分（`fitScore` 须实测后填、勿投机）、把 `evaluateCandidate` 的 `needs_human_review` 接入 Phase 13 M6 审核队列；**Phase 14 M2**：JSON-LD 结构化数据（Solution→Product/Offer、Case→Article、BreadcrumbList、CollectionPage）、OG 分享图、hreflang

## 本地运行（待依赖与数据库就绪后）

```bash
npm install                 # 触发 prisma generate
cp .env.example .env.local  # 填入 DATABASE_URL 等，切勿提交真实密钥
npm run db:migrate          # 需要可连接的 PostgreSQL
npm run dev                 # http://localhost:3000
```

## 给创始人的一句话

网站、后台、代码都可以简单；但**产业案例质量、技术匹配质量、中国本土化质量、解决方案质量、证据质量和真实商业价值绝不能简单化**（宪法第28条）。
