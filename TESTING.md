# TESTING — 测试策略与质量门禁

来源：总控 Prompt 第28–30、43–44 节；宪法第4/18/20 条。铁律：**没有测试就不宣布完成；禁止虚构"测试通过"**。

## 1. 测试层级（总控第28节）

| 类型 | 覆盖 | 工具（拟） |
|---|---|---|
| Unit | 纯函数：评分算法、财务计算、许可证判定、脱敏 | Vitest |
| Integration | 服务层 + DB：案例/方案/订单流程 | Vitest + Prisma（测试库） |
| API | Route Handlers / Server Actions 契约 | Vitest + supertest/next 测试工具 |
| Database | 迁移可应用、约束/索引/关系正确 | prisma migrate + 集成测试 |
| AI Workflow | 各 Agent 输入/输出 Schema 校验、重试、FAILED_REVIEW | Vitest + 模型 mock/录制回放 |
| Permission | 越权/隔离/角色/解锁鉴权 | 集成测试 |
| Security | 密钥不泄露、日志脱敏、IDOR、文件访问控制 | 集成测试 + npm audit |
| E2E | 第42节验收闭环 | Playwright |

## 2. 重点测试场景（总控第28节）

用户注册、登录、企业画像、文件上传、AI 任务、报告生成、订单、后台、权限、搜索、方案发布。

## 3. AI/Agent 测试（总控第29节）

每个 Agent 必须有：输入 Schema、输出 Schema、成功标准、失败处理、超时处理、Retry、日志。测试要点：
- LLM 输出**必须过 Schema Validation** 才算成功；不合规 → 重试（≤3 次）→ `FAILED_REVIEW`。
- 关键方案链路 Research→Bull→Bear→Judge→QA 的**门禁**：QA 低于阈值必须退回，测试须覆盖"退回"路径。
- 用**录制/回放或 mock** 降低对真实模型与网络的依赖，保证可重复、低成本（宪法第15条）。

## 4. 财务与数字测试（宪法第7条）

评分（100 分制各维度）与财务（ROI/IRR/回收期/敏感性）必须由**程序计算**并有单元测试固定用例（黄金样本），断言可复算；禁止用模型口算结果作为断言基准。

## 5. 质量门禁（Definition of Done）

一个 Phase/功能"完成"当且仅当：代码改完 → 相关测试通过 → 检查日志无异常 → 核心流程手动/自动验证 → 修复问题 → 更新文档（含 CHANGELOG）。任一不满足不得宣布完成，须如实报告（宪法第20条 / 总控第44节）。

## 6. 现状（诚实记录，随 Phase 更新）

- **测试框架已就位**：`vitest ^5.0.0` + `tsx ^4.23.13`（devDeps）；`vitest.config.ts` 定义 node env、`@/*` 路径别名、30s 超时（跨太平洋连 Neon）、forks pool（避免 Prisma Engine 在 worker_threads 里偶发段错误）。npm scripts：`test` / `test:watch` / `test:unit` / `test:integration` / `typecheck`。
- **单元测试基线**（`tests/unit/`，**724 cases**（R8.5 起 713→724，新增 11 条 `sandbox-provenance-store` 沙盘来源方案 ASSUMPTION→FACT 升级写路径编排；R8.4 曾加 25 条 `sandbox-solution-provenance` 溯源审计与升级契约纯函数），~1s）：
  - `errors.test.ts`（19）— AppError 状态码映射 / 序列化 / cause 保留 / isAppError 跨 realm / toErrorResponse Prisma P2002 P2025 P#### 映射 / 生产屏蔽原始 message。
  - `logger.test.ts`（15）— JSON 单行 / level 阈值 / silent / stdout-stderr 分流 / 敏感字段脱敏（顶层 + 深层嵌套 + 数组）/ Error 序列化 / 循环引用 / child bindings / __redact 边界（Date / BigInt / Function）。
  - `env.test.ts`（10）— Zod 校验 DATABASE_URL / NODE_ENV / LOG_LEVEL / NEXT_PUBLIC_SITE_URL / 缓存单例 / 各失败分支 / 多字段错误汇总。用 `vi.stubEnv`（@types/node v22 把 NODE_ENV 声明为只读）。
  - `cn.test.ts`（9）— className 合并：字符串拼接 / falsy 过滤 / 数字 0 保留 / 条件表达式 / 嵌套数组扁平 / 对象 truthy / 空白裁剪 / 空输入 / 混合类型。
  - `request-id.test.ts`（12）— UUID 格式 / 万次生成唯一性 / 字符集校验（8–64 `[A-Za-z0-9-]`）/ 过短过长拒绝 / null-undefined 拒绝 / CRLF 与控制字符拒绝（日志注入防护）/ 客户端合法 id 采纳 / 非法 id 重新生成 / sanitizeForLog 净化。
  - `validation.test.ts`（41）— Cuid/Uuid/Slug 校验 / Pagination 默认值与 pageSize 上限 100 与 offset 计算与 coerce / makeSortSchema 字段白名单（防 SQL 注入）与 orderBy 生成 / SearchQuery trim 与去控制字符与限长 / **12 个业务枚举成员数与值断言（与 prisma/schema.prisma 同步兜底；Phase 7 M4 新增 EvidenceGradeSchema=S/A/B/C/D 与 prisma 枚举同步）** / paginatedResponseSchema / HealthResponseSchema / **（Phase 6 新增 7）认证 schema**：UserRoleSchema 与 prisma 枚举同步 / EmailSchema 归一小写（`Foo@Bar.COM`→`foo@bar.com`）与超长(>254)拒绝 / PasswordSchema 8·128 边界 / RegisterInputSchema 空串 name 归一化为 undefined / LoginInputSchema 口令只要求非空 min1（不以长度泄露账号是否存在）。
  - `api-client.test.ts`（27）— URL 构造（baseUrl + path + query 跳过空值 + 绝对路径）/ request-id 注入与透传与非法忽略 / JSON body 序列化 + content-type / defaultHeaders 与单次 headers 合并 / 响应解析（JSON / text / 204）/ 6 种状态码 → 对应 AppError 子类 / 5xx → UpstreamError / 上游 code 还原 + details 透传 / 非 JSON 错误体兜底 / 网络错误包装 / 幂等 GET 重试与非幂等 POST 不重试与重试耗尽与 429 重试与 404 不重试 / AbortSignal 超时传递。用 `vi.stubGlobal("fetch", ...)` mock。
  - `industries.test.ts`（15）— 行业数据层纯函数与常量守护：INDUSTRIES 数量与 IndustrySchema 对齐 / 枚举无重复无遗漏 / slug 合法（SlugSchema）且唯一 / 中英文名·简介·图标非空 / OTHER 兜底在最后 / PUBLIC_CASE_STAGES 只含 DEEP_CASE 及以后 / getIndustryBySlug·ByEnum·getIndustrySlug·isValidIndustrySlug 正常与未知输入（含枚举名≠slug、未知枚举回落 other）/ slug↔enum 双向自洽。
  - `demo.test.ts`（7）— DEMO 可见性纯函数：DEMO_SOURCE_TYPE / DEMO_TITLE_PREFIX 常量守护 / isDemoEntity 对匹配·不匹配·null·undefined 的判定 / caseDemoVisibility(true)→空片段、(false)→`OR:[{sourceType:null},{sourceType:{not:DEMO}}]`（规避 Prisma `{not}` 连带排除 NULL 真实行的陷阱）/ solutionDemoVisibility 把门控包裹到关联 `case` 上。
  - `search.test.ts`（5）— 搜索编排层 `searchPublic`（`vi.mock` 隔离 `@/server/cases` + `@/server/solutions`）：默认 limit=SEARCH_PREVIEW_LIMIT(8) / q·industry·includeDemo 正确透传两个子查询 / ok 聚合(两侧都 ok 才 ok) + hits=案例命中数+方案命中数 + q 原样回显 / 一侧 ok:false 则整体 ok:false / 子查询 reject 时 catch 降级为 ok:false·hits:0 且不向上抛（页面据此显示 danger 提示而非崩溃）。
  - `password.test.ts`（8，Phase 6）— 口令哈希 `src/lib/password.ts`（node:crypto scrypt，零依赖）：hashPassword 返回自描述串 `scrypt$N$r$p$salt$hash`（段数=6、算法名、参数、64 字节 hash base64）/ 同口令两次哈希因随机盐而不同（盐段相异）/ 超过 1024 字节 DoS 上限返回 `{ok:false,reason:"too_long"}`（不抛）/ verifyPassword 正确口令 true·错误口令与空口令 false / 同口令对不同盐哈希都能校验 / 畸形与非法存储串（空·非哈希·`bcrypt$`·参数非整数·N≤0·段数不足）一律 false（不抛，防用户枚举/崩溃）/ 超长口令校验 false。
  - `scoring.test.ts`（26，Phase 7）— 案例评分内核 `src/server/scoring.ts` **黄金样本**（TESTING §4「评分必须由程序计算并固定用例，禁止用模型口算作基准」）：常量守护（满分恒 100=各维度 max 之和 / 恰 10 维度且权重逐字对齐总控 §10 / 恰两个 inverse 维度=竞争强度·实施难度 / 类型权重 FACT>ASSUMPTION>INFERENCE>PREDICTION 单调递减）；机会评分黄金值（**最优 正向max+反向0→100 / 全填 max→90（反向贡献归零，非 100）/ 全填 0→10（仅两反向各贡献 5）/ 工作样例→88 且逐维度贡献锁定 + breakdown 求和=total**）/ 反向极性 raw↑则贡献↓（竞争 0→100、5→95）/ 越界·缺维度·非整数·负数 → ok:false 且 issues 指名维度（不静默截断）/ 确定性（同入参多调一致）；证据可信度黄金值（混合集 → **69** 且 Σw=2.8·Σnum=1.94 精确锁定 / 空集 → 0 / 单条 FACT conf100 有源 → 100 / **单条时类型权重约掉只由 q·sf 决定**（FACT 与 PREDICTION conf100 无源都 → 60）/ 未填 confidence 用缺省 50 / 越界 confidence clamp 到 0..100 / 未知 type 跳过不污染结果 / 确定性）；`countKeyUnknowns`=非 FACT 条数（全 FACT·空集 → 0、未知类型不计）；`computeCaseScores` 组合契约（三件套 + `CaseScoresSchema` 入库前 Zod 复核一致 / 机会评分非法时 ok:false 但证据两项仍独立计算 / 无证据时 confidence=0·unknowns=0）。
  - `parameter-engine.test.ts`（29，中途重构 R1）— 分层参数引擎 `src/server/parameter-engine.ts` **黄金样本**（§4 命脉「改参数→下游重算」须可复算，同 `scoring.ts` 纯函数无 DB）：版本正则守护 / 四组枚举集合等值（`PARAM_GROUPS`·`EXPOSURE_TIERS`·`PARAM_KINDS`·`VALUE_ORIGINS`）/ `ParameterSpecSchema` 缺省值（editable=true·confidence=50·evidenceKind=ASSUMPTION·derived=false）与 `superRefine` 拒 `min>max`·derived 无 dependsOn / `ParameterSetSchema` 拒重复 key·confidence>100 / 非法 spec 集 → `ok:false` + `issues` 且空快照（不静默放过）/ 单层默认（origin=default·overridden=false·clamped=false·confidence 取 spec）/ **`numeric` 快照只收 `kind==="numeric"` 数值参数、绝不含布尔·选择**（实测修出的真 bug：初版按「值可否转数字」过滤，`defaultValue:1` 的布尔参数被误收 → 改严格按 kind 过滤，复测绿）/ 分层优先级 region<policy<user、多条政策后者覆盖前者 / 数值裁剪到 max·min 且置 `clamped` + note / `spec ∩ layer` 区间取交集更紧者胜 / 不可编辑（`editable:false`）参数的用户覆写被忽略并留痕 / 政策 `effectiveUntil<now` 过期跳过落回地区 + note、`effectiveFrom` 未到跳过、在效政策正常生效 / **派生参数在所有基础值解析后求值且 origin=derived**、**改上游（如 capex）→ 派生值随之重算（直接实证 §4「改参数→下游变」机制成立）**、派生缺依赖 / 返回非有限值 / 注册表无函数三条各自诚实降级保留占位默认 + note（不裸抛不假装）/ 确定性（同输入两次 `toEqual`）/ helpers `getValue`·`filterByExposure`（basic/advanced/pro 累进可见）·`ResolvedParameterSchema` 校验解析产物防结构漂移。
  - `sandbox-params.test.ts`（20，中途重构 R1.2）— 光储充重卡沙盘参数模板 `src/server/sandbox-params.ts` 黄金样本（建于已测 `parameter-engine.ts` 之上、无 DB）：**★诚实不变式（第 20 条）遍历 `SANDBOX_PARAMS` 全部参数断言 `evidenceKind` 恒 `ASSUMPTION`·`source` 恒以`【占位假设`起头·`confidence≤50`**（把"占位数字绝不当事实"焊进测试，日后谁偷偷给某默认标 FACT 就红）；键全局唯一 / 五组（region·policy·project·technology·finance）全覆盖 / 派生键集合 ↔ `spec.derived` ↔ `SANDBOX_DERIVED` 注册表函数三者一致、且每条 `dependsOn` 指向的键真实存在（无悬空依赖）/ 基线派生真值 `dailyChargeEnergy=60×250=15000`·`chargerTotalPower=8×360=2880`·`storageDuration=400÷200=2` 且 origin=derived / **`numeric` 快照含全部数值参数与派生值、绝不含 boolean「是否配储能」开关**（验非数值透传不入计算口）/ §4 命脉在参数层——用户改 `chargerCount` 8→10 → 派生 `chargerTotalPower` 即时重算 3600 且不误伤 `dailyChargeEnergy`、改车数与单车电量 → 重算日充电量 / 试图直接覆写派生参数（`editable:false`）仍被计算层覆盖回真值 / 储能功率置 0 → 时长除零无定义 → 诚实回落占位默认 2 + note、值仍 `Number.isFinite` 绝不算成 Infinity / 过期政策（`effectiveUntil<now`）跳过落回默认并留「过期」note / `filterByExposure` basic 档只含核心滑块（不含 `finance.discountRate`）/ 两次基线解析深相等（确定性）。
  - `sandbox-finance.test.ts`（33，中途重构 R2.2）— 财务评价原语 `src/server/sandbox-finance.ts` **黄金样本**（§7「关键经济指标必须程序算、LLM 只解释」的评价数学内核，纯函数无 DB）：版本/calcRef 守护 / `round` 定点舍入去浮点尾噪（-0 归一、非有限原样）/ `npv` 手算黄金值（`npv(0.1,[-100,110])=0`、`flows[0]` 落 t=0 不折现、rate=0 退化为求和）+ 空数组/rate≤−1/含非有限一律 `NaN`（诚实"算不出"）/ `irr` **区间二分法**手算根（`[-100,10,110]=0.1` 且求出的 IRR 代回 `npv≈0` 自洽、`[-100,150]=0.5`）+ **四条诚实降级路径**（全同号→`no_sign_change`、根在界外两端同号→`no_bracket`、非法→`invalid_input`、`maxIter=0`→`not_converged`）、符号变化>1 置 `multipleRootsPossible`、弱项目仍找到 (-1,0) 内负 IRR 的诚实证明 / `simplePaybackYears` 线性插值（`[-1000,300×4]≈3.33`、整年恰回本、从不回本→null 不假设"迟早回本"、flows[0]≥0→null）/ `discountedPaybackYears` 比静态更晚且 rate=0 时与静态一致 / `roiPct` 手算（`[-100,110]=1.1`、无投资→`no_investment`）/ `buildAnnualNetCashFlow` 形状 `[-capex,net×life]` 且可与 npv/irr 串联、life<1/非有限→null / 确定性（同入参深相等）。
  - `sandbox-tech.test.ts`（25，中途重构 R2.1）— 技术能耗模型 `src/server/sandbox-tech.ts` **黄金样本**（§4 命脉计算层技术侧，透明简化年度能量平衡法，纯函数无 DB）：版本/calcRef/`TECH_METHODOLOGY`/`TRANSPARENT_SIMPLIFICATIONS`(S1–S5) 透明度声明守护 / 原子纯函数手算（`pvAnnualEnergyYear1` 500×1200×0.82=492000、`pvDegradedAnnualEnergy` 第 2 年 ×(1−0.5%)、`annualChargeEnergyDelivered` 15000×350=5,250,000、`acLoadFromDelivered` 5,250,000÷0.94≈5,585,106.38 且 eff≤0→NaN 除零诚实、`annualEnergyBalance` 自用=min/余电·下网=max 及 pv=0|load=0 比率记 0 及非有限→null、`storageAnnualThroughput` 400×0.88×min(350,600)=123,200 及寿命折算更紧时封顶年循环）/ 聚合 `computeTechModel` 用 R1.2 基线逐项手算全链（492000·5,250,000·5,585,106·损耗 335,106·自用 492,000·下网 5,093,106·自用率 100%·渗透率 8.8%·储能吞吐 123,200）+ `needsProfessionalReview:true`·`calcRef`·`methodology` 透传 / **★§4 命脉实证**（改 `dailyChargeEnergy`→负荷与下网全部重算、改 `pvCapacity`→出力与渗透率联动）/ 诚实降级（缺必备键→`missing_inputs` 列键名绝不猜默认、含非有限/除零→`invalid_inputs`、储能键为 0→降级但 `ok:true` `storageIncluded:false`）/ 确定性（同快照两次深相等）。
  - `sandbox-model.test.ts`（18，中途重构 R2.4）— 沙盘经济编排 `src/server/sandbox-model.ts` **黄金样本**（§4 命脉端到端——第一次把 R2.1 能量与 R1.2 价格参数撮合成现金流，§7 指标全程序算，纯函数无 DB）：版本/calcRef/`ECON` 契约守护 / 全链逐项手算（基线 CAPEX 毛 3,710,000·补贴后净 3,524,500；OPEX_Y1 336,300；收入_Y1 4,987,500；能耗成本_Y1 3,565,174；税前净现金流_Y1 1,086,026；折现后 `flows[1]=814,519`；`flows` 长度=计算期+1=16）+ 指标（NPV>0 ≈ 4,277,409、IRR `ok:true` ≈ 0.2376 且 `signChanges:1`、静态回收 4.2 年·动态回收 5.28 年、ROI ≈ 4.0035、盈亏平衡充电单价_Y1 ≈ 0.7431）/ **★§4 命脉实证**（`runSandboxModel({user:{values:{"project.chargingPrice":1.2}}})`→NPV/IRR/现金流全升；改 `region.elecPrice` 1.0→能耗成本升 NPV 降；`project.pvCapacity` 0→`capex.pv` 0 NPV 降；`technology.storagePowerKw` 0→`capex.storage` 0 且 `opex.storage` 0——证改任一关键参数下游经济结果即时重算，非只改页面数字）/ 诚实降级（缺经济键→`missing_econ_inputs` 列键、含非有限→`invalid_econ_inputs`、缺技术必备键→`tech_error` 不猜、chargingPrice 压到 0.5→NPV<0 如实回报不粉饰）/ `needsProfessionalReview:true` 恒在 + notes 透明 / 确定性（同 layers 两次 `toEqual`）。
  - `sandbox-sensitivity.test.ts`（15，中途重构 R2.3）— 敏感性 tornado + 高风险标记 `src/server/sandbox-sensitivity.ts` **黄金样本**（§14 优先级第 8 敏感性、§16 高风险须人工确认，one-at-a-time，建于已测 `runSandboxModel` 之上，纯函数无 DB）：版本/calcRef 守护 / `DEFAULT_SENSITIVITY_PARAMS` 8 关键参数全为合法已注册键 / `computeTornado`（默认指标 npv→`rows` 按 |摆动幅| 降序、`mostSensitiveKey` = 基线下 `project.chargingPrice`（±15% 摆动 +10,201,149）、`region.elecPrice` 次之（−7,718,787）、每行含 `low/base/high/swing/widthPct`、归一化宽度最大恒 100、`baseValue` 与直接 `runSandboxModel` 一致）/ 指标可切（metric:"irr"·"roi" 各自排序，irr 非 ok 时该值诚实记 null 排末）/ 扰动可覆写（`deltaPct` 传入覆盖参数缺省）/ `deriveRiskFlags`（两条基础标记恒在 + 条件触发：NPV<0 / IRR 非 ok / `multipleRootsPossible` / 回收期超计算期 / 模型整体失败，各点名理由）/ 确定性（同入参两次深相等）。**注**：`project.chargingPrice`（综合充电单价）系本版随 R2.4 一并补入 `sandbox-params.ts`（`SANDBOX_PARAMS_VERSION` 1.0.0→**1.1.0**，经 `num()` 工厂自动落 `ASSUMPTION`/`【占位假设·待核实】`/`confidence≤50`，受 R1.2 诚实不变式遍历守护），它是 §4 命脉要求的收入侧价格杠杆、必须为参数而非硬编码。
  - `sandbox-store.test.ts`（11，中途重构 R3）— 沙盘持久层纯映射 `src/server/sandbox-store.ts` 的 `projectCalcToColumns`（无 DB，`vi.mock` 顶掉 prisma/logger）：把 R2 golden `CalcResult`（NPV≈4,277,409 / capexNet 3,524,500 / IRR 0.237553 / 折现回收 5.28 / ROI ratio 4.0035）映射为 Prisma Decimal **字符串**入参并逐键断言 `capexNet "3524500.00"`·`npv "4277409.00"`·`irrPct "23.7553"`（×100）·`paybackYears "5.28"`（仅取折现回收期）·`roiRatio "4.0035"`（存 ratio）且小数位与列精度吻合 / `npv NaN→null`、`irr ok:false→irrPct null`、`折现回收期 null→paybackYears null`、`roi ok:false→roiRatio null` 四条降级不塞脏值 / `decimalStr` 非有限→`null`（防 `Infinity` 崩 Decimal）/ `chargingPrice 0.5 使 npv 以 "-" 开头`（**§4 命脉：输入变→持久化汇总列即变**）/ 失败态 `CalcResult`（`tech_error`）→ 五列全 `null` + `calcStatus=reason` / 确定性（同 `CalcResult` 两次深相等）。**注**：本文件覆盖纯映射逻辑；`createProject`/`updateScenarioLayers`/版本冻结回滚等连库行为见集成 `sandbox-store.test.ts`（下述 6 例）。
  - `sandbox-view.test.ts`（21，中途重构 R4）— 沙盘可视化视图模型 `src/lib/sandbox-view.ts` **黄金样本**（§8「图表绑定计算模型」的投影层——把引擎 `CalcResult`/`TechResult`/`Tornado` 纯映射为 recharts 可直接消费的系列，UI 层不再自己算数，防「图表数字 ≠ 模型输出」漂移，纯函数无 DB、`import type` only）：`VIEW_VERSION` 语义化版本守护 / 格式化器黄金值（`formatMoney` ≥1e8→「亿」、≥1e4→「万」、否则「元」且保符号、非有限/null→「—」；`formatPct` 分数→%、`formatPctRaw` 已×100→%、`formatRatio`→「×」、`formatYears` null→「从不回本」NaN→「—」、`formatEnergyKwh`）/ `cashFlowSeries` 前缀累加（`cumulative` = 逐年 flowing sum、非有限流量在累加中记 0 但保留原值供热力图 tooltip）/ `capexItems·opexItems·revenueItems·year1MoneyComparison·energyBalanceItems` 分解条形状（缺分量诚实省略不塞 0）/ `tornadoSeries` 相对基线增量（`deltaLow=lowMetric−base`、`deltaHigh=highMetric−base` 供发散条形图、`swing=null` 行原样保留不冒充）/ `summaryCards` 判别着色（NPV pos/neg/muted、IRR `beatsDiscount?pos:warn` 且 `multipleRootsPossible` 出提示、否则 muted + reason、动态回收期 null→「从不回本」、ROI ×100 走 `formatPct`、盈亏平衡单价）/ `buildSandboxViewModel` 失败态只回 `{ok:false,error:{reason,detail,missingInputs,invalidInputs}}` **绝不附带任何脏图表**、成功态组装全套卡片 + `meta.projectLifeYears=flows.length−1` + 格式化标签 / **★§8 真引擎绑定**（拉 `runSandboxModelBaseline` + `computeTechModel` + `computeTornado` 真实输出，断言 `cashFlow.length=annualCashFlow.length`、`cumulative` 末项 ≈ Σ flows、NPV 卡片 tone/值随引擎、`mostSensitiveLabel=tornado.rows[0].label`、能量平衡项与 tech 一致——证视图层数字真从模型流出非手搭）/ **★§4 动态实证**（动态 `import("@/server/sandbox-model")` 对比基线 vs `runSandboxModel({user:{values:{"project.chargingPrice":1.2}}})`，断言改充电单价→视图 NPV 卡即升，端到端证「改参数→图表数字变」）。**注**：本文件只测投影逻辑；`/sandbox` 工作台组件（`SandboxWorkbench.tsx`）在浏览器内用 `useMemo` 现算整链（客户端可算，无 prisma/logger 依赖）故无需 API 路由，交互活由下一行 `next build` 静态路由 `○ /sandbox` + HTTP 冒烟实证。
  - `sandbox-regions.test.ts`（10，中途重构 R5）— 沙盘地区 / 政策参数包 `src/server/sandbox-regions.ts` **黄金样本**（§6「选地区→载默认电价/光照/补贴、用户区间内覆写、过期政策不默认」的兑现层，纯数据 + 纯函数、无 DB/网络/时钟副作用，`import type` only）：`SANDBOX_REGIONS_VERSION`/`SANDBOX_REGION_IDS`/`DEFAULT_REGION_ID` 常量与目录守护 / `getRegionPack` 未知 id 诚实回落全国通用包**绝不裸抛** / `listRegionOptions` 与目录一一对应 / **★诚实不变式遍历**——凡给出覆写值的地区/政策层一律 `evidenceKind==="ASSUMPTION"`·`confidence≤50`·`source` 匹配 `/示例|待核实|占位/`（把"地区默认仍是占位假设、绝不当 FACT"焊进测试）/ `buildSandboxLayers(regionId,userValues,now?)` 组合契约（region/policy 层垫底、user 层在上、`now` 缺省注入真实 `Date` 而非引擎 `epoch0`、返回的 `policy` 与目录同一引用）/ §6 地区默认命中（山西 `region.elecPrice` 0.55 且 `origin==="region"`·`overridden:true`、`pvEquivalentHours` 1400、`policy.constructionSubsidy` 10 `origin==="policy"`、`feedInTariff` 0.33；全国通用包落回 R1.2 全局默认）/ **★§6 命脉双向过期实证**（固定 `now=2026`→故意已过期的碳价加计政策被跳过、`policy.carbonPrice` 落回默认 80 且 `notes` 含「过期」；回拨 `now=2022`→该碳价政策在效取 120，而 2024 起才生效的现行补贴政策**尚未生效**→`constructionSubsidy` 回落默认 5，双向证明窗口裁决不偏向"总是启用"）/ §6 用户覆写盖过地区默认（`elecPrice` 0.4 `origin==="user"` 未被裁剪）且**本省 `region.bounds` 透传到 user 层**后才真正对用户覆写限幅（`user.bounds["region.elecPrice"]` 等值 `{min:0.35,max:0.9}`、越界 5→0.9、0.1→0.35 且标 `clamped`）/ **★§4/§6 命脉端到端**（`runSandboxModel` 换山西→NPV 显著高于全国通用；`computeTornado({layers:山西包})` 的 `baseValue` ≈ 山西情景 NPV=锚当前情景；`computeTornado({})` 不传 `layers` 的 `baseValue` ≈ 全国基线 NPV=证向后兼容）/ 确定性（同参多调深相等）。**顺带**：R5 给 `sandbox-sensitivity.ts` 加性扩 `computeTornado` 可选 `layers` 入参（`SENSITIVITY_VERSION` 1.0.0→**1.1.0**），既有 `sandbox-sensitivity.test.ts` **15 例逐字不变、全绿**（省略 `layers` 即回落旧「锚全局基线」行为，向后兼容由上条端到端用例二次坐实）。
  - `sandbox-report.test.ts`（14，中途重构 R6.1 · R7 企业个性化 +3）— 沙盘动态报告纯函数 `src/lib/sandbox-report.ts` **黄金样本**（§14 第 5 项 / §9「报告读最新 `CalcResult`」的确定性叙述层——把 R4 `SandboxViewModel`（数字全源自 R2 引擎）翻译成结构化中文决策报告，本模块**零 AI / 零网络 / 零 DB / 零时钟 / 零随机、只 `import type` 视图模型、绝不重算任何指标**，是 §7「程序算、LLM 只解释」里程序负责的那一半，R6.2 的 AI 解释只会在此骨架上追加点评、不改数）：`REPORT_VERSION` 语义化版本守护 + 成功报告 `ok:true`·带 `needsProfessionalReview`·标题含地区名 / **★单一真源（第 16 条）**——执行摘要里 NPV/IRR/回收期/ROI 四串**逐字等于** `vm.cards` 对应卡值、投资结构分节六项金额**逐字等于** `vm.meta` 标签（含 `计算期=${projectLifeYears} 年`）、溯源分节 `calcRef`/模型·参数模板版本/`REPORT_VERSION` 逐字等于 `vm.calcRef`/`vm.engineVersions`/常量（证报告不自己换算、正负判断仅看已格式化 NPV 串是否 `-` 起头属呈现层）/ **★诚实边界（§16/第 20 条）**——喂真实失败 `runSandboxModel({user:{values:{"project.pvCapacity":NaN}}})`→视图 `ok:false`→报告 `ok:false`、`sections` 恰 `["error"]` 一节、**不含任何 `exec`/`structure` 脏结论**、但三条免责（程序算非 LLM / 需专业人工确认 / §17）恒在 / 交互来路叙述（传 2 项 `changedParams`→执行摘要显「改动了 2 项」+ 各「标签=值 单位」；空改动→显「未作个性化改动」；有龙卷风→敏感性分节点名 `vm.mostSensitiveLabel`）/ **★§4/§9 联动**——基线 vs `runSandboxModel({user:{values:{"project.chargingPrice":1.5}}})` 两报告不同且都 `ok:true`、抬高充电单价后 NPV 卡串随之变化且该串出现在新报告执行摘要里（实证报告随最新 `CalcResult` 整份重写）/ 确定性（同 `vm` + 同上下文两次 `toEqual`）。**注**：`okViewModel()` 喂**真实引擎输出**（`runSandboxModelBaseline` + `resolveSandbox` + 从 `@/server/sandbox-tech` 取 `computeTechModel` + `computeTornado` → `buildSandboxViewModel`）端到端证报告数字真从模型流出非手搭；`SandboxReportPanel.tsx` 为纯展示件不新增单测，其交互活由「生成/收起动态报告」按钮在浏览器内经 `useMemo` 现算整份报告（`next build` 静态路由 `○ /sandbox` 不变、零新路由、零 DB）实证。**（R7 扩 +3）**：不传 `profile`→报告无 `"profile"` 节（向后兼容 1.0.0）；传 `fleet` 画像→`"profile"`「企业个性化视角」节**紧随执行摘要**、其金额/百分比**逐字取自 `vm.cards`**（不重算）且带「依画像裁剪·可覆写·需专业人工确认」诚实脚注；**失败报告忽略 profile**→`sections` 仍恰 `["error"]`（脏场景绝不套个性化结论）。
  - `sandbox-profiles.test.ts`（13，中途重构 R7）— 沙盘企业画像 `src/server/sandbox-profiles.ts` **黄金样本**（§14 第 7 项「依企业画像裁剪方案」的兑现层，纯数据 + 纯函数、无 DB/网络/时钟/随机、`import type` only）：`SANDBOX_PROFILES_VERSION`/`profileCalcRef()`/`DEFAULT_PROFILE_ID`/`SANDBOX_PROFILES[0].id==="generic"` 常量与目录守护 / **`validateProfileCatalog()===[]`**（一把纯函数把「默认置首 + id 唯一 + 全 `ASSUMPTION` + `confidence≤50` + 预设键命中 `REGISTERED_KEYS` + 值类型 + `emphasis.metricKeys⊆PROFILE_METRIC_KEYS`·`focusTags⊆PROFILE_FOCUS_TAGS`」整套不变式钉死）/ `getEnterpriseProfile` 未知 id 诚实回落 `generic` **绝不裸抛** / **★诚实不变式遍历**凡画像 `evidenceKind==="ASSUMPTION"`·`confidence≤50`·`source` 冠「【示例·待核实】」（把"画像是典型量级占位预设、绝不当 FACT"焊进测试）/ 预设键全部为已注册参数键且落在其 `bounds` 界内 / `emphasis` 合法 / **★`generic`（空预设）经 `buildProfileLayers` ≡ 直接 `buildSandboxLayers` 深相等**（护住 R6 黄金基线逐字节不变）/ **★优先级实证**画像预设胜全局默认（`resolveSandbox` 后该键 `origin==="user"` 且取预设值）、用户覆写再胜画像（同键传入 `userValues`→取用户值、`isProfileDefault` 转 false）、未覆写键保留画像预设、`generic` 时 `region.elecPrice` 落回通用默认 0.7 / **地区 `bounds` 透传裁剪**（画像叠在山西包上时越界电价仍被裁到本省上下界且标 `clamped`）/ **★§4 命脉端到端**（`investor`·`operator` 画像喂 `runSandboxModel`→NPV 与 `generic` 相异且皆 `Number.isFinite`，证画像真改变经济结果非只换标题）/ 确定性（同 `(profileId,regionId,overrides,now)` 两次解析 `numeric` 深相等）/ `isProfileDefault` 真值矩阵（`generic` 恒 false、预设内且未被覆写→true、被用户覆写→false、非预设键→false）。
  - `sandbox-solution.test.ts`（10，中途重构 R8.1）— 沙盘结果 → 产业方案（Solution）**草案**纯映射桥 `src/lib/sandbox-solution.ts` **黄金样本**（总控最高优先级「商业闭环」的第一块拼图——把一次沙盘 `CalcResult` + 视图模型 +（可选）企业画像确定性地映射成符合 34 分节规范的 `Solution` 草案，供后续 UI 导出 / 后台发布 / 挂购买；**无 DB、无网络、无时钟/随机，`okDraft()` 喂真实引擎输出**（`runSandboxModelBaseline` + `resolveSandbox` + `computeTechModel` + `computeTornado` → `buildSandboxViewModel`），只搬运不重算）：`SANDBOX_SOLUTION_VERSION` 语义版本守护 + `sandboxSolutionCalcRef()` 格式 / 成功草案字段齐备（`title/slug/summary/body/financials/unknowns/riskDomains`、`needsProfessionalReview:true`、`evidenceGrade:"ASSUMPTION"`、`provenance` 钉 `calcRef`+各内核版本+`profilesVersion`、恒挂 `publishBlockers`）/ **★§7/§8 单一真源**——`financials` 各 Decimal 串**逐字 ===** `calc` 原值（`capex=calc.capex.net` 等经 `decimalOr` 定点化）且 NPV 原始值入 `assumptions`、`body` 各节数字串**取自 `vm.cards`**（不再算第二遍）/ **★诚实降级**——负数/非有限一律省略不塞进 `financials`（不用 `DecimalStringSchema` 假通过）、`chargingPrice` 压到使 NPV<0 → 该负 NPV 被**点名进 `riskAnalysis` 节 + `publishBlockers`**（不粉饰）/ **失败草案**——喂 `!calc.ok || !vm.ok` → `ok:false` 带诚实 `error`、`(d as any).body`/`.financials` **均 undefined**（脏场景绝不产出编造方案）/ `unknowns` 恒 ≥1（含「全部入参为占位假设」severity 90）/ **定价门**——未给 `price` → `publishBlockers` 含「未定价」；给 `price` → 该阻塞消除且 `draft.price` 置值 / 画像注入 `targetEnterprises` 节与 `summary`（`profile` 传入时）/ 确定性（同 `calc`+同 `vm`+同上下文两次 `toEqual`）。**注**：本桥刻意**止于草案、绝不写库**——`Solution.caseId` 为必填非空外键，直写会重蹈 R6.4 `regionId` 的 `P2003`，`publishBlockers` 里显式列「未附 `caseId`」把「选真实案例挂载 → 发布」这一关键决策交回人（AI 做劳动、人做关键决策），落库端点留 R8.2。
  - `sandbox-solution-store.test.ts`（9，中途重构 R8.2）— 沙盘草案 → `Solution` 落库编排 `src/server/sandbox-solution-store.ts`（`vi.mock` 顶掉 `@/lib/prisma`+`@/lib/logger`、`vi.mock("@/server/solution-admin", importOriginal)` **只 mock 三个写函数 `createSolution`/`addSolutionFinancial`/`addSolutionUnknown`、保留真 Zod schema 入 `importOriginal`**，**无 DB/无网络/无时钟**）：`SANDBOX_SOLUTION_STORE_VERSION` 语义版本守护 / `SandboxSolutionPersistSchema` 接受良构草案（caseId·title·slug·financials·unknowns·price 等）/ **★成功路径**——`createSolution` 以 `{title,slug,caseId,price,currency,riskDomains,needsProfessionalReview}` 被调且 `actor` 透传，返回 `ok` 后逐条转交 `addSolutionFinancial`/`addSolutionUnknown`（各带回填的 `solutionId`）、回 `{status:"ok",financialCount,unknownCount}` 且 `echo` 原 `publishBlockers` / `needsProfessionalReview` **缺省→true**（诚实默认须人工确认）/ **坏 `caseId`（非 cuid 形状）→ `invalid`+`fieldErrors.caseId` 且 `createSolution` 零调用**（早退不触库）/ **负价格串 → `invalid.fieldErrors.price`**（本地 `PriceStringSchema` 拒负号）/ `createSolution` 返 `invalid`（如 FK 预检失败）→ **直接前转状态、`addSolutionFinancial`/`addSolutionUnknown` 零调用**（草案不残入半份方案）/ **财务单条落库失败 → 整体仍 `ok` 但降级为 `warnings`（`财务条目落库未成功…`）且 `financialCount` 只计成功条**（一份 DRAFT 不因一条脏财务报废）/ 未知单条失败同理 + 空 `financials/unknowns` 数组不调用。**注**：本文件覆盖结构化校验 + 复用既有写函数的编排契约；真实 Neon 列精度/JSONB/FK/级联/`ChangeLog` 见集成 `sandbox-solution-store.test.ts`（下述 3 例）。
  - `sandbox-solution-lineage.test.ts`（10，中途重构 R8.3）— 沙盘来源 Solution 溯源检测 `src/lib/sandbox-solution-lineage.ts` **黄金样本**（总控最高优先级「商业闭环」上的**诚实声明地基**——购买者看一份已发布方案时，须能机器判定「这套数字是不是沙盘推演出来的占位假设」，故把 R8.1 草案写进每条 `SolutionFinancial.assumptions.solutionCalcRef` 的 `sandbox-solution@<semver>` 前缀，**读回来识别来源**；client-safe 纯函数、**无 DB/无网络/无时钟/无随机、`import type` only、绝不重算任何指标**）：`SANDBOX_SOLUTION_LINEAGE_VERSION`（`1.0.0`）语义版本守护 + `SANDBOX_SOLUTION_ORIGIN_PREFIX`（`"sandbox-solution@"`）前缀常量守护 / **空 `financials`→`describeSandboxLineage` 回 `null`、`isSandboxSourcedSolution` 回 `false`**（无财务条目即无从判源，诚实不猜）/ **手工方案不误伤**——`calcRef:null`、`assumptions` 非对象、无 `solutionCalcRef` 键、`solutionCalcRef` 为**别的前缀**（如 `"manual@…"`、`"model@1.0.0"`）一律 `null`/`false`（**精确前缀匹配、非启发式**，防把普通方案错标成沙盘）/ 命中→**逐字段原值读出**（`solutionCalcRef`/`draftVersion`/`engineCalcRef`/`engineVersions`/`regionName`/`profileName`/`evidenceKind`/`generatedBySandbox:true`，检测层**只搬运不改写**）/ 多条财务取**首条命中**（`some`/`find` 语义自洽）/ 非字符串字段诚实降 `null`（`regionName:123`→`null`、`engineVersions:"nope"`→`null`、`evidenceKind:""`→`null`，脏值不冒充合法标签）/ `npvNonPositive` 判定真值表（`npv<0`→`true`、`npv>0`→`false`、`NaN`/`null`/`undefined`/非数字→`false`——**只在确有负 NPV 时点名，缺值绝不谎报「非正」**）/ **★漂移守护（单一真源 §16）**——跑**真实引擎链**（`runSandboxModelBaseline`→`resolveSandbox`→`computeTechModel`→`computeTornado`→`buildSandboxViewModel`→`buildSandboxSolutionDraft`）产出的 `draft.financials` **必须**被本检测器识别为沙盘来源，且 `draftVersion === SANDBOX_SOLUTION_VERSION`、`engineCalcRef` 匹配 `/^model@/`、`evidenceKind==="ASSUMPTION"`、`npvNonPositive===false`——把**草案写入端（R8.1）与溯源读取端（R8.3）两份契约焊死**，谁改了 `assumptions.solutionCalcRef` 的写法却忘了同步本检测器就红 / 确定性（同 `financials` 两次 `toEqual`）。**注**：本文件只测纯识别逻辑；`/solutions/[id]` 详情页据 `describeSandboxLineage(s.financials)` 渲「沙盘推演生成」徽章 + `info` `Alert` 诚实披露（非沙盘方案 `lineage=null` 故完全不出现），该呈现由集成 `sandbox-solution-loop.test.ts`（下述 2 例）连真 Neon 端到端坐实。
  - `sandbox-solution-provenance.test.ts`（25，中途重构 R8.4）— 沙盘来源 Solution 溯源审计与 ASSUMPTION→FACT 升级契约 `src/lib/sandbox-solution-provenance.ts` **黄金样本**（总控最高优先级「商业闭环」的**数据治理脊柱**——识别只答「数字从哪类来源来」，本层答宪法第 7/12 条更要紧的两问：落库途中数字是否仍可由引擎源值复算（公式可复算）、要从占位假设升级为可售事实闸门在哪；client-safe 纯函数、**无 DB/网络/时钟/随机、`import type` only、只比存量绝不重跑引擎**（§8 禁再算一遍））：`SANDBOX_SOLUTION_PROVENANCE_VERSION`（`1.0.0`）语义版本守护 + `provenanceCalcRef()`（`"sandbox-solution-provenance@1.0.0"`）/ **`isUsableSourceUrl` 真值矩阵**（`http/https`（含大写、可 trim）→真；无协议 `example.com`、伪协议 `javascript:`、空串、`null`/`undefined`/非串、含空格 →假，宁可误拒）；**★`verifyReproducibility`（公式可复算）**——把已落库十进制串与同一行 `assumptions` 源值经确定换算带容差比对：`roiPct/irrPct/paybackYears` 三项全命中→`reproducible:true`+`checks` 长度 3、各自被篡改成明显错值（`999.99`/`10.00`/`12.00`）→`reproducible:false` 且 `issues` 点名对应 metric、**★源值缺失（`roiRatio:null`，沙盘对负/算不出指标会诚实省略）或落库串缺失各自记 `skipped` 且不误判失败**（无从复算≠复算失败）、`assumptions` 非对象三项全 skip 不抛、末位舍入（`roiPct=400.35` 对 `4.003502×100`）落容差内为通过；**★`summarizeTraceability`（来源可追溯）**——判「可追溯 = `evidenceKind==="FACT"` **且** 带合法 http(s) `sourceUrl`」两者缺一即不可追溯：全 ASSUMPTION→`traceableCount:0`·`fullyTraceable:false`、FACT+链接→计 1 但混入 ASSUMPTION 即 `fullyTraceable:false`、**FACT 无链接→不可追溯**、**ASSUMPTION 有链接→仍不可追溯**、全 FACT+链接→`fullyTraceable:true`；`evaluateSandboxSolutionProvenance` 综合体检（空数组诚实空态 count 0·摘要含「未携带」·不臆断可追溯、基线导出 `allReproducible:true`+`0/N`+摘要含「复算」「0/1」、有异常→`allReproducible:false`+`issues` 非空+摘要含「对不上」）；**★`planProvenanceUpgrade`（受控升级闸门）**——合法 http(s) 链接 + `[0,100]` 数值置信度→`willUpgrade:true`+目标 `FACT`+规范化 `sourceUrl`（trim）+`confidence`，缺链接/缺置信度/置信度越界（150·-1）/伪协议（`javascript:`）各自 `willUpgrade:false`+**保留 ASSUMPTION + 给明确 reason**（无出处绝不粉饰成事实、绝不静默）；**★反漂移**喂真实引擎链（同 lineage 那套）产出的 `draft.financials`→`allReproducible:true`（写侧 `toFixed` 与读侧换算同一口径）+ 未附来源 `traceableCount:0`；确定性（同输入两次 `toEqual`）。**注**：`/solutions/[id]` 详情页对沙盘来源方案追加的一行只读溯源审计，由集成 `sandbox-solution-provenance.test.ts`（下述 2 例）连真 Neon 坐实真实 Decimal 列舍入下的可复算性。
  - `sandbox-provenance-store.test.ts`（11，中途重构 R8.5）— 沙盘来源 Solution 的 **ASSUMPTION→FACT 升级写路径编排** `src/server/sandbox-provenance-store.ts` 黄金样本（R8.4 那条 `planProvenanceUpgrade` 升级闸门一直**只产预案不落库**，本层补上「提交真实来源→受门禁认证落库」写路径；`importOriginal` **保留真实的 `planProvenanceUpgrade`**（升级判据正是本模块要验证被原样消费的核心，断言必须走同一份闸门逻辑，绝不 mock）、`vi.hoisted` 造可编程 spy 顶掉 `@/lib/prisma`（工厂会被 hoist 故不能用后定义的 `const`）、顶掉 logger，**不触真库、无 DB/网络/key/时钟**）：`SANDBOX_PROVENANCE_STORE_VERSION`（`1.0.0`）语义版本守护 + 意图 schema 收合法 `{sourceUrl,confidence,note}`；`financialId` 非 cuid → `not_found` 且 `findUnique` 都不必查、无写；记录不存在 → `not_found` 无写；**★缺来源链接 → `blocked`**（透传闸门 `reason` 点名「来源链接」）且 `update`/`changeLog.create` **绝不被调用**（§20 拒不粉饰占位假设）、非法/相对 URL（`ftp://`）同样被闸门拒 `blocked` 无写、**缺置信度** → `blocked` 点名「置信度」无写、**置信度越界(140)** → `blocked` 点名「越界」无写；**★合法来源 + 置信度 → `ok`**：断言 `solutionFinancial.update` 收到 `where:{id}` + `data.sourceUrl`（trim 后）+ `data.version:{increment:1}`，写入的 `assumptions` **`evidenceKind:"FACT"` + `sourceUrl` + `confidence`**、且 ★**合并保留 `solutionCalcRef/roiRatio/irrFraction/discountedPaybackYears` 逐一等于原值**（否则 R8.3 识别 / R8.4 复算会失效）、`provenanceUpgrade` 升维戳 `from:"ASSUMPTION"`/`to:"FACT"`/`upgradedBy`/`note` 齐；`ChangeLog` 断言 `entityType:"Solution"`/`entityId=solutionId`/`action:"UPDATE"`/`changedBy=actor`/`before.financial.version=1`/`after.financial.evidenceKind="FACT"`；落库抛 `P2025` → `not_found`（并发删除诚实回落）、抛未知错 → `error` 透传 message（不裸抛）。**注**：升级「只改溯源、绝不碰 Decimal 列/源值」这一不破坏脊柱的核心保证，由集成 `sandbox-provenance-store.test.ts`（下述 3 例）连真 Neon 坐实（升级后复算仍 `allReproducible` + `isSandboxSourcedSolution` 仍 true）。
  - `sandbox-explain.test.ts`（12，中途重构 R6.2）— 沙盘「AI 解释」编排 `src/server/sandbox-explain.ts`（**无 DB、无网络、无 key、无时钟/随机**——注入确定性 `StubProvider + fixture` 离线跑满契约，与 `research-pipeline`/`solution-generation` 同构）：常量守护（`EXPLAIN_VERSION` 语义版本 / `EXPLAIN_TIER="medium"`（§32 非最贵）/ `EXPLAIN_AGENT="sandbox:explain"`）；`sandboxReportInputSchema`（接受良构报告 / 拒空 `sections` / 拒非法 `kind:"table"`）；**`buildExplainPrompt` 纯函数**（同入参两次全等确定性、**★单一真源——报告里的数字（"NPV 427.74 万元"等）逐字进入 prompt**、禁算数关键词齐备（"不得""重新计算""定性""需专业人工确认"）、用户追问截断 >2000）；**`explainSandboxScenario` 判别联合契约**（① **★诚实拒绝**：`report.ok=false` 直返 `blocked` 且 `createMemoryRecorder().calls.length===0` 证拒绝发生在调用 provider **之前**（喂只 throw 的 `BOOM_PROVIDER` 仍不触发）② 成功：`StubProvider + 完整 fixture` → `ok`、回 `explanation`、`cost.calls===1` 且 `totalCostUsd` 有限非负（§31 程序算）、`recorder` 收到 `agent=sandbox:explain`/`taskKind=structured_output`/`tier=medium`/`status=ok` ③ 最小 fixture `{interpretation}` → `keyDrivers/whatIf/risks` 缺省 `[]`、`needsHumanReview` 缺省 `true`（宁可多标）④ **schema 不过归一 `error`**（`interpretation:""` 违 `min(1)` → `status:"error"` 含「结构校验」、recorder 记 `schema_invalid`，绝不半渲染）⑤ provider 抛错归一 `error`（`BOOM_PROVIDER` 的 `"boom-network"` 透出、不崩不误报））；`explanationSchema` 边界（空 interpretation 拒、缺省数组/needsHumanReview）。**注**：真实 DeepSeek 端到端冒烟为一次性本地脚本（喂真引擎报告、证 LLM 逐字引用报告数字 + what-if 全定性），跑毕移入回收站、**绝不入仓**。**★`SandboxExplainPanel.tsx` 客户端 fetch 无专项单测**——薄封装 `mutateJson` 已被 `mutate.test.ts`（8）锁死，面板只是其调用方；路由门禁 `requireSameOriginActor`+登录判定复用既有已测原语。
  - `sandbox-projects.test.ts`（20，中途重构 R6.3）— 沙盘「项目/情景/版本」编排 + 所有权门禁 `src/server/sandbox-projects.ts`（server 域逻辑，`vi.mock` 顶掉 `@/lib/prisma`+`@/lib/logger`+`@/server/sandbox-store`，**无 DB/无网络/无时钟**）：`SANDBOX_PROJECTS_VERSION` 语义版本守护 / **★`canAccessProject` 纯函数真值矩阵（§28 权限重点）**——`ownerId===user.id`→true、同 id 但空 ownerId→false、非属主的普通 USER→false、`STAFF_ROLES`（REVIEWER/ADMIN）对任意 ownerId→true、USER 角色即便 id 巧合仍需 ownerId 命中 / `createSandboxProject` **★强制服务端身份（防冒名）**——回传的 `ownerId` 恒取 `ctx.user.id`、请求体里客户端夹带的 `ownerId` 被无视、`actor=human:<id>` 由会话派生、`layers` 原样交 `sandbox-store` 现算（服务端重跑引擎非搬页面数字，§4）/ `Zod` 边界——name trim 且 1–200、description≤2000、regionId≤100、缺省 `layers:{}`；非法 name→`invalid{fieldErrors}` 且**根本不调 store**（早退不触库）/ **★`layersSchema` 不透明透传（`z.record(z.string(),z.any())`）**——断言政策层的 `effectiveFrom/effectiveUntil/bounds` 等日期窗与限幅键**逐字保留**（若做深层 `ValueLayer` 精校会被剥掉这些字段 → 破坏 §6 过期窗回放，故刻意松校）/ `updateSandboxScenario`·`saveSandboxScenarioVersion`·`restoreSandboxVersion` 三条**访问检查先于 store**——`accessByScenarioId` 取不到（情景不存在）→`not_found`、取到但非属主且非 staff→`forbidden`（`canAccessProject:false`）且对应 store 函数**零调用**（越权时连引擎都不重跑）、放行时才透传调用 / restore 把 store 返回的 `reason:"forbidden"`（跨情景回滚）映射成 `status:"forbidden"` / 读侧 `readSandboxProject`·`readSandboxScenarioVersions` 同样先鉴权后读、`toScenarioView` 把 Decimal→`Number`、Date→ISO **且刻意省略体积最大的 `calcResult`**（列表视图不带完整快照防 payload 膨胀）/ 判别联合四态（ok/invalid/not_found/forbidden/error）不裸抛、`error` 分支由 store 抛错被 catch 归一。
  - `sandbox-store-now.test.ts`（7，中途重构 R6.3）— `toEngineLayers` 时钟/日期窗复活纯函数（`src/server/sandbox-store.ts`，**无 DB**，直接 import 不 mock store 的 DB 函数因其不在被测路径）：**★核心实证（R6.3 修的真 bug）**——`resolveParameters` 里 `now < layer.effectiveFrom` 若把 JSON 反序列化出的 **ISO 字符串**与真实 `Date` 混比，关系运算符两侧被 `Number()` 强转 → 字符串侧 `NaN` → 比较恒 false → 过期/未生效政策被**误判为现行**；本文件喂只带 ISO 日期串的持久化分层，断言 `toEngineLayers` 把 `now` 与各层 `effectiveFrom/effectiveUntil` **全部复活成真正 `Date` 实例**（`instanceof Date` 且 `getTime()` 有效）后再交引擎，窗口裁决随之正确（回拨 `now` 使过期政策落回默认、现行政策生效）/ `region/policy[]/user` 三层缺省即不注入（空分层→ `{}`，向后兼容 R3 仅传 `user` 无日期窗的既有 golden 逐字不变）/ `policy` 数组逐条 `reviveValueLayer`、无有效日期键则**删除该键而非塞 Invalid Date**（`Number.isNaN(getTime())`→`undefined`）/ `now` 为非法串/`null`→省略回落引擎 epoch-0（诚实不猜时钟）/ 已是真 `Date` 的输入幂等原样透传。
  - `authz.test.ts`（15，Phase 6 M2）— 鉴权原语 `src/server/authz.ts`（总控 §28「权限测试」重点）：`hasRole` 纯函数真值矩阵（ADMIN/REVIEWER∈STAFF、USER∉、undefined 保守拒绝、允许集合空一律 false、非员工集合精确匹配）/ `STAFF_ROLES=[REVIEWER,ADMIN]` 守护（不含 USER）/ `getCurrentUser`（有效会话→身份视图、无会话→null、**会话缺 id 保守视为未登录**）/ `requireUser`（已登录 ok、未登录 unauthenticated）/ `requireRole(STAFF)`（未登录 unauthenticated、USER→forbidden 且带 required 集合、REVIEWER→ok、ADMIN→ok）。用 `vi.mock("@/auth")` 固定会话，无 DB。
  - `api-guard.test.ts`（22，Phase 13 M1 + Phase 12 M2）— 后台写端点门禁 `src/server/api-guard.ts`（`vi.mock("@/server/authz")` 固定 `requireRole`+`getCurrentUser`）：`isSameOrigin` 纯函数边界（无 Origin/Referer→放行 / 同源 host 精确相等→放行 / 跨站 host·异端口·异子域→拒 / 无法解析→拒 / 空串视作无头→放行）+ `actorOf`（`human:<userId>`，不接受客户端 actor）+ `mutationResponse` 五态映射（ok→200 剥 status 透出 caseId/recompute/**orderId/deduped/order**、invalid→400 details.fields、not_found→404、blocked→409、error→500 非生产透出原始 message）+ 未知状态兜底 500 + `errorResponse` 带/不带 details 两形态 + `requireStaffWrite` 组合判定（跨站 Origin 先挡 403 且**根本不调 requireRole**、未登录 401、USER 403 透出 required、ADMIN 放行注入 actor、无 Origin 但同源 Referer 放行、跨站 Referer 拒）+ **（Phase 12 M2）`requireSameOriginActor` 3 例**（公开下单门禁：跨站→403 且根本不查会话、登录→ok 且注入 user+actor、游客→ok 且 user/actor 为 null，三者均**绝不调用 `requireRole`**——证明下单不设角色门禁）。CSRF + 角色/身份门禁的纯逻辑边界，禁口算、与真实 HTTP/DB 解耦。
  - `orders-schema.test.ts`（7，Phase 12 M1）— 订单入参 `OrderCreateSchema` 纯契约（`vi.mock("@/lib/prisma")`，无 DB）：游客带归一 buyerEmail 通过且 buyerType 缺省 INDIVIDUAL / 登录带 userId 通过 / 二者皆缺 → invalid 且错误在 `identity` 路径 / **白名单解析把客户端夹带的 amount/status/currency/price 全部剥离（守"钱"路径、防 0.01 篡改下单）** / 非法邮箱拒 / 非法 buyerType 拒 / solutionId 非 cuid 形状拒。金额快照、幂等、状态机等写入行为由集成测试 `orders.test.ts` 覆盖。
  - `site.test.ts`（17，Phase 14 M1）— SEO 站点级单一配置源 `src/lib/site.ts` 纯函数（客户端/服务端通用，仅 type-import Next `Metadata`）：因 `SITE_URL` 是模块级 const，每个用例用 `loadSite(env)`=`vi.resetModules()`+`vi.stubEnv(...)`+动态 `import("@/lib/site")` 重新求值。覆盖 SITE_URL 归一（去尾斜杠 / **空串回落到本地开发地址——`?.trim() || fallback` 而非 `??`，因空白 `.env` 值是 `""` 非 `undefined`**）/ `isIndexable()` 判定矩阵（NODE_ENV≠production→false / production+真实域名·子域→true / production+空域名→false / production+localhost·127.0.0.1·0.0.0.0 等本地主机→false / production+无法解析→false，未部署期与预览环境恒不进索引）/ `absoluteUrl`（根路径不产生双斜杠 / 子目录 / 无前导斜杠 / 已是绝对 URL 原样透传）/ `seoMetadata` 输出形状 + **「缺字段不写空值」不变式**（无 title/description 时对应 OG/twitter 键根本不出现，避免 `<meta content="">` 污染）。
  - `model-router.test.ts`（18，Phase 9 M1）— 统一 Model Router 骨架 `src/server/model-router.ts` 纯函数与离线契约（无 DB、无 HTTP、无真实模型、无 API Key）：常量守护（恰 6 类 `MODEL_TASK_KINDS`·3 档 `MODEL_TIERS`·版本字符串 `MODEL_ROUTER_VERSION=1.1.0`（M3 起 `MODEL_CATALOG` 改为 env 可覆写、缺省回落与 1.0.0 完全一致的占位默认，故离线单测成本黄金值不变） / `ROUTING_POLICY` 每类任务映射到合法档位（`research→high`、`generate_text/embedding→low`）/ `MODEL_CATALOG` 三档 id 唯一且单价均为正数）；`estimateCost` **黄金值**（high 档 1K prompt+1K completion=0.032 USD / low 档 2K=0.001 / 负数·NaN·非数一律 clamp 到 0 / 结果保留 ≤6 位小数——**成本由程序算非 LLM 口算**，宪法第 7 条）；`StubProvider` 确定性（同 prompt 两次 `complete` 的 embedding 向量与 usage 全等 / `respondWith` 指定 json 时原样透传 / embedding 恒返回 dim-8 单位向量）；`runTask` 判别联合契约（`generate_text`→ok 且落 low 档 / 显式 `tier:"high"` 覆写策略→`stub-max` / `structuredOutput` schema 合法→ok 且 `estimatedCostUsd>0` / schema 非法→`schema_invalid` 且 `issues[0].path` 含字段名 **但 `CallRecord` 仍照常产出计费（成败都记成本）** / provider 抛错→`provider_error` 不裸抛、usage·cost 归零但 status·tier 仍记录 / `embed`→dim 8 / `taskId` 匹配 `/^generate_text-\d+$/` 且自增唯一 / `createMemoryRecorder` 累计 `totalCostUsd` / `agent·caseId·solutionId` 透传进 `CallRecord`）。
  - `deepseek-provider.test.ts`（24，Phase 9 M3）— 第一个真接网络的 `ChatProvider` 实现 `src/server/deepseek-provider.ts`（OpenAI 兼容 `/chat/completions`），**注入假 `fetch`、绝不依赖 `process.env`（单测不加载 `.env`）亦无需真 key/网络**，把「桩→真实供应商」接缝的整套契约离线测死：**`deepSeekConfigFromEnv`**（无/纯空白 key→`null` / 有 key 无 base→默认 `https://api.deepseek.com` / 自定义 base 剥尾斜杠 / key 首尾空白裁剪）；**`createChatProvider` 工厂**（无 key→回落确定性 `StubProvider`、id `stub`；有 key→`DeepSeekProvider`、id `deepseek`——开发/CI 无 key 自动走桩、生产配 key 即真实调用，业务零分支）；**构造期早失败**（空 apiKey 抛错且**错误信息不回显 key**）；**`complete` 请求构造**（`POST` 到 `<base>/chat/completions`、`Authorization: Bearer <key>` 头、`body.model` 用目录注入的真实模型 id、`messages[0].content=prompt`、**非结构化任务不带 `response_format`、结构化任务带 `{type:json_object}`**、`signal` 透传给 fetch、自定义 baseUrl 正确拼接）；**响应解析**（文本→`text` / 合法 JSON content→同时回 `json`+`text` / **非法 JSON→诚实只回 `text`、`json` 留 `undefined`** / `usage` 缺失或负数→归零不 NaN / 响应无 `model` 字段→回落请求 `model.id`）；**边界**（`embedding` 任务直抛「不支持」且**根本不发起 fetch** / 非 2xx→抛「`HTTP <status>`」且**错误信息不含 key、不含响应体**）；**`runTask` × 真 provider 集成**（结构化：假 fetch 回合法 JSON + Zod schema→`status:ok`·解析后 `json`·`record.providerId:"deepseek"`·`estimatedCostUsd=0.008`（medium 默认单价 1K+1K 程序计算）/ fetch 抛错→`runTask` 归一 `provider_error` **不外溢异常** / content 非法 JSON→`schema_invalid` 诚实暴露不静默放过）。**另附本地一次性真实冒烟**（脚本放 workspace scratch、不进仓库）：`createChatProvider()` 走 env 真 key 打真 DeepSeek——`generate_text`→`ok`「你好！」`modelId=deepseek-chat`、`structured_output`→经 Zod 校验的 `{"language":"中文","greeting":"你好，世界！"}`、成本均由程序计算，端到端证接缝可用。
  - `research-pipeline.test.ts`（15，Phase 9 M2）— §33 多角色研究流水线骨架 `src/server/research-pipeline.ts` 纯函数与离线编排契约（无 DB、无网络、无 API Key，全委托 M1 `runTask` + 确定性 `StubProvider`）：**常量守护**（恰 5 角色固定序 `research→bull→bear→judge→qa` / `PIPELINE_VERSION 1.1.0` / 每角色 tier 合法且 research·judge→high·bull·bear·qa→medium / `EVIDENCE_KINDS=FACT·ASSUMPTION·INFERENCE·PREDICTION`）；**各角色 schema 契约**（黄金样本全通过 / `research.summary` 空→拒 / `judge.verdict` 非枚举→拒 / **空 `points`/`findings` 数组被允许且 `.default([])`——诚实原则：没查到不硬编造**）；**complete 全路径**（五角色 fixtures 齐备→`status:complete`、typed 输出齐全、`cost.calls=5` 且 `totalCostUsd>0` **且恰等于各 `CallRecord.estimatedCostUsd` 之和**（自洽）、5 条记录按 `agent=pipeline:<role>` 归因、`caseId/solutionId` 透传进每条、`roleTiers` 反映默认与 `tierOverrides` 覆写）；**QA 门禁三态**（`approved=false`→`needs_human_review` 含「未批准」/ `qualityScore<阈值`→含分数与阈值数字 / `needsHumanReview=true`→含「人工复核」/ `qaThreshold` 覆写翻转同一 75 分输出：默认放行 vs 抬到 80 变暂缓 / 暂缓仍保留五段输出与成本，证「暂缓≠丢失成果」）；**诚实早退**（bull 非法→`failed stage=bull kind=schema_invalid` 保留 research 记录、bear/judge/qa `undefined` 不臆造、2 条记录；bear provider 抛→`failed stage=bear kind=provider_error` **不裸抛**、保留前两段、抛错记录 usage 归零 `{0,0}`；首角色即抛→`stage=research`、`outputs` 空、仍产出 1 记录）；**确定性**（同输入+同 fixtures 两次跑 `outputs` 深相等、`totalCostUsd` 相等——可复算）；**顺序与上下文链**（`scriptedProvider` 记录每次调用，断言 `taskKind` 序为 `research,structured_output×4` 且 bull/bear/judge/qa 的 prompt 分别含「已有研究摘要」「正方证据」「反方证据」「裁判裁决」，证前段结构化输出被并入后段）。用注入 provider（`StubProvider` 与自建 `scriptedProvider`），无 DB、无真实模型。**本里程碑纯 server 逻辑，不新增任何 HTTP 路由**（见下构建清单不变）。
  - `scout.test.ts`（24，Phase 10 M1）— GitHub Scout 治理逻辑骨架 `src/server/scout.ts` 纯函数与离线契约（无 DB、无网络、无 API Key、无新表，直接复用 `OpenSourceProject` 字段）：**常量守护**（`SCOUT_VERSION` 语义化版本串 / 恰 5 风险分档固定序 `permissive·weak_copyleft·strong_copyleft·proprietary·unknown` / `LICENSE_CATEGORY_BY_TYPE` 键集**逐字等于** `LicenseTypeSchema.options` 全 11 成员防漏 / 分类黄金值 MIT·Apache·BSD→permissive、MPL·LGPL→weak、GPL·AGPL→strong、PROPRIETARY→proprietary、UNKNOWN·OTHER→unknown）；**`decideLicense` 判定矩阵**（人工 `APPROVED`→`approved` 且 GPL 也尊重 / `REJECTED`→`rejected` 且 MIT 也尊重否决 / `NEEDS_HUMAN_REVIEW`→`needs_human_review` / **`NOT_REVIEWED` 五种分档各→`needs_human_review`（自动层不越权批准法律门）且 reasons 含「不越权批准」+ 分档提示语**、`category` 正确附带）；**`projectReadiness` 就绪聚合**（已批准+三检查全 true→`blockers:[]`·`readyForReuse:true` / **关键守护：`NOT_REVIEWED`+三检查全 true 仍不就绪、`blockers` 恰为 1 条「许可证待人工复核」证明法律门优先于检查位** / 缺依赖·安全·实测各计对应阻断 / `REJECTED`→「许可证被拒」阻断 / 未复核+全未检查=4 阻断自洽）；**`normalizeRepoUrl` 去重键**（`https://GitHub.com/Owner/Repo.git/`·`http://www.github.com/owner/repo`·`git://…`·带查询/片段·含空格 **五种写法归一为同一 `github.com/owner/repo`** / host 与仓库段区分不误合 / 空输入归一空串 / 幂等 `normalize(normalize(x))===normalize(x)`）；**`ScoutCandidateSchema`**（仅 `name`+`repoUrl` 时回落最保守默认 UNKNOWN·NOT_REVIEWED·三 false / `owner` 空串归一 `undefined` / 非法 `licenseType` 被拒）；**`evaluateCandidate` 判别联合**（缺 `name`→`status:invalid` 且 `issues` 指名 / `null`·字符串·数组·空对象脏输入一律 `invalid` **不裸抛** / 已批准+全检查→`evaluated` 且 `readyForReuse:true` 带归一 `dedupKey` / GPL 未复核→`evaluated` 但 `readyForReuse:false`·`license.category:strong_copyleft` / 同仓库不同写法两候选 `dedupKey` 相等）。无 DB、无真实模型。**本里程碑纯 server 逻辑，不新增任何 HTTP 路由**。
  - `scout-github.test.ts`（16，Phase 10 M2）— GitHub 输入管道 `src/server/scout-github.ts` 纯逻辑 + 可注入 `fetch`（无 DB、无真实网络、无令牌）：**常量守护**（`SCOUT_GITHUB_VERSION` 语义化版本串 / `SPDX_TO_LICENSE_TYPE` 覆盖 mit·apache-2.0·bsd-*·mpl-2.0·lgpl*·gpl*·agpl*·gfdl-1.3 等常见 SPDX）；**`parseSpdxLicense` 黄金值**（`MIT`→MIT、`NOASSERTION`/`null`/表外 SPDX 名 → 保守落 `OTHER`/`UNKNOWN` **绝不猜宽松档**、原始 SPDX 名保留进 `licenseNote` 供人对照、大小写无关）；**`mapGitHubRepoToCandidate`**（元数据 → `ScoutCandidateInput`，**断言刻意不含 `dependencyChecked/securityChecked/tested`** 交 schema 落最保守 false、owner 从 `full_name` 推、脏 stars（负/NaN）省略、小数 `trunc`）；**`parseRepoRef`**（裸 `owner/repo` / 完整 URL / 大小写混写 / 带 `.git`·尾斜杠均归一，仅首段含点才剥 host、拒 `.`/`..` 路径穿越）；**`fetchGitHubRepo`**（注入假 fetch 断言 URL 构造 + `Accept`/`User-Agent` 头、无令牌不带 `Authorization`、有令牌注入 `Bearer`、非 2xx 归一 `ScoutFetchError` 且**错误消息不含令牌/响应体**、非法 ref 根本不发请求、网络错归一不回显细节）。**本里程碑纯 server 逻辑，不新增任何 HTTP 路由**。
  - `scout-ingest.test.ts`（7，Phase 10 M2）— 抓取→判定→落库编排 `src/server/scout-ingest.ts`（`vi.hoisted` + `vi.mock("@/lib/prisma")`，`$transaction=(fn)=>fn(mockTx)`，无连库）：新建路径落 `licenseReviewStatus:NOT_REVIEWED` + 三检查位 false + 一条 CREATE `ChangeLog`（entityType=`OpenSourceProject`、before=null、after 快照、changedBy=actor）；**更新（重抓）路径核心不变式守护——`update.data` 只含客观元数据 name/owner/stars/licenseType/licenseNote/lastCheckedAt + `version:{increment:1}`，断言 `data` 绝不出现 `licenseReviewStatus`/`dependencyChecked`/`securityChecked`/`tested`（防自动层冲回人工法律门）**；脏候选 → `status:invalid` 且 `mockTx` 写方法零调用（不写库）；不同 URL 写法经 `evaluateCandidate` 得同一 `dedupKey` 命中同一条 `findUnique`；`ingestGitHubRepo` 成功（注入假 fetchRepo）/ `fetch_error`（回 `httpStatus`、不写库）/ 通用异常 → `error` 不外溢。**本里程碑不鉴权、不新增任何 HTTP 路由**。
  - `review-readiness.test.ts`（5，Phase 13 M6）— 审核发布队列的「发布就绪度」判定 `solutionPublishBlockers`（`src/server/solution-admin.ts` 里对既有私有 `publishGuard` 的只读包装，`vi.mock("@/lib/prisma")` 无 DB）——**刻意复用真正阻止发布的那个 `publishGuard`（单一真源，宪法第 16 条防漂移），保证「队列里显示可发布」与「点了发布真被服务端拦下」永不一刀切两份逻辑**：有价·无风险域·未勾专业确认 → `[]`（条件齐备）/ 无价 → 命中「价格」阻塞项 / 带 `["法律","环保"]` 风险域但未勾 `needsProfessionalReview` → 阻塞项点名「2 个高风险领域」并列出「法律」「环保」/ 同风险域已勾专业确认 → 该阻塞消解为 `[]`（证勾选动作确实解除拦截）/ 无价 + 风险域 `["金融"]` 未勾 → 同时得 2 条阻塞（多因并存不短路只报第一条）。Decimal 用 `{ toString }` 替身注入，纯逻辑边界。
  - `solution-generation.test.ts`（15，Phase 8 M3）— §33 流水线 → 真实方案正文生成映射 `src/server/solution-generation.ts` 纯函数与离线契约（无 DB、无网络、无 API Key，注入确定性 `StubProvider` + fixtures）：常量守护（`SOLUTION_GENERATION_VERSION 1.0.0` / `SOLUTION_SECTION_KEYS` 与既有 34 分节契约一致）；**`mapPipelineToSolution` 纯映射黄金样本**（complete 五角色输出 → 只落 `bullCase`/`bearCase`/`riskAnalysis`(bear 风险按 severity 倒序 + judge)/`unknowns`(非 FACT findings `{statement,kind,confidence}`)/`aiAnnotations`({generationVersion,pipelineVersion,factualStatementCount,assumptions,inferences,predictions,judge,qa,notGenerated:["costModel","revenueModel","roi","payback","sensitivity","sources"]}) **五节**、`filledSectionKeys ⊆ SOLUTION_SECTION_KEYS`、空 `points`/`findings` 不写对应节、**财务/来源等 `notGenerated` 节绝不伪造**、`aiAnnotations.factualStatementCount=1`·`judge.verdict=mixed`·`qa.qualityScore=85`·`pipelineVersion="1.1.0"`、`JSON.stringify(gen)` **不含 `"PUBLISHED"`**、同输入两次深相等（确定性））；**QA/状态到人工门控的映射**（complete→`needsHumanReview:false` 且 cost 透传 / needs_human_review→保留五段输出 + `reviewReason`含未批准 / failed-at-bull→仅保留 research 段、`reviewReason` 含 `"bull"`+`"schema_invalid"`、无 bullCase/bearCase/riskAnalysis / failed-at-research→空 body·`filledSectionKeys:[]`·`reviewReason` 含错误信息）；**`generateSolutionContent` 契约**（`vi.mock("@/server/solution-admin")`+注入 provider/fixtures 无 DB：无内容可写时 `ok` 且 `wroteSections:0` 不落库 / 有内容时 `updateSolution` 以 `{...existingBody,...gen.body}` 合并、`actor` 缺省 `"ai:pipeline"`、**绝不改 status 为 PUBLISHED**）。无 DB、无真实模型。**本里程碑新增 1 条受门禁 HTTP 路由 `POST /api/admin/solutions/[id]/generate`（见下构建清单）**。
  - `mutate.test.ts`（8，Phase 13 M7）— 后台 client 共用写请求薄封装 `src/components/admin/mutate.ts`（`vi.stubGlobal("fetch", vi.fn())`，无 DB/无 Next）锁 `mutateJson`/`fieldHints` 归一契约：① **成功**（2xx 且 body.ok）→ `{ok:true}`、**M7 新增透出 `data`=剔除 `ok` 后的完整响应体**（供 AI 生成按钮读 `generation` 元数据）、带 `content-type: application/json`、body 被 `JSON.stringify` 发出 ② 非 2xx + `error` → `ok:false`、`message`=后端 error.message、`fields`=error.details.fields、**`data` 缺席** ③ 2xx 但 `body.ok` 非真 → 按失败回落「请求失败（HTTP 200）」 ④ 响应非 JSON（`res.json()` 抛）不崩溃、归一失败 ⑤ `fetch` reject（网络错）→ `{ok:false,status:0,message:"网络错误，请稍后重试"}` ⑥ `body=undefined`（如 DELETE）→ **不设 `content-type` 头、不发 body** ⑦⑧ `fieldHints` 把按字段归类原因摊平为一维串数组、空/undefined → `[]`。锁死「五组件共用同一 fetch/解析真源」（宪法第 16 条防漂移）与 M7 加性扩展 `data` 的行为契约。
  - `model-calls.test.ts`（13，Phase 9 M5）— AI 成本观测层 `src/server/model-calls.ts`（`vi.mock("@/lib/prisma")` 顶掉 `prisma.modelCall.create`，纯函数无需连库/无网络/无 key）：**`toModelCallCreateData` 纯映射**（全字段照抄 `CallRecord` 且 `timestamp→createdAt` ISO 往返 / 归因列 `agent·caseId·solutionId` 缺省 `undefined→null` / 负数与 NaN 的 `latencyMs·promptTokens·completionTokens·estimatedCostUsd` 全归零 / 小数 tokens `Math.trunc` 截断 / 非法时间戳则**省略** `createdAt` 交 DB 默认而非塞 Invalid Date）；**`summarizeModelCalls` 纯聚合**（空集全零且 `avgLatencyMs=null` 绝不除零 / 总额·均值 200·byTier·byStatus / `errorCalls` = 混合状态里非 `ok` 计数（4 条含 2 错→2）/ `byAgent` 稳定序 `["expensive","many","one","(unattributed)","cheap"]`（null 归 `(unattributed)`、按成本降→次数降→`localeCompare`）且 many `{calls:2,costUsd:0.005}` / 同入参两次 deep-equal 确定性 / `round6`（3e-7→0、1.2345678→1.234568））；**`createDbCallRecorder` 旁路落库**（成功：`await recorder.flush()` 后 `create` 以映射入参被调 1 次 + `loggingRecorder.record` 1 次（先日志后落库、成本单一真源复用 `CallRecord` 不重算）；多条 `record` 后 `flush` 等定、`create` 计数=3）。**吞异常路径刻意不做 mock 单测**——vitest 单反检测器会在 `create` 拒绝被链 `.catch` 收养前的一微任务窗口误报 unhandled rejection（`mockRejectedValue`·同步 throw·可控 pending·预挂 `.catch`·async throw 四策略实测皆然），改由链的静态 `.catch` 保证 + 生产 warn 日志实证 + `tests/integration/model-calls.test.ts` 真连 Neon 成功往返覆盖；测试文件内已注释说明此取舍。
- **集成测试**（`tests/integration/`，**119 cases**（★自 R8.5 起整套连库套件默认串行跑 `--no-file-parallelism`——见下方「测试基建修复」；R8.5 加 3 条 `sandbox-provenance-store` 沙盘来源方案 ASSUMPTION→FACT 升级写路径真连 Neon（116→119）；R8.4 曾加 2 条 `sandbox-solution-provenance` 溯源复算审计真实 Decimal 舍入穿越；R8.3 曾加 2 条 `sandbox-solution-loop` 端到端商业闭环），~90s，真连 Neon）：`db-smoke.test.ts`（5）— SELECT 1 / Region 全 CRUD / 14 枚举存在性（Phase 6 加 UserRole、Phase 7 M4 加 EvidenceGrade、R3 加 ProjectStatus）/ 22 表存在性（Phase 6 加 User、Phase 9 M5 加 ModelCall、R3 加 Project/ProjectScenario/ProjectVersion）/ `_prisma_migrations` 记录检查，afterAll 双兜底清理；`industries-count.test.ts`（2）— `getIndustryCaseCounts()` 返回 ok 且每个行业 slug 有非负整数计数 / counts 键恰好等于 7 个 slug，含 Neon 冷启动预热重试；`cases-solutions.test.ts`（11）— 自建 realCase(DEEP_CASE)/demoCase(DEMO_FIXTURE)/candidateCase(CANDIDATE 内部态) + publishedSolution(PUBLISHED,price 1999.00,riskDomains,needsProfessionalReview,财务+未知变量)/draftSolution(DRAFT) 全生命周期夹具（标题嵌唯一 runId 便于搜索断言），afterAll 按 Restrict 外键顺序（先 solution 后 case）清理 + runId 兜底：`listPublicCases` 默认排除 DEMO 与内部阶段但含真实公开案例 / includeDemo=true 纳入并标记 isDemo / 行业筛选生效；`getPublicCaseById` 返回完整详情（证据分层+能力关联+商业模式）/ DEMO 门控（默认 not_found、includeDemo 时 found）/ 内部阶段与不存在 id 均 not_found；`listPublishedSolutions` 只含 PUBLISHED 排除 DRAFT 且 priceDisplay="¥1999.00"；`getPublishedSolutionById` 返回财务与未知变量 + **（Phase 12 M3）`isFree` 计算正确（¥1999 方案 `isFree=false`、新加的 price "0.00" 免费方案 `isFree=true`，供详情页解锁门控判定）** / DRAFT 与不存在 id → not_found；**`searchPublic` 关键词(=runId)命中自建真实案例 + 已发布方案且默认排除 DEMO/DRAFT / includeDemo=true 纳入匹配到的 DEMO 案例 / 无意义关键词 → ok:true 且 hits:0**；`users.test.ts`（5，Phase 6）— 用户数据层真连 Neon：`registerUser` 建号存 scrypt 哈希非明文·role 默认 USER·邮箱大写输入归一小写 / 已存在邮箱 → email_taken / 非法入参 → invalid + fieldErrors（不建号）/ `getAuthUserByEmail` 返回 passwordHash 且 `verifyPassword` 正确口令 true·错误口令 false·不存在邮箱 null / `getProfileUserById` **select 排除 passwordHash**（`"passwordHash" in profile === false`）·不存在 id → null，afterAll 按 runId 邮箱清理；`case-scores.test.ts`（8，Phase 7 M2+M3）— 评分持久化、复算与详情页透传真连 Neon：合法 scoreInput(机会→88)+4 条证据(可信度→69,未知→2) → `recomputeCaseScores` computed 且 DB 读回 scoreBreakdown 过 `CaseScoresSchema`·两标量同步·breakdown 10 维贡献和=88 / 无 scoreInput → skipped 且标量与 breakdown 保持 null（不编造）/ 非法 scoreInput(commercialValue 99 越界) → invalid+issues 指名维度且不写库 / 不存在 id → not_found / 复算幂等（连跑两次 breakdown 全等）/ **（M3）`getPublicCaseById` 对已复算公开案例透传 scoreBreakdown（opp 88·conf 69·10 维·贡献和 88·标量与拆解一致）/ 对无评分输入的公开案例 `scoreBreakdown` 诚实为 null** / `recomputeAllCaseScores` 汇总自洽（各状态和=total）且三夹具分别落 computed·skipped·invalid、error=0，afterAll 按 ids+runId 清理；`case-admin.test.ts`（8，Phase 7 M5）— 案例 & 证据「数据层」CRUD 真连 Neon：`createCase` 最小建 → stage 默认 CANDIDATE + 写 CREATE `ChangeLog`（entityType=Case）且无 scoreInput 时 `recompute=none` / 带 scoreInput(→88)+4 证据(→69) 建 → 级联建证据 + `recompute=computed`，`getPublicCaseById`（升 DEEP_CASE 后）读回 scoreBreakdown 与 4 证据 / 非法 industry → `invalid`+fieldErrors 且不写库 / 越界评分 → 案例仍建成但 `recompute=invalid` 且 scoreBreakdown null（不编造）/ `updateCase` version 1→2 + UPDATE `ChangeLog`、空 patch → `invalid`、不存在 id → `not_found` / `addCaseEvidence`+`removeCaseEvidence` 可信度随之变化再还原 + 两条 UPDATE `ChangeLog` + 证据数 4→3 / `deleteCase` 被 PUBLISHED 方案引用 → `blocked`（不硬删），删方案后成功并级联删证据 + DELETE `ChangeLog` / 删不存在 id → `not_found`，afterAll 按 Restrict 外键顺序（solution→case）+ runId + entityId 清理；`admin.test.ts`（4，Phase 6 M2）— 后台聚合 `getAdminDashboardData` 真连 Neon：beforeAll 建 USER→更新为 ADMIN 用户 + DEEP_CASE 案例 + PUBLISHED 方案 + CREATE `ChangeLog`（entityType=Case），四断言：① **groupBy 之和 ≡ total**（byStage/byStatus/byRole/byGrade 四个 map value 求和分别等于对应 total，验证同事务内 groupBy 派生 total 的一致性——单独立 count 会与并行测试文件race，故取消独立 count 查询）；② byRole.ADMIN ≥ 1（用户 role 更新反映到聚合）；③ byStatus.PUBLISHED ≥ 1 且 `recentChanges` 能按 entityId 找到自建的 CREATE 审计；④ `recentLimit=1` 只返回 1 条（分页参数生效）。afterAll 按 solution→case→user→changeLog 清理；`solution-admin.test.ts`（9，Phase 8 M1）— 方案数据层 CRUD 真连 Neon：`createSolution` 强制 DRAFT + 落 CREATE 审计（**Prisma Decimal 默认去尾零，比对须用 `toFixed(2)` 不是 `toString()`**）/ slug 冲突 P2002 → invalid.slug / case FK 预检（**合法 cuid 格式但库无行才走 invalid+fieldErrors.caseId；格式错会被 CuidSchema regex 提前拒**）/ `updateSolution` version 递增 + UPDATE 审计 + 空 patch 拒 + 不存在 not_found / `publishGuard` 无价 → blocked 不改状态不落 publishedAt / 有 riskDomains 未勾 needsProfessionalReview → blocked（消息含"高风险领域"） / 全条件通过升 PUBLISHED 且 publishedAt 首次落库、二次 update 不刷新 / `deleteSolution` 有 Order → blocked（消息含"仍有 N 条订单"）→ 撤单后成功 + 级联删 financials+unknowns + DELETE 审计 / `addSolutionFinancial`+`removeSolutionFinancial` / `addSolutionUnknown`+`removeSolutionUnknown` 归属回查（unknownId → solutionId）+ **unknownVariableCount 自动同步**表内实时条数（2→1→0），afterAll 严格按外键序 order→solution→case + entityId + runId 三层兜底清理；`admin-cases.test.ts`（4，Phase 13 M2）— 案例「后台」只读视图 `listAdminCases` 真连 Neon（beforeAll 直插三夹具：CANDIDATE+2证据+1方案 / KEY_RESEARCH 的 DEMO_FIXTURE / DEEP_CASE 带评分 88·69，afterAll 按外键序 solution→case + runId 兜底）：① **内部阶段（CANDIDATE/KEY_RESEARCH）与 DEMO 夹具均可见**（后台读层区别于公开橱窗 `listPublicCases` 的立身之本——后者刻意门控掉内部态与 DEMO）且 `isDemo` 标记正确 ② `evidenceCount`/`solutionCount` 与真实建表数一致（2/1 与 0/0）+ 评分标量透传 ③ **`updatedAt` 倒序全局不变式**（相邻两项非递增）+ `total ≥ items.length`、`items ≤ ADMIN_CASE_LIST_LIMIT`、`truncated ⟺ total > items.length` 自洽 ④ 行业 enum→可读中文名/slug（断言 `industryName` 非枚举字面量）；`admin-solutions.test.ts`（4，Phase 13 M3）— 方案「后台」只读视图 `listAdminSolutions` 真连 Neon（beforeAll 在两案例上直插四夹具：真实 NEW_ENERGY/DEEP_CASE 案例挂 带价 DRAFT（¥2999、1 财务、2 未知变量、riskDomains 法律+环保、needsProfessionalReview）+ 未定价 DRAFT + PUBLISHED（¥199），DEMO_FIXTURE 案例挂 DEMO 关联 DRAFT；afterAll 按外键序 order→solution→case + changeLog + runId 兜底）：① **DRAFT/审核中/已发布全部可见 + DEMO 关联方案可见且 `isDemo` 由关联案例判定**（后台读层区别于公开橱窗 `listPublishedSolutions` 只露 PUBLISHED·排除 DEMO）② 价格 `price.toFixed(2)` + `priceDisplay` "¥…" 前缀 + 未定价为 null + `financialCount`/`unknownVariableCount`（取实时 `_count.unknowns` 非缓存字段）/`orderCount` 与真实建表数一致 + `riskDomains` + `needsProfessionalReview` 透传 ③ 关联 `caseTitle` 正确 + 行业 enum 映射为中文名（`industryName` 非枚举字面量）④ **`updatedAt` 倒序全局不变式** + `total ≥ items.length`、`items ≤ ADMIN_SOLUTION_LIST_LIMIT(500)`、`truncated ⟺ total > items.length` 自洽；`admin-solution-detail.test.ts`（5，Phase 13 M4）— 方案「后台内容编辑台」读层 `getAdminSolutionDetail` 真连 Neon（beforeAll 在真实 NEW_ENERGY/DEEP_CASE 案例上直插带 DECISION 意图 price "2999.00"/CNY 的 DRAFT 方案，body 含 canonical key + 中文 title 键「目标行业」+ 数组 roadmap + 契约外 legacy 键，2 条财务（一满一空数字）+ 2 条未知变量（一含严重度 70 一 null），另在 DEMO_FIXTURE 案例上挂一条方案验 `isDemo`；afterAll 按外键序 order→solution→case + slug/title contains runId + changeLog 兜底）：① DRAFT 可读 + `price`/`priceDisplay`（¥ 前缀两位小数）+ `body` 结构 + 案例标题/行业中文名（非枚举字面量）② 财务 Decimal→字符串「1200000.00」/「42.50」/「3.50」、null 如实保留、createdAt 倒序、`financialCount`=2 ③ 未知变量严重度 70/null 透传 + `unknownVariableCount` 取实时=2 ④ DEMO 关联方案 `isDemo=true` ⑤ 合法 cuid 形状但库无行 与 "nope" 非法形 均 `notFound`；`admin-case-detail.test.ts`（6，Phase 13 M5 + M5b）— 案例「后台内容编辑台」读层 `getAdminCaseDetail` 真连 Neon（beforeAll 在一条 CANDIDATE 真实案例上直插 3 条多形态证据（FACT 满字段 / ASSUMPTION 缺 grade·sourceUrl·sourceType / PREDICTION 缺 confidence）+ 1 PUBLISHED·1 DRAFT 两方案，另建一条 DEMO_FIXTURE 夹具案例；afterAll 按外键序 solution→evidence→case + slug/title contains runId + changeLog 兜底）：① **内部阶段 CANDIDATE 可全量读到**（后台读层不受公开橱窗 DEEP_CASE+ 门控）+ 行业 enum→中文名（非枚举字面量）+ 版本/机会分/可信度标量透传 + `hasScoreBreakdown=true` ② 证据逐条透传、缺 `grade/sourceUrl/sourceType/confidence` 一律诚实留 **null 不伪造 0**、`evidenceCount=3`（取 `evidences.length` 实时）③ `solutionCount=2`、`publishedSolutionCount=1`（只数 PUBLISHED，删除守卫依据）④ DEMO 夹具 `isDemo=true`（由自身 `sourceType`）、无评分明细 `hasScoreBreakdown=false`、无证据→`evidenceCount=0`、`opportunityScore=null` ⑤ 合法形状但库无行 与 "nope" 非法形 均 `notFound` 且无 `error` ⑥ **（Phase 13 M5b）10 维评分录入的读回契约**：另建一条 DEEP_CASE 案例直插**合法 10 维 `scoreInput`**（golden 值）+ 2 条 FACT 证据后 `recomputeCaseScores`，断言 `getAdminCaseDetail` 透传 `scoreInput`（`toMatchObject` 十维逐键）+ `hasScoreBreakdown=true` + `scoreBreakdown` 非空 + `opportunityBreakdown.length=10` + `opportunityScore=74`（标量与逐维贡献和一致）+ `evidenceConfidence=69` + `unknownVariableCount=0`；同时反向守护漂移降级——CANDIDATE 夹具那条伪造 `scoreBreakdown:{total:73,dims:{}}`（不过 `CaseScoresSchema`）经 `getAdminCaseDetail` 重解析失败**降级为 `null`（绝不半渲染脏拆解）**、其 `scoreInput` 亦 `null`；`orders.test.ts`（10，Phase 12 M1）— 订单数据层真连 Neon（beforeAll 建 DEEP_CASE + PUBLISHED 方案 price 1999.00/CNY + DRAFT 方案 88.00，afterAll 按外键序 order→solution→case + entityId + runId 清理）：① **金额服务端快照**（夹带 `amount:"0.01"/currency:"USD"/status:"PAID"` 全被 Zod 白名单剥离，订单实得 `1999.00 CNY ¥1999.00`、status 仍 PENDING、CREATE `ChangeLog`=1）② 缺身份 → `invalid.identity` ③ DRAFT 方案 → `blocked` ④ 不存在方案 → `invalid.solutionId` ⑤ **同买家同方案重复下单幂等复用**同一 orderId、DB 仅 1 张 ⑥ `confirmOrderPaid` PENDING→PAID·paidAt 首落·version=2·UPDATE 审计=1、二次确认幂等 paidAt 不刷新 ⑦ 已 PAID 再下单 → `blocked` ⑧ PAID 不可 cancel（blocked）、新 PENDING 可 cancel→CANCELED ⑨ `hasPaidEntitlement` 确认前 false / 确认后属主 email true / 陌生人 email false / 无身份 false ⑩ `listOrdersForBuyer` 按 email 找回且全属该方案 + `listOrdersForAdmin` PAID 过滤 + pageSize=1 时 `hasNext ⟺ total>1` 自洽 + `getOrderById` found/not_found。**修测记录**：⑨ 反例邮箱初误写 `buyerEmail`（其在 ⑥ 已真实 PAID，entitlement 返 true 是代码对、断言错），改 `stranger-${runId}` 后 10/10 绿；`seo.test.ts`（4，Phase 14 M1）— sitemap 条目来源 `getSitemapEntries()`（`src/server/seo.ts`）真连 Neon（beforeAll 直插公开 DEEP_CASE 案例 + DEMO DEEP_CASE 案例 + CANDIDATE 内部案例 + 公开案例下 PUBLISHED 方案 + DEMO 案例下 PUBLISHED 方案，afterAll 按外键序 solution→case + runId 兜底）：① 公开案例 `/cases/${id}` 与公开方案 `/solutions/${id}` 出现在返回条目、`lastModified` 为 `Date` ② **内部阶段 CANDIDATE 与 DEMO 夹具案例、以及挂在 DEMO 案例下的方案均被排除**（sitemap 只收录公开可售橱窗，与 `listPublicCases`/`listPublishedSolutions` 的 DEMO+阶段门控同一口径，复用 `caseDemoVisibility`/`solutionDemoVisibility`）③ 路径为相对 `/cases|solutions/…`（非绝对 URL，交给 Next 用 `metadataBase` 拼域名）④ `SITEMAP_PER_TYPE` 为正整数字界常量守护。DATABASE_URL 缺失时 `describe.skip` 而非 fail，允许 CI 无库跑单元测试；`admin-review.test.ts`（4，Phase 13 M6）— 审核发布队列只读聚合 `getReviewQueue()`（`src/server/admin-review.ts`）真连 Neon（beforeAll 直插：caseA/审核中方案 solA **未定价**（应出「价格」阻塞）、caseB/solB 已定价带 1 未知变量（经嵌套 `create` 建、缓存标量为 0 故**必断 `_count.unknowns` 实时=1** 守护「队列显示与实际一致」不因缓存字段撒谎）、DEMO_FIXTURE 案例 caseC 挂已定价但风险域 `["法律"]` 未勾专业确认的 solC（应出「法律」阻塞且 `isDemo=true` 由关联案例判定），另置一条 DRAFT、一条 PUBLISHED 作**干扰项须被队列排除**（队列只收 `UNDER_HUMAN_REVIEW`），caseD(CANDIDATE,2 证据)/caseE(KEY_RESEARCH·DEMO) 进「待发布案例」而 DEEP_CASE 案例 casePub 须被排除；afterAll 按外键序 order→solution→evidence→case + title contains runId + changeLog 兜底清理）：① 队列成员/排除精确（三审核中方案全在、DRAFT+PUBLISHED 不在；两内部阶段案例在、DEEP_CASE 不在）② 每方案的 `publishBlockers` 来自**真正发布用的 `publishGuard`**（solA 含价格、solC 含「法律」，solB 为空=可发布）③ `unknownVariableCount` 取实时 `_count` 非缓存标量、`priceDisplay` ¥ 前缀两位小数、`isDemo` 由关联案例 `sourceType` 判定、行业 enum 映射中文名（`industryName` 非字面量）④ `updatedAt` 倒序 + `solutionsTotal ≥ items`、`truncated ⟺ total > items` 自洽。DATABASE_URL 缺失时 `describe.skip`。；`solution-generation.test.ts`（5，Phase 8 M3）— §33 流水线灌真实方案正文 `generateSolutionContent` 真连 Neon（**注入确定性 `StubProvider` + fixtures，故无论本地 `.env` 是否配真实 key 结果都可复算**；beforeAll 建 DEEP_CASE 案例 + DRAFT 方案，afterAll 按外键序 order→solution→case + runId 清理）：① complete fixtures → 五节（bullCase/bearCase/riskAnalysis/unknowns/aiAnnotations）合并进 `Solution.body`、**人工已填的既有 body 键（如 `name`）原样保留不被覆盖**、`version` 1→2、status 仍 DRAFT、`publishedAt` 仍 null、落一条 `ChangeLog` 且 `changedBy="ai:pipeline"` ② 已 PUBLISHED 方案 → `blocked`、生成后行 `version` 等于发布后版本（证生成完全没改动线上正文）且 bullCase 不出现 ③ bull 非法 fixtures → `pipelineStatus:failed`、bullCase 缺省、但 research 段的 unknowns/aiAnnotations 仍诚实写入 ④ research 空 fixtures → `wroteSections:0`、`version` 保持 1、body 原样（无内容可写就不落库不空递增）⑤ 不存在 id → `not_found`。DATABASE_URL 缺失时 `describe.skip`。；`model-calls.test.ts`（3，Phase 9 M5）— AI 成本观测层真连 Neon（`HAS_DB` 门控，`runId=mc-<ts>` 前缀隔离、`afterAll` 按 `createdIds` + `agent startsWith runId` 清库 + `disconnectPrisma`）：① `createDbCallRecorder` 三次 `record`（alpha `ok`/成本 0.0042/medium、beta `provider_error`/high/0、第三条**无 agent 归因**）→ `flush` → `prisma.modelCall.findMany` 按 runId 读回 3 行、逐字段等值含 `Number(estimatedCostUsd)≈0.0042`、beta 各字段、无归因行 `agent` **为 `null`**（**成功落库往返证记录器 → 建库 → 读回链路，覆盖单测刻意不 mock 的旁路写入路径**）② `getCostSummary()` 聚合 `totalCalls≥3`、`byAgent` 命中 alpha `{calls:1,costUsd:0.0042}` 与 beta `{…,costUsd:0}`、`byStatus.provider_error≥1`、各 tier 次数和 = totalCalls、`errorCalls` = 非 ok 状态和（自洽）③ 直读行喂 `summarizeModelCalls` 对黄金期望（3 次、总成本 $0.0084、1 错、均值 420ms）。DATABASE_URL 缺失时 `describe.skip`。；`scout-ingest.test.ts`（4，Phase 10 M2）— GitHub Scout 抓取→判定→落库编排 `src/server/scout-ingest.ts` 真连 Neon（`HAS_DB` 门控、`runId` 前缀隔离、`afterAll` 按 `repoUrl startsWith` 清理 + `disconnectPrisma`）：① `ingestCandidate` 新建落库逐字段验（NOT_REVIEWED + 三检查位 false + `ChangeLog` `entityType=OpenSourceProject`/`action=CREATE`/`changedBy=actor`）② 同一仓库换 URL 写法再抓命中同一条不重复插、`version` 1→2、UPDATE `ChangeLog` ③ **核心不变式——先手工把该行升为 `licenseReviewStatus=APPROVED` + 三检查位 `true`（模拟人已复核实测），再抓同一仓库：断言 review 态与三检查位原样保留、仅 `stars` 随新元数据刷新（证自动抓取层绝不越权冲回人工法律门）** ④ 缺 `name` 脏候选 → `status:invalid` 且 `OpenSourceProject.count` 不变（不写库）。DATABASE_URL 缺失时 `describe.skip`。；`sandbox-store.test.ts`（8，中途重构 R3 · R6.4 加 2 例）— 沙盘项目/情景/版本持久层 `src/server/sandbox-store.ts` 真连 Neon（`HAS_DB` 门控、`runId=sbx-<ts>` 前缀隔离、`actor=human:<runId>`，`afterAll` 按 `projectId` + name 前缀 + `ChangeLog` 清理）：① `createProject` 建项目 + 基线情景、现算落库 `calcStatus:"ok"` 且 Decimal 汇总列 `Number()` 读回对齐 R2 golden（`capexNet≈3524500`/`npv≈4277409`/`irrPct≈23.7553`/`paybackYears≈5.28`/`roiRatio≈4.0035`）+ 一条 CREATE `ChangeLog`（`changedBy=actor`）② `initialLayers` 传 `chargingPrice:1.2` → 落库汇总列即随参数变（**§4 命脉持久化实证**）+ 空 name → `{ok:false,reason:"invalid",detail:"项目名称不能为空"}` ③ `updateScenarioLayers` 改 1.3 → NPV 升 + `version` 递增 + UPDATE `ChangeLog` ④ 越界单价 999 被裁剪到边界仍算得出（`calcStatus:"ok"`、npv 非 null，**clamp 不致失败、非缺参**）⑤ 版本冻结：`saveScenarioAsVersion` v1(1.2)/改 1.5/`saveScenarioAsVersion` v2 → `listScenarioVersions` 时间线 `[2,1]`、`restoreScenarioFromVersion` 回滚 → npv 回到旧值 + `version` 再递增、跨情景回滚 → `forbidden` ⑥ `getProjectWithScenarios` 不存在 → `null`、`listScenarioVersions` 无版本 → `[]` ⑦ **（R6.4 外键护栏·1）** `createProject` 传沙盘地区包代码 `regionId:"shanxi"`（非规范 `Region` 行、此值来自 `SandboxSavePanel` 上行）→ **`ok:true` 且落库后 `proj.regionId` 被安全置空 `null`**（不再触发 `P2003` 外键 500）、基线情景仍 `calcStatus:"ok"` 现算落库 ⑧ **（R6.4 外键护栏·2）** 先直插一条真 `Region` 再以其 `region.id` 建项目 → `proj.regionId` **原样保留等于该 id**（证护栏只挡非法值、不误伤合法外键）。**⚠️ 已知环境抖动（非 R3 引入、非代码缺陷）**：整套集成**并行**跑在共享 Neon 免费连接池上时，`admin-review.test.ts`（`updatedAt` 排序"total≥items"不变式）与本 store 版本回滚用例偶发失败（伴 `Field case is required…got null` / slug 唯一约束报错，系跨文件并行清理 + 连接池争用），**单独跑各文件均绿**（本文件 8/8、db-smoke 5/5）；后续可专项把连库套件串行化（`fileParallelism:false` 或独立库）根治；`sandbox-solution-store.test.ts`（3，中途重构 R8.2）— 沙盘草案落库转 DRAFT Solution `src/server/sandbox-solution-store.ts` **真连 Neon**（`HAS_DB` 门控、`runId=it-sbxsol-<ts>` 前缀隔离 + `sanitizeSlug`、`actor=human:<runId>`，**★关键：`buildRealDraft()` 跑整条真引擎链**（`resolveSandbox`→`runSandboxModel`→`computeTechModel`→`computeTornado`→`buildSandboxViewModel`→`buildSandboxSolutionDraft`）**产出真 Decimal 串打真列精度**，非手搭 fixture）：① 真草案 + `prisma.case.create` 建的真案例 → `status:"ok"`、`prisma.solution.findUnique` 读回 **`status=DRAFT`**、`caseId` 等于所建案例、`currency=CNY`、`price` `toFixed(2)="18000.00"`、`riskDomains` 透传、`body` JSONB 含字符串 `roi` 节、`financials` 长度符合且 `capex≈3524500`（**证沙盘搬运的净 CAPEX 逐字落进 Decimal(14,2) 列未溢出/未失真**）、`calcRef="model@1.0.0"`、`unknownVariableCount === draft.unknowns.length`（既有函数自动回写）、一条 `ChangeLog`（`entityType=Solution`·`action=CREATE`·`changedBy=actor`）② **外键诚实护栏**——格式合法但库中不存在的 `caseId`（`"c"+随机`）→ `status:"invalid"`+`fieldErrors.caseId`（复用 `createSolution` 的 FK 预检、**绝不 500/绝不 P2003 裸抛**）且 `prisma.solution.count` 不变（草案残块不落库）③ 无 `price` 草案 → 仍建成 DRAFT 但 `price` 为 `null` + 返回 `publishBlockers` 含「价格」（发布守卫据将在后续发布时拦下）。`afterAll` 严格按外键序 order→solution(by id + by caseId)→case(id + title contains runId)→changeLog 清理 + `disconnectPrisma`，DATABASE_URL 缺失时 `describe.skip`；`sandbox-solution-loop.test.ts`（2，中途重构 R8.3）— 沙盘来源 Solution 的**端到端商业闭环真连 Neon**（§17 式一次性实证——总控最高优先级「案例→…→产业方案→查看→购买」这最后一环**历史上从未有自动化脚本把「沙盘草案→发布→查看→下单→确认→解锁」整条串起来跑通过**，本轮补上；`HAS_DB` 门控、`runId=it-sbxloop-<ts>` 前缀隔离、`actor=human:<runId>`，**买家刻意用游客 `buyerEmail`（不建 `User` 行，测既有下单门禁本就允许游客）**，复用 R8.2 集成同款 `buildRealDraft()`（跑**完整真引擎链**产真 Decimal）+ `persistDraft()` 助手，`afterAll` 严格按 FK 序 order→solution→case→changeLog 清理）：① **★主闭环全线打通（14 连断言）**——真草案带价 `"19800.00"` + 真案例经 `persistSandboxSolutionDraft` 落 `DRAFT` → `getPublishedSolutionById` **`not_found`**（草稿绝不进公开橱窗）→ `updateSolution({status:"PUBLISHED"})` **`ok`**（经 `publishGuard`：有价 + `needsProfessionalReview` 已勾→放行，**这是全程唯一由"人"按下的发布动作**）→ `getPublishedSolutionById` **`found`** 且 `isFree:false`·`needsProfessionalReview:true`·`financials.some(f=>f.calcRef==="model@1.0.0")`·**★`describeSandboxLineage` 非空且 `draftVersion` 匹配 `/^\d+\.\d+\.\d+/`·`evidenceKind==="ASSUMPTION"`**（证已发布方案的溯源披露数据真在库、详情页徽章/Alert 有料可渲）→ `hasPaidEntitlement` **`false`**（未购买不解锁）→ `createOrder({solutionId,buyerEmail})` `ok`·`status:PENDING`·`amount≈19800`·`currency:CNY`（金额服务端快照非客户端可篡改）→ `hasPaidEntitlement` **仍 `false`**（PENDING 不解锁，付款未确认）→ `confirmOrderPaid` **`ok`**（PENDING→PAID）→ `hasPaidEntitlement` **`true`**（★付款确认后权益解锁，与 Phase 12 M3 UI 冒烟同一判据）→ 已 PAID 再下单 → **`blocked`**（不重复收钱）→ Solution `CREATE` `ChangeLog`=1 + 订单相关 `ChangeLog`≥1（审计留痕）；② **★发布后门堵死**——未定价草案直接 `updateSolution({status:"PUBLISHED"})` → `publishGuard` 拦截回 `fieldErrors.price`、`getPublishedSolutionById` 仍 `not_found`、`createOrder` 亦 `blocked`（沙盘导出**只建 DRAFT、绝不自动发布**，无价方案连"人想发"都发不出去，§10/§21 高风险须人工确认 + 定价门在真库层面坐实）。DATABASE_URL 缺失时 `describe.skip`。；`sandbox-solution-provenance.test.ts`（2，中途重构 R8.4）— 沙盘来源 Solution 的**溯源复算审计 + 可追溯汇总**纯读侧真连 Neon（`src/lib/sandbox-solution-provenance.ts` 的黄金值只搬运不重算，本节把「已发布方案的真实 Decimal 列」喂进审计函数，验证**复算判据在真库舍入下不误伤、篡改下能揪出**；`HAS_DB` 门控、`runId=it-sbxprov-<ts>` 前缀隔离、`actor=human:<runId>`，复用 R8.2/R8.3 同款 `buildRealDraft()`（跑完整真引擎链产真 Decimal）+ `persistPublishedDraft()`（落 DRAFT→人工 `updateSolution` 置 PUBLISHED→`getPublishedSolutionById` 读回带真 Decimal 串的 financials），`afterAll` 严格按 FK 序 order→solution→case→changeLog 清理）：① **★复算绿 + 可追溯诚实地 0/N**——真发布方案读回的 `financials` 喂 `evaluateSandboxSolutionProvenance` → `reproducibility.allReproducible === true`（证 `roiPct`/`irrPct`/`paybackYears` 与同行 `assumptions` 源值在各自容差内对得上，**真实 Decimal 列 `toFixed`/列精度舍入穿越容差未被误判为异常**，这是 §7/§12「公式可复算」以**事后审计**而非重跑引擎兑现）、`traceability.traceableCount === 0` 且 `fullyTraceable === false`（沙盘入参仍 ASSUMPTION、无合法 sourceUrl → 诚实报「0/N 具备来源可追溯真实数据」）、`buyerSummary` 含「复算」（对得上分支）；② **★篡改必被揪出**——直连 `prisma.solutionFinancial.update` 把某行 `roiPct` 改成 `"999.9900"`（背离同行 `roiRatio×100` 源值远超 0.006 容差）→ 重读喂审计 → `allReproducible === false`、`issues` 点名 `roiPct`、`buyerSummary` 含「对不上」（证复算判据既能穿越合法舍入不假警、又能捕获越轨篡改——非橡皮图章）。DATABASE_URL 缺失时 `describe.skip`。；`sandbox-provenance-store.test.ts`（3，中途重构 R8.5）— 沙盘来源 Solution 的 **ASSUMPTION→FACT 升级写路径** `src/server/sandbox-provenance-store.ts` 真连 Neon（R8.4 的 `planProvenanceUpgrade` 只出预案不落库，本层把「提交真实来源 + 置信度 → 受门禁认证 → 落库」这条**写路径**在真库坐实，且证明**升级只改溯源列、绝不破坏 R8.3 识别 / R8.4 复算这条脊柱**；`HAS_DB` 门控、`runId=it-sbxpvup-<ts>` 前缀隔离、`actor=human:<runId>`，复用 R8.2/R8.3/R8.4 同款 `buildRealDraft()`（跑完整真引擎链产真 Decimal）+ `persistPublishedDraft()`（落 DRAFT→人工 `updateSolution` 置 PUBLISHED→按 `financialId` 读回真财务），`afterAll` 严格按 FK 序 order→solution→case→changeLog 清理）：① **★升级绿且脊柱不断**——对真发布方案的（唯一一条）财务 `upgradeSolutionFinancialProvenance(finId,{sourceUrl:"https://…",confidence:82,note},actor)` → `status:"ok"`、`version` +1、行 `sourceUrl` 已落库、`assumptions.evidenceKind` 翻 `FACT` 且 `provenanceUpgrade.from==="ASSUMPTION"`、`ChangeLog` 存「升级财务溯源 ASSUMPTION→FACT」；★**再喂 `evaluateSandboxSolutionProvenance` 复算仍 `allReproducible:true`+`issues:[]`**（升级合并 `{...existing}` 保住了 `roiRatio/irrFraction/discountedPaybackYears/solutionCalcRef` 全部源值与指纹，没重算也没抹掉任何数）、`traceability.traceableCount===1` 且 `fullyTraceable===true`（草案恰 1 条财务，升级即 1/1）、`buyerSummary` 含「FACT」、`isSandboxSourcedSolution` **仍 true**（沙盘来源识别未被升级抹掉——R8.3↔R8.5 契约一致）；② **★拒绝即不写**——只给 `{confidence:90}` 不给合法 `sourceUrl` → `status:"blocked"`、`reason` 含「来源链接」，回读行 `sourceUrl`/`version`/`assumptions`（`JSON.stringify` 逐字）**原封不动**、审计 `traceableCount` 仍 0（§20 拒不把占位假设粉饰成事实，闸门拒绝时零副作用）；③ **边界与版本守护**——`SANDBOX_PROVENANCE_STORE_VERSION` 匹配 `/^\d+\.\d+\.\d+$/`；给合法 `sourceUrl` 但 `confidence:200`（越 `[0,100]` 界）→ `blocked` 且 `reason` 含「越界」（闸门先验来源链接后验置信度区间，两处任一不满足皆不落库）。DATABASE_URL 缺失时 `describe.skip`。
- **构建验证**：`tsc --noEmit` 0 错误；`eslint .` 0 问题；`next build`（Turbopack）成功，产出 `ƒ /` / `○ /_not-found` / `○ /about` / `○ /privacy` / `○ /terms` / `○ /ui` / `ƒ /api/health` / `ƒ /industries` / `● /industries/[slug]`（SSG 预渲染 7 个 slug）/ `ƒ /cases` / `ƒ /cases/[id]` / `ƒ /solutions` / `ƒ /solutions/[id]` / `ƒ /search` / **`ƒ /login` / `ƒ /register` / `ƒ /account` / `ƒ /api/auth/[...nextauth]`（Phase 6）** / **`ƒ /admin`（Phase 6 M2，动态·门禁·noindex）** / **10 条 `ƒ /api/admin/**` 写端点（Phase 13 M1：案例 create/update/delete + evidence add/remove、方案 create/update/delete + financial/unknown add/remove）** / **`ƒ /admin/cases`（Phase 13 M2，案例后台「列表+新建」·动态·门禁·noindex）** / **`ƒ /admin/cases/[id]`（Phase 13 M5，案例「内容编辑台」·基本信息/证据增删/删除·layout+page 双层防御·动态·门禁·noindex）** / **`ƒ /admin/solutions`（Phase 13 M3，方案后台「列表+新建+发布」·动态·门禁·noindex）** / **`ƒ /admin/solutions/[id]`（Phase 13 M4，方案「内容编辑台」·正文 34 分节/财务/未知变量/删除·layout+page 双层防御·动态·门禁·noindex）** / **`ƒ /api/admin/solutions/[id]/generate`（Phase 8 M3 新增受门禁生成端点，`POST` 触发 §33 流水线灌真实方案草稿正文、经 `requireStaffWrite` 门禁、绝不自动发布）** / **4 条 `ƒ /api/orders`·`/api/admin/orders`·`/api/admin/orders/[id]/confirm`·`/cancel`（Phase 12 M2，购买闭环 HTTP 端点·下单公开/后台 staff）** / **`ƒ /orders/[id]`（Phase 12 M3，支付说明页·所有权门禁）· `ƒ /account/orders`（Phase 12 M3，我的订单·登录门控）· `ƒ /admin/orders`（Phase 12 M3，后台订单确认·双层防御·noindex）** / **`ƒ /admin/review`（Phase 13 M6，审核发布队列·待处理方案与待发布案例聚合·layout+page 双层防御·动态·门禁·noindex·复用既有 `PATCH /api/admin/solutions/[id]`·`/api/admin/cases/[id]` 端点、无新写端点）** / **`ƒ /robots.txt`·`ƒ /sitemap.xml`（Phase 14 M1，两个 `MetadataRoute` handler，均 `force-dynamic` 运行期求值 `isIndexable()`）** / **`○ /sandbox`（中途重构 R4，产业项目可视化决策沙盘工作台·静态预渲染页，`SandboxWorkbench` 客户端组件在浏览器内 `useMemo` 现算整条 §4 计算链、绑 recharts 图表即时联动，无新增 API 路由/无 DB；本版新增运行时依赖 `recharts ^3.10.1`；R6.1 起该工作台另在浏览器内渲染**确定性动态报告面板** `SandboxReportPanel`（`buildSandboxReport` 吃当前情景 `SandboxViewModel` 现算、改参数/切地区即整份重写，仍无新增路由/无 DB/无 AI））** / **`ƒ /api/sandbox/explain`（中途重构 R6.2，沙盘 AI 解释端点·`POST`·`requireSameOriginActor` CSRF + 必须登录、`createDbCallRecorder` 成本入 `ModelCall`）** / **5 条 `ƒ /api/sandbox/**`（中途重构 R6.3，项目/情景/版本持久层端点·全 `force-dynamic`+`requireSameOriginActor` CSRF + 必须登录 401：`POST /api/sandbox/projects`（建项目+基线情景）、`GET /api/sandbox/projects/[id]`（读项目）、`PUT /api/sandbox/scenarios/[id]`（改情景分层现算）、`POST`+`GET /api/sandbox/scenarios/[id]/versions`（存版本快照/列版本时间线）、`POST /api/sandbox/scenarios/[id]/restore`（回滚版本）；所有权门禁 `canAccessProject` 属主或 staff、越权 403、服务端重跑引擎非搬页面数字；`SandboxSavePanel.tsx` 客户端消费上述端点，`/sandbox` 工作台本身仍 `○` 静态）** / **`ƒ /api/sandbox/solution`（中途重构 R8.2，沙盘「导出产业方案」落库端点·`POST`·`requireStaffWrite` CSRF + `requireRole(STAFF_ROLES)` 与普通手工建方案同权限、服务端结构化校验草案后经 `createSolution` 强制 DRAFT + FK 预检落库、绝不 500；`SandboxSolutionPanel.tsx` 客户端消费，`/sandbox` 仍 `○`）** / **`ƒ /api/admin/cases`（中途重构 R8.2 于既有 Phase 13 M1 `POST` 之外**新增 `GET`**·`requireRole(STAFF_ROLES)` 只读门·供导出面板惰性拉取真实案例清单挂 `caseId`，`/sandbox` 工作台本身仍 `○` 静态）** + `ƒ Proxy (Middleware)`。**（中途重构 R8.3）路由清单不变**——沙盘来源溯源诚实披露为**纯读侧呈现**，仅在既有 `ƒ /solutions/[id]` 详情页按 `describeSandboxLineage(s.financials)` 追加「沙盘推演生成」徽章 + `info` `Alert`（非沙盘方案 `lineage=null` 故不渲、既有方案零扰动），**零新增路由/端点、`/sandbox` 仍唯一静态 `○`**。**（中途重构 R8.4）路由清单仍不变**——沙盘来源 Solution 的「溯源复算审计 + 可追溯汇总 + 升级计划」为**纯读侧 + 纯映射地基**（`src/lib/sandbox-solution-provenance.ts` client-safe 零 DB/网络/时钟/随机），仅在既有 `ƒ /solutions/[id]` 详情页沙盘 `info` `Alert` 内**追加**一行 `溯源审计（auditRef）：{buyerSummary}`（非沙盘方案 `lineage=null` 故该 Alert 整体不渲、普通方案零扰动），**`planProvenanceUpgrade` 只出升级计划不落库**（写路径留待 R8.5），**零新 schema 迁移、零新路由/端点、`/sandbox` 仍唯一静态 `○`**。**（中途重构 R8.5）路由清单 `+1` 条动态 `ƒ /api/sandbox/solution/provenance`**——沙盘来源 Solution 的 **ASSUMPTION→FACT 升级写路径**落地：新增 1 个 server 编排模块 `src/server/sandbox-provenance-store.ts`（委托 R8.4 已测的 `planProvenanceUpgrade` 闸门，拒绝即不写）+ 1 条 `POST /api/sandbox/solution/provenance` 端点（`force-dynamic`·`requireStaffWrite` CSRF + `requireRole(STAFF_ROLES)` 与普通方案后台同权限，复用既有列 `sourceUrl`/`assumptions`/`version`），**★零 schema/DB 迁移**（升级只写已存在的溯源列、绝不新增列/表）、**`/sandbox` 仍唯一静态 `○`、`/solutions/[id]` 仍 `ƒ`**，除新增该 1 条 API 路由外其余路由不变。
- **HTTP 端到端冒烟**：里程碑 3 起 `next start -p 3117`，**31 项全过** —— 首页 `/` 200 且含 hero 文案·"我们如何工作"·"按行业浏览"·搜索表单指向 `/search`·导航含 `/search` 链接；`/search` 无查询 200 + "输入关键词开始搜索"；`/search?q=沼气&demo=1` 200 + 命中种子 DEMO 沼气案例标题 + DEMO 标记；`/search?q=沼气` 默认 200 + 不泄露 DEMO 种子标题 + 诚实空态"没有找到"；`/search?q=zzznonsense` 0 命中空态；`/search?q=空白` 回引导态；`/search?q=超长150字` → "搜索关键词无效"；**M2 404 回归** `/cases/nonexistent-zzz-9999`·`/solutions/nonexistent-zzz-9999` 仍真 **404**（确认根 loading.tsx 保持删除）；`/cases?demo=1` 显示种子标题·`/cases` 默认不泄露·`/cases?q=沼气&demo=1` 命中。（里程碑 2 的 14/14 冒烟在 `-p 3116`，含 3 个真 404；里程碑 1 的 9/9 冒烟在 `-p 3111`，含 `/industries/not-a-real-one` 真 404 验证 `dynamicParams=false`。）冒烟脚本放 workspace scratch（不进仓库）。**踩坑**：断言"默认排除 DEMO"勿用 `!body.includes("【DEMO】")`——该字面量出现在默认页帮助文案"开发期可加 ?demo=1 查看【DEMO】示例数据"里会误报；应断言具体种子案例标题片段是否泄露。
- **HTTP 认证端到端冒烟（Phase 6）**：`next start -p 3119`，**26 项全过**（cookie jar 走 Auth.js v5 Credentials 全流程）。脚本先直插 Neon 种一个测试用户（用与 `password.ts` 相同的 scrypt 参数/格式复刻哈希），跑完 afterAll 删除。校验：`/login`·`/register` 200 且含表单标记 / 首页导航含登录·注册链接 / 未登录 `/account` → 307 重定向 `/login` / `/api/auth/providers` 含 credentials / `GET /api/auth/csrf` 返 csrfToken + 下发 csrf cookie / `POST /api/auth/callback/credentials`（正确口令）下发 session cookie / `GET /api/auth/session` 返 user.email·role=USER·id 且**响应体不含 passwordHash** / 登录后 `/account` 200 展示邮箱·角色 / **错误口令**与**不存在邮箱**均不下发 session cookie·session 无 user（无差别失败防枚举）/ `POST /api/auth/signout` 清 session cookie·登出后 `/account` 重定向 `/login`。**踩坑**：① `next-auth` v5 的 JWT 模块增强须打在 `@auth/core/jwt`（打在 `next-auth/jwt` 会 TS2664）；② 本仓 vitest/纯 node 下 `import "server-only"` 会抛 "Cannot find module 'server-only'"，约定只在文档注释标注 server-only、**不 import**；③ 脚本日志**绝不拼接密钥明文**（AUTH_SECRET 只记长度/掩码），一旦泄露立即轮换。
- **评分拆解详情页冒烟（Phase 7 M3）**：`next start -p 3111` + 预热 health 后，`GET /cases/demo_case_biogas?demo=1` 渲染 `ScoreBreakdownCard`——实测机会评分 **64** / 证据可信度 **39** / 关键未知变量 **3** 与 seed 内联 `computeCaseScores` 结果一致，10 个维度标签（商业价值…实施难度）齐全，两个 inverse 维度显示「越低越好」提示与录入值，公式版本 v1.0.0 与「综合评分 ≠ 项目一定成功」免责条出现；无 `scoreInput` 的公开案例走"暂未录入/复算不展示"占位分支（数据层由集成测试断言 scoreBreakdown=null）。
- **证据来源等级标签冒烟（Phase 7 M4）**：`next start -p 3111` + 预热 health 后，`GET /cases/demo_case_biogas?demo=1`（该案例 FACT=来源 B、假设/推断/预测=来源 D）实测渲染每条证据叠级徽章「来源 B」「来源 D·AI推断」，并出现 `Alert`「存在 AI 推断 / 待验证来源（D 级）」复核条；DB 侧 `evidence.groupBy({by:['grade']})` 实测 6 DEMO 案例 16 证据分布 A1/B2/C2/D10/S1=16、`db:recompute-scores` 仍 6 computed / 0 error（grade 不进公式，分数不变）；`getPublicCaseById` 随证据透传 grade 由集成测试 `cases-solutions.test.ts`（FACT→A、PREDICTION→D）锁定。
- **方案 34 分节正文渲染端到端冒烟（Phase 8 M2）**：`next start -p 3111` + 预热 health，**15 项全过**。脚本用 `createRequire` 载 `@prisma/client`，把一条「已发布 + 带 body」的临时方案挂到既有 `DEMO_FIXTURE` 案例上（因刻意不种子可售方案），`GET /solutions/[id]?demo=1` 断言：出「方案正文」标题 + 分节完成度徽章 + 按 key 命中内容 + 按中文 title 命中（`目标行业`→industry 节）+ 数组分节渲染成列点 + 嵌套对象分节渲染键值（capex）+ 未填节「待补充」+ 契约外字段 extras 审计提示；再建一条空 body 方案 → 出**诚实占位**（"尚未填充 34 分节"+"禁止单模型直出"）且不含完成度徽章；finally 硬删两条临时方案 + 兜底按 slug 前缀清残留，末尾 `count(slug startsWith "smoke-body-")===0` 确认无脏数据。**踩坑**：① DEMO 案例的 `sourceType` 值是 `DEMO_FIXTURE` 非 `DEMO`；② 精确 `filledCount`（如 5/34）不在 HTML 里正则匹配——React SSR 会在相邻动态文本节点间插 `<!-- -->` 注释把 `5` 与 `/34` 隔断，改由 `solution-body.test.ts` 单测锁定、HTTP 侧只断言徽章标签存在。冒烟脚本放 workspace scratch（不进仓库）。
- **真实 DeepSeek 方案生成端到端冒烟（Phase 8 M3）**：脚本放 workspace scratch（不进仓库、跑完即删），走 `createChatProvider()` 真 key 打真 DeepSeek、对一条临时 DRAFT 方案跑完整 `generateSolutionContent`——实测五角色流水线全跑通、成本由程序算约 **$0.0042**、**5 节真实中文正文**（bullCase/bearCase/riskAnalysis/unknowns/aiAnnotations）成功合并进 `Solution.body`（英文枚举 `FACT/ASSUMPTION/INFERENCE/PREDICTION` + 整数 confidence 0–100 均正确落库）、QA 门把草稿正确判为 `needs_human_review`（质量分低于放行阈值）→ **status 仍 DRAFT 绝未被自动发布**、`version` 递增、`ChangeLog.changedBy="ai:pipeline"`。**本轮真实供应商暴露两个离线桩永远测不出的坑并已在适配器修复**：① DeepSeek 的 `json_object` 模式要求 prompt 字面含 "json" 否则 400（→ `deepseek-provider.ts` 对结构化任务补 "respond with valid JSON only" 尾串，业务/流水线仍 provider-agnostic）；② 模型回 `finding` 而非 `statement`、中文证据类型、0~1 小数置信度导致 `schema_invalid`（→ `research-pipeline.ts` 各 `roleInstruction` 内嵌精确 JSON 契约 + `PIPELINE_VERSION` 1.0.0→1.1.0，依宪法第13条为 prompt 变更升版本）。**⚠️ 安全**：本轮曾在环境出现 `NODE_TLS_REJECT_UNAUTHORIZED=0` 告警（经排查为机器/运行时注入非本仓代码，生产须确保不禁用 TLS 校验）；用户明文贴出的 DeepSeek key 已泄露，强烈建议后台轮换。
- **HTTP /admin 门禁端到端冒烟（Phase 6 M2）**：`next start -p 3111` + 预热 health，**15 项全过**。直插 Neon 3 个测试用户（USER/REVIEWER/ADMIN，同一 scrypt 参数），cookie jar 走 Auth.js 全流程。校验：① 未登录 `GET /admin` → 3xx 重定向 `/login`；② **USER 登录 → `GET /admin` 200 但页面渲染 forbidden `Alert`（「无访问权限」）而非「运行概览」**——同时严格断言响应体 HTML **与 RSC flight payload** 均不出现「运行概览」「最近审计流水」「按阶段分布」等 dashboard 独有关键字（防 App Router 布局短路不彻底的反例，见下）；③ REVIEWER/ADMIN 登录 → `GET /admin` 200 且含「运行概览」「按阶段分布」「最近审计流水」，topbar 显示角色徽章与邮箱；④ `/account` 对 USER 不显示「进入管理后台」链接、对 REVIEWER/ADMIN 显示；⑤ `/api/health` 版本 0.14.0；⑥ afterAll 清理 3 用户。**关键教训**：仅在 `admin/layout.tsx` 里对 forbidden 分支不渲染 `{children}` **不足以阻止 Next.js 把叶 `page.tsx` 的 RSC flight 数据序列化到响应流**——`page` 段仍会被 prerender 一次并把 `PageHeader title="运行概览"` 等内容写入 `<script>self.__next_f.push(...)</script>`；正解 = **layout + page 双层防御**，page.tsx 首行 `const res = await requireRole(STAFF_ROLES); if (!res.ok) return null;` 保证未授权时 `getAdminDashboardData` 根本不被调用；同时把 `page.metadata.title` 从「运行概览」改成「管理后台」，即便有 `<title>` fallback 也不泄露看板指纹。冒烟脚本放 workspace scratch（不进仓库）。
- **后台写 HTTP 端点端到端冒烟（Phase 13 M1）**：`next start -p 3121` + 预热 health，**24 项全过**。脚本直插 Neon 三测试用户（USER/REVIEWER/ADMIN，同一 scrypt 参数），各走真实 Auth.js（csrf→callback/credentials）拿 session cookie，再带同源 `Origin` 打 `/api/admin/**` 写端点。校验门禁矩阵 + 全链路写：① 未登录 `POST /api/admin/cases` → **401 UNAUTHORIZED**；② USER 登录 → **403 FORBIDDEN** 且 body 透出 `details.required`；③ ADMIN 但 `Origin: https://evil.com` 跨站 → **403 含 CSRF 字样**（且不查角色）；④ ADMIN 合法但缺 `title` → **400 VALIDATION_ERROR**；⑤ ADMIN 全链路写成功——建案例 200（回 `caseId`）+ DB 侧 `ChangeLog.changedBy === human:<adminId>`（**证明 actor 由服务端会话注入、非客户端可伪造**）+ PATCH 改案例 + 加证据（回 `evidenceId` + `recompute`）+ 删证据 + 建方案 DRAFT（回 `solutionId`）+ 加关键未知量后 `Solution.unknownVariableCount=1` + 删未知量后回落 `0`（证明数据层自动同步经 HTTP 层透传生效）+ 删方案 + 删案例；⑥ finally 硬删临时 case/solution/user/其 ChangeLog，末尾 `count(title contains runId)===0` 确认无脏数据。**踩坑**：BASE 用 `127.0.0.1` 时 `isSameOrigin` 全量误拒（Next 归一化 `request.url` host 与手设 Origin `host` 不一致 → 判跨站），改用 `localhost` 让 host 与 Origin 精确一致后全过——正是同源判定「host 精确相等」语义的现实体现，勿在冒烟里混用两种 host。冒烟脚本放 workspace scratch（不进仓库）。
- **后台最小管理 UI 渲染端到端冒烟（Phase 13 M2）**：`smoke-admin-ui.mjs`，`next start -p 3122` + 预热 health，**18 项全过**。脚本自种 USER/ADMIN 两账号（同一 scrypt 参数）+ 一条唯一标题案例，各走真实 Auth.js（csrf→callback/credentials）拿 session cookie，再 `GET /admin/cases` **验页面渲染**（非 API）：① 未登录 → **307 跳 `/login`**（layout redirect）；② USER → **200 但只渲染「无访问权限」面板**，且严格断言 HTML **不含**「新建案例」表单、**不含**真实案例标题（证 layout+page 双层防御下 page 首行 `requireRole` 不过即 `return null`、`listAdminCases` 根本不被调用，数据不进响应）；③ ADMIN → **200 且见「案例管理」+「新建案例」表单 +「案例列表」**，并断言**服务端 `listAdminCases` 的真实案例标题透传进 SSR HTML**（端到端 DB→Server 组件→HTML 打通，非仅静态壳）；④ `/admin` 仪表盘 HTML 出现指向 `/admin/cases` 的「案例管理」入口链接；⑤ finally 硬删临时 case/user，末尾 leftover=0。**踩坑**：BASE 必须 `localhost`（与 M1 同理，Origin/host 精确相等）；渲染冒烟只断言标题文本存在（标题是单一文本节点，不受 React SSR 相邻动态文本间插 `<!-- -->` 影响），涉及数字比值仍交单测锁。冒烟脚本放 workspace scratch（不进仓库）。
- **后台方案管理 UI 端到端冒烟（Phase 13 M3）**：`smoke-admin-solutions.mjs`，`next start -p 3125` + 预热 health，**33 项全过**。脚本自种 USER/ADMIN 两账号 + 一条唯一标题 DEEP_CASE 案例（供新建方案挂载），走真实 Auth.js 拿 session cookie，验**页面渲染 + 发布守卫经 HTTP 端到端生效**：**金钱 shot**——对**未定价草稿** `PATCH {status:'PUBLISHED'}` → **409 CONFLICT** 且 `details.fields.price` 含「发布前必须填写价格」；填价后再发布 → **200** 且 DB 复核 `status=PUBLISHED`+`publishedAt` 落库；已发布方案标题**透传进公开 `/solutions`**、未定价草稿**不出现**在公开列表（证后台只露全部、公开橱窗只露可售）。**门禁**：USER `GET /admin/solutions` → 200 但只「无访问权限」不含「新建方案」「方案列表」；ADMIN → 200 见表单+列表且真实草稿标题透传（DB→SSR）；`POST /api/admin/solutions` 跨站 `Origin:evil.com` → **403 CSRF**、非法 slug → **400**、未登录 → **401**；`/admin` 出现指向 `/admin/solutions` 的「方案管理」入口链接。finally 按外键序 order→solution→case + user 硬删，末尾 leftover=0。**踩坑**：① BASE 必须 `localhost`（Origin/host 精确相等）；② 发布守卫的 409 `details.fields` 结构须在冒烟里精确断言字段名（`price`），证明拦截来自数据层而非 UI 硬编码；③ 公开 `/solutions` 是否泄露以具体草稿标题是否出现判定，勿用泛化关键词。冒烟脚本放 workspace scratch（不进仓库）。
- **后台方案内容编辑台端到端冒烟（Phase 13 M4）**：`smoke-admin-solutions-edit.mjs`，`next start -p 3126` + 预热 health，**33 项全过**。脚本自种 USER/ADMIN 两账号 + 一条唯一标题 DEEP_CASE 案例，走真实 Auth.js 拿 session cookie，验 `/admin/solutions/[id]` 编辑台的**页面渲染 + 各写端点经 HTTP 端到端生效 + 删除守卫**：① 详情页 SSR 含「基本信息 / 方案正文（34 分节）/ 财务测算 / 关键未知变量」四块；② `PATCH` 正文 + 价格 → 200、重开详情编辑初值透传自 DB、价格回填 1888.00；③ 新增财务（capex 1000000.00/roiPct 33.30）+ 新增未知变量（严重度 60）均 200、详情回读见 capex 数字与「严重度 60/100」；④ **金钱 shot（删除守卫）**——先 `POST /api/admin/solutions` 发布带价方案 200、买家 `POST /api/orders` 下单 200，随即 `DELETE /api/admin/solutions/[id]` → **409** 且 `details.fields.orders` 含「仍有 1 条订单，禁止删除」（撤单/退款属 ROADMAP #5）；再建一条**无订单**草稿 → `DELETE` 200、删后详情 → **404**；⑤ **门禁**——跨站 `Origin:evil.com` PATCH → **403 CSRF**；USER `GET` 详情 → 200 但「无访问权限」且**不泄露**「方案正文（34 分节）」块 / 财务编辑表单；未登录 → **307** 跳 `/login`；不存在 id → **404**。finally 按外键序 order→solution→case→user 硬删，末尾方案/案例/用户/订单四项 leftover=0。**踩坑**：① BASE 必须 `localhost`（Origin/host 精确相等）；② 「严重度 60/100」子串断言初**误判失败**——React SSR 会在相邻动态文本节点间插 `<!-- -->`，`严重度 {u.severity}/100` 渲染成 `60<!-- -->/100` 令 `includes("60/100")` 落空（与 Phase 8 M2 `5/34` 同一坑），修法是把整段严重度标签渲染成**单一模板字符串表达式** `{`严重度 ${u.severity}/100`}` 使其自然连续，而非放宽断言；③ 删除拦截须精确断言 409 + `details.fields.orders` 字段名，证明拦截来自数据层守卫非 UI 硬编码。冒烟脚本放 workspace scratch（不进仓库）。
- **后台案例内容编辑台端到端冒烟（Phase 13 M5）**：`smoke-admin-cases-edit.mjs`，`next start -p 3127` + 预热 health，**33 项全过**。脚本自种 USER/ADMIN 两账号 + 一条唯一标题 DEEP_CASE 主案例（**带合法 10 维 `scoreInput`**，见踩坑②）+ 一条 CANDIDATE 门禁案例，走真实 Auth.js 拿 session cookie，验 `/admin/cases/[id]` 编辑台**页面渲染 + 各写端点经 HTTP 端到端生效 + 证据联动复算 + 删除守卫**：① 详情页 SSR 含「基本信息 / 证据（案例拆解）/ 删除案例」三块、已存证据「置信度 80/100」回填；② `PATCH` 改标题（→ renamedTitle）与阶段（→ KEY_RESEARCH）→ 200 且详情回读透传；③ 新增 FACT 证据（confidence 88）→ 200、详情见「置信度 88/100」，**且 DB 侧 `evidenceConfidence > 0`（实测 50）——证 `addCaseEvidence` 触发 `recomputeCaseScores` 端到端生效**；删该证据 → 语句消失、`evidenceCount` 回落；④ **金钱 shot（删除守卫）**——对案例直插一条 `PUBLISHED` 方案后 `DELETE /api/admin/cases/[id]` → **409** 且 `details.fields.solutions` 含「已发布方案」、页面渲染该提示；撤方案后 `DELETE` → 200、删后详情 → **404**；⑤ **门禁**——跨站 `Origin:evil.com` PATCH → **403 CSRF**；USER `GET` 详情 → 200 但「无访问权限」且**不泄露**「证据（案例拆解）」块；未登录 → **307** 跳 `/login`；不存在 id → **404**。finally 按外键序 solution→evidence→case→user 硬删，末尾案例/证据/用户/方案四项 leftover=0。**踩坑**：① BASE 必须 `localhost`（Origin/host 精确相等）；② 首轮「证据可信度复算」断言**误判为 `null`（32/33）**——非代码 bug，而是 `recomputeCaseScores` 的**刻意门控**：`scoreInput == null` 时直接 `{status:"skipped"}`、绝不在无评分录入的情况下编造 `evidenceConfidence`（宪法第 20 条「程序计算 > 口算、不虚构」）。修法改冒烟不改代码——给主案例种合法 10 维 `scoreInput` 并把断言放宽为 `> 0`，复跑 33/33；③「置信度 88/100」「N 个已发布方案」子串断言须把整段渲染成**单一模板字符串表达式**（`{`置信度 ${x}/100`}` / `{`该案例仍有 ${n} 个已发布方案…`}`），否则 React SSR 在相邻动态文本节点间插 `<!-- -->` 令 `includes` 落空（与 Phase 8 M2 `5/34`、M4 `60/100` 同一坑，勿放宽断言要改渲染）。冒烟脚本放 workspace scratch（不进仓库）。
- **案例 10 维机会评分录入台端到端冒烟（Phase 13 M5b）**：`smoke-admin-cases-score.mjs`，`next start -p 3128` + 预热 health，**32 项全过**。脚本自种 USER/ADMIN 两账号 + 一条**尚无 `scoreInput`** 的 DEEP_CASE 案例，走真实 Auth.js 拿 session cookie，验 `/admin/cases/[id]` 评分卡片的**页面渲染 + `PATCH {scoreInput}` 经 HTTP 端到端触发复算 + 越界不污染既有拆解 + 门禁**：① 无评分态——详情页 SSR 见评分表单与「尚未评分」占位、**不含**评分拆解卡（拆解仅在服务端有合法 breakdown 时渲染）；② `PATCH` 合法 golden 10 维 → **200 且 `recompute:"computed"`**、DB 复核 `opportunityScore=74` 且 `scoreBreakdown` 已落、详情回读见拆解卡（含「商业价值」维度、「（反向）」标注、「综合评分 ≠ 项目一定成功」免责）；③ 追加一条 FACT 证据（conf 90、带 sourceUrl）→ DB `evidenceConfidence=90`、拆解页脚透出「证据可信度」「关键未知变量」（单一模板串连续可见）；④ **越界不污染金钱 shot**——`PATCH {commercialValue:99}`（越界）→ **200 但 `recompute:"invalid"`**，DB `opportunityScore` **仍为 74**、`scoreBreakdown` **仍在**（数据层对非法输入诚实地不覆盖上一份好拆解，宪法第 20 条）；⑤ **门禁**——跨站 `Origin:evil.com` PATCH → **403 CSRF**；USER PATCH → **403**；USER `GET` → 200「无访问权限」且**不泄露**评分表单；未登录 → **307**；finally 按外键序 evidence→case→user 硬删，末尾案例/证据/用户/评分四项 leftover=0。**踩坑**：① BASE 必须 `localhost`（Origin/host 精确相等）；② 拆解卡数字（如「录入 16 / 20」「证据可信度 90」）一律渲染成**单一模板字符串表达式**，规避 React SSR 相邻动态文本 `<!-- -->` 隔断（与 M4/M5 同源，勿放宽断言）；③ 复算是否落库须回 DB 直读 `opportunityScore`/`scoreBreakdown` 判定，不能只看 HTTP 200——越界分支正是 200 但 `recompute:invalid` 且不写拆解。冒烟脚本放 workspace scratch（不进仓库）。
- **SEO robots/sitemap 端到端冒烟（Phase 14 M1）**：`smoke-seo.mjs`，同一份构建起**两个 `next start` 实例**分别代表「未配置域名的开发/预览态 A」与「已配置 `NEXT_PUBLIC_SITE_URL=example.com` 的可索引态 B」，**20 项全过**。A（无域名）：`/robots.txt` 出 `Disallow: /`（拒绝一切抓取）、`/sitemap.xml` 为空（无 `<url>`）、首页 `<meta name="robots">` 含 `noindex`；B（真实域名）：`/robots.txt` 出 `Allow: /` + 各禁用前缀（`/admin`·`/account`·`/orders`·`/api`·`/login`·`/register`·`/search`·`/ui`）+ `Host` + 指向 `/sitemap.xml` 的 `Sitemap:` 行；`/sitemap.xml` 含 7 个行业页与 `/about` 等静态页、**不含** `/search`·`/admin`，首页 `<link rel="canonical">` 与 `og:url` 为绝对 `https://example.com/…` 且 meta robots 为 `index, follow`。**关键教训**：`robots.ts`/`sitemap.ts` 这类 `MetadataRoute` handler **默认会被构建期预渲染成静态快照**（`next build` 输出里显示为 `○`）——若构建环境的 `NEXT_PUBLIC_SITE_URL` 与运行环境不同，静态快照的 robots 会与 force-dynamic 的 sitemap/页面「分裂」（实测一度出现 sitemap 已带域名、robots 仍 `Disallow:/`）。正解 = **两个 handler 都 `export const dynamic = "force-dynamic"`**，让它们在运行期各自求值同一个 `isIndexable()`，构建输出转为 `ƒ /robots.txt`·`ƒ /sitemap.xml`，索引闸门始终一致。另注：断言 sitemap「无 `<loc>`」不能写 `!/https?:\/\//`（会命中 xmlns 声明里的 `http://www.sitemaps.org/…`），须精确匹配 `/<loc>/`。BASE 用 `localhost`。冒烟脚本放 workspace scratch（不进仓库）。
- **购买闭环 HTTP 端到端冒烟（Phase 12 M2）**：`smoke-orders.mjs`，`next start -p 3123` + 预热 health，**29 项全过**。脚本自种一条 DEEP_CASE 案例 + 一条 `PUBLISHED` 方案（price ¥2999.00/CNY）+ USER/ADMIN 两账号（同一 scrypt 参数），各走真实 Auth.js（csrf→callback/credentials）拿 session cookie，再带同源 `Origin` 打 `/api/orders` 与 `/api/admin/orders/**`。校验整条购买闭环经 HTTP 层成立：**A. 下单端点（公开·游客亦可）**——游客下单 200 且**金额服务端快照 = 2999.00/CNY/PENDING**（请求体夹带 `amount:"0.01"/currency:"USD"/status:"PAID"` 全被 M1 白名单剥离，DB 侧 `order.amount.toFixed(2)` 复核）、同游客重复下单幂等复用同 `orderId`（`deduped:true`、DB 仅 1 张）、跨站 `Origin:evil.com` 下单 → **403 CSRF**（游客态也防）、缺身份（无 userId 无 email）→ **400 VALIDATION_ERROR(identity)**、登录用户请求体伪造 `userId` → **被会话身份覆盖**（DB `order.userId===会话用户 id`、非伪造值）；**B. 后台端点**——未登录 `GET /api/admin/orders` → **401**、USER → **403**（非 staff）、ADMIN → **200** 且 items 含游客单+用户单，`POST .../confirm` → **200 PAID** 且 DB `paidAt` 落库·`version=2`·`ChangeLog.changedBy===human:<adminId>`（**actor 由服务端会话注入非客户端可伪造**）、重复确认幂等 `deduped:true` 仍 PAID、已 PAID 买家再对同方案下单 → **409 blocked**（不重复收钱）、新 PENDING 可 `.../cancel` → **200 CANCELED**、取消已 PAID → **409**、跨站确认 → **403 CSRF**、`?status=PAID` 过滤 items 全 PAID；finally **先按 solutionId 收集本次全部 orderId** 再删单→清其 `ChangeLog`→删 solution/case/user，末尾断言订单/审计/案例/用户四项 `leftover=0`。**踩坑**：① BASE 必须 `localhost`（与 Phase 13 M1 同理，Origin/host 精确相等，`127.0.0.1` 会被 `isSameOrigin` 全量误拒）；② 清理须在删 order **之前**先查回 orderId，否则删单后 `entityId in orderIds` 的 ChangeLog 无从收集会残留（本轮已在 finally 修正为先收集后删除）。冒烟脚本放 workspace scratch（不进仓库）。
- **购买闭环 UI 渲染端到端冒烟（Phase 12 M3）**：`smoke-orders-ui.mjs`，`next start -p 3124` + 预热 health，**29 项全过**。脚本自种一条 DEEP_CASE 案例 + 一条 `PUBLISHED` 方案（price ¥3999.00/CNY，body 塞入解锁正文独有标记 `解锁正文独有标记_${runId}`）+ 三账号（buyer USER / admin / other USER），各走真实 Auth.js 拿 session cookie，验**页面渲染 + 权益门控**（非仅 API）：**金钱 shot**——后台确认 PAID **之前**，游客 / 未购买家 / 陌生人访问 `/solutions/[id]` 的 SSR HTML **完全不含**该标记（证付费正文被 `hasPaidEntitlement` 门控在数据前、`locked=true` 走「正文未解锁」占位分支）；ADMIN `POST .../confirm` 之后，**仅属主**访问其 `/orders/[id]` 支付说明页与 `/solutions/[id]` 详情页 HTML **出现**该标记（DB→PAID→entitlement→RSC→HTML 端到端解锁打通），游客与陌生人**始终不见**标记。**所有权 / IDOR**——陌生人 `GET /orders/<buyer 的订单 id>` → **404**（与不存在不可区分，防枚举）；`/account/orders` 登录买家见自己单、不含陌生人单。**后台**——USER `GET /admin/orders` → 200「无访问权限」且不含真实 orderId（双层防御挡在数据前），ADMIN → 200 见 orderId + 确认/取消按钮位。finally 先按 solutionId 收集 orderId 再删单→清 ChangeLog→删 solution/case/user，末尾订单/审计/案例/用户四项 leftover=0。**踩坑**：① BASE 必须 `localhost`（Origin/host 精确相等）；② `getOrderById` 返回**非判别联合**（`data?` 可选），须 `if (res.status!=="found" || !res.data) notFound()` 才能收窄，否则 TS18048；③ 详情页购买按钮复用 `BuyButton` 只 POST `{solutionId}`（绝不在前端夹带 amount/status，金额由服务端快照），与 M2 端点口径一致；④ 解锁标记断言只匹配单一文本节点（不受 React SSR 相邻动态文本 `<!-- -->` 隔断影响）。冒烟脚本放 workspace scratch（不进仓库）。
- **后台审核发布队列端到端冒烟（Phase 13 M6）**：`smoke-admin-review.mjs`，`next start -p 3129` + 预热 health，**29 项全过**。脚本自种 USER/ADMIN 两账号（同一 scrypt 参数），走真实 Auth.js（csrf→callback/credentials）拿 session cookie，直插一条 `UNDER_HUMAN_REVIEW` 未定价方案（阻塞项应含「价格」）+ 一条 `UNDER_HUMAN_REVIEW` 已定价·无风险域方案（可发布）+ 一条 CANDIDATE 待发布案例 + 一条 PUBLISHED 干扰方案（须被队列排除），验**队列页 SSR 渲染 + 两个动作经既有 `PATCH` 端点端到端生效 + 门禁**：① 未登录 `GET /admin/review` → **307 跳 `/login`**；② USER `GET` → 200 但只「无访问权限」、**不含**「待审核方案」「待发布案例」区块（layout+page 双层防御挡在数据前）；③ ADMIN → 200 见两区块标题 + 两审核中方案标题与待发布案例标题**透传进 SSR**（DB→Server 组件→HTML）+ 未定价方案卡片出「价格」阻塞提示、可发布方案出「条件齐备」；④ **动作**——对可发布方案 `PATCH {status:"PUBLISHED"}`（带同源 Origin）→ 200 且 DB 复核 `status=PUBLISHED`+`publishedAt` 落库、该方案**从队列消失并出现在公开 `/solutions`**（证队列只读聚合与真实发布同一口径）；对未定价方案同样 `PATCH` 发布 → **409 CONFLICT**（`publishGuard` 拦截，证「队列里点了发布」仍受服务端真守卫约束、非前端硬编码可绕）→ 退回草稿 `PATCH {status:"DRAFT"}` → 200 且 DB `status=DRAFT`、仍在后台不在公开橱窗；对待发布案例 `PATCH {stage:"DEEP_CASE"}` → 200 且 DB `stage=DEEP_CASE`、案例**升入公开 `/cases`** 并从「待发布案例」队列消失；⑤ 跨站 `Origin:evil.com` 的发布 `PATCH` → **403 CSRF**；finally 按外键序 order→solution→case→user 硬删，末尾方案/案例/用户/订单四项 leftover=0。**踩坑**：① BASE 必须 `localhost`（Origin/host 精确相等，与 M1/M2/M3 同理）；② 队列「能否发布」的判定与真实发布拦截**同源于 `publishGuard`**（`solutionPublishBlockers`），冒烟须验证「未定价方案在队列里显阻塞、点发布又真被 409 拦下」两端一致，防两条逻辑漂移；③ 动作是否落库须回 DB 直读 `status`/`stage`/`publishedAt` 判定，不能只看 HTTP 200。冒烟脚本放 workspace scratch（不进仓库）。
- **沙盘 §17 端到端主链冒烟（中途重构 R6.4）**：`smoke-sandbox-e2e.mts`（scratch·**绝不入仓**、跑完移入回收站），`next start`（子进程由脚本 `node ./node_modules/next/dist/bin/next build`→`next start` 起、`.env` 经 `node --env-file` 注入并被继承），**40 项断言全过**——以真实 HTTP + 真连 Neon + 真实 DeepSeek（`deepseek-chat`、单次成本约 $0.0005–0.0008）串起 §17 要求的整条 V1 主链「选地区→改参数→跑→技术/经济/风险/敏感性→报告→AI 解释→保存→多情景→版本快照→回滚」，这是**首次以自动化脚本实证跑通全链**（此前 R1–R6.3 仅以单元/集成/分段冒烟分证各环）。覆盖：① 客户端按 `SandboxWorkbench` 权威组合顺序现算山西情景 → 报告 `ok`、7 分节齐、NPV>0、改单价单调升；② 直插 owner(USER)/staff(REVIEWER)/other(USER) 三账号各走真实 Auth.js 拿 session cookie；③ **HTTP 门禁真 fetch 实证**（§28 + CSRF）——匿名建项目/解释 → **401**、跨站 `Origin:evil.com` 解释/建项目 → **403 CSRF**；④ owner 同源 `POST /api/sandbox/explain` → 200、`interpretation` 非空、回 `modelId`、成本有限非负、DB `ModelCall` 中 `agent="sandbox:explain"` 计数 +≥1（§31 落库实证）；⑤ **owner 建项目 → 200**、DB 复核 `ownerId===会话 owner.id`（客户端夹带 ownerId 被无视）、`calcStatus:"ok"`、**★落库 npv 与页面纯函数逐位一致（11424894）**、`ChangeLog.changedBy=human:<owner>`；⑥ **§28 越权**——other `GET`/`PUT` 他人项目 → **403 且引擎零重跑**、staff `GET` → 200；**§4 持久层回环**——`PUT` 单价 1.2→v2 NPV 升、1.5→v3 再升、`saveVersion` 冻结、改回 1.2 NPV 回落、`restore` → NPV **精确复现存版本 1.5 水平（34094112）**、`GET versions` 含该版本；finally 级联清 Project/Scenario/Version/User/ChangeLog/Region，leftover=0。**★跑通即暴露并修复一颗真实用户侧隐患**——`SandboxSavePanel` 把沙盘地区包代码（`shanxi`/`national`）当 `regionId` 上行、该值非规范 `Region` 行 → `Project.regionId` 外键 `P2003` 直接 500（真实点「保存为项目」必崩），修于 `sandbox-store.ts`（`STORE_VERSION 1.0.1→1.0.2`）`createProject` 落库前对 `regionId` 做外键安全护栏（非法值记日志置空、合法 id 保留），并补 2 条集成回归用例锁死。**结论：§17 端到端主链全线跑通、V1 沙盘核心完成条件正式达成**。冒烟脚本放 workspace scratch（不进仓库）。
- **已知环境行为（非 bug）**：① Neon 免费库闲置挂起后首次连接可能 5s 超时返 500（health 正确报 `db.ok:false` 并记录日志），第二次唤醒后恢复——冒烟脚本已加预热重试；② CRLF 头注入在 undici 客户端层即被拒（`Headers.append: invalid header value`），到不了服务端，Proxy 的 `sanitizeForLog` 为纵深防御；③ **根 `loading.tsx`（Suspense）与"运行时 DB 查找 + notFound()"的动态详情页冲突**：Suspense 边界会先 flush 200 响应头 + 骨架 shell，等异步页面组件里 `notFound()` resolve 时状态码已无法回退成 404（里程碑 2 首轮冒烟实测 `/cases/[id]`·`/solutions/[id]` 无效 id 全返 200，渲染了 not-found UI 但状态码错，会被 SEO 误收录）。SSG 路由（`generateStaticParams` + `dynamicParams=false`，如 `/industries/[slug]`）在路由器层先判、**免疫**。对取值来自任意 DB id、无法预渲染的详情页，解法是**移除制造提前 flush 的根 `loading.tsx`**（里程碑 2 已删除，重建后无效 id 真 404）；若日后要恢复骨架屏，应改用不覆盖详情段的分段级 loading 或在页内内联骨架，勿放回根级；④ **数组事务的 READ COMMITTED 快照竞态**：`$transaction([findMany, count])` 里两条语句各取自己的读快照，当有并行兄弟测试文件在两次读之间插/删行时，`count` 可能小于已返回的 `items.length`，令列表视图出现 `total < items` 的自相矛盾（`admin-solutions.test.ts` 与新增 `admin-solution-detail.test.ts` 并行时实测触发）。这不是逻辑错而是隔离级别使然，`listAdminSolutions`/`listAdminCases` 已用 `total = Math.max(rawCount, items.length)` 兜底——**绝不报出小于已返回行数的 total**（与 `admin.test.ts` 早先「取消独立 count 查询、改由同事务 groupBy 派生 total」是同一类并行计数竞态的两种应对）。
- **测试基建修复（中途重构 R8.5）**：`package.json` 的 `test:integration` 脚本加 `--no-file-parallelism`——**整套连库套件由「默认并行」改为「文件级串行」跑**。缘由：上一节「已知环境行为 ④」所述的数组事务 READ COMMITTED 快照竞态，在共享 Neon 免费连接池 + 多测试文件并行时会间歇性放大（`admin-review.test.ts` 的「`total ≥ items`」全局审核队列不变式尤甚——`getReviewQueue` 数的是**全库**待审方案，并行兄弟文件在 list 与 count 两次读之间插入的瞬时 DRAFT 会让 `total < items`，尽管 `Math.max` 兜底仍可能因两条聚合跨更多并行窗口而自相矛盾；本仓历史上 `sandbox-store.test.ts` 已把此竞态记为「后续可专项串行化根治」）。R8.5 新增的连库写用例（真引擎链造草案 + 发布 + 升级写回，单文件耗时较长）进一步抬高并行争用概率，故采纳该专项建议、以 `--no-file-parallelism` **根除跨文件并行干扰**（代价是全量集成墙钟时长上升，可接受）。★此为**测试执行方式（harness）修复，不改任何被测业务代码、不削弱任何断言**；全量连库套件串行跑 **23 文件 / 119 例全绿**。若日后迁到独立每-shard 数据库或连接池扩容，可视情恢复并行。
- **仍未引入**：Playwright（E2E）、@testing-library/react（组件测试）、msw（HTTP mock）、testcontainers（本机无 Docker）。Phase 5 引入公共页面后补 React 组件测试；Phase 15 补 E2E。
