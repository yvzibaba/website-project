# ROADMAP — Phase 0–18 任务树、MVP 边界、阻塞与开放决策

来源：总控 Prompt 第37–43 节；宪法第2/4/18/26 条。执行方式：**小步、逐 Phase**，每完成一个 Phase 必须 修改代码 → 运行测试 → 检查错误 → 修复 → 更新文档 → 汇报状态（总控第37节 / 宪法第18条）。禁止一次性重写整个系统。

## 0. ⚠️ 战略转向（2026-09-05）：V1 优先路线已切换为 R0→R7 沙盘任务树

创始人 2026-09-05 下发《项目中途重构总控 Prompt》，产品重定位为「AI 产业案例与可视化解决方案平台」，V1 唯一焦点=「新能源重卡 + 光伏 + 储能 + 充电」一体化**产业项目可视化决策沙盘**。**本节的 R0→R7 任务树即日起取代下方 Phase 0–18 表作为 V1 优先路线**（旧表保留作历史记录，其成果——认证/后台/审计/Evidence 四分类/GitHub Scout/Order 闭环——全部复用，不推倒重来）。缺口评估与 KEEP/ENHANCE/FREEZE/DEFER/REMOVE 分类见仓库 `REFACTORING_ASSESSMENT_V1.md`。命脉（§4）=改参数须触发 技术结果→经济结果→图表→风险→AI 解释→动态报告 全链重算；§7=关键财务指标（CAPEX/OPEX/CashFlow/NPV/IRR/ROI/回收期）必须程序计算、LLM 只解释不代算；§17=沙盘 E2E 主链未跑通不得宣布核心完成。§14 优先级顺序：①参数系统 ②项目模型 ③计算引擎 ④可视化 ⑤动态报告 ⑥地区政策 ⑦企业个性化 ⑧敏感性 ⑨情景 ⑩AI 解释。

| 里程碑 | 名称 | 关键交付 | 依赖 | 状态 |
|---|---|---|---|---|
| R0 | 契约设计 | 参数/计算/结果的数据契约（`parameter-engine.ts` 类型 + `REFACTORING_ASSESSMENT_V1.md` 任务树） | — | ✅ **v0.36.0** |
| R1 | 参数引擎 | 分层解析（default<region<policy<user）、裁剪、过期政策跳过、派生重算、仅数值快照；纯函数 + 29 黄金样本 | R0 | ✅ **v0.36.0**（`src/server/parameter-engine.ts`，基线 541=440 单元+101 集成、`tsc`/`eslint` 0 错） |
| R1.2 | 沙盘参数模板 | 「重卡+光伏+储能+充电」逐参数 `source/confidence/占位 ASSUMPTION/min·max/exposure/派生关系` | R1 | ✅ **v0.37.0**（`src/server/sandbox-params.ts` 40 参数 + `resolveSandbox` 接缝 + 20 黄金样本，基线 561=460 单元+101 集成） |
| R2 | 计算引擎 | R2.1 技术模型 · R2.2 财务（NPV/IRR/ROI/回收期）· R2.3 敏感性+高风险标记 · R2.4 `runSandboxModel` | R1.2 | ✅ **v0.38.0 + v0.39.0 收官**（四块纯函数全部落地、基线 652=551 单元+101 集成、`tsc`/`eslint` 0 错、build 无新增路由；§7「指标必须程序算」计算内核成链，§17 E2E 主链仍待 R6） |
| R2.1 | 技术能耗模型 | `sandbox-tech.ts` PV出力/充电负荷/自用余电/储能吞吐/能量平衡（透明简化 + needsProfessionalReview）+ 25 黄金样本 | R1.2 | ✅ **v0.38.0**（基线 619=518 单元+101 集成、`tsc`/`eslint` 0 错、build 无新增路由） |
| R2.2 | 财务评价原语 | `sandbox-finance.ts` npv/irr(区间二分)/回收期/roi/现金流构造（无除零 + calcRef + 诚实降级）+ 33 黄金样本 | R1.2 | ✅ **v0.38.0**（同上基线） |
| R2.3 | 敏感性 + 高风险标记 | `sandbox-sensitivity.ts` 关键参数 one-at-a-time tornado（`computeTornado` 复用 `runSandboxModel`、摆动幅排序）+ `deriveRiskFlags` 高风险「需专业人工确认」标记 + 15 黄金样本 | R2.1, R2.2, R2.4 | ✅ **v0.39.0**（`project.chargingPrice` 参数补入 sandbox-params 1.1.0） |
| R2.4 | `runSandboxModel` 编排 | `sandbox-model.ts` 消费 `resolveSandbox`→`computeTechModel`→现金流（E1–E8 透明经济模型）→npv/irr/payback/roi→`CalcResult` + 18 黄金样本（§4 命脉端到端焊通） | R2.1–R2.2 | ✅ **v0.39.0**（基线 652=551 单元+101 集成、`tsc`/`eslint` 0 错、build 无新增路由） |
| R3 | 项目模型持久层 | additive 迁移 `Project/ProjectScenario/ProjectVersion` | R2 | ✅ **v0.40.0**（新枚举 `ProjectStatus` + 三表落库、`sandbox-store.ts` 存**输入 paramLayers + 现算 CalcResult**（§4 真源）、货币仅持久边界用 Decimal、版本快照重算式回滚；单元 +11=562、集成 +6（sandbox-store 6/6 单跑绿、db-smoke 枚举 13→14/表 19→22 计数如实修正）；`tsc`/`eslint` 0 错、build 无新增路由；§17 E2E 仍待 R6） |
| R4 | 可视化 | recharts 绑模型、改参数图表即时联动 + 交互主链 | R2 | ✅ **v0.41.0**（新纯函数视图模型 `src/lib/sandbox-view.ts`（`CalcResult`/`Tech`/`Tornado`→图表数据+中文格式化、`VIEW_VERSION 1.0.0`）+ client 交互工作台 `SandboxWorkbench`（引擎打进浏览器、`useMemo` 即时重算整链、参数驱动 UI、只读派生量联动）+ `SandboxCharts`（recharts 3.10.1：现金流/回收、年收入成本、CAPEX·收入·OPEX·能量分解、敏感性龙卷风）+ 新静态路由 `○ /sandbox`；单元 +21=583（含★§8 图表绑定真引擎实证 + §4 改参数→视图 NPV 升穿透呈现层）、集成 107 不变（零 DB）；`tsc`/`eslint` 0 错、build 唯一新增 `/sandbox`；**§17 E2E 仍待 R6**——项目持久化/版本 UI、地区种子、AI 解释尚未接到本页） |
| R5 | 地区政策 | 先 1 省（山西）载默认电价/光照/补贴参数包 + 工作台地区切换 | R1, R3 | ✅ **v0.42.0**（新纯数据 + 纯函数目录 `src/server/sandbox-regions.ts`（`SANDBOX_REGIONS_VERSION 1.0.0`）：全国通用兜底包（空覆写=黄金基线逐字不变）+ 山西包（地区层电价 0.55/光照 1400h + `bounds` 限幅、政策层现行补贴·上网价 + **一条故意已过期碳价实证 §6「过期政策绝不作现行默认」**），**★诚实不变式**凡覆写层恒 `ASSUMPTION·confidence≤50·【示例·待核实】`，`buildSandboxLayers` 把 region/policy 垫底、user 在上并透传本省 `bounds` 到 user 层、`now` 绕开引擎 epoch0 坑；改 `sandbox-sensitivity.ts`（`SENSITIVITY_VERSION 1.0.0→1.1.0`·`computeTornado` 新增可选 `layers` 使龙卷风可锚当前情景、省略则逐字保旧行为向后兼容）；`SandboxWorkbench` 加「选地区」pill + `origin` 分层徽标（地区默认/政策/已改/裁剪）、敏感性 `useMemo` 依赖 `[]→[layers]` 随地区即时重算；单元 +10=593（含★诚实遍历/§6 双向过期/§4+§6 端到端换山西 NPV↑且龙卷风锚当前情景）、集成 107 不变（零 DB）；`tsc`/`eslint` 0 错、build 路由清单不变（地区为纯前端状态）；**刻意边界**：地区包是版本化纯代码目录刻意不落 Region DB、只 1 省、数字全占位假设；§17 E2E 仍待 R6） |
| R6 | 动态报告 + AI 解释 | 报告读最新 `CalcResult`、`runTask` 解释「为何变/最敏感变量/what-if」、存项目版本、**§17 E2E 主链跑通** | R2–R5 | ✅ **v0.43.0 → v0.46.0 收官（R6.1 动态报告骨架 · R6.2 AI 解释 · R6.3 项目保存/版本/回滚接线 · R6.4 §17 E2E 主链跑通）**（R6.1 交付**确定性动态报告骨架**：新纯函数 `src/lib/sandbox-report.ts`（`REPORT_VERSION 1.0.0`、只 `import type` 视图模型、零 AI/网络/DB/时钟，报告数字逐字取自 `vm.cards`/`vm.meta` 不重算、`generatedFrom` 钉 `calcRef`+内核版本、失败态只回 `error` 不出编造结论、成败皆恒挂三条免责）+ client 展示件 `SandboxReportPanel.tsx` + `SandboxWorkbench`「生成/收起动态报告」按钮按当前情景 `useMemo` 整份重写。**R6.2 交付 AI 解释**：新 server-only `src/server/sandbox-explain.ts`（`EXPLAIN_VERSION 1.0.0`，复用 `runTask`+`createChatProvider`，★只把 R6.1 报告喂给 LLM、prompt 明令不得重算/杜撰、what-if 只定性，Zod `explanationSchema` 校验、`report.ok=false` 诚实拒绝、状态对齐 `mutationResponse`，层级 medium、归因 `sandbox:explain`）+ 新受门禁端点 `POST /api/sandbox/explain`（`requireSameOriginActor` CSRF + **必须登录**、`createDbCallRecorder`+flush 成本入 `ModelCall`）+ client `SandboxExplainPanel.tsx`（`mutateJson` POST、渲染解读/关键变量/定性 what-if/风险 + 成本声明）+ `SandboxWorkbench`「AI 解释此结果」pill；单元 +11=604 再 +12=616（含★单一真源/★诚实边界/★§4+§9 联动/喂真引擎端到端 + ★拒绝解释不完整报告/★成本入 recorder/schema 不过归一 error/provider 抛错不崩）、**集成 107 不变**（零 DB、无集成用例 import 新模块）、build **唯一新增 `ƒ /api/sandbox/explain`（动态）**、`/sandbox` 仍 `○`；**★真实 DeepSeek 冒烟证 LLM 逐字引用报告数字、what-if 全定性、成本 $0.000622**。**R6.3 把 R3 持久层接上 `/sandbox`**：新 server-only `src/server/sandbox-projects.ts`（`SANDBOX_PROJECTS_VERSION 1.0.0`·编排 + 资源级鉴权收一处、★`ownerId` 只从服务端会话取绝不采信客户端、纯函数 `canAccessProject` owner-or-staff 越权在动库前拦、`layers` 用不透明 `z.record(z.string(),z.any())` 承载以免剥掉日期窗）+ 5 条 `force-dynamic` 受门禁端点 `POST /api/sandbox/projects`·`GET .../projects/[id]`·`PUT .../scenarios/[id]`·`POST+GET .../scenarios/[id]/versions`·`POST .../scenarios/[id]/restore`（`requireSameOriginActor`+**未登录 401**、写/回滚经 `sandbox-store` **服务端现算落库**非搬页面数字）+ `api-guard` 加 `forbidden→403` + **修 R3 复算时钟隐患**：`sandbox-store.ts`（`STORE_VERSION 1.0.0→1.0.1`）新纯函数 `toEngineLayers` 落库回放前复活 `now`/政策日期窗为真 Date（防 `now<effectiveFrom` 一 Date 一串变 NaN 致过期政策误判现行），★把判生效时钟作可持久回放输入（规则 7 可复算）+ client `SandboxSavePanel.tsx` 串「建→改→存版本→回滚」、`SandboxWorkbench`「保存 / 版本」pill、`mutate.ts` method `+="GET"`；单元 +27=643（`sandbox-projects` 20 + `sandbox-store-now` 7，含★`canAccessProject` 真值表/★layers 逐字保留/★强制会话 ownerId/★非属主 forbidden 且 store 未调/★Date 分层 JSON 往返复活重算 npv toBeCloseTo + calcRef 相等/★混合 Date-字符串 NaN 守护）、**集成 107 不变**（连 Neon 证 `toEngineLayers` 对 R3 空/user-only 分层惰性）、build **恰 +5 条动态 `ƒ /api/sandbox/**`**、`/sandbox` 仍 `○`。**R6.4 收官 §17 E2E 主链跑通**：新增端到端冒烟脚本（scratch·**不入库**，40 项断言全绿）以真实 HTTP + 真连 Neon + 真实 DeepSeek 串起「选地区→改参数→跑→技术/经济/风险/敏感性→报告→AI 解释→保存→多情景→版本快照→回滚」全链，并首次以路由级实证 §28 鉴权（未登录 401 / 跨站 Origin CSRF 403 / 非属主 403 且不重跑引擎 / staff 放行）；★跑通即暴露并修复一颗真实用户侧隐患——`SandboxSavePanel` 把沙盘地区包代码（`shanxi`/`national`）当 `regionId` 上行，而该值非规范 `Region` 行 → `Project.regionId` 外键 `P2003` 直接 500（真实点「保存为项目」必崩）；修于 `sandbox-store.ts`（`STORE_VERSION 1.0.1→1.0.2`）`createProject` 落库前对 `regionId` 做外键安全护栏（非规范 `Region` 行则记日志并置空、合法 id 原样保留），+2 集成回归用例（沙箱代码→置空 / 真 `Region.id`→保留），**集成 107→109**、单元 643 不变。**实证服务端重算 NPV 与页面纯函数逐位一致（11424894）、回滚精确复现存版本水平（34094112）——§4「程序算、服务端重算非搬页面数」从此被 E2E 锁死**。**§17 端到端主链全线打通、V1 沙盘核心完成条件达成**） |
| R7 | 企业个性化 | 依企业画像裁剪方案 | R6 | ✅ **v0.47.0**（新纯数据 + 纯函数目录 `src/server/sandbox-profiles.ts`（`SANDBOX_PROFILES_VERSION 1.0.0`·`profileCalcRef()=profiles@1.0.0`）：六画像（`generic` 通用置首即默认 + `fleet` 车队 + `operator` 充电运营商 + `park` 园区 + `transit` 公交 + `investor` 投资人），每个 `presetValues`（键须命中 `REGISTERED_KEYS`）+ `emphasis`（`metricKeys`/`focusTags`/`headline`）；★诚实铁律凡画像 `ASSUMPTION·confidence≤50·【示例·待核实】`（占位预设非经核事实），`validateProfileCatalog()` 钉成可回归校验。核心 `buildProfileLayers` 把画像预设**并入既有 user 层**（`{...preset,...user}`）→ 优先级 `default<region/policy<画像<用户覆写` 天然成立、**无需给冻结参数引擎加 "enterprise" origin（免 bump `PARAM_ENGINE_VERSION` + 重写 26+ 黄金样本）**、`bounds` 透传裁剪；`generic`（空预设）委派 `buildSandboxLayers`→与 R6 基线逐字节等价；`isProfileDefault` 供 UI 分「画像默认 / 已改」；未知 id 诚实回落通用绝不裸抛。改 `src/lib/sandbox-report.ts`（`REPORT_VERSION 1.0.0→1.1.0`）`ReportInput` 加可选 `profile`、成功路径在「执行摘要」后插「企业个性化视角」节——**只按 `emphasis` 选取/重排既有 `vm.cards` 原值绝不重算**，因 `flattenReportForPrompt` 遍历全节故个性化**自动流入现成 AI 解释端点**、无 profile 时结构 100% 向后兼容；`SandboxWorkbench` 加「选企业画像」pill + 「画像默认」徽标 + 溯源 `profiles@1.0.0`。单元 643→659（新 `sandbox-profiles` 13 + `sandbox-report` +3，含★`generic`≡基线深相等 / ★画像胜默认且用户胜画像 / ★§4 investor·operator NPV 与 generic 相异 / ★失败报告忽略 profile）、**集成 109 不变**（零 DB、纯函数与客户端态）、`tsc`/`eslint` 0 错、build 路由清单不变（`/sandbox` 仍 `○`、`/api/sandbox/**` 仍 6 `ƒ`）。**至此 R0–R7 沙盘任务树全收官、§14 V1 十项优先级地基齐备**；Playwright 组件/E2E 仍「未引入」不变） |
| R8 | 商业闭环（沙盘 → 产业方案 → 查看 → 购买） | 把一次沙盘 `CalcResult` + 视图模型 + 企业画像映射成符合 34 分节规范的 `Solution`（产业方案），接上平台既有「Solution + Order 购买」闭环 | R7 | 🟡 **R8.1 ✅ v0.48.0（纯映射桥）· R8.2 ✅ v0.49.0（导出 UI + 受 staff 门禁接案落库转 DRAFT Solution）· R8.3+ 待做（查看→购买 Order 真闭环接线）**：新增 client-safe 纯函数 `src/lib/sandbox-solution.ts`（`SANDBOX_SOLUTION_VERSION 1.0.0`），`buildSandboxSolutionDraft({calc,vm,regionName,projectName?,scenarioName?,profile?,price?,currency?})`→判别联合：成功态产出 `title/slug/summary/body`（逐节填充 34 分节里沙盘能真给者，**全部数字逐字引用 `vm.cards`/`calc` 原值绝不再算**，§7/§8）+ `financials`（镜像 `SolutionFinancial` 入参形状、负/非有限诚实省略）+ `unknowns`（恒含「入参皆占位假设」severity 90）+ `riskDomains` + `needsProfessionalReview:true` + `evidenceGrade:"ASSUMPTION"` + `publishBlockers`（须核实/未附 `caseId`/需人工确认/未定价/NPV 非正）；失败态只回诚实 `error` 不编造。**★R8.1 刻意只产草案不写库**——`Solution.caseId` 为必填非空外键，直写会重蹈 R6.4 `regionId` 的 `P2003`，故「选哪份真实案例挂载 → 发布」这一关键决策交回人（AI 做劳动、人做关键决策）。**R8.2 补上受门禁落库那一跳**：新 server-only `src/server/sandbox-solution-store.ts`（`SANDBOX_SOLUTION_STORE_VERSION 1.0.0`）`persistSandboxSolutionDraft(input,actor?)` 对草案做结构化 Zod 校验后**复用既有 `createSolution`（强制 `status=DRAFT` + `caseId` 外键预检，缺失案例诚实返 `invalid.fieldErrors.caseId` 绝不 500）+ `addSolutionFinancial` + `addSolutionUnknown`** 逐条落库（财务/未知单条失败降级为 `warnings` 不毁整份 DRAFT、`unknownVariableCount` 由既有函数自动回写），新受 staff 门禁端点 `POST /api/sandbox/solution`（`requireStaffWrite` CSRF + `requireRole(STAFF_ROLES)`，与普通手工建方案同权限、**不给沙盘导出越权**）+ `/sandbox`「导出产业方案」client 面板 `SandboxSolutionPanel.tsx`（**强制从既有真实案例清单里选一个 `caseId`、绝不伪造外键**，案例清单走新 `GET /api/admin/cases`（`requireRole` 只读门）按钮触发惰性加载、不 mount-fetch）+ `SandboxWorkbench`「导出产业方案」pill。**R8 前两块纯加性竖切**：零 schema/DB 迁移（复用既有 Solution/SolutionFinancial/UnknownVariable/ChangeLog 表）、R8.1 零端点/路由/UI（`/sandbox` 仍 `○`），R8.2 仅 +2 路由（`ƒ /api/sandbox/solution`、`/api/admin/cases` 加 GET）+ 1 面板，`/sandbox` 仍 `○`。单元：R8.1 659→669（新 `sandbox-solution` 10 例）、R8.2 669→678（新 `sandbox-solution-store` 9 例，含★成功转发 financials/unknowns + 计数 + `createSolution` 入参/actor 断言 / needsProfessionalReview 缺省→true / 坏 caseId→invalid 且 `createSolution` 未调 / 负价→invalid.price / `createSolution` invalid→中止不落 fin·unk / 财务部分失败→ok+warnings+count）；集成：R8.1 109 不变，R8.2 109→112（新 `sandbox-solution-store` 3 例**跑整链真引擎产出真 Decimal 串打真 Neon 列精度/JSONB/FK/级联/ChangeLog**：真草案 + 真建 Case → DRAFT 读回逐字段 / 格式合法但不存在的 caseId → invalid 且 Solution 计数不变 / 无价 → price null + publishBlockers 含「价格」）；`tsc`/`eslint` 0 错、build R8.2 新增 `ƒ /api/sandbox/solution` 且 `/api/admin/cases` 现 GET+POST、`/sandbox` 仍 `○`。**R8.3+ 待做**：沙盘来源方案的「查看方案详情→Order 购买」真闭环接线 + 把 ASSUMPTION 占位升级为可追溯 FACT 的数据接入（§12 来源可追溯 / 公式可复算）。§17 沙盘核心已完成，R8 属 V1→V2「商业闭环」推进、非沙盘核心完成的前置 |

> 建议执行序：R0→R1→R1.2→R2→R3→R4→R5（✅ 参数地基 + 计算内核 + 项目持久层 + 可视化交互 + 地区政策全部已成，v0.36.0–v0.42.0）→ **R6 收官（R6.1 动态报告骨架 ✅ v0.43.0 → R6.2 AI 解释 ✅ v0.44.0 → R6.3 项目保存/版本/回滚接线 ✅ v0.45.0 → R6.4 §17 E2E 主链跑通 ✅ v0.46.0——端到端全链实证、核心完成条件达成）** → **R7 企业个性化 ✅ v0.47.0（依企业画像裁剪方案，§14 第 7 项，最后一块地基）——至此 R0→R7 沙盘任务树全收官、§14 V1 十项优先级地基齐备** → **R8 商业闭环（🚀 进行中：R8.1 沙盘结果→Solution 草案纯映射桥 ✅ v0.48.0 → R8.2 导出 UI + 受 staff 门禁接案落库转 DRAFT Solution ✅ v0.49.0 → R8.3+ 查看方案→Order 购买真闭环 + ASSUMPTION→FACT 数据接入待做——对应总控最高优先级「商业闭环」）**。

## 1. Phase 任务树

| Phase | 名称 | 关键交付 | 依赖 | 状态 |
|---|---|---|---|---|
| 0 | 项目分析 | 环境检查、现状盘点 | — | ✅ 本次完成 |
| 1 | 需求与信息架构 | PRODUCT_SPEC（用户/页面 IA/MVP） | 0 | ✅ 本次完成 |
| 2 | 技术架构 | ARCHITECTURE（选型/分层/Model Router） | 1 | ✅ 本次完成 |
| 3 | 数据库设计 | DATABASE（ER）+ prisma V1 核心 schema | 2 | 🟡 V1 核心已建；全量表分层待逐 Phase 补 |
| 4 | 项目骨架 | 目录结构、DB 连接、基础 UI、env、日志、错误处理、测试框架 | 3 + Postgres/依赖 | ✅ **Phase 4 全部完成**（里程碑 1+2+3）：DB 上线（17 表 + 11 枚举）、`src/lib/{env,logger,errors,prisma}` 核心库、Next.js shell（layout/page/error/not-found + `/api/health`）；里程碑 3 补设计 tokens（颜色/圆角/阴影亮暗双模式）、`src/components/ui/*` 11 组件 + `/ui` 演示页、`src/lib/{cn,api-client,validation,request-id}`（统一 fetch + AppError 映射 + 重试、Zod 校验含 11 个与 prisma 同步枚举、request-id 追踪）、`src/proxy.ts`（request-id 注入下游 + 访问日志 + 安全头）。Vitest 单元 **125** + 集成 5 全绿、`tsc` 0 错、`next build` Turbopack 产出 **4 路由 + Proxy**、HTTP 冒烟：request-id 服务端生成/客户端透传/非法拒绝全通过、`/ui` 200 全组件渲染 |
| 5 | 公共页面 | 首页/行业/案例列表/方案列表/搜索/关于/隐私/协议 | 4 | ✅ **Phase 5 全部完成**（里程碑 1+2+3）：**M1**——页面级布局组件（PageHeader/Breadcrumb/EmptyState）、行业数据层 `src/server/industries.ts`、`/industries` 列表（force-dynamic + 实时计数）、`/industries/[slug]` 详情（SSG + `dynamicParams=false` 保证真 404）、`/about`、`/privacy`+`/terms` 结构化占位（标注待法务审定，宪法第 21 条）、导航/页脚更新。**M2**——DEMO 可见性层 `src/server/demo.ts`（默认排除、`?demo=1` 显式纳入并打角标）、DEMO 种子脚本 `prisma/seed.ts`（**仅 6 个标注【DEMO】的案例，按创始人裁决不种子任何方案**——方案涉及定价与购买闭环须走真实多角色流水线，宪法第 20 条禁伪造可售商品；生产环境拒跑）、案例数据层 `src/server/cases.ts`（列表 `$transaction[findMany,count]` + 行业筛选/分页/排序 + 详情含证据分层/能力关联/商业模式，公开阶段白名单 + DEMO 门控 → `not_found`）、方案数据层 `src/server/solutions.ts`（仅 PUBLISHED，金额两位小数精显）、`/cases`+`/cases/[id]`+`/solutions`+`/solutions/[id]` 四页（购买按钮占位 disabled，Phase 12 接入）。**关键修复**：删除根 `src/app/loading.tsx`——实测其 Suspense shell 会让 force-dynamic 详情页对无效 id 先 flush 200、`notFound()` 无法回退状态码（3 个无效 id 冒烟全返 200）；移除后无效 id 真 404，SSG 路由本就免疫。**M3**——搜索编排层 `src/server/search.ts`（`searchPublic` 并行案例+方案、聚合 ok/hits、DB 失败软降级）、cases/solutions 列表加关键词维度（`buildCaseWhere`/`buildSolutionWhere` 用 `{AND:[...]}` 容纳关键词 OR 片段与 DEMO OR 片段）、`/search` 页（无 JS 可用 GET 表单 + 行业 chips + 引导/非法/失败/空/结果 五态 + 深链"查看全部"）、首页 `src/app/page.tsx` 照总控 §6 重写为六段体验（Hero+内联搜索 / 今日案例 / 今日方案 / 企业诊断 V1-B 即将开放 / 六步工作流 / 按行业浏览，移除 Phase 4 pg_stat 调试面板）、`/cases`+`/solutions` 关键词回显条、导航加 `/search`。测试基线 **152 单元 + 18 集成**全绿、`tsc`/`eslint` 0 错、`next build` 产出 `ƒ /search`、HTTP 冒烟 **31/31**（含 2 个真 404 回归 + `?demo=1` 命中/默认不泄露种子标题）。V1-A「用户查看」入口层（发现→检索→详情）打通；"购买"仍 Phase 12 占位 |
| 6 | 用户系统 | 注册/登录/会话/最小权限（下单身份） | 4 | 🟡 **M1 完成**：认证方案经创始人裁决选定 **Auth.js 自建**（总控 §21）——`next-auth@5` Credentials（邮箱+密码）+ **JWT 会话**（`trustHost:true`，**不挂 Prisma Adapter**，依赖最少）；`prisma` 加 `enum UserRole{USER/REVIEWER/ADMIN}` + `model User`（email unique / passwordHash / role 默认 USER），`Order` 加可空 `userId`（`ON DELETE SET NULL`），迁移 `20260905000000_add_user_auth` **纯加性**已 `migrate deploy` 到 Neon（表 17→18、枚举 11→12）；口令哈希 `src/lib/password.ts` 用 **node:crypto scrypt**（自描述串 + `timingSafeEqual`，畸形/超长一律 false，**零额外依赖**、绝不存明文）；`src/lib/validation.ts` 加 Email/Password/Register/Login/UserRole schema（邮箱归一小写、注册 8–128 位、登录口令 min1 防枚举）；`src/server/users.ts`（registerUser 判别联合 + P2002 归一 email_taken、getAuthUserByEmail、getProfileUserById 排除 passwordHash）；`src/auth.ts` + `api/auth/[...nextauth]`（authorize 对账号不存在/密码错误一律返 null，防枚举；jwt/session 透传 id/role；JWT 类型增强打在 `@auth/core/jwt`）；`/login`·`/register`·`/account` 三页（`useActionState` 共用 `AuthForm` + `"use server"` actions，注册后自动登录，账号页无会话重定向 /login + 登出）。测试基线 **167 单元 + 23 集成**全绿、`tsc`/`eslint` 0 错、`next build` 产出 `ƒ /login`·`ƒ /register`·`ƒ /account`·`/api/auth/[...nextauth]`、**HTTP 认证冒烟 26/26**（csrf→callback→session→account→signout，错误口令/不存在邮箱无差别失败）。V1-A「购买」下单身份就绪（订单绑定 Phase 12）。**M2 完成（RBAC 鉴权原语 + /admin 后台门禁，双层防御）**：`src/server/authz.ts` `hasRole` 纯函数 + `getCurrentUser`（角色只从 JWT 会话读、绝不信任客户端）+ `requireUser`/`requireRole(allowed)` 判别联合（unauthenticated/forbidden/ok，越权记非敏感审计 warn），`STAFF_ROLES=[REVIEWER,ADMIN]`；`src/server/admin.ts` `getAdminDashboardData` 只读聚合（总数由 groupBy 求和得，天然自洽、少查询）；`/admin` layout 拦未登录跳 /login + 越权渲染「无访问权限」面板不渲染 children，page 取数据前**再自鉴权**（`requireRole` 不过 → `return null`）——实测修正了"仅 layout 拦截时 Next 仍为 leaf page 生成 RSC flight 负载泄露概览"的问题，构成 layout+page 双层防御；`/account` 对员工条件显「进入管理后台」入口；`scripts/promote-user.ts`（npm `user:promote`）受控提权 CLI（只改 role、账号不存在不自动建号，注册仍永远只建 USER）。测试基线 **209 单元（+15 authz）+ 43 集成（+4 admin，真连 Neon）**全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /admin` 无回归、**HTTP 门禁冒烟 15/15**（未登录跳登录 / USER 得无权限且不泄露概览 / ADMIN 得概览+审计表）。版本 **0.14.0**。解锁后续：以 `requireRole` 保护案例 CRUD 写路由（Phase 13）、订单确认（Phase 12）。**M3+ 待做**：更细粒度权限矩阵（按资源/动作）随 Phase 8/12/13 需要再补，V1 保持最小三角色 |
| 7 | 案例系统 | 案例 CRUD、证据、评分、行业关联、案例详情页 | 5,6 | 🟡 **M1+M2 完成（评分内核 + 持久化复算）**：`src/server/scoring.ts` 纯函数把总控 §10「案例评分体系」落成可复算程序（宪法第 7 条：程序计算 > LLM 口算）——`OPPORTUNITY_DIMENSIONS` 10 维度权重**逐字照抄总控 §10**（满分恒 100，`reduce` 求和防失真），`竞争强度`/`实施难度` 标 **inverse** 反向极性（录入按直觉填负面强度、程序负责 `max-raw`，防填反方向）；`computeOpportunityScore` 经 Zod 逐维度校验、非法 → `{ok:false,issues}` **指名维度绝不静默截断**；`computeEvidenceConfidence`（类型权重 FACT1.0/ASSUMPTION0.5/INFERENCE0.4/PREDICTION0.3 + 无来源 0.6 折 + 缺省 confidence 50，`round(100·Σ(w·q·sf)/Σw)`，**总控只给示例值未给公式 → 标注为本项目 v1 假设、参数可调**）；`countKeyUnknowns`=非 FACT 证据数；`computeCaseScores` 一次出三件套 + `CaseScoresSchema` 供 M2 入库校验。`docs/SCORING_V1.md` 版本化公式文档（每个数字标注事实/假设/推断、两个可复算样例 机会→88 与总控示例交叉验证一致·证据→69、诚实记录缺口=总控 §11 证据等级 S/A/B/C/D 轴尚未建模 + 权重未经真实案例校准、变更流程 + 版本历史、重申"综合评分 ≠ 一定成功"规则 9）。`tests/unit/scoring.test.ts` **26 黄金样本**（最优100/全填max90/全填0 10/样例88/反向极性/越界拒绝/确定性/证据可信度精确Σ）。测试基线 **193 单元 + 23 集成**全绿、`tsc`/`eslint` 0 错、`next build` 全路由无回归（本里程碑不动 DB/页面，纯函数验证以单元黄金样本为准）、版本 **0.9.0**。**M2 完成（评分持久化 + 复算）**：Case 加 `scoreInput Json?`（10 维度录入分=输入）+ `scoreBreakdown Json?`（computeCaseScores 完整输出=可审计结果），迁移 `20260905100000_add_case_score_fields` 纯加性已 deploy Neon（表数不变 18）；`src/server/case-scores.ts` `recomputeCaseScores`/`recomputeAllCaseScores` 读输入+证据→复算→`CaseScoresSchema` 入库前复核→写 breakdown 与两标量（判别联合 computed/skipped/invalid/not_found/error，**无 scoreInput 诚实跳过绝不编造、非法不写库**）；`scripts/recompute-scores.ts`（`npm run db:recompute-scores`）批量复算 CLI 如实汇报计数（实跑 6 DEMO 无输入全 skipped）；`tests/integration/case-scores.test.ts` 6 cases（持久化读回/跳过/非法不写库/幂等/全量汇总自洽）。基线 **193 单元 + 29 集成**全绿、版本 **0.10.0**。**M3 完成（评分拆解上详情页）**：`getPublicCaseById` 返回 `scoreBreakdown`（`CaseScoresSchema` 校验库中 JSON，缺失/非法诚实返 null）；`/cases/[id]` 新增 `ScoreBreakdownCard`——机会总分 + 10 维贡献条（inverse 标「越低越好」并显录入值）+ 证据可信度/证据数/关键未知变量数 + 证据构成 + 公式版本 + 重申「综合评分 ≠ 一定成功」，无拆解则显示"暂未录入/复算不展示"；`prisma/seed.ts` 给 6 DEMO 案例补 `scoreInput` 并**内联 `computeCaseScores`** 派生 breakdown/标量（`db:recompute-scores` 现全 6 案例 skipped→computed）；集成测试 +2 例。基线 **193 单元 + 31 集成**全绿、`next build` 无回归、HTTP 冒烟 `?demo=1` 详情页实测渲染 64/39/3、版本 **0.11.0**。**M4 完成（§11 证据来源权威度分级 = 元数据 + 标签，不动分数）**：按创始人裁决「只记录 + 标签，不动分数」——`prisma` 加 `enum EvidenceGrade{S/A/B/C/D}` + `Evidence.grade?`（纯加性迁移 `20260905110000_add_evidence_grade` 已 deploy Neon，表数仍 18、枚举 12→13）；`validation.ts` 加 `EvidenceGradeSchema`；`getPublicCaseById` 随证据透出 grade，`/cases/[id]` 每条证据叠来源等级徽章 + 对「存在 D 级」「FACT 仅 D 级支撑」给 `Alert` 复核告警（呼应 §11「D 级不得表述为已确认事实」）；`prisma/seed.ts` 6 DEMO 案例 16 证据补 grade（保留 ai_vision FACT=D 演示告警）。**可信度公式与 `SCORING_RUBRIC_VERSION` 保持 1.0.0、不并入打分、历史 breakdown 仍有效无需重算**。基线 **194 单元 + 31 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归、grade groupBy 实测 A1/B2/C2/D10/S1、HTTP 冒烟 `?demo=1` 渲染「来源 D/B」+ D 级告警、版本 **0.12.0**。**M5 完成（案例 & 证据「数据层」CRUD，不含 HTTP/UI）**：`src/server/case-admin.ts`（server-only）落地 `createCase`/`updateCase`/`deleteCase`/`addCaseEvidence`/`removeCaseEvidence`，全部走 Zod 校验（录入边界只做结构校验，10 维度/区间合法性仍单一归评分内核，避免两套规则漂移）+ `$transaction` 写库与 `ChangeLog` 审计（entityType=Case，CREATE/UPDATE/DELETE 记 before/after 快照 + actor + reason），`updateCase` 对 `version` 手 +1（宪法第 13 条版本化）、`deleteCase` 对被 PUBLISHED 方案引用的案例返回 `blocked` 不硬删（Solution.caseId Restrict），凡评分输入变动即复用 `recomputeCaseScores` 回算并折叠进 `recompute` 状态；`actor` 仅审计、**非鉴权**（HTTP 写路由 + 角色门禁统一延 Phase 13，避免暴露无鉴权公共写端点）。无 schema 变更。`tests/integration/case-admin.test.ts` 8 例真连 Neon（最小建/带分建 computed 88-69/非法 industry 不写库/越界评分 invalid 不写库/update version 递增+空 patch 拒+不存在 404/增删证据可信度变then还原/被发布方案阻断删除后删方案成功级联/删不存在 404）。基线 **194 单元 + 39 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归、版本 **0.13.0**。**后续待做**：案例 CRUD 的 HTTP 路由 + 后台 UI + 角色门禁（Phase 13）、把 §11 grade 并入可信度公式的后续待决（属公式形态变更须走 SCORING §5 升版）、行业关联收尾 |
| 8 | 解决方案系统 | Solution.body 34 分节规范、方案详情、版本化、财务/未知变量 | 7 | 🟡 **M1 完成（方案数据层 CRUD + 审计 + 发布守卫）**：Phase 5 里程碑 2 刻意**不灌 DEMO 方案**（宪法第 20 条禁伪造可售商品），因此 Solution 表 0 行；M1 把 Phase 7 M5 案例侧的 CRUD 模式横向复制到方案侧，为 Phase 8 M4 AI 流水线与 Phase 13 后台 UI 打底。`src/server/solution-admin.ts`（server-only）：`createSolution`（**强制 status=DRAFT**、不允许直建 PUBLISHED，case FK 预检 + slug 冲突 P2002 归一 invalid）/`updateSolution`（version 自增、状态迁移、`publishedAt` 只在非 PUBLISHED→PUBLISHED 首次打时间戳、二次更新不刷新）/`deleteSolution`（守卫：**任一 Order 关联即 blocked**，V1 无退款流程属 ROADMAP #5）/`addSolutionFinancial`·`removeSolutionFinancial`（Decimal 一律 string 输入避 JS 浮点污染）/`addSolutionUnknown`·`removeSolutionUnknown`（**自动同步 `Solution.unknownVariableCount` = 表内实时条数**，规则 6/9：不确定性显式列出）；每个写操作 `$transaction` [写库 + 写 `ChangeLog`(entityType=Solution, before/after+actor+reason)]；**发布守卫 `publishGuard`（宪法第 21 条）**：升 PUBLISHED 需 price 非 null + 有 riskDomains 时必勾 needsProfessionalReview，否则返 `blocked` 且不改状态不落 publishedAt；**不做鉴权**（延 Phase 13 用 M2 的 `requireRole` 薄包一层）；**不自动评分**（方案侧公式形态未定，M1 留 null/人工填，`unknownVariableCount` 是唯一自动派生标量因为它是可计算事实）；Prisma 错误归一 P2002→invalid(slug)、P2003→invalid(relation)、P2025→not_found。无 schema/DB 变更。`tests/integration/solution-admin.test.ts` 9 例真连 Neon（成功建+CREATE 审计/slug 冲突/case 不存在/version 递增+空 patch 拒+not_found/无价 blocked/风险域未勾 review blocked/全条件通过 publishedAt 落库+不刷新/有 Order blocked→撤单成功+级联 financial+unknown+DELETE 审计/removeSolutionFinancial·Unknown 归属回查+unknownVariableCount 自动回写）。基线 **209 单元 + 52 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归、版本 **0.15.0**。**M2 完成（方案 34 分节正文结构化渲染）**：总控 §3「Solution Package」**已逐字给定 34 分节**（并非待与创始人共同定稿——规范来源就是总控本身），M1 能写 body 但详情页只占位不渲染，M2 补齐展示契约。`src/server/solution-body.ts`（新，纯函数零 DB 零 Next 依赖）：`SOLUTION_SECTIONS` 为 34 分节**单一真源**（稳定 key + 照抄总控的中文 title），`parseSolutionBody` 把任意入库 body 归一为固定 34 节有序视图（按 key 或中文 title 命中即 filled；空串/空数组/空对象/null/undefined 判 pending；数字 0/布尔 false 算有内容；契约外未知键收进 `extras` 不静默丢弃；回 `filledCount/totalCount/empty`）。`getPublishedSolutionById` 透出 `body:ParsedSolutionBody`；`/solutions/[id]` 新增 `SolutionBodySection`——空 body 出**诚实占位**（须走多角色流水线、禁单模型直出，与 Phase 7 M3 `scoreBreakdown=null` 同构），有 body 按 canonical 序渲染 34 张分节卡 + 完成度徽章 n/34 + 未填标「待补充」+ extras 审计提示，`renderSectionContent` 兼容字符串/标量/数组/嵌套对象。刻意保留与结构化卡片（财务/未知变量/证据）的可控冗余，待真实流水线接入再收敛。**不动 schema、不开 HTTP 写端点**。`tests/unit/solution-body.test.ts` 11 例锁死归一契约；基线 **220 单元 + 52 集成**全绿、`tsc`/`eslint` 0 错、`next build` 路由清单不变、**HTTP 渲染冒烟 15/15**（临时发布带 body 方案挂既有 DEMO 案例→验证各类型内容渲染 + 空 body 占位→即时硬删、leftover=0），版本 **0.16.0**。**M3 完成（§33 流水线接真实方案生成——灌 DRAFT 正文、人工门控、绝不自发布，DeepSeek 真跑通）**：Phase 9 M3 解 `#4` 后，把 §33 多角色研究流水线产出**映射进一份可售方案的 34 分节正文**，恪守「禁 AI 自动公开发布」「禁单模型直出」「区分事实/假设/推断/预测」。**加性竖切、零 schema 变更**（复用既有 `Solution.body` JsonB + 已测 `updateSolution`（自动 version++/`ChangeLog`）+ `api-guard`）。`src/server/solution-generation.ts`（server-only 纯函数 + 编排）：`mapPipelineToSolution` 纯映射→只填 `bullCase/bearCase/riskAnalysis`(bear 风险 severity 倒序 + judge)/`unknowns`(非 FACT findings)/`aiAnnotations`(generationVersion/pipelineVersion/四分类计数/judge/qa + **显式 `notGenerated` 标注财务·来源·ROI·回收期刻意不伪造**) 五节、空 points/findings 不写、`filledSectionKeys⊆SOLUTION_SECTION_KEYS`；`generateSolutionContent`——不存在 `not_found`、**已 PUBLISHED 直接 `blocked`（不自动改写线上正文）**、`createChatProvider()` 跑 `runResearchPipeline`、有内容才合并（人工键原样保留）经 `updateSolution`（`actor` 缺省 `"ai:pipeline"`）落库、无内容 `wroteSections:0` 不落库、**返回体绝不含 `status:"PUBLISHED"`**。新薄路由 `POST /api/admin/solutions/[id]/generate`（`requireStaffWrite`→可选 `{question}`→`mutationResponse`）。顺带修两处真实供应商才暴露的缺陷：① DeepSeek `json_object` 要 prompt 含字面 "json"→适配器补尾串（业务/流水线仍 provider-agnostic）；② 模型回 `finding`/中文枚举/小数置信度致 `schema_invalid`→`research-pipeline.ts` 各 `roleInstruction` 内嵌精确 JSON 契约并按第 13 条升 `PIPELINE_VERSION 1.0.0→1.1.0`。新增单测 15 + deepseek +1（23→24）+ 集成 5（注入确定性 `StubProvider`+fixtures，无论本地是否配真 key 皆可复算）；基线 **461（367 单元 + 94 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` 新增 **`ƒ /api/admin/solutions/[id]/generate`** 无回归；**真实 DeepSeek 端到端冒烟成功**（五角色跑通、成本程序算约 $0.0042、5 节真实中文正文落库、QA 门把低质草稿判 `needs_human_review`→status 仍 DRAFT 绝未自发布），版本 **0.32.0**。**M4+ 待做**：方案评分内核建模（引入复用度/供应链等新维度须走 SCORING §5 升版）、~~把生成结果接后台「一键生成」按钮 UI~~（✅ 已于 **Phase 13 M7 / v0.33.0** 落地——方案编辑台正文上方挂 `GenerateBodyButton`，一键触发 M3 生成端点、回显「写了 N 节 / 终态 / 成本」并 `router.refresh()`，仍人决定发布）、从审核队列直接触发生成（现仅编辑台按钮 + 端点 RSC/脚本可调）、更细的按分节增量重生成（现为整批五节合并）、购买闭环增量（阻塞 ROADMAP #5 支付）|
| 9 | AI Agent 系统 | Model Router、Agent 框架、Schema 校验、成本记录、评分算法程序 | 4,8 | 🟡 **M1 完成（Model Router 骨架——provider 解耦 + 任务路由 + 结构化输出校验 + 成本记录契约，纯离线可测，零 DB/零 HTTP/零 UI）**：立本系统的立身之本「**AI 驱动**」此前悬空——所有案例/方案纯手填、无任何模型接入层，Phase 8 M3 流水线 / Phase 10 Scout / Phase 11 诊断全部卡在缺这层。`src/server/model-router.ts`（与 `scoring.ts` 同构的纯函数 + 判别联合）把总控 §16「业务代码只能调用 Model Router、不得直接依赖具体模型」+ §17「生成内容用结构化 JSON 中间格式」+ §31「每 AI 任务记录 model/tokens/estimated_cost/latency/status」+ §32「按复杂度分档路由、不要都用最贵模型」落成可复算契约：`MODEL_TASK_KINDS`（6 入口）+ `runTask` 唯一原语 + `generateText/structuredOutput/research/embed/vision/code` 六个薄封装（全委托 runTask，「只调 Router」可被 grep 审计）；`ROUTING_POLICY`（**v1 设计假设**：research→high、generate_text/embedding→low、vision/code/structured_output→medium，可 `tier` 显式覆盖为「核心财务=模型+程序+QA」预留升档）；`MODEL_CATALOG`（每层级占位 `ModelSpec`，**单价是 v1 占位假设、真实值到位替换并升 `MODEL_ROUTER_VERSION`**）+ `estimateCost` 纯函数**程序计算 > 口算**（round 6 位、**负/NaN token 按 0** 防脏输入）；`ChatProvider` 接口 + **确定性 `StubProvider`**（零网络/key/时钟/随机，同入恒同出：`respondWith` 原样回放、embedding 由 FNV-1a 派生固定向量、其余 `[stub:kind] prompt`），使**契约可在无 API Key 下离线测死**，真实供应商只需另实现 `ChatProvider` 注入、`runTask` 一行不改；`CallRecord`（含成本归因 `agent/caseId/solutionId` 为看板预埋）+ `CallRecorder` 接口（默认 `loggingRecorder` 结构化日志**绝不落密钥**、`createMemoryRecorder` 供测试/聚合）；编排契约：provider 抛错→**归一 `provider_error` 绝不裸抛**、带 `schema` 走 `z.safeParse`→不过即 `schema_invalid`+**指名 issues 不静默放过**、**无论何种结局都产出一条 CallRecord**（成本可追踪）。`tests/unit/model-router.test.ts` **18 例**（常量守护/成本黄金值含负NaN归零/stub 确定性/runTask 三态/taskId 唯一/成本聚合/归因透传）。**顺带纠一处既往误解**：Phase 9 交付项「评分算法程序」实已于 Phase 7 完成（`scoring.ts` 26 黄金样本），本里程碑不重复。基线 **369（284 单元 + 85 集成）全绿**、`tsc`/`eslint` 0 错、`next build` **无新增路由**（纯 server 逻辑）；版本 **0.27.0**。**M2 完成（§33 多角色研究流水线骨架——Research→Bull→Bear→Judge→QA 编排契约，纯离线可测，零 schema/DB/HTTP/UI）**：宪法/总控 §33「高价值方案须走多角色对抗式研究、**禁止单模型直出最终方案**、结论须区分事实/假设/推断/预测、AI 做劳动人做决策」此前无任何编排承接——M1 只立单调用解耦缝，若不先立多角色契约，Phase 8 M3 灌真实方案极可能退化成 `generateText` 一把梭。`src/server/research-pipeline.ts`（与 `scoring.ts`/`model-router.ts` 同构的纯函数 + 判别联合，全委托 `runTask`，**不依赖 API Key #4 即可离线测死**）：`PIPELINE_ROLES` 固定序 `research→bull→bear→judge→qa`（`PIPELINE_VERSION 1.0.0`，顺序不可打乱不可跳步）；`DEFAULT_ROLE_TASKKIND`/`DEFAULT_ROLE_TIERS`（research→`research`/high、judge→high、bull/bear/qa→`structured_output`/medium，**v1 设计假设**、可 `tierOverrides` 逐角色覆写）；各角色 Zod schema——research（`summary` 必填 + `findings[]` 带 `evidenceKind∈FACT/ASSUMPTION/INFERENCE/PREDICTION`+`confidence`）、bull 正方（claim+evidence+strength）、bear 反方（claim+evidence+severity）、judge（`verdict∈supported|mixed|weakened`+rationale+confidence）、qa（approved+qualityScore+needsHumanReview+issues[]），**数组 `.default([])` 允许空 = 诚实原则**（没查到不逼模型编造，宪法第 20 条）；`buildPrompt` **确定性**把已完成角色结构化输出并入下一角色 prompt（无时钟无随机可复算：bull/bear/judge/qa 带 research 摘要、judge/qa 带正反方、qa 带裁决）；`runResearchPipeline(input,deps)` 编排——逐角色 `runTask`（打 `agent=pipeline:<role>`、透传 `caseId/solutionId` 归因、离线 `fixtures[role]→respondWith`），**任一角色 `provider_error`/`schema_invalid` → 立即诚实早退 `status:"failed"`（带 stage+kind），已产生 CallRecord 全保留、绝不臆造后续输入**，五段皆 ok 后过 **QA 门禁**：`approved=false` 或 `qualityScore<threshold`（默认 70）或 `needsHumanReview=true` → `status:"needs_human_review"`（暂缓、非终态、**绝不静默放行成 complete**，落地「人做关键决策」），否则 `complete`；返回三态判别联合各携 `outputs/callRecords/cost{calls,totalCostUsd}/roleTiers`，末尾聚合**程序计算成本**（§31）供按方案/案例归因；`runTask` 永不裸抛故本函数亦不抛。真实 provider 到位只需注入实现 `ChatProvider` 的类、本模块一行不改。`tests/unit/research-pipeline.test.ts` **15 例**（无 DB/key：常量守护 + schema 黄金/缺字段拒/空数组允 + complete 全路径含 5 记录按 agent 归因·cost 自洽·归因透传·roleTiers 覆写 + QA 门禁三态 + `qaThreshold` 覆写翻转 + 诚实早退 schema_invalid 保留前段不臆造/provider 抛错 stage 定位不裸抛/首角色即抛 + 确定性复算 + `scriptedProvider` 断言 taskKind 序与上下文链）。基线 **393（304 单元 + 89 集成）全绿**、`tsc`/`eslint` 0 错、`next build` **无新增路由**（纯 server 逻辑、未接任何页面）；版本 **0.29.0**。**M3 完成（真实供应商接入 Model Router——DeepSeek·OpenAI 兼容，依赖注入离线测死，`runTask` 一行不改）**：用户裁决「给 #4 模型 Key 解核心锁」并提供 DeepSeek 密钥，把 M1 预留的 `ChatProvider` 注入接缝真正填上，「AI 驱动」的立身之本从桩升级为可真跑。新增 `src/server/deepseek-provider.ts`（第一个真接网络的 `ChatProvider`，`id="deepseek"`，走 `POST <base>/chat/completions`）：**依赖注入 `fetchImpl`**（单测注假 fetch、无网络无 key 也把请求构造/响应解析/错误归一整套离线测死）、**模型名不硬编码进业务**（用 `req.model.id`，真实模型来自可 env 覆写的 `MODEL_CATALOG`）、**成本仍由 Model Router 程序计算**（只回传真实 `usage`）、**绝不在日志/错误落密钥**（`Bearer` 头只在发出请求、非 2xx 抛「`HTTP <status>`」不回显响应体）、**诚实边界**（DeepSeek 无 embedding→直抛不支持交 `runTask` 归一 `provider_error` 不假装返回向量；结构化任务带 `response_format:json_object` 解析 content→`json` 交 Zod，解析不出即 `json` undefined→诚实 `schema_invalid`）；附 `deepSeekConfigFromEnv`（无/空白 key→null）+ 工厂 `createChatProvider`（**有 key 用真实 DeepSeek、无 key 回落确定性 `StubProvider`**，开发/CI 自动走桩、生产配 key 即真调、业务零分支）。改 `src/server/model-router.ts`（最小侵入）：`MODEL_ROUTER_VERSION` `1.0.0→1.1.0`，`MODEL_CATALOG` 改为**经 env 可覆写、缺省回落与 1.0.0 完全一致的占位默认**（`MODEL_ID_<TIER>`·`MODEL_PRICE_IN/OUT_<TIER>`），故无 env 时离线单测成本黄金值不变、业务代码与 `runTask` 一行不改；`.env.example` Model Router 段扩为带文档的 DeepSeek 块（单价全标「示例占位、以官网为准」，宪法「关键数字来源可追溯、假设可标注可调」），真实密钥只进本地 gitignored `.env`、经 allowlist 提交机制双层保护永不进远端。`tests/unit/deepseek-provider.test.ts` **23 例**（配置解析三态/工厂有无 key 回落/构造期空 key 早失败不回显/请求构造含 `Bearer`·真实 model id·结构化才带 `response_format`·`signal` 透传·自定义 base 拼接/响应解析合法·非法 JSON·usage 缺失或负数归零·model 回落/embedding 直抛不 fetch·非 2xx 不含 key/`runTask`×真 provider 集成 ok+程序算成本 0.008·抛错归一 provider_error 不外溢·非法 JSON→schema_invalid）；`model-router.test.ts` 版本黄金同步 `1.1.0`。基线 **351 单元（328→+23）+ 89 集成** 全绿、`tsc`/`eslint` 0 错、`next build` **无新增路由（仍 45 条）**；**并做一次真实端到端冒烟**（脚本放 scratch 不进仓库）：`createChatProvider()` 经 env 真 key 打真 DeepSeek——`generate_text`→「你好！」`modelId=deepseek-chat`、`structured_output`→经 Zod 校验的 `{"language":"中文","greeting":"你好，世界！"}`，成本均程序计算，端到端证「桩→真实供应商」接缝可用；版本 **0.31.0**。**M4 完成（§33 流水线接真实方案生成——已于 Phase 8 M3（v0.32.0）落地）**：`createChatProvider()` 注入真实 provider 后，`src/server/solution-generation.ts` 把五角色流水线输出**纯映射**进可售方案 34 分节正文（只填 bullCase/bearCase/riskAnalysis/unknowns/aiAnnotations 五节、财务/来源显式 `notGenerated` 不伪造），经已测 `updateSolution` 合并落库、**已 PUBLISHED 直接 blocked、返回体绝不含 `status:"PUBLISHED"`**（禁 AI 自动公开发布），新受门禁路由 `POST /api/admin/solutions/[id]/generate`；真实 DeepSeek 端到端冒烟跑通（约 $0.0042、5 节中文正文、QA 门判 `needs_human_review` → status 仍 DRAFT）。详见 Phase 8 行。**M5 完成（`ModelCall` AI 成本落库 + 后台成本看板——§31 从「随调用打日志」升级为「跨进程可查询、可累计、可视」）**：此前 M1–M3 已把成本随调用返回并打结构化日志（`loggingRecorder`），但日志易逝、重启即散、聚不了看不了，更谈不上 §32「据成本自动选模型」。本里程碑**加性竖切、零改动既有 `runTask`/流水线/生成端点业务逻辑、零新 HTTP 端点、零新路由**：`prisma` 新增 append-only 观测表 `ModelCall`（镜像 `ChangeLog` 约定——维度列全用 `String` 防枚举漂移、`estimatedCostUsd Decimal(12,6)` 防次分成本浮点漂移、归因列 `agent/caseId/solutionId` **不建外键**令成本史比业务实体活得更久、只 `createdAt`；迁移 `20260905120000_add_model_call` 经 `migrate diff --from-url` 生成**纯 additive** 已 deploy Neon，表数 18→19）；`src/server/model-calls.ts`（server 域逻辑）`toModelCallCreateData`（**纯映射**、复用 M1 `CallRecord` 作成本单一真源绝不重算、非法时间戳省略 `createdAt` 交 DB 默认、负/NaN 归零）+ `createDbCallRecorder`（旁路落库记录器，先走 `loggingRecorder` 保可观测性再从 `Promise.resolve()` 起链 `prisma.modelCall.create`、**整链 `.catch` 吞任何失败只 warn 绝不拖垮业务调用**、`flush()` 等在途写完）+ `summarizeModelCalls`（纯聚合：总额/次数/失败数/均值·byTier·byStatus·byAgent，空集 `avgLatencyMs=null` 不除零）+ `getCostSummary`（唯一触库读层，行数窗口默认近 2000）；`POST /api/admin/solutions/[id]/generate` 以**请求作用域**注入 recorder（`PipelineDeps.recorder`/`GenerateDeps.recorder` 早已存在并透传进 `runTask`，只是终于有人注入真记录器）+ 响应前 `flush()`；`/admin` 概览加「AI 成本观测（ModelCall）」只读卡（四指标 + 诚实空态 + byTier/byStatus/byAgent 徽章 + tokens 页脚）。新增单测 13（纯映射/聚合/recorder 成功路径；吞异常路径**刻意不 mock**——vitest 单反检测器误报，改由链静态 `.catch` + 生产 warn + Neon 集成成功往返覆盖）+ 集成 3（真连 Neon recorder→建库→读回逐字段 + `getCostSummary` 聚合自洽 + `summarizeModelCalls` 黄金；`db-smoke` 表存在性 18→19）。基线 **485（388 单元 + 97 集成）**全绿、`tsc`/`eslint` 0 错、`next build` **无新增路由**（复用既有生成端点与 `/admin` 页）；版本 **0.34.0**。**M6+ 待做**：各角色 prompt 继续调优（现 v1 假设须随真实模型表现升 `PIPELINE_VERSION`，现 `1.1.0`）、真实 `embedding`（DeepSeek 无此端点、须配独立向量供应商用于 Scout/搜索）、成本看板时间序列（按日/按方案，现为近窗口行数聚合）、按成本/成败自动选模型（§32，数据源已就绪）|
| 10 | GitHub Scout | 开源发现/去重/评分/许可证检查/技术匹配 | 9 | 🟡 **M1 完成（Scout 治理逻辑骨架——许可证风险分档 + 法务/工程准入门 + 复用就绪度聚合 + 确定性去重键，纯离线可测，零 schema/新表/HTTP/UI）**：商业闭环「GitHub/开源匹配→本土化重构」风险最高的一环是把带传染性许可证/有漏洞/未实测的开源项目直接包装成售卖方案，宪法第 11 条明列「GitHub=能力供应链非成品」「禁止 发现→复制→包装→出售」「须 需求定义→能力匹配→筛选→许可证检查→依赖检查→安全检查→实测→二次封装」「许可证未知/GPL/AGPL/含商业限制→人工复核」。`src/server/scout.ts`（与 `scoring.ts`/`model-router.ts`/`research-pipeline.ts` 同构纯函数 + 判别联合）把这条法务+工程准入门落成可复算契约，**直接复用 `OpenSourceProject` 已有字段、零新表**：`LICENSE_CATEGORIES`（permissive/weak_copyleft/strong_copyleft/proprietary/unknown，**v1 假设非法律意见**）+ `LICENSE_CATEGORY_BY_TYPE` 逐字覆盖 `LicenseTypeSchema` 全 11 成员、`licenseCategory` 未知值保守回落 unknown；`decideLicense`→`{verdict:approved\|needs_human_review\|rejected,category,reasons[]}`，**自动层只分类不越权批准**（NOT_REVIEWED 一律 needs_human_review 按分档补强理由，仅人显式 APPROVED/REJECTED 才尊重，落地「法律=高风险须人工确认」）；`projectReadiness`→`{blockers,readyForReuse,license}`——**强传染/专有/未知即便三检查位全 true 也不自动就绪**，从代码焊死捷径；`normalizeRepoUrl` 确定性去重键（小写·剥协议/www/查询/`.git`/尾斜杠·保留 host·幂等）；`ScoutCandidateSchema` 复用 `@/lib/validation` 枚举防漂移、缺省即最保守；`evaluateCandidate`→判别联合 `invalid(指名 issues)\|evaluated(candidate+dedupKey+readiness)` 永不裸抛。`tests/unit/scout.test.ts` **24 例**（常量守护/分类黄金值/判定矩阵含 APPROVED·REJECTED·NEEDS·五种 NOT_REVIEWED 分档/就绪聚合含「未复核+全检查仍不就绪只剩法律门 1 条」/去重五写法同键·host 区分·幂等/schema 最保守默认/evaluateCandidate 脏输入不裸抛·dedupKey 相等）。基线 **352 单元（+24）+ 89 集成** 全绿、`tsc`/`eslint` 0 错、`next build` **无新增路由**（纯 server 逻辑）；版本 **0.30.0**。**M2 完成（GitHub 输入管道 + 抓取→判定→落库编排——把「开源匹配」这一环从离线纯函数缝上真实输入与可查询持久层，零 schema/零 HTTP/零 UI）**：M1 的治理判定悬在无数据来源上——`OpenSourceProject` 表有字段却无任何写入路径。M2 补两端可离线测死的一段。`src/server/scout-github.ts`（新，纯逻辑 + 依赖注入 fetch、无 DB）：`SPDX_TO_LICENSE_TYPE` + `parseSpdxLicense` 把 GitHub `license.spdx_id` 大小写无关归一成 `LicenseType`，**关键诚实边界**——无 LICENSE→`UNKNOWN`、`NOASSERTION`（GitHub 判不出）或表外 SPDX 一律保守落 `OTHER`（→ 归 unknown 风险档 → 强制人工复核，绝不猜成宽松档），原始 SPDX 留 `licenseNote` 供人对照；`mapGitHubRepoToCandidate` **刻意省略 `dependencyChecked/securityChecked/tested`**（元数据不能证明扫描完成，交 schema 落最保守 false，第 20 条）；`parseRepoRef`（裸 `owner/repo` 或 URL，仅首段含点才剥 host 不误剥 owner、拒 `.`/`..` 穿越）；`fetchGitHubRepo`（注入 `fetchImpl` 无网络可测、非 2xx/网络错归一 `ScoutFetchError` **只带状态绝不回显响应体或令牌**、非法 ref 不发请求）；`SCOUT_GITHUB_VERSION 1.0.0`。`src/server/scout-ingest.ts`（新，server-only 编排、判别联合永不裸抛）：`ingestGitHubRepo`/`ingestCandidate`——拉元数据→走**既有 `evaluateCandidate`（单一真源第16条）→按 `dedupKey`（归一 repoUrl）upsert `OpenSourceProject`** + 落 `ChangeLog`(CREATE/UPDATE, before/after 快照)；**最重要治理不变式写进代码——更新（重抓）分支的 data 只带客观元数据 name/owner/stars/licenseType/licenseNote/lastCheckedAt/version++，绝不带 `licenseReviewStatus` 与三检查位**（否则冲掉人工 APPROVED/已实测态=自动层越权改法律门，第11条）；脏候选/归一空→invalid 绝不写库、P2002 竞态如实回报；**不做鉴权**（信任已过 `requireRole` 调用方，沿用 case-admin 口径），HTTP/UI 延 M3；`SCOUT_INGEST_VERSION 1.0.0`。新单测 `scout-github.test.ts` **16 例**（SPDX 归一黄金/NOASSERTION·未识别→OTHER·保留原始/大小写无关 + 候选映射省略三检查位守护/owner 推导/脏 stars + parseRepoRef 多写法·穿越拒绝 + fetchGitHubRepo 注假 fetch URL 构造·无令牌不带 Authorization·Bearer 注入·非2xx 不含令牌·非法 ref 不发请求·网络错归一）+ `scout-ingest.test.ts` **7 例**（`vi.hoisted`+`vi.mock prisma`：新建落 NOT_REVIEWED+三检查位 false+CREATE 审计/更新 data 绝不含 review 态与检查位·version 自增/脏候选 invalid 不写库/不同 URL 去重键一致 + ingestGitHubRepo 成功·fetch_error 不写库·通用异常不外溢）；新集成 `scout-ingest.test.ts` **4 例**真连 Neon（runId 前缀隔离：①新建字段逐验+审计 ②同仓库换写法命中同一条不重复插 version→2 ③**人工 APPROVED+已实测重抓后 review 态与三检查位原样保留、仅 stars 刷新**（核心不变式）④缺 name invalid 且库 count 不变）。基线 **512（411 单元 + 101 集成）** 全绿、`tsc`/`eslint` 0 错、`next build` **无新增路由**（纯 server 逻辑）；版本 **0.35.0**。**M3+ 待做**：把 `needs_human_review` 的开源候选接入 Phase 13 M6 审核队列（现落库项目已带 NOT_REVIEWED+三检查位 false 成为待办数据）、后台开源项目列表页 + 受 `requireStaffWrite` 门禁的 ingest/复核 HTTP 写端点（现仅 server 编排可被脚本/RSC 调）、依赖/安全扫描的**执行**接线（M2 只诚实置 false、须外部工具链 npm audit/CVE 扫描器）、能力匹配打分 `fitScore`（须实测后填、勿投机）、真实 GitHub 抓取端到端冒烟（`fetchGitHubRepo` 已就绪、可注 `GITHUB_TOKEN` 提配额）|
| 11 | 企业诊断 | 企业画像、诊断会话、方案匹配、Top5、定制方案（V1-B） | 8,9 | ⬜ |
| 12 | 订单 | 下单、最简支付确认、后台确认、解锁；预留多渠道 | 6,8 | ✅ **M1 完成（订单「数据层」——购买闭环的可信底座，不含 HTTP/UI）**：`Order` 表早在 Phase 4/5 随 schema 建好（PENDING/PAID/REFUNDED/CANCELED 状态机 + `amount Decimal` + `paymentProvider` 预留），V1 购买流按总控/PRODUCT_SPEC 明确为「下单 → 支付说明（站外、无网关）→ 后台人工确认 PAID → 解锁」——**刻意不接支付网关**（ROADMAP #5 非阻塞），故本里程碑**零 schema 迁移**。`src/server/orders.ts`（server-only，判别联合返回，不抛裸异常，不做鉴权·信任上层门禁）：`createOrder` **金额只从服务端读取的 `Solution.price` 快照进 `Order.amount`、绝不采信客户端传入金额**（否者可把 1999 改成 0.01，"钱"的路径来源唯一、程序可复算，宪法第 7/20 条）；只能对 `status=PUBLISHED` 且有价方案下单（DRAFT/审核中→blocked，与公开详情门控一致）；**同一买家（userId 或归一 buyerEmail）对同一方案重复下单幂等去重**——已有 PENDING 直接复用（`deduped:true` 不新建）、已 PAID 返回 blocked 不重复收钱；身份至少 userId/email 其一否则 `invalid(identity)`。`confirmOrderPaid`（仅 PENDING→PAID、`paidAt` 只首次落库、再次确认幂等不刷新时间戳、version 自增）、`cancelOrder`（仅 PENDING→CANCELED，V1 无退款流程 PAID 不可撤，属 #5）、`getOrderById`、`listOrdersForBuyer`（OR userId/buyerEmail 找回名下）、`listOrdersForAdmin`（状态过滤 + 分页自洽）、`hasPaidEntitlement`（解锁判定：无身份/DB 异常一律保守 false，绝不错误放行付费内容）。每个写操作 `$transaction` 写库 + `ChangeLog`(entityType=Order, before/after+actor+reason)；Prisma 错误归一 P2003→invalid(relation)、P2025→not_found。**不动 schema、不开 HTTP 端点**（延 M2 复用 `api-guard` 统一门禁，届时 `MutationLike` 加 `orderId/deduped` 字段）。`tests/integration/orders.test.ts` **10 例**真连 Neon（金额服务端快照+status 不采信/缺身份/仅 PUBLISHED/不存在方案/重复下单幂等复用/confirmPaid 首落+幂等不刷新/已购 blocked/PAID 不可撤/hasPaidEntitlement 前后翻转·非属主 false·无身份 false/买后台列表分页）+ `tests/unit/orders-schema.test.ts` **7 例**无 DB 锁 schema 契约（白名单剥离 amount/status/currency/price、身份二选一、邮箱归一、buyerType 默认与枚举）。基线 **245 单元 + 66 集成**全绿、`tsc`/`eslint` 0 错、`next build` 无回归（数据层不新增路由）、版本 **0.19.0**。**M2 完成（订单「HTTP 端点」——购买闭环经 HTTP 打通，复用 api-guard，不含 UI）**：把 M1 数据层用已就绪门禁薄包一层，**零业务、零 schema**。`api-guard.ts` 抽出共享纯函数 `sameOriginBlock`、**新增 `requireSameOriginActor`**（只做 CSRF 同源 + 会话注入 `userId/actor`、**不设角色门禁**——下单是任何登录用户/游客都可做的公开动作，与后台 staff 写明确分层）、`MutationLike` 显式补 `orderId/deduped/order`。4 路由全 `force-dynamic` + Next16 `params:Promise` await + 只「门禁→调数据层→`mutationResponse`」：`POST /api/orders`（公开下单，先 `delete raw.userId/actor` 再从会话注入，客户端伪造身份被丢弃；金额防篡改由 M1 白名单守）、`GET /api/admin/orders`（staff 只读，`requireRole`+`PaginationSchema`+状态过滤）、`POST /api/admin/orders/[id]/confirm`·`/cancel`（staff 写，`requireStaffWrite`）。**`hasPaidEntitlement` 刻意不暴露为公开端点**（无额外鉴权会引 IDOR），留 M3 在 RSC 服务端调用。`tests/unit/api-guard.test.ts` 扩到 **22 例**（+`requireSameOriginActor` 3 例、`mutationResponse` 透传 1 例）。基线 **249 单元（245+4）+ 66 集成**全绿、`tsc`/`eslint` 0 错、`next build` 新增 **4 条 `ƒ` 动态路由**无回归、**HTTP 端到端冒烟 29/29**（`smoke-orders.mjs`：金额服务端快照防篡改/游客+登录幂等去重/跨站 CSRF/缺身份 400/伪造 userId 被会话覆盖/后台 401·403·200 门禁矩阵/确认 PAID+paidAt+version=2+审计 `human:<adminId>`/重复确认幂等/已购再下单 409/取消状态机/跨站确认 403/状态过滤，末尾 leftover=0）、版本 **0.20.0**。**M3 完成（购买闭环「用户界面」——详情页解锁门控 + 支付说明 + 我的订单 + 后台确认台）**：给 M2 端点接上"人面"，**零 schema、零新数据层逻辑**，全复用已测 `orders.ts`+`api-guard`。`solutions.ts` 的 `SolutionDetail` 加 `isFree`（price 空/0 即免费）；`components/solutions/BuyButton.tsx`（client）`fetch` `POST /api/orders` **只提交 `solutionId`**（身份服务端注入、金额服务端快照，杜绝改价），成功跳支付说明页、409/401/错误如实提示（走 HTTP 而非另起 Server Action，第 16 条防漂移）；`/solutions/[id]` 服务端算 `entitled=isFree||isDemo?true:hasPaidEntitlement(...)`、购买卡四态渲染（免费·DEMO→开放徽章 / 已购→已解锁 + `#solution-body` 锚 / 待购登录→BuyButton / 游客→登录引导），**`SolutionBodySection` 加 `locked` 入参——未解锁时服务端根本不渲染 34 分节、只出「正文未解锁」锁面板**（正文是付费交付物，前端无从绕过）；`/orders/[id]` RSC 支付说明页（属主校验 userId 命中或归一 email 命中、不匹配统一 `notFound()` 防 IDOR、金额快照 + V1 站外付款三步指引·未接网关、PAID 引导回正文）；`/account/orders` 我的订单（登录门禁 + `listOrdersForBuyer` 属主过滤）+ `/account` 入口；`/admin/orders` 后台确认台（layout+page 双层防御 + 状态过滤/分页）+ `components/admin/AdminOrderActions.tsx`（确认收款/取消消费 M2 端点 + `router.refresh()`）+ `/admin` 订单卡入口。V1 有意识假设：只有 price>0 且非 DEMO 方案正文才锁（免费/DEMO 恒开放）、购买需登录（游客下单数据层支持但 UI 不暴露）、人工确认到账即解锁不接网关。`cases-solutions.test.ts` 扩 `isFree` 断言（¥1999=false、新种 price=0 免费=true）；基线 **315（249 单元 + 66 集成）**全绿、`tsc`/`eslint` 0 错、`next build` 新增 **3 条 `ƒ` 路由**无回归、**购买闭环 UI 端到端冒烟 29/29**（`smoke-orders-ui.mjs`：钱-shot——付款前游客/未购/他人详情页 HTML 全不含正文标记显锁面板、后台确认 PAID 后仅属主标记出现且显已解锁、他人访问该单支付页 404 防 IDOR、我的订单按属主隔离、后台 USER 无权限/ADMIN 见单、末尾 leftover=0），版本 **0.21.0**。**✅ Phase 12 全部完成（M1 数据层 + M2 端点 + M3 UI）**——「下单 → 支付说明（站外）→ 后台确认 PAID → 解锁读正文」购买闭环从用户侧端到端可用且经真实 HTTP + 服务端门禁验证成立；**不依赖 ROADMAP #5 支付网关**（人工确认即解锁），退款/企业批量授权/在线支付自动化属后续增量 |
| 13 | 后台 | 每日任务中心、各领域管理、审核发布、日志、成本看板 | 7–12 | 🟡 **M1+M2 完成（后台「写」HTTP 端点统一门禁 + 最小管理 UI）**：**M1** 兑现 Phase 7 M5 / Phase 8 M1 数据层「不做鉴权、HTTP 写路由延 Phase 13」的安全承诺，`src/server/api-guard.ts` 作为**唯一门禁 + 结果翻译点**（宪法第 16 条防逐端点漂移）——`actorOf` 只从服务端会话派生 `human:<id>`（绝不信客户端 actor/role）、`isSameOrigin` 纯函数做 CSRF 同源判定（无头放行 / 有头 host 精确相等 / 解析失败拒）、`requireStaffWrite` 先挡跨站 403（且不查角色）再 `requireRole(STAFF_ROLES)`（未登录 401 / USER 越权 403 带 `required`）、`mutationResponse` 把数据层判别联合统一翻译（ok→200、invalid→400、not_found→404、blocked→409、error→500 生产屏蔽）；10 个薄封装写路由，**零业务逻辑、零 schema 变更**，校验/审计/守卫/复算全在已测数据层；**仍不含限流/幂等**（V1 延后）。`tests/unit/api-guard.test.ts` 18 例锁门禁与状态映射、**HTTP 端到端冒烟 24/24**，版本 **0.17.0**。**M2** 补上「方向一」另一半——`src/server/admin-cases.ts` `listAdminCases()` 后台**全量**读层（含内部阶段 + DEMO、倒序、`_count`、硬上限 500 超出标 `truncated`），`NewCaseForm`（client）`fetch` 到 `POST /api/admin/cases`（刻意走端点而非 Server Action 防逻辑抄两遍漂移），`/admin/cases` layout+page 双层防御、`/admin` 加「案例管理」入口，有意最小竖切（仅案例「列表+新建」）。新增集成 4 例、基线 **238 单元 + 56 集成**全绿、`next build` 新增 `ƒ /admin/cases`、**HTTP 渲染冒烟 18/18**，版本 **0.18.0**。**M3 完成（后台「方案管理 UI」——复用 M1 写端点 + 补后台读层，打通「建方案 → 定价 → 发布可售」）**：兑现「方向二」对方案侧的管理，**零 schema、零新数据层写逻辑**，写操作全复用 M1 已测 `POST/PATCH /api/admin/solutions`。新增 `src/server/admin-solutions.ts` `listAdminSolutions()` 后台**全量**读层（含 DRAFT/审核中/已发布、倒序、`_count` 关联 `financials/unknowns/orders`、硬上限 500 超出标 `truncated`；`price` 经 `Decimal.toFixed(2)` 归一 + `¥/$` `priceDisplay`，`unknownVariableCount` 取实时 `_count.unknowns` 而非缓存字段，`isDemo` 由**关联案例**判定，行业 enum→中文名映射），`NewSolutionForm`（client）`fetch` `POST /api/admin/solutions` **只提交 title/slug/caseId + 可选定价/风险域**（状态服务端强制 DRAFT，金额防篡改由数据层白名单守）、`PublishSolutionButton`（client）`PATCH {status:'PUBLISHED'}` 触发 M1 发布守卫（未定价/风险域缺确认→回 409 如实透出「发布被拦：<原因>」），`/admin/solutions` layout+page 双层防御、`/admin` 加「方案管理」入口。`tests/integration/admin-solutions.test.ts` **4 例**真连 Neon（DRAFT+DEMO关联可见·isDemo 随案例/价格 priceDisplay+null+关联计数+风险域+需专业确认透传/案例标题+行业中文化映射/updatedAt 倒序·total·truncated 自洽）、基线 **249 单元 + 70 集成**全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /admin/solutions`、**方案管理 UI 端到端冒烟 33/33**（`smoke-admin-solutions.mjs`：钱-shot——发布未定价→409 CONFLICT 带 `price` 字段拦截、发布带价→200 且 DB `status=PUBLISHED`+`publishedAt` 落库、已发布标题透传进公开 `/solutions`、未定价草稿**不出现**在公开列表、USER 页不泄露「新建方案/方案列表」、CSRF evil.com Origin→403、非法 slug→400、未登录建→401，末尾 leftover=0），版本 **0.22.0**。**M4 完成（后台「方案内容编辑台」——把可售方案的付费交付主体变得可录入）**：M3 能建/发布方案，但正文 34 分节 / 财务测算 / 关键未知变量 / 删除此前只能裸 API 写，卖的是空壳、拖累「方案质量」；M4 把一份可售方案的全部可编辑面收敛到一处，**零 schema、零新数据层写逻辑**，写操作全复用 M1 已测端点。`src/server/admin-solutions.ts` 新增 `getAdminSolutionDetail(id)`（早退挡无形 id 探测、带 case/financials/unknowns 倒序 + `_count:{orders}`、所有 Decimal `toFixed(2)`、`unknownVariableCount`/`financialCount` 取实时数、`isDemo` 由关联案例判定、body/unknowns 原样带回供编辑初值），`src/components/admin/mutate.ts` 共享 `mutateJson`/`fieldHints` 收敛「fetch + 解析 api-guard 判别联合」供五组件复用（第 16 条防漂移）；`EditSolutionMetaForm`（PATCH 基本信息、刻意不提交 status）、`SolutionBodyEditor`（按 `SOLUTION_SECTIONS` canonical key 渲染 34 受控 Textarea、非空节写入空节省略、可粘贴 JSON、34 外历史键原样透传不丢弃 + 已填 n/34 徽章）、`AdminSolutionFinancials`/`AdminSolutionUnknowns`（行内 POST/DELETE 增删 + 严重度客户端先校验 + `router.refresh()`）、`DeleteSolutionButton`（`orderCount>0` 提前拦、被守卫拒时回显「仍有 N 条订单」）；`/admin/solutions/[id]` RSC 双层防御 + noindex、列表行加「编辑内容 →」链接。**顺带修一处真实竞态**：`listAdminSolutions`/`listAdminCases` 的 `$transaction([findMany, count])` 各有 READ COMMITTED 快照，并发插删可致 `total<items` 矛盾 → `total=Math.max(rawCount,items.length)` 兜底。`tests/integration/admin-solution-detail.test.ts` **5 例**真连 Neon（DRAFT 可读+价格 priceDisplay/财务 Decimal→字符串·null·倒序·financialCount/未知变量严重度·unknownVariableCount/DEMO 关联 isDemo/notFound），基线 **324（249 单元 + 75 集成）**全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /admin/solutions/[id]`、**内容编辑台端到端冒烟 33/33**（`smoke-admin-solutions-edit.mjs -p 3126`：SSR 四块齐·正文+价 PATCH 初值回读·新增财务/未知变量回读见「严重度 60/100」·买家下单后删方案→409 含「仍有…订单」·无订单草稿删→200 后详情 404·跨站 403 CSRF·USER 无权限不泄露正文/财务表单·未登录 307·不存在 id 404·末尾四项 leftover=0），版本 **0.23.0**。**M5 完成（后台「案例内容编辑台」——案例元信息 + 证据增删 + 删除的闭环 UI）**：M2 只能「列表 + 新建」案例，M1 的 PATCH/DELETE/证据端点无 UI 消费；案例是"发现→拆解→证据可信度"最上游数据源，缺编辑台卡住后续评分与方案。M5 把一份案例的可编辑面收敛一处，**零 schema、零新数据层写逻辑**，写操作全复用 M1 已测端点，与 M4 方案编辑台合龙成"案例 + 方案"两条后台内容竖切。`src/server/admin-cases.ts` 新增 `getAdminCaseDetail(id)`（早退挡无形 id、带 `evidences` 正序 + `_count:{solutions}` + `solutions:{where:PUBLISHED}` + `scoreBreakdown`、`isDemo` 由自身 `sourceType` 判定、证据 null 如实保留、`publishedSolutionCount` 作删除守卫依据、评分标量只读透传、不鉴权），`EditCaseMetaForm`（PATCH 基本信息、**刻意不提交 `scoreInput`**——10 维度评分留 M5b，复用 `mutate.ts`）、`AdminCaseEvidences`（证据逐条列 + 行内 `POST/DELETE`、置信度 0–100 客户端先校验、增删联动数据层 `recomputeCaseScores`、`router.refresh()` 重取可信度、置信度单模板串渲染规避 SSR `<!-- -->` 坑）、`DeleteCaseButton`（案例删除守卫专指"仍挂 PUBLISHED 方案"→服务端 409、`publishedSolutionCount` 作提示、被拒回显「仍有 N 个已发布方案」）；`/admin/cases/[id]` RSC 双层防御 + 只读评分快照卡 + noindex、列表行加「编辑内容 →」链接；顺带修 `NewSolutionForm` 内链裸 `<a>`→`<Link>`（新增 `[id]` 路由后 `no-html-link-for-pages` 开始拦截）。`tests/integration/admin-case-detail.test.ts` **5 例**真连 Neon（CANDIDATE 全量可读·行业中文名·评分标量透传/证据逐条 null 保留·evidenceCount/`solutionCount`·`publishedSolutionCount` 只数 PUBLISHED/DEMO `isDemo`·`hasScoreBreakdown` false/notFound 与读失败可区分），基线 **329（249 单元 + 80 集成）**全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /admin/cases/[id]`、**案例编辑台端到端冒烟 33/33**（`smoke-admin-cases-edit.mjs -p 3127`：SSR 三块齐·证据回填「置信度 80/100」连续·PATCH 标题/阶段透出·新增 FACT→DB 确证 `evidenceConfidence` 复算（关键发现：recompute 有意 gated on `scoreInput`、无输入分诚实跳过第 20 条，故预置合法 `scoreInput` 才演示联动）·删证据后陈述消失·案例挂 PUBLISHED 方案→删 409 含「仍有 1 个已发布方案」·撤方案后删 200 后详情 404·跨站 403 CSRF·USER 无权限不泄露「证据（案例拆解）」块·未登录 307·不存在 id 404·末尾四项 leftover=0），版本 **0.24.0**。**M5b 完成（案例「10 维度机会评分录入台」——把已测评分内核接上运营录入路径）**：M5 刻意把 `scoreInput` 排除在基本信息表单外（10 维评分是"程序复算"高敏感区），但测好的评分内核 `scoring.ts` 一直没有后台录入入口，机会分/可信度只能裸 API 或直接写库。M5b 补上这条竖切，**零 schema、零新数据层写逻辑、不动 `SCORING_RUBRIC_VERSION`（仍 1.0.0）也不改 26 黄金样本——纯加性 UI 读取既有 `OPPORTUNITY_DIMENSIONS` 常量**（早前"须改黄金样本"的顾虑经核实不成立：录入台只是把已有维度喂给已有内核复算，不新增维度/不改权重）。`src/server/admin-cases.ts` 的 `getAdminCaseDetail` 加透传 `scoreInput`（对象且数值键过滤回显）+ `scoreBreakdown`（`CaseScoresSchema` 服务端重解析，schema 漂移即降级 `null` 绝不半渲染脏拆解）；新 `src/components/admin/CaseOpportunityScoreForm.tsx`（client）按 10 维渲染受控数字输入（inverse 维标「越低越好」、各维 help 显满分、客户端按与内核**同一口径** 0..max 整数 + 全填才可提交，避免"存了但 invalid→无拆解"的悬空态），`PATCH {scoreInput}` 触发数据层 `recomputeCaseScores`、成功 `router.refresh()` 回读**服务端权威分数**；`/admin/cases/[id]` 把原只读评分卡拆成「紧凑汇总卡 + 录入表单 + 服务端渲染的逐维拆解审计卡（gated on `scoreBreakdown`，含合计/公式版本/证据可信度/关键未知变量/免责条，数字全部单模板串渲染规避 SSR `<!-- -->`）」。`tests/integration/admin-case-detail.test.ts` 扩至 **6 例**（+ golden `scoreInput` round-trip：`opportunityScore=74`·`scoreBreakdown` 10 维·`evidenceConfidence=69`；并反向守护伪造 breakdown 降级 null）；基线 **330（249 单元 + 81 集成）**全绿、`tsc`/`eslint` 0 错、`next build` 无新增路由（复用 `ƒ /admin/cases/[id]`）、**评分录入台端到端冒烟 32/32**（`smoke-admin-cases-score.mjs -p 3128`：无评分态见表单不含拆解卡·golden `PATCH`→200 `recompute:computed` 且 DB `opportunityScore=74`+拆解落库·追加 FACT 证据→`evidenceConfidence=90`·**越界 `{commercialValue:99}`→200 但 `recompute:invalid` 且既有 `opportunityScore`/`scoreBreakdown` 不被污染（第 20 条诚实不覆盖上一份好拆解）**·跨站 403 CSRF·USER PATCH 403·USER GET 不泄露表单·未登录 307·末尾四项 leftover=0），版本 **0.25.0**。**M6 完成（后台「审核发布队列」——把"人做关键决策"落成可操作的待办工作台，零 schema/零 DB/零新写端点）**：此前后台是"按对象分栏"（案例管理/方案管理各自列表），运营要判断"哪些方案卡在待审核、哪些内部案例该发布到橱窗"只能逐页翻找，**缺一块把"待人工裁决"的东西聚成一屏的横切视图**——正是宪法「AI 做大量劳动、人做关键决策」在工作台上的落点。M6 是纯加性只读竖切：`src/server/solution-admin.ts` 导出对既有私有 `publishGuard` 的只读包装 `solutionPublishBlockers`（**刻意复用真正阻止发布的同一守卫，使"队列里显示可发布"与"点了发布真被拦下"永不分叉两套逻辑**，宪法第 16 条防漂移）；新 `src/server/admin-review.ts`（server-only）`getReviewQueue()` 用**一条 `$transaction`** 拉两类待办——`status=UNDER_HUMAN_REVIEW` 的方案 + `stage∈{CANDIDATE,KEY_RESEARCH}` 的内部案例（各 `take:LIMIT+1` 溢出判 `truncated`、`total=Math.max(rawCount,items)` 兜并发竞态、方案 `publishBlockers` 实时算、`unknownVariableCount` 取 `_count.unknowns` 非缓存字段、`isDemo` 由关联案例判定、`priceDisplay` ¥/＄前缀），不含鉴权（错误上抛由页面 error boundary 兜）；新 `components/admin/ReviewQueueActions.tsx`（client）以 `{label,body}` 声明式动作消费**既有** `PATCH /api/admin/{solutions,cases}/[id]` 端点（通过并发布→`{status:PUBLISHED}`、退回草稿→`{status:DRAFT}`、发布到橱窗→`{stage:DEEP_CASE}`，409 时 `fieldHints` 回显拦截原因、成功 `router.refresh()`），**零新写端点**；新 `ƒ /admin/review`（layout+page 双层防御 · force-dynamic · noindex，两区块"待审核方案/待发布案例"）+ `/admin` 加「审核发布队列（待我处理）→」入口。`tests/unit/review-readiness.test.ts` **5 例**（`solutionPublishBlockers` 复用 `publishGuard` 的阻塞判定：无价/风险域未勾专业确认/多因并存/勾选后消解）+ `tests/integration/admin-review.test.ts` **4 例**真连 Neon（审核中方案全在且 DRAFT/PUBLISHED 干扰项被排、内部阶段案例在且 DEEP_CASE 被排、`publishBlockers` 同源 `publishGuard`、`_count` 实时/`priceDisplay`/`isDemo`/行业中文名、倒序·total·truncated 自洽）。基线 **378（289 单元 + 89 集成）**全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /admin/review` 无回归（复用既有写端点）、**审核发布队列端到端冒烟 29/29**（`smoke-admin-review.mjs -p 3129`：SSR 两区块·未定价方案显「价格」阻塞且点发布真被 409 拦（证队列判定与守卫同源非前端硬编码）·可发布方案 `PATCH`→200 且 DB `PUBLISHED`+`publishedAt`、从队列消失并入公开 `/solutions`·退回草稿→`DRAFT` 不再公开·待发布案例 `PATCH{stage:DEEP_CASE}`→升入 `/cases`·跨站 403 CSRF·USER 无权限不泄露区块·未登录 307·末尾四项 leftover=0），版本 **0.28.0**。**M7 完成（后台「一键生成 AI 正文草稿」按钮——给 Phase 8 M3 的生成能力接上运营人面入口，复用受门禁端点，零 schema/零新数据层写逻辑/零新端点）**：Phase 8 M3 已把 §33 流水线接入真实方案生成但只交付裸端点，后台无运营可点入口、「AI 做大量劳动」的能力对人是断链的。M7 落成「人点一下 → AI 起草 → 人在正文编辑台核对 → 人决定是否发布」的最小运营闭环，严守「禁 AI 自动公开发布」「人做关键决策」。新 `components/admin/GenerateBodyButton.tsx`（client，挂编辑台正文上方）：可选「研究聚焦问题」文本框（留空则服务端按方案/案例标题确定性派生、前端二次 `slice(0,2000)` 与端点同口径），点击经共享薄封装 `mutateJson` 打**已受 `requireStaffWrite` 门禁**的 M3 端点（不另起 Server Action 防漂移第 16 条），成功读服务端回传的 `generation` 元数据如实回报「已生成 N 节 / 流水线终态（研究完成·需人工复核·未完成）/ 成本约 $x」并 `router.refresh()` 让正文编辑台即时反映新分节、无可写分节诚实显示「未改正文」、被守卫拒（如已发布 409）原样回显字段原因且 **UI 不自判能否发布、不自证成功**；**已发布方案禁用按钮 + `Alert` 说明**（与端点 `blocked` 契约一致但服务端仍唯一权威）、常驻说明转达 M3 `notGenerated` 语义（成本/收入/ROI/回收期/来源刻意不生成、须程序计算 + 人工溯源才算完整可售）。`mutate.ts` **加性扩展** `MutateResult.data`（成功时透出剔除 `ok` 后的完整响应体，供需读服务端元数据的调用方复用同一薄封装，既有 5 组件行为不变）；`/admin/solutions/[id]` 于基本信息与正文编辑台间插说明性 `Card` 承载按钮。新增单测 `mutate.test.ts` **8 例**（`vi.stubGlobal("fetch")` 锁归一契约）；基线 **469（375 单元 + 94 集成）**全绿、`tsc`/`eslint` 0 错（含消除一处 `_ok` 未用变量告警改 delete 法）、`next build` **无新增路由**（复用既有页与 M3 端点）、**（真跑冒烟属后续，本轮以离线单测 + 集成契约锁死）**；版本 **0.33.0**。**后续待做**：成本看板（阻塞 Phase 9 M2 `ModelCall` 落库）、更细粒度权限矩阵（按资源/动作）、审核队列的驳回理由留痕 / 重提交状态机 / 审核人分配 / 批量操作（当前为最小可用工作台）、审核队列页直接触发生成（现仅方案编辑台按钮）|
| 14 | SEO | 独立 URL、meta/OG/结构化数据/canonical、sitemap | 5,7,8 | 🟡 **M1 完成（元数据契约 + robots.txt + sitemap.xml，环境门控）**：**M1** 立「能否被索引」的**单一裁决**——`src/lib/site.ts`（`SITE_URL/SITE_NAME/SITE_DESCRIPTION` + 纯函数 `isIndexable()`/`absoluteUrl()`/`seoMetadata()`，client/server 两侧安全、仅类型引用 next `Metadata`）：`isIndexable()` 仅「生产构建 **且** `NEXT_PUBLIC_SITE_URL` 为非本地域名」才放开，localhost/127.0.0.1/0.0.0.0/::1/解析失败一律保守不索引（未部署站点绝不进搜索索引，宪法第 20 条诚实）；空串/纯空白 `SITE_URL` 归一回落本地（`??` 只兜 null 的坑自查修正）。根 `layout.tsx` 改复用该真源、`robots` 由写死 `index:false` 改为 `isIndexable()` 裁决；公开页（首页/行业列表/行业详情 SSG/案例列表/方案列表/about + 案例/方案详情 `generateMetadata` found 分支）加 `canonical`+OpenGraph（详情页 `type:article`、not_found/error 补 `robots noindex`、**DEMO 视图 `?demo=1` 恒 noindex**）；`/search` `robots:{index:false,follow:true}`，隐私/条款仍各自 noindex（占位草稿）。`src/server/seo.ts` `getSitemapEntries()` 只读枚举公开案例 + 已发布方案（AND 数组容纳 DEMO OR 片段、`take:4000` 兜底、**任一侧失败 `log.warn` 降级不拖垮整张 sitemap**）。`src/app/robots.ts`+`src/app/sitemap.ts` 两 MetadataRoute **均 `force-dynamic`**（关键教训：MetadataRoute 默认构建期快照，若构建/运行域名不一致会致 robots 与 sitemap 状态分裂，故都转动态令二者永远同源）——不可索引时 robots `Disallow:/`、sitemap 空 urlset；可索引时 robots `Allow:/`+禁抓 `/admin /account /orders /api /login /register /search /ui`+`Host`/`Sitemap`、sitemap = 静态页 + 7 行业页 + 动态案例/方案去重。**零 schema、零新数据层写逻辑**。`tests/unit/site.test.ts` **17 例** + `tests/integration/seo.test.ts` **4 例**（真连 Neon，含「挂 DEMO 案例下的已发布方案被顺 case 排除」），基线 **351（266 单元 + 85 集成）**全绿、`tsc`/`eslint` 0 错、`next build` 新增 `ƒ /robots.txt`+`ƒ /sitemap.xml` 无回归（行业 SSG 不受影响）、**SEO 端到端冒烟 20/20**（`smoke-seo.mjs` 起无域名/`example.com` 两实例对验二分枝），版本 **0.26.0**。**M2 待做**：JSON-LD 结构化数据（Solution→Product/Offer、Case→Article、BreadcrumbList、CollectionPage）、OG 分享图、hreflang；sitemap 分页（超 50000 时索引文件）|
| 15 | 测试 | Unit/Integration/API/DB/AI Workflow/Permission/Security/E2E | 各 Phase | ⬜ 随 Phase 进行 |
| 16 | 部署 | Serverless/低运维上线、环境变量、域名 | 4–14 + **托管资源** | ⬜ 阻塞 |
| 17 | 每日自动任务 | 定时流水线编排（23:00）、重试、FAILED_REVIEW | 9,13 | ⬜ |
| 18 | 生产验证 | 端到端验收（总控第42节）、成本可查、文档完整 | 全部 | ⬜ |

## 2. MVP 边界（宪法第2/24/26条）

- **V1-A（必须真正跑通）**：免费案例 → 标准方案 → 购买（含最小用户/登录）。对应 Phase 4–8 + 12 + 必要后台。
- **V1-B（闭环稳定后紧接）**：企业画像 + 企业诊断 + 方案适配（Phase 11）。
- **V2+ 延后**：POC、项目库、孵化、联合投资、实体运营；复杂社交/社区/融资交易/投资商城/供应商市场/知识图谱/微服务/K8s/模型训练/原生 App/小程序/复杂 CRM-ERP。

## 3. 阻塞与所需资源（总控第40节"停止条件"，诚实记录 — 宪法第20条）

进入 Phase 4 前必须由创始人提供/决策，否则无法真实"可运行/可测试"：

1. ~~**数据库**：本机无 PostgreSQL、无 Docker。~~ **已解除**：创始人提供 Neon 免费托管 Postgres `DATABASE_URL`（region `us-east-2`），已写入本地 `.env`（gitignored），`prisma migrate deploy` 成功建 17 表 + 11 枚举。
2. ~~**依赖安装网络**：npm 可达但重型依赖（Prisma 引擎等）下载 stall。~~ **已解除**：改用 npmmirror 源 + `PRISMA_ENGINES_MIRROR`，`npm install`（393 包）与 `prisma validate` 均跑通，首迁移已离线生成。
3. **代码推送**：命令行无法访问 `github.com`（仅 `api.github.com`）。目前经 API 提交；若要走正常 `git push`/CI，需要系统级/TUN 代理。
4. **模型密钥**：Model Router 需要 Qwen3.8-Max 等模型的 API Key（走环境变量，禁止入 Git）。→ 属"需要付费资源授权"。
5. **部署与支付**：Serverless 部署目标、域名、以及支付渠道（V1 可先"支付说明+后台确认"，但需确定收款方式）。→ 属"付费资源/业务决策"。
6. **对象存储**：企业文件/PDF 导出所需的对象存储供应商与凭证。

## 4. 开放决策（需创始人拍板 — 总控第40节"必须由人工决定的业务规则"）

1. **V1 范围**：企业诊断/适配放 V1-A 还是 V1-B？（本文档集默认 V1-B，理由见 PROJECT_RULES.md「已知冲突」）
2. ~~**认证方案**：Auth.js（自托管、免费、需自己配）vs 托管认证（Clerk/NextAuth 云等，省事但可能付费）。~~ **已决策**：创始人选定 **Auth.js 自建**（Credentials + JWT，用户表留自有 Neon 库、零厂商锁定），已于 Phase 6 M1 落地。
3. **数据库/部署供应商**与预算上限（影响成本路由与 Serverless 选择）。
4. **定价与币种**：标准方案定价区间、是否多币种（默认 CNY）。
5. **`Solution.body` 34 分节的最终 JSON 结构**（Phase 8 前定稿；我可先出草案供你改）。

## 5. 验收标准（总控第42–43节，Phase 18）

用户侧闭环可真实运行：访问首页 → 浏览案例 → 打开方案 → 注册 → （企业诊断 → 生成推荐）→ 查看方案 → 提交订单 → 管理员后台确认 → 用户解锁内容。
管理侧：查看每日任务 → 60 候选 → 筛选结果 → Agent 过程 → 3 个方案 → 审核 1 个 → 发布。
质量门槛：核心流程正常、AI 输出结构稳定、关键数据可追溯、无密钥泄露、DB 无结构性错误、普通用户能完成主流程、管理员能管理系统、可在生产运行、能查看 AI 成本、开发文档完整 —— 全部满足才可宣布 **V1.0 READY**。禁止虚构"已完成/测试通过/API 可用/许可证允许商用"（总控第44节）。
