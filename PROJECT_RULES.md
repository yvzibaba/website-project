# PROJECT_RULES — 规则索引与优先级

本文件是项目所有规则/规格文档的**索引与冲突裁决说明**。它本身不重复规则内容，只规定"谁说了算"。

## 优先级（从高到低）

1. **项目宪法 V2.0** — [`docs/PROJECT_RULES_V2.md`](./docs/PROJECT_RULES_V2.md)
   最高优先级。定义唯一目标（商业闭环）、决策优先级、MVP 原则、研究/证据/数字/许可证/版本化等硬约束。任何冲突以它为准。
2. **总控 Prompt V1.0** — [`docs/MASTER_PROMPT_V1.md`](./docs/MASTER_PROMPT_V1.md)
   工程化的产品与开发蓝图：商业模式、页面清单、Agent 架构、数据表清单、Phase 0–18、验收标准。当它与宪法冲突时，**以宪法为准**；宪法未覆盖的工程细节以总控 Prompt 为准。
3. **本设计文档集**（PRODUCT_SPEC / ARCHITECTURE / DATABASE / AI_WORKFLOW / SECURITY / TESTING / ROADMAP）
   对上面两者的落地设计，可随开发迭代，但不得违背宪法与总控 Prompt。
4. **框架自动生成约定** — [`AGENTS.md`](./AGENTS.md) 中的 Next.js 自动块
   仅约束 Next.js 写法，优先级最低。

## 冲突裁决原则（来自宪法第27条）

当无法判断时，一律选择：**更简单、更便宜、更可验证、更易维护、更接近商业闭环、更少依赖人工**；而不是更先进、更复杂、更炫、更像大公司。

## 已知冲突（待创始人裁决，见 ROADMAP「开放决策」）

**V1 是否包含"企业诊断/企业适配"与完整用户系统？**

- 宪法第26条（终局）把"企业适配"列为 **V2**，V1 = 案例 + 解决方案。
- 总控 Prompt 第21节把 V1.0 必须打通的闭环定义为"免费案例 → 标准方案 → 购买 → **企业适配**"，第42节验收流程也包含"注册 → 企业诊断 → 生成推荐"。

**本文档集采用的临时裁决（可被推翻）**：分两小步——
- **V1-A（先做，必须跑通）**：案例 → 方案 → 购买（含最小用户/登录，因为下单需要身份）。
- **V1-B（核心闭环稳定后紧接做）**：企业画像 + 企业诊断 + 方案适配。

理由：宪法优先级高于总控 Prompt，且宪法第24条明确"功能增加 vs 核心闭环稳定冲突时，永远选核心闭环稳定"。故先把"能卖出一份方案"跑通，再做企业适配。此为**需创始人确认的业务决策**。

## 版本化要求（宪法第13条）

上述所有文档（含 Prompt、Agent 定义、DB 结构、AI 工作流）都必须版本化：修改走 Git 提交，重大变更在 [`CHANGELOG.md`](./CHANGELOG.md) 记录**版本号 + 时间 + 原因 + 内容 + 效果**，不得直接覆盖生产版本，必要时可回滚。
