# 中途重构评估 V1

> 响应《项目中途重构总控 Prompt》§19。时间：2026-09-05（Asia/Shanghai）。角色：AI CTO / 产品 / 架构 / 全栈 / AI / 测试。
> 最高纪律：**不推倒重来、优先复用现有有效代码**；仅在现有架构确实挡住核心产品时才重构。
> 本文 = §19 第二步（评估）+ 第三步（KEEP/ENHANCE/FREEZE/DEFER/REMOVE）+ 第四步（精简任务树）。§19 第五步（动手建沙盘）另立里程碑执行。

---

## 结论先行（一句话）

当前代码库本质是一座**「内容/研究型商务站」（案例 → 方案 → 购买）**，而目标核心是**「可交互的产业项目可视化决策沙盘」（选地区 → 载默认参数 → 改参数 → 跑模型 → 技术/经济/风险/敏感性 → AI 解释 → 动态报告 → 存版本）**。把目标拆成六根支柱逐一比对：

| 支柱（§14 优先级） | 现状 | 是否阻塞新核心 | 处置方向 |
|---|---|---|---|
| ①参数系统 Parameter Engine | **完全没有**（全仓无任何"参数化变量 + 来源/生效时间/置信度/可编辑/分层暴露"结构；`tier` 一词只用于 AI 成本档位） | **是，根节点** | **新建（绿地）** |
| ②项目模型 Project Model | **没有**（无 Project/ProjectVersion/参数快照实体；`SolutionFinancial` 只是手填 Decimal 存值） | **是** | **新建 + 复用现有版本化/审计模式** |
| ③计算引擎 Calc Engine | **财务部分完全没有**（NPV/IRR/ROI/回收期/现金流/敏感性无任何公式；文档早已把"Finance Agent 程序算"列为未兑现义务）。**但已有一个真·可复算程序引擎 `scoring.ts`**（10 维机会评分 + 证据可信度，输入绑定、幂等、Zod 校验、版本化） | **是，命脉** | **新建财务计算引擎，复用 scoring.ts 的"纯函数 + 黄金样本 + 版本化"范式** |
| ④可视化 | **零图表**（无 recharts/d3/chart.js；仅文本徽章 + 评分贡献条） | **是** | **新建（选轻依赖图表库）** |
| ⑤动态报告 | **部分**：34 分节结构化正文 + AI 填充 + 逐记录 version/ChangeLog 版本化——但**未绑定模型**，财务分节永远空白（`notGenerated` 明确拒算） | 部分 | **增强：从"AI 写死文字"改为"读最新模型结果再渲染"** |
| ⑥地区/政策 | **完全没有**（`Region` 只有 name/code/country，3 条种子仅名字；选地区不加载任何东西） | **是** | **增强 Region + 新建地区/政策默认参数集** |
| ⑦企业个性化 | 未建（PRODUCT_SPEC 里企业资源采集列为 V1-B） | 否（V1 后置） | **延后** |
| ⑧敏感性分析 | 仅作为报告分节键存在，无计算 | 是（依赖③） | **随③一起建** |
| ⑨情景分析 | 无 | 否（V1 后置到⑧后） | **延后半步** |
| ⑩AI 解释 | **已建好且完全对口**：`model-router` + `deepseek-provider` + `research-pipeline` 已是"只解释/研究、不代算"层，成本程序算、密钥不落、无 key 回落桩 | **否，可直接复用** | **KEEP** |

**最重要的判断：现有架构没有把核心产品堵死——不需要推倒重来。** 缺的是三根"计算内核柱子"（参数引擎 / 程序化经济计算引擎 / 模型联动可视化），而项目**最难的工程资产**——可复算程序引擎范式、结构化正文、AI 解耦 + 真实模型 + 成本追踪、证据四分类 + 可信度、append-only 审计 + 版本化、认证 + 后台 + 购买闭环、GitHub Scout 供应链——恰好都是新核心的**支撑层**，可原样复用或轻量增强。因此这是"**在既有地基上补三根新柱 + 把已有能力重组到新核心的服务位置**"，不是重写。

---

## 一、为什么"不能只改页面上的数字"（§4 命脉）现在可满足

`scoring.ts` 已经证明这套代码库能把"输入 → 程序计算 → 结构化输出（可复算、版本化、防漂移）"落地并测死（10 维 + 证据可信度 + 关键未知数计数，全黄金样本）。把同一范式套到项目经济模型上，即：

`参数快照(params) → 计算引擎(纯函数) → {技术结果, 现金流序列, CAPEX/OPEX/NPV/IRR/ROI/回收期/敏感性} → 图表绑定结果 → 报告读结果 → AI 只解释结果差异`

改任一参数 → 重算 → 全链路数值/图表/文字**同步变化**，且每个数字可回溯到"来源 + 公式 + 假设"。这条链正是 §17 的验收主链，当前**完全未打通**——它是本次重构唯一真正必须先补的能力。

---

## 二、KEEP / ENHANCE / FREEZE / DEFER / REMOVE 清单（§13）

分类原则：**没有分析就没有删除**；本清单**不含任何立即 REMOVE（删代码/删表）**，REMOVE 仅登记候选、须单独评审 + 迁移，符合"优先复用、绝不轻删"。

### KEEP（原样保留，是新核心的支撑或已对齐 §11/§12）
- **认证与用户**：`User` + next-auth v5 Credentials/JWT、`/login` `/register` `/account`。§12 商业产品需要登录态与买家身份。
- **后台与审计**：`/admin` 全套 CRUD 台、`api-guard`（CSRF + 角色 + `mutationResponse`）、`ChangeLog` append-only、逐实体 `version/updatedAt`。是"人做关键决策"(§16)的运营面 + §13 版本化基础设施。
- **证据体系**：`Evidence`（FACT/ASSUMPTION/INFERENCE/PREDICTION + grade S–D + confidence）——直接兑现 §16"区分事实/假设/推断/预测"和 AI 解释的可信度地基。
- **AI 栈（支柱⑩）**：`model-router`（成本程序算、无 key 回落桩）、`deepseek-provider`、`research-pipeline`（多角色 Research→Bull→Bear→Judge→QA）、`ModelCall` 成本观测 + 后台看板。作新核心的"解释层"，**一行不改**。
- **程序可复算范式**：`scoring.ts` / `case-scores.ts`（作为③计算引擎的**工程模板**复用其纯函数 + 黄金样本 + 版本化写法，非复用其业务公式）。
- **GitHub Scout（§11 仍核心）**：`scout.ts` 治理门 + `scout-github.ts`/`scout-ingest.ts` 抓取落库（刚于 v0.35.0 推全绿）+ `OpenSourceProject`/`CapabilityProject` 表。
- **购买闭环（§12）**：`Order` + 金额服务端快照 + `/orders` `/account/orders` `/admin/orders` 幂等确认。V1 卖"可操作解决方案"仍需下单。
- **公开内容橱窗基座**：`/cases` `/solutions` `/industries` + SEO 基座（`/robots` `/sitemap`）。**降级为次要入口**但不删（承载案例/方案叙事，是沙盘的"证据来源"）。
- **工程纪律**：五道门（tsc/eslint/单测/集成/build）、CHANGELOG/README/ROADMAP/TESTING 四件套、NEON + Git Data API 无侵入推送。

### ENHANCE（保留并增强，向新核心靠拢）
- **`Region`（支柱⑥）**：从"三字段引用表"升级为**地区默认参数载体**——新增地区参数集（电价/光照/风资源/负荷曲线/税费/土地/并网/储能/充电等），带**默认值 / 来源 / 生效时间 / 允许覆写区间 / 政策有效期**；过期政策不得当现行默认。**这是沙盘入口。**
- **结构化正文（支柱⑤）**：`solution-body.ts` 34 分节 + `Solution.body JsonB` + `parseSolutionBody` 保留，但新增"**由最新模型结果自动渲染财务/敏感性分节**"的路径，把当前 `notGenerated`（成本/ROI/回收期/敏感性空白）改为"**计算引擎填、非 AI 编**"。
- **财务实体（支柱②③）**：`SolutionFinancial` 从"手填 Decimal 存值"演进为"**存参数 + 计算引擎产出的结果快照（含公式引用/假设/置信度）**"，或直接由新的 `ProjectScenario` 结果表承接（保留旧表兼容已发布方案）。
- **评分内核（支柱③模板）**：沿用 `scoring.ts` 的"10 维权重可配 + 升版即改黄金样本"的版本化做法，作为经济计算引擎的版本治理模板。

### FREEZE（暂不开发、但**保留代码与数据**，§"暂时冻结"非删除）
- **六大行业全面开发**：`Industry` 六枚举 + `/industries/[slug]` SSG 保持可用，但 V1 **只深耕"新能源重卡 + 光伏 + 储能 + 充电"一条沙盘**，其余行业停止横向铺量。
- **非核心公开页面**：`/about` `/privacy` `/terms` `/ui` 保持现状不动。
- **大量 §33 流水线正文分节的 AI 生成扩量**：现有生成能力保留，但 V1 重心从"生成更长的静态报告"转向"跑通可交互模型"，暂停继续堆正文分节。
- **企业诊断（Phase 11 / 支柱⑦）**：`PRODUCT_SPEC` 已列 V1-B，冻结到沙盘核心跑通后。
- **成本看板时间序列、按成本自动选模型（§32）**：地基（ModelCall）已就绪，冻结为"数据已备、暂不做高级分析"。

### DEFER（明确推迟，未来阶段；不建表不写码，避免提前复杂化）
- 社区 / 专家平台 / 融资交易市场 / 项目交易（总控"终局 V4–V7"）。
- 复杂知识图谱、原生 App、大型微服务、自训模型、大规模推荐。
- 情景分析（支柱⑨）排到敏感性分析（⑧）之后半步；多行业沙盘横扩排到 V2。
- 真实 embedding（DeepSeek 无端点，须独立向量供应商）。

### REMOVE（**候选登记，本轮不执行**；删除前须单独分析 + 迁移 + 用户裁决）
- 暂无必须立即删除的代码。`Case/CaseCapability/Localization/Supplier/Market/BusinessModel` 等研究侧实体在沙盘 V1 里退居"证据/案例叙事"支撑位，**保留不删**；仅在核心稳定后再评估是否有真正低价值页面可下线（且走回收站/迁移，不硬删）。

---

## 三、新的精简开发任务树（§19 第四步，V1 = 新能源重卡+光伏+储能+充电 决策沙盘）

沿用既有"**加性竖切 + 纯函数可离线测死 + 每里程碑四件套文档 + 版本递增 + 无侵入推送**"节奏；严守 §15（一次一任务 READ→PLAN→IMPLEMENT→TEST→VERIFY→DOCUMENT）、§7（关键数字程序计算）、§16（事实/假设区分 + 高风险标人工确认）、§17（E2E 未通不得宣布核心完成）。

### R0 地基与规格（1 步，先做）
- **R0.1 沙盘数据契约设计**：定义"参数定义(ParameterSpec) / 参数集(ResolvedParams：默认+地区+政策+用户覆写+来源+生效时间+置信度+可编辑) / 项目(Project) / 情景快照(Scenario) / 计算结果(CalcResult：技术流+现金流序列+关键指标) / 项目版本(ProjectVersion)"的**类型契约（Zod + Prisma schema 草案）**，明确哪些进 DB、哪些只做内存计算态。**先纸面评审再建表**（避免提前复杂化）。

### R1 参数系统（支柱①，优先级最高）
- **R1.1 参数引擎纯函数**（无 DB）：参数定义 schema（含 8 属性）、默认值解析、来源与生效时间、允许覆写区间裁剪、分层暴露（basic/advanced/pro）、计算型派生参数（dependent params）。`SCHEMA_VERSION` 版本化 + 黄金样本单测。
- **R1.2 沙盘参数模板**：为"重卡 + 光伏 + 储能 + 充电"建一套**具体参数模板**（车队规模/年行驶/电耗/光伏容量/等效利用小时/储能功率容量/充电站功率利用率/电价结构/补贴/折现率…），每参数带默认值 + 来源占位 + 置信度。

### R2 计算引擎（支柱③，命脉；复用 scoring 范式）
- **R2.1 技术模型计算**（纯函数）：由参数 → 光伏发电量/自用/上网、储能 SOC 时序与套利、充电负荷曲线、重卡用电需求、能量平衡。**结果可复算 + 黄金样本**。
- **R2.2 财务模型计算**（纯函数）：CAPEX/OPEX → 年度收入 → **逐年现金流 → NPV / IRR / ROI / 回收期**（Decimal/整数安全、边界不除零、公式 `calcRef` 可引用）。全黄金样本单测。
- **R2.3 敏感性 + 风险标注**（纯函数）：单/多参数 ±扰动对 IRR/回收期/NPV 的影响（龙卷风/蛛网数据），输出"关键未知变量 + 高风险领域 → 需专业人工确认"标记（§16）。
- **R2.4 引擎编排入口**：`runSandboxModel(params) → CalcResult`（把 R2.1–R2.3 串成单一纯函数入口，输入绑定、幂等、可离线测死）。

### R3 项目模型 + 持久层（支柱②）
- **R3.1 Prisma 迁移（纯加性）**：`Project` / `ProjectScenario`（参数快照 + 结果快照 + 版本）等新表；不动既有 19 表。
- **R3.2 项目 CRUD 数据层 + 版本化**：建/改/跑/存版本，复用 `ChangeLog` + `version` 模式；参数与结果分离存储（结果可由参数重算，不臆造）。

### R4 可视化（支柱④，绑定模型）
- **R4.1 图表基座**：引入**单一轻量图表库**（recharts，与 React 19 兼容优先），封装受控图表组件。
- **R4.2 沙盘 UI 主链（可交互）**：选地区 → 载默认参数 → 参数编辑器（分层）→"运行"→ 技术/经济结果卡 + **现金流/IRR/回收期/能源流/SOC/敏感性等图表全部绑定当前结果** → 改参数即时重算重绘。**这是 §17 人可操作的那一段。**

### R5 地区/政策默认（支柱⑥）
- **R5.1 地区参数集实体 + 种子**：给沙盘地区（如晋/粤/鲁…）挂电价/光照/补贴/并网等默认值 + 生效时间；选地区即载入；允许区间内覆写；过期政策默认排除。

### R6 动态报告 + AI 解释（支柱⑤+⑩，串 E2E）
- **R6.1 报告由结果生成**：报告财务/敏感性分节改由 `CalcResult` 渲染（非 AI 口算）。
- **R6.2 AI 解释层**：复用 `runTask`/DeepSeek，**只解释**"为何变化/影响最大变量/推荐依据/what-if"，输入 = 当前参数 + 模型结果 + 证据，**绝不新增/编造数字**（§10）。
- **R6.3 E2E 主链打通 + 存版本可恢复**：落 §17 全链冒烟脚本（改参数→结果变→图表变→敏感性变→AI 解释变→报告变→存版本→重开恢复）。**此链未全绿不得宣布核心完成。**

### R7 企业个性化方案（支柱⑦，沙盘跑通后）
- 在沙盘结果上叠企业画像/约束 → 生成定制方案（复用已有 Solution 生成 + 购买闭环）。

> 建议执行顺序严格 **R0→R1→R2→（R3 与 R4 可并行推进，但 R2 必须先于 R4 有真数据）→R5→R6 打通 E2E→R7**。在 **R6.3 E2E 主链跑通之前，产品不得宣布"核心完成"**；R7 及以后属增强。

---

## 四、关键决策点（须创始人拍板；AI 不擅自定夺业务规则）

1. **技术栈新增图表库**（§15"不引不必要依赖"）：建议 **recharts** 单库，是否认可？
2. **沙盘 V1 地区范围**：先做 **1 个省份**（如山西，光照/电价数据相对可得）打通全链，还是直接 2–3 省对比？建议**先 1 省跑通 §17 全链**再横向复制。
3. **参数与结果的存储边界**：结果一律"可由参数重算、不单独存死值"（防漂移），仅存参数快照 + 版本号；关键结果做只读缓存。是否认可此"单一真源 = 参数"的原则？
4. **旧 `SolutionFinancial` 手填路径**：保留兼容已发布方案，新沙盘走 `ProjectScenario` 结果表——认可"新旧并存、V1 不删旧"吗？
5. **参数默认值的真实数据来源**：光照/电价/补贴须**来源可追溯**（§7/§16），V1 是否允许先用**明确标注的占位/示例假设**（诚实标 ASSUMPTION，绝不当 FACT）把引擎与 UI 跑通，再逐步替换为经核数据？建议允许——否则 E2E 主链无法在缺真实数据集时验证。

（以上不阻塞我开始 **R0/R1/R2 纯函数地基**——这些无论怎么定都要建、且可离线测死、零风险、零 UI 依赖。默认我将按本任务树自主推进 R0→R1→R2，遇到上述业务裁决或需真实数据/密钥时如实停下汇报。）
